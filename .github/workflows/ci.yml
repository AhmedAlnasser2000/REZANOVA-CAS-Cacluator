name: CI

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  codex-agent-workflow:
    name: codex-agent-workflow
    runs-on: ubuntu-24.04

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Setup Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version-file: package.json
          cache: npm

      - name: Install npm dependencies
        run: npm ci

      - name: Validate controlled Codex agent workflow
        run: npm run test:codex-agent-workflow

  canonical-result-v2-enforcement:
    name: canonical-result-v2-enforcement
    runs-on: ubuntu-24.04

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version-file: package.json
          cache: npm

      - name: Install npm dependencies
        run: npm ci

      - name: Enforce Canonical Result V2 producers
        run: npm run test:canonical-result-v2-enforcement:ci

  ci-linux:
    name: ci-linux
    runs-on: ubuntu-24.04

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version-file: package.json
          cache: npm

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install Linux system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            build-essential \
            curl \
            file \
            libayatana-appindicator3-dev \
            librsvg2-dev \
            libssl-dev \
            libwebkit2gtk-4.1-dev \
            libxdo-dev \
            wget

      - name: Install npm dependencies
        run: npm ci

      - name: Validate memory protocol
        run: npm run test:memory-protocol

      - name: Validate controlled Codex agent workflow
        run: npm run test:codex-agent-workflow

      - name: Enforce Canonical Result V2 producers
        run: npm run test:canonical-result-v2-enforcement

      - name: Validate CI gate alignment
        run: npm run test:ci-gate-alignment

      - name: Validate seam impact selector
        run: npm run test:seam-impact-selector

      - name: Run additional seam impact evidence
        run: npm run seam:impact -- --github-event --run

      - name: Validate printer migration ratchet
        run: npm run test:printer-migration

      - name: Validate detail segment migration ratchet
        run: npm run test:detail-segment-migration

      - name: Validate Display contract inversion ratchet
        run: npm run test:display-contract-inversion

      - name: Validate MathJSON coverage ratchet
        run: npm run test:mathjson-coverage

      - name: Validate Equation solve result contract
        run: npm run test:equation-solve-result

      - name: Validate clipboard contract
        run: npm run test:clipboard-contract

      - name: Validate app identity
        run: npm run test:app-identity

      - name: Validate Surface Protocol
        run: npm run test:surface-protocol

      - name: Validate public pillars
        run: npm run test:pillars

      - name: Run golden math corpus
        run: npm run test:golden

      - name: Validate Labs catalog
        run: npm run test:labs-catalog

      - name: Validate source mirror registry
        run: npm run test:source-mirrors

      - name: Validate area study templates
        run: npm run test:area-studies

      - name: Validate OOE boundaries
        run: npm run test:ooe-boundaries

      - name: Validate compartment boundaries
        run: npm run test:compartments-boundaries

      - name: Validate source file sizes
        run: npm run test:file-sizes

      - name: Validate workspace canary registry
        run: npm run test:canary-registry

      - name: Run workspace runtime probes
        run: npm run test:runtime-probes

      - name: Run unit tests
        run: timeout --signal=TERM --kill-after=30s 30m npm run test:unit:ci

      - name: Run UI tests
        run: npm run test:ui

      - name: Run lint
        run: npm run lint

      - name: Build frontend
        run: npm run build

      - name: Check Tauri Rust crate
        run: cargo check --manifest-path src-tauri/Cargo.toml

  e2e-linux:
    name: e2e-linux
    runs-on: ubuntu-24.04

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Setup Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version-file: package.json
          cache: npm

      - name: Install npm dependencies
        run: npm ci

      - name: Install Playwright Chromium
        run: npx playwright install --with-deps chromium

      - name: Build frontend for preview server
        run: npm run build

      - name: Run workspace canaries
        run: npm run test:canaries:browser

      - name: Run focused browser smoke
        run: npx playwright test e2e/calc-audit0-smoke.spec.ts --project=chromium
