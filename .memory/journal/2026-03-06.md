# 2026-03-06

## Historical Attribution
- primary_agent: codex
- primary_agent_model: gpt-5.3-codex
- attribution_basis: historical-user-confirmed
- scope: entries recorded before protocol enforcement and preserved through historical backfill

- Repaired the Track `R` runtime layout regression in `src/App.tsx`: extracted workspace components were accidentally mounted inside the display shell, which collapsed the visible workbench area into the light LCD panel.
- Moved the `display-editor` / `display-panel` closing tags back above the extracted workspace render calls so workspaces render in the main workspace region again.
- Reproduced the bad layout from the user recording in a live browser capture, then rechecked the fixed layout visually after the patch.
- Re-verified the hotfix with:
  - `npm run build`
  - `npm run lint`
  - `npm test -- --run`
  - `cargo check`
- After reviewing `rec import.mp4`, confirmed a deeper regression: the top `MathEditor` and result shell paths for core modes were missing, leaving the app effectively non-interactive (`displayOutcome` had no visible render path).
- Performed an emergency UI recovery by restoring `src/App.tsx` and `src/App.css` to the last committed stable state (`dd126ac`) so main input, preview/result cards, and soft-menu routing are visible again.
- Re-verified after the rollback with:
  - `npm run build`
  - `npm test -- --run`
  - `npm run tauri:dev` (blocked only by port `1420` already in use, no compile regression shown)
- Closed remaining Track `R` follow-up items:
  - updated R0-R7 manual verification checklists with 2026-03-06 pass notes tied to the full regression gate
  - aligned Track `R` completion docs and session summaries with the current runtime wiring (`App.tsx` shell + `AppMain.tsx`)
  - cleaned root temporary import-capture PNG artifacts
- Refactored shared app helpers out of `AppMain.tsx` into existing extracted modules:
  - `src/app/logic/appUtils.ts`
  - `src/app/logic/solveSummary.ts`
- Re-ran full verification after the helper extraction:
  - `npm test -- --run`
  - `npm run build`
  - `npm run lint`
  - `cargo check`
- Executed Track C bundle `P0 + P1` implementation:
  - completed Geometry solve-missing request/type/parser/core wiring with strict `?` unknown policy
  - added bounded formula families (square/circle/cube/sphere/triangle-area/rectangle/cylinder)
  - added bounded coordinate families (distance/midpoint/slope) including two-branch distance warnings and unresolved-but-eligible equation handoff
  - added Geometry workspace solve-missing template seed actions
- Added and filled Track C manual verification artifact:
  - `.memory/research/TRACK-C-P0-P1-MANUAL-VERIFICATION-CHECKLIST.md`
- Added Track C session dossier:
  - `.memory/sessions/2026-03-06__track-c-p0-p1-bundle/`
- Re-verified after the Track C implementation:
  - `npm test -- --run src/lib/geometry/core.test.ts src/lib/geometry/parser.test.ts src/lib/geometry/navigation.test.ts`
  - `npm test -- --run`
  - `npm run build`
  - `npm run lint`
  - `cargo check`
- Completed Track C `P2` Geometry milestone:
  - added bounded inverse solve-missing families (`cone`, `cuboid`, `arcSector`, `triangleHeron`)
  - added `lineEquation(...)` one-unknown + one-constraint routing into coordinate solve-missing requests
  - added unresolved handoff mapping warning for coordinate sends (`x` placeholder mapping)
- Re-verified P2 gate with:
  - `npm test -- --run src/lib/geometry/parser.test.ts src/lib/geometry/core.test.ts src/lib/geometry/navigation.test.ts src/lib/guide/content.test.ts`
  - `npm test -- --run`
  - `npm run build`
  - `npm run lint`
  - `cargo check`
- Implemented Track D `D1 + D2` Statistics bundle:
  - added explicit dataset vs frequency-table source-sync state with stale markers cleared only by explicit source actions
  - hardened manual frequency-table validation to reject duplicate values
  - expanded descriptive summaries with sample variance and sample standard deviation
  - added `Statistics > Inference > Mean` with structured one-sample mean confidence interval and two-sided `t` test workflows
  - added bounded Student `t` helpers and dataset/frequency-table backed inference execution
- Added Track D D1+D2 checklist and session dossier artifacts:
  - `.memory/research/TRACK-D-D1-D2-MANUAL-VERIFICATION-CHECKLIST.md`
  - `.memory/sessions/2026-03-06__track-d-d1-d2-statistics/`
- Re-verified Track D D1+D2 with:
  - `npm test -- --run src/lib/statistics/parser.test.ts src/lib/statistics/core.test.ts src/lib/statistics/navigation.test.ts src/lib/statistics/engine.test.ts src/lib/statistics/inference.test.ts src/lib/guide/content.test.ts`
  - `npm test -- --run`
  - `npm run build`
  - `npm run lint`
  - `cargo check`
