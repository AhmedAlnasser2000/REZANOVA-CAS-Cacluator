# 2026-03-02

## Historical Attribution
- primary_agent: codex
- primary_agent_model: gpt-5.3-codex
- attribution_basis: historical-user-confirmed
- scope: entries recorded before protocol enforcement and preserved through historical backfill

- Created the initial Vite + React + TypeScript scaffold.
- Initialized Tauri v2 in the existing project.
- Added the project-local Memory structure.
- Started wiring a calculator-focused Rust shell contract for persistence and mode management.
- Refactored calculator execution around explicit mode handlers and `DisplayOutcome` states.
- Added parse-based math classification and blocked accidental equation solving from `Calculate` mode.
- Added inline redirect behavior from `Calculate` to `Equation`, plus an opt-in `Auto-switch to Equation` setting persisted through Tauri.
- Added a dedicated `Vitest` unit harness covering math analysis, engine action semantics, calculate/equation mode behavior, and table generation.
- Added a repo-local `AGENTS.md` policy that requires automatic `.memory` updates after future completed tasks.
- Added a dedicated launcher data model and Rust `get_launcher_tree()` command for Casio-style app navigation.
- Replaced keypad mode cycling with a full-screen `MENU` launcher that supports keyboard, keypad, and mouse selection.
- Hid top mode tabs in normal use and kept them available only behind a development flag.
- Added unit coverage for launcher navigation helpers and verified the full test, lint, web build, and Rust compile stack after the navigation refactor.
- Added guided quadratic, cubic, and quartic solver branches under `Equation` with coefficient entry, generated textbook equations, and exact-first solving through the existing symbolic engine.
- Extended equation-mode unit coverage for polynomial solving and leading-coefficient validation, and updated launcher copy so `Equation` now advertises symbolic, polynomial, and simultaneous-system solving.
- Added an in-repo complex-number utility layer plus numeric polynomial root solving for degree 2 through 4, including quadratic analytic roots and Durand-Kerner fallback for cubic/quartic cases.
- Replaced the flat `Equation` selector with a Casio-style tree flow: `Symbolic`, `Polynomial`, and `Simultaneous`, with submenus for `Quadratic/Cubic/Quartic` and `2x2/3x3`.
- Kept `Symbolic` solve symbolic-only while guided polynomial flows now fall back to numeric complex roots and render `i` instead of raw internal syntax.
