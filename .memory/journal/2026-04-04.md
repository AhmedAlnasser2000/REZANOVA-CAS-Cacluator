## Historical Attribution
- primary_agent: codex
- primary_agent_model: gpt-5.4
- attribution_basis: historical-user-confirmed
- scope: entries recorded before protocol enforcement and preserved through historical backfill

- 2026-04-04: Reworded guarded Equation candidate-rejection messaging so preserved-domain failures in same-base log/root PRL4 cases no longer mention denominator exclusions; both the direct symbolic validation path and substitution-stage validation now derive more honest user-facing text from the actual rejected-candidate reasons.
- 2026-04-04: Committed the verified `PRL4` checkpoint as `744bdf5 feat(equation): expand bounded exp root and log solving` before starting the numeric-output follow-up.
- 2026-04-04: Implemented `NP1` numeric output controls with typed `Approximate digits` input, `Decimal/Scientific/Auto` notation, selectable scientific style (`×10^n` or `e`), and one shared formatter across Equation, Calculate, Table, and other app-owned numeric summaries.
- 2026-04-04: Tightened Equation exact-vs-approx handling so decimal-only symbolic outcomes no longer populate the exact line, while guarded validation now keeps full-precision accepted candidates and uses a correct zero-form for additive right-hand sides.
- 2026-04-04: Verified `NP1` with `npm run test:gate`.
- 2026-04-04: Added a dedicated roadmap note for bounded composition and nested-function solving (`COMP1`-`COMP3`) to `.memory/research/PLAN.md`, centered on safe outer inversion, image/range impossibility checks, bounded inner recursion, and clearer composite-solver provenance.
- 2026-04-04: Fixed direct trig angle handling so `Calculate` and the bounded trig-function parser now apply the selected `DEG/RAD/GRAD` mode even when the numeric argument uses `\pi` (for example `sin(\pi/2)`), instead of silently preserving radian meaning outside `RAD`.
- 2026-04-04: Verified the angle-unit fix with targeted unit tests, `src/AppMain.ui.test.tsx`, and a focused Playwright smoke for the `sin(\pi/2)` mode-switch flow; the broader full e2e gate still hit the previously separate overlay-settings preview-server dropout.
- 2026-04-04: Re-ran the full gate after the angle-unit fix and got a clean `npm run test:gate`; the previously separate full Playwright dropout did not reproduce on the clean rerun, so the angle-unit checkpoint is now fully gate-verified.
- 2026-04-04: Confirmed the direct-trig angle-unit behavior also holds for plain numeric arguments (for example `sin(90)`), not only `\pi`-based arguments, and added explicit unit/UI/Playwright regression coverage for both forms.
- 2026-04-04: Implemented `COMP1` as a new guarded Equation composition stage for one-layer `f(g(x)) = c` families, covering bounded outer inversion for `ln/log/exp/root/power` compositions plus image-aware trig impossibility checks and finite trig branch recursion.
- 2026-04-04: Verified `COMP1` with `npm run test:gate`, including new unit, UI, and browser smoke coverage for successful composite inversion, impossible nested trig proofs, finite composition branches, and explicit numeric-guidance stops for recognized unresolved families.
- 2026-04-04: Implemented `COMP2` as bounded multi-step composition solving with an explicit two-step outer-inversion cap, supported-family handoff into the shipped trig/PRL/algebra backends, and a new `Nested Recursion` provenance badge.
- 2026-04-04: Verified `COMP2` with `npm run test:gate`, including chained non-periodic solves, inversion-to-trig handoff, inversion-to-PRL handoff, explicit depth-cap stops, and honest numeric-guidance messaging for unresolved periodic composition branches.
- 2026-04-04: Fixed Equation numeric interval solving and shared candidate validation so direct trig residuals now honor the selected `RAD` / `DEG` / `GRAD` mode instead of silently evaluating in radians.
- 2026-04-04: Changed explicit `Run Numeric Solve` in Equation to prioritize the numeric interval stage before symbolic-family stops, so recognized-but-unresolved composition cases like `tan(ln(x+1))=1` can still solve numerically when the user provides a good interval.
- 2026-04-04: Verified the Equation angle-unit consistency patch with targeted Playwright smokes and a clean `npm run test:gate`.
- 2026-04-04: Added unit-aware numeric interval guidance for missed trig roots in Equation, so no-root results can now describe the sampled inner image and the current-unit branch family the user should aim for (for example `45 deg + 180 deg * k` or `50 grad + 200 grad * k`).
- 2026-04-04: Verified the interval-guidance follow-up with focused unit/UI/Playwright coverage and another clean `npm run test:gate`.
- 2026-04-04: Extended the shared signed-number parser to accept scientific notation (`3e19`, `3.4e19`, `-5e-2`), so Equation numeric interval inputs can accept the large branch windows suggested by trig guidance without forcing users to type giant decimal literals.
- 2026-04-04: Verified scientific-notation numeric interval input support in helper tests, the real Equation UI flow, and a clean full `npm run test:gate`.
- 2026-04-04: Implemented `COMP3` periodic branch families for Equation composition solving, adding symbolic family output, representative branches, and interval suggestions for bounded trig follow-on cases such as `ln(sin x)=0`, `tan(ln(x+1))=1`, and `sin(e^x)=1/2`.
- 2026-04-04: Verified `COMP3` with a clean `npm run test:gate`, including RAD/DEG/GRAD-aware periodic family formatting, unresolved periodic guidance for nonlinear-in-`k` carriers like `sin(x^2)=1/2`, and updated UI/browser coverage for the new periodic-family result sections.
