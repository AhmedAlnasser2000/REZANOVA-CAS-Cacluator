## Historical Attribution
- primary_agent: codex
- primary_agent_model: gpt-5.3-codex
- attribution_basis: historical-user-confirmed
- scope: entries recorded before protocol enforcement and preserved through historical backfill

- Verified and completed Exact Algebra Core `R4`:
  - added shared bounded algebra transform actions (`Combine Fractions`, `Cancel Factors`, `Use LCD`, `Rationalize`, `Conjugate`)
  - wired `F4 Algebra` in `Calculate` and `Equation > Symbolic` with inline transform trays
  - separated transform badges/summaries from solve badges/summaries in the result card
  - expanded unit, UI, and browser smoke coverage for the new transform flows
- Full automation gate passed:
  - `npm run test:gate`
- Patched the `R4` algebra tray contrast on the light display panel so transform chips use dark readable text and stronger borders/backgrounds inside the result area.
- Rechecked the shared result UI coverage:
  - `npm run test:ui -- --run src/AppMain.ui.test.tsx`
- Verified and completed Exact Algebra Core `R5`:
  - widened bounded rational solving from affine-only factors to one-variable monomial/binomial denominator and numerator families
  - widened bounded radical solving to monomial/binomial radicands with safe depth-2 square-root transform chaining
  - promoted supported square-root-binomial conjugate equations from recognized-only into actual solve paths when the transformed equation stays inside the bounded surface
  - preserved original exclusions/conditions and rejected excluded or extraneous roots after chained transforms
- Full automation gate passed for `R5`:
  - `npm run test:gate`
- Verified and completed a post-`R5` algebra display polish pass:
  - fixed square-power denominator rendering so repeated factors compact to exponent form (`x^2`) instead of leaking as repeated variable text (`xx`) in exact results and transform summaries
  - kept symbolic result cards from leaking raw LaTeX/plain-text symbolic approximations in exact rational output paths
- Verification passed:
  - `npm run test:unit -- --run src/lib/symbolic-engine/rational.test.ts src/lib/algebra-transform.test.ts src/lib/modes/calculate.test.ts`
  - `npm run test:ui -- --run src/AppMain.ui.test.tsx`
  - `npm run test:gate`
- Appended the next preferred roadmap to memory after Exact Algebra Core `R5`:
  - `SX1` settings foundation
  - `PRL1` power/root/log display normalization
  - `PRL2` broad numeric support
  - `PRL3` bounded symbolic support
  - `PRL4` bounded solve expansion
- Recorded settings as a canonical app surface decision and left the exact panel/page presentation as an explicit open question.
- Verified and completed `SX1`:
  - added a dedicated top-bar `Settings` control and a shell-owned settings surface
  - implemented a hybrid presentation: docked right inspector on wide layouts, right overlay sheet on narrower layouts
  - added live, persisted controls for display scale, math scale, result scale, high contrast, symbolic-display preferences, angle unit, output style, auto-switch to Equation, and history recording
  - kept top-row quick toggles as synced shortcuts over the same persisted state
  - added jsdom and browser smoke coverage for docked/overlay settings behavior and Settings/History mutual exclusion
- Full automation gate passed:
  - `npm run test:gate`
