# 2026-04-28

- [agent: codex | model: gpt-5.5] Investigated the remaining GitHub `ci-linux` failures after the unit timeout fix: the failing `test:ui` cases were existing heavy AppMain UI flows timing out under the UI Vitest config, including one test with its own 15s cap. Raised the UI Vitest timeout and that explicit heavy-test timeout so GitHub Actions can complete the checks instead of killing them early.
- [agent: codex | model: gpt-5.5] Completed `REL1 + SRC0`: added `.memory/sources/` for verbatim source snapshots, preserved the REL/PILLARS roadmap with separate metadata, hardened the Linux release workflow pre-package gate, added reusable release-process docs and a first preview changelog, aligned Tauri package metadata with MIT, proved local `.deb`, `.rpm`, and `.AppImage` bundle generation, and kept math/runtime behavior unchanged.
- [agent: codex | model: gpt-5.5] Completed `PILLARS0`: added eight compact public pillar docs, a tiny `test:pillars` validator, CI/release/test-gate wiring, README link, and memory closeout while keeping math/runtime behavior unchanged and leaving `MATH-GOLDEN0` as the next clean-base correctness milestone.
- [agent: codex | model: gpt-5.5] Completed `MATH-GOLDEN0`: committed `PILLARS0` first, then added a typed 24-case shipped-behavior golden corpus, `test:golden`, CI/release/test-gate wiring, golden-regression docs, and memory closeout without changing math/runtime behavior.
