- Migrated Geometry 3D solids and triangle tools into the shared top-editor Geometry core, removing the last preview-only Geometry screens and deleting the old screen-based Geometry execution bridge.
- Extended Geometry parsing, serialization, and evaluation to support cube, cuboid, cylinder, cone, sphere, triangle area, and Heron requests in both structured and screen-aware shorthand flows.
- Added optional `geometryScreen` history metadata so Geometry shorthand entries replay back into the correct tool instead of relying on brittle string matching.
- Validation passed with `npm test`, `npm run build`, `npm run lint`, and `cargo check`.
- Migrated Trigonometry onto the shared core-mode contract with one top trig draft/editor, explicit `Send to Calc` / `Send to Equation` actions, and no remaining implicit `Calculate` fallback from trig editing.
- Added trig draft parsing, serialization, shared-core execution, and `trigScreen` history metadata so trig replay restores the correct screen and guided state without string heuristics.
- Rebuilt the trig workspace around the shared top editor, kept guided numeric builders below it, removed the old trig screen-dispatch engine, and added parser/core/navigation tests for the new flow.
- Validation passed with `npm test -- --run`, `npm run build`, `npm run lint`, and `cargo check`.
- Migrated Statistics onto the shared core-mode contract with one top Statistics draft/editor, parser/core/serializer modules, real probability/regression/correlation engines, and `statisticsScreen` history metadata for replay.
- Reworked Statistics UX around explicit dataset vs frequency-table sources, manual frequency-table editing, dataset/table import-expansion actions, and guided point-set builders for regression and correlation.
- Added a narrow Calculate-mode trig-angle fix that rewrites direct numeric trig arguments by angle unit without using the global Compute Engine angular-unit switch, so `sin(90)` follows DEG/GRAD while explicit radian input like `sin(pi/2)` stays intact.
- Validation passed with `npm test -- --run`, `npm run build`, `npm run lint`, and `cargo check`.
- Polished shared dark-panel button contrast across the app by strengthening guide chips, quick-template buttons, top-strip controls, workspace action buttons, and statistics row actions while keeping light display-panel actions readable through scoped overrides.
- Polished Statistics, Trigonometry, and Geometry workbenches with tighter card spacing, stronger section hierarchy, table/point column labels, and subtler per-core card tinting; also added a shared accessibility-style control sweep for hover, active, focus-visible, and disabled states across app buttons and form fields.
- Validation passed with `npm run build` and `npm run lint`.
- Implemented Memory V2 and the repo workflow overhaul: durable `.memory` docs are now meant to be tracked in git, `.task_tmp/` is the ignored task/gate workspace, `docs/checkpoints/` holds verified app-state summaries, and PowerShell helpers now support task-session tracking plus pre-commit/pre-push planning.
- Validation passed with PowerShell smoke tests for `tools/dev/task_session.ps1` and `tools/dev/git_plan.ps1`, plus `npm run build` and `npm run lint`.
- Excluded `.tmp/` from the tracked repo baseline so temporary media captures stay out of the initial commit and future git status output.
- Added a shared canonical math-input layer plus a semantic execution planner so typed reserved functions normalize before execution, safe embedded derivatives reduce before solve, repeated multiplicative factors compact structurally, and resolved-form badges/history metadata now show planner-adjusted input when it differs from the original draft.
- Rewired Equation to own post-reduction `x` solving through a shared solve backend, moved supported derivative reduction onto the app-owned symbolic engine, and upgraded bounded trig matching from raw string patterns to AST-based normalization/matching for identities and trig-equation fallback.
- Validation passed with `npm test -- --run`, `npm run build`, `npm run lint`, and `cargo check`.
- Restored MathLive inline shortcuts in shared editors and overrode the common math-function tokens (`sin`, `cos`, `tan`, `asin`, `acos`, `atan`, `ln`, `log`, `sqrt`, `abs`, `pi`) so live typing now uses the same structured-function path as editor-native shortcuts instead of relying only on pre-execution canonicalization.
- Validation passed with `npm run build` and `npm run lint`.
- Reviewed the external `Calcwiz-Refinement-Tasks-for-Codex.md` refactor/test backlog: the large App/component extraction and custom-hook work was judged important but not urgent, while shared ComputeEngine/helper consolidation and extra calculus/test coverage remain good low-risk stabilization tasks to revisit after the next symbolic milestone.
- Expanded exact bounded trig solving through the shared Equation-owned solve backend: selected double-angle rewrites such as `sin(x)cos(x)=1/2`, cosine double-angle templates such as `2cos^2(x)-1=0`, and bounded `sin^2` / `cos^2` branch-split families now solve without broad CAS-style trig search.
- Added solve-layer provenance (`Trig Rewrite`, `Trig Square Split`) plus short rewrite summaries to Equation/Trigonometry result metadata, and updated trig help/guide copy to advertise the new bounded rewrite families without overstating solver scope.
- Validation passed with `npm test -- --run`, `npm run build`, `npm run lint`, and `cargo check`.
- Built the first Unified Guarded Solver milestone around `Equation > Symbolic`: added guarded solve orchestration, bounded substitution solving for trig and exp/log families, shared domain guards, candidate validation, and inline interval numeric solving with provenance badges and replayable interval metadata.
- `Trigonometry > Equation Solve` now shares the broader exact backend and surfaces an explicit handoff into Equation mode when interval-based numeric solving is the next step.
- Added focused tests for guarded solve orchestration, substitution solving, domain guards, candidate validation, and interval numeric solving; validation passed with `npm test -- --run`, `npm run build`, `npm run lint`, and `cargo check`.
- Fixed two guarded-solver regressions: cosine double-angle matching now accepts the normalized `cos^2(x)-sin^2(x)=0` term ordering, and exponential substitution branches such as `e^(2x)-5e^x+6=0` now route through inverse isolation instead of recursing back into substitution until the depth limit.
- Validation passed with `npm test -- --run`, `npm run build`, `npm run lint`, and `cargo check`.
- Added a solve-only real-range impossibility layer ahead of bounded family expansion so provably impossible real equations like `sin(x^2)=5`, `sin(x^2)cos(x)=5`, `sin(x)+cos(x)=3`, and `e^x=-1` now hard-stop with `Range Guard` plus precise no-real-solution text instead of falling through to unsupported-family messaging.
- Updated Equation and Trigonometry solve UX so range-guarded impossible cases no longer invite numeric interval solving or `Send to Equation`, while supported exact families and numeric interval flows continue unchanged.
- Validation passed with `npm test -- --run`, `npm run build`, `npm run lint`, and `cargo check`.
