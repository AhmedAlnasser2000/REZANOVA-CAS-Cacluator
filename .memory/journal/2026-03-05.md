# 2026-03-05

## Historical Attribution
- primary_agent: codex
- primary_agent_model: gpt-5.3-codex
- attribution_basis: historical-user-confirmed
- scope: entries recorded before protocol enforcement and preserved through historical backfill

- Captured a dedicated multi-track roadmap in `.memory/research/PLAN.md` so planning can proceed by focus area instead of one app-wide end-state plan.
- Recorded five track lanes (`Solver/Equation`, `Trigonometry`, `Geometry`, `Statistics`, and cross-core `Error Semantics & UX Consistency`) plus the recommended sequencing `A -> B -> C -> D` with Track E in parallel.
- Clarified that Track E means failure-taxonomy consistency (impossible/domain/unsupported/numeric/parse), not generic unified error wording.
- Implemented Track A A1-A3 in solver code: expanded guarded substitution coverage (including trig tan-polynomials and robust `exp(...)` handling), hardened range-guard cases, and upgraded numeric interval solving with bracket-first + local-minimum recovery.
- Added solver diagnostics fields (`substitutionDiagnostics`, `numericMethod`) to `DisplayOutcome` for better provenance without changing mode boundaries.
- Verified the milestone with `npm test -- --run`, `npm run build`, `npm run lint`, and `cargo check`.
- Added a new memory protocol rule: each completed roadmap track must have a user-facing manual verification checklist before the next track starts.
- Added `TRACK-E-MANUAL-VERIFICATION-CHECKLIST.md` with concrete "achieved now / manual steps / expected results" checks for the current Track E scope.
- Implemented Track A4 (bounded exp/log solve completion): added substitution diagnostics family metadata, hardened guarded-solve cycle keying with normalized zero-form keys, added visible numeric-method provenance in Equation results, and expanded exp/log coverage tests across shared/equation/trig solver paths.
- Added `.memory/research/TRACK-A4-MANUAL-VERIFICATION-CHECKLIST.md` with achieved-now scope, app verification steps, expected outcomes, and pass/fail notes before Track B.
- Re-verified full gates after A4 changes: `npm test -- --run`, `npm run build`, `npm run lint`, and `cargo check`.
- Implemented roadmap sequence `B1 -> A5 -> B2/B3`: affine trig solve expansion (`kx+b`, wrappers), bounded mixed same-argument linear trig solve, bounded log-combine sum transforms, and shared normalization updates for trig identity/equation flows.
- Added new bounded provenance surface for A5 with solve badge `Log Combine` and substitution diagnostics family `log-combine`.
- Updated Trigonometry and Guide copy to describe affine trig support and bounded log-combine scope while keeping broader log transforms deferred.
- Added required manual verification artifacts for this delivery:
  - `.memory/research/TRACK-B1-MANUAL-VERIFICATION-CHECKLIST.md`
  - `.memory/research/TRACK-A5-MANUAL-VERIFICATION-CHECKLIST.md`
  - `.memory/research/TRACK-B2-B3-MANUAL-VERIFICATION-CHECKLIST.md`
- Re-verified full gates after B1/A5/B2/B3 changes: `npm test -- --run`, `npm run build`, `npm run lint`, and `cargo check`.
- Ran the B1, A5, and B2/B3 checklist-equivalent app-path verification command and recorded PASS notes plus evidence directly in the three checklist files.
- Cleaned solve-note scene organization for Equation/Trig result cards by adding a dedicated `Solve note` block and stripping raw LaTeX wrapper tokens (`\left`, `\right`) from substitution summary text.
- Patched remaining LaTeX leakage in trig warnings: normalized-equation warning now renders as inline plain math text, and the mixed-linear warning string now uses `phi` text instead of a glyph/encoding artifact.
- Re-verified solver/trig regression after UI text cleanup with targeted tests and `npm run lint`.
- Implemented Track B4 (bounded log base expansion): same-base `ln/log/log_a` combine support, mixed constant-base change-of-base recognition, and invalid-base hard-stop handling.
- Implemented Track B5 (bounded trig sum-to-product core): two-term `sin/cos` sum/difference normalization, zero-target branch splitting, and explicit recognized-unresolved guidance for non-zero families outside exact bounded coverage.
- Added and filled new manual checklist artifacts:
  - `.memory/research/TRACK-B4-MANUAL-VERIFICATION-CHECKLIST.md`
  - `.memory/research/TRACK-B5-MANUAL-VERIFICATION-CHECKLIST.md`
- Updated guarded solve to preserve recognized-family messaging (avoids generic unsupported fallback) for mixed-base log and trig sum-product unresolved paths.
- Improved solve-note readability by tightening inline summary sanitization and filtering substitution branch candidates against original-equation constraints when domain rules apply.
- Re-verified full gates after B4/B5 delivery:
  - `npm test -- --run`
  - `npm run build`
  - `npm run lint`
  - `cargo check`
- Started Refactor Track R with gate `R0` (tests-first baseline) before structural decomposition.
- Added characterization contracts:
  - `src/lib/equation/solver-parity.contract.test.ts`
  - `src/lib/guide/content.contract.test.ts`
- Added manual verification artifact `.memory/research/REFACTOR-R0-MANUAL-VERIFICATION-CHECKLIST.md`.
- Verified R0 gate with:
  - `npm test -- --run`
  - `npm run build`
  - `npm run lint`
  - `cargo check`
- Began decomposition scaffolding:
  - moved `GeneratedPreviewCard` from `App.tsx` into `src/app/components/GeneratedPreviewCard.tsx`
  - moved solve-summary text formatting into `src/app/logic/solveSummary.ts`
  - added initial internal app contracts in `src/app/logic/types.ts` and `src/app/workspaces/types.ts`
- Re-ran full verification after scaffolding extraction:
  - `npm test -- --run`
  - `npm run build`
  - `npm run lint`
  - `cargo check`
- Completed Refactor Track R `R2` routing extraction slice with no behavior change:
  - extracted expression routing to `src/app/logic/expressionRouting.ts`
  - extracted primary action routing to `src/app/logic/primaryActionRouter.ts`
  - extracted soft action routing to `src/app/logic/softActionRouter.ts`
  - extracted keypad routing to `src/app/logic/keypadRouter.ts`
  - wired `App.tsx` to use these modules while keeping side effects in app orchestration.
- Added targeted routing characterization tests:
  - `src/app/logic/expressionRouting.test.ts`
  - `src/app/logic/primaryActionRouter.test.ts`
  - `src/app/logic/softActionRouter.test.ts`
  - `src/app/logic/keypadRouter.test.ts`
- Verified the refactor slice with:
  - `npm test -- --run`
  - `npm run build`
  - `npm run lint`
  - `cargo check`
- Closed Refactor Track R fully under strict no-behavior-change rules:
  - `App.tsx` reduced to `3892` lines as the composition root
  - `App.css` reduced to an `11`-line import manifest over `src/styles/app/*`
  - workspaces, app logic, solver internals, guide content, and calculator types are all decomposed behind stable compatibility barrels/facades
  - added the remaining manual verification artifacts:
    - `.memory/research/REFACTOR-R1-MANUAL-VERIFICATION-CHECKLIST.md`
    - `.memory/research/REFACTOR-R2-MANUAL-VERIFICATION-CHECKLIST.md`
    - `.memory/research/REFACTOR-R3-MANUAL-VERIFICATION-CHECKLIST.md`
    - `.memory/research/REFACTOR-R4-MANUAL-VERIFICATION-CHECKLIST.md`
    - `.memory/research/REFACTOR-R5-MANUAL-VERIFICATION-CHECKLIST.md`
    - `.memory/research/REFACTOR-R6-MANUAL-VERIFICATION-CHECKLIST.md`
    - `.memory/research/REFACTOR-R7-MANUAL-VERIFICATION-CHECKLIST.md`
  - removed leftover temporary range-debug artifacts from the repo root
- Reconfirmed the final Track R gate with:
  - `npm test -- --run`
  - `npm run build`
  - `npm run lint`
  - `cargo check`
