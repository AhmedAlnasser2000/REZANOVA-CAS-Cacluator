# Journal — 2026-04-14

- [agent: codex | model: gpt-5.4] Captured `PGL-VIS` as a separate post-`PGL` roadmap family for any future calculator-visible Playground surface. Clarified that the core `PGL` sequence remains backend/workflow incubation first, and that any visible Playground work should start only after `PGL` completion with an internal developer/operator console rather than a normal-user-facing mode.
- [agent: codex | model: gpt-5.4] Implemented `PGL4` as an external-compute foundations lane inside Playground. Added provider-neutral runner/job/artifact contracts, JSON config templates with ignored local SSH-shaped profiles, a local harness that reuses the real symbolic-search workload, and explicit non-executable SSH behavior so remote provider work stays deferred.
- [agent: codex | model: gpt-5.4] Implemented `PGL5` as the first real SSH VM pilot on the user-owned `calcwiz-box` target. The external-compute lane now supports one bounded remote run for `sym-search-planner-ordering`, pulls back remote artifacts into `.task_tmp/pgl5-external-compute/`, and writes a local parity report instead of treating SSH as a non-executable stub.
- [agent: codex | model: gpt-5.4] Applied a follow-up `PGL5` runner fix after live-VM debugging: the remote entrypoint now runs as `npm exec -- vitest ...` so the VM passes the target test path through to Vitest correctly during SSH execution.
- [agent: codex | model: gpt-5.4] Applied a second live-VM `PGL5` follow-up fix: the remote SSH entrypoint test now uses a `30_000ms` timeout so the symbolic-search workload can complete on the VM instead of failing under Vitest’s default `5_000ms` test budget.
