# 2026-03-09

## Historical Attribution
- primary_agent: codex
- primary_agent_model: gpt-5.3-codex
- attribution_basis: historical-user-confirmed
- scope: entries recorded before protocol enforcement and preserved through historical backfill

- Implemented Exact Algebra Core `R2` as bounded radical normalization for `Calculate` and `Equation`, including supported perfect-power extraction, like-radical combination, monomial/binomial denominator rationalization, and radical-condition preservation in solve flows.
- Added `src/lib/symbolic-engine/radical.ts`, wired radical normalization into `Calculate` simplify/factor/expand and `Equation` guarded pre-solve normalization, and added automated coverage for the new radical engine and integrations.
- Added a repo-level `.gitattributes` policy to stop line-ending warning noise from git by enforcing LF for source/docs and CRLF only for Windows-native scripts.
- Added Track `QA1` browser-first automation with repo-owned UI integration tests, Playwright smoke coverage for the core modes, and `test:ui` / `test:e2e` / `test:gate` scripts so milestone verification runs in-repo before any optional manual smoke.
- Reverted an aborted QA test-layout reorganization, restoring `src/AppMain.ui.test.tsx` and `src/test/renderAppMain.tsx` to their committed locations instead of carrying a half-moved `src/test/ui/` structure.
- Implemented Exact Algebra Core `R3` as bounded rational/radical equation solving in `Equation > Symbolic`, adding a guarded algebra-transform stage for LCD clearing, radical isolation, and simple square-root conjugate transforms before the existing trig/substitution/numeric stages.
- Extended solver provenance with `LCD Clear`, `Radical Isolation`, and `Conjugate Transform`, and marked transformed symbolic validations with `Candidate Checked` so result cards expose when original-equation filtering ran.
- Added R3 regression coverage across shared/backend Equation flows plus browser-first automation for the new Equation rational solve path, then closed the milestone with `npm run test:gate`.
- Ran the R3 browser-driven checklist against the built Vite preview, recorded pass notes for the rational/radical solve cases, and confirmed the controlled conjugate-family error path keeps the preserved condition line.
- Added `docs/workflow/cloud-local-sync-reference.md` as a concise command-level handoff checklist for switching between cloud and local VS Code without losing sync.
- Added a task dossier under `.memory/sessions/2026-03-09__workflow-cloud-local-sync-reference/` with completion, verification, and commit notes.
