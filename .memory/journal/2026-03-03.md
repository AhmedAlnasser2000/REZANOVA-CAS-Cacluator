# 2026-03-03

## Historical Attribution
- primary_agent: codex
- primary_agent_model: gpt-5.3-codex
- attribution_basis: historical-user-confirmed
- scope: entries recorded before protocol enforcement and preserved through historical backfill

- Added an Equation UX fidelity pass with breadcrumb chips, branch badges, route-aware help text, and Equation-specific menu framing.
- Added deterministic Equation focus behavior for menu, symbolic, polynomial, and simultaneous screens.
- Added conservative Equation history replay inference so guided polynomial and simultaneous results restore their branch context when it can be inferred without changing the persisted history schema.
- Added unit coverage for Equation UX metadata and Equation history replay helpers, and re-verified tests, lint, web build, and Rust compile checks.
- Added display-level clipboard ergonomics so expressions and results can be copied, loaded back into an editor, or pasted from the system clipboard.
- Replaced direct `type="number"` parsing with a signed-number input component so coefficient, system, matrix, vector, and table fields accept typed negative and positive numbers without snapping intermediate `-` input back to zero.
- Replaced the generic MathLive virtual keyboard with curated custom layouts for the active `Keyboard Foundation + Algebra Core` milestone, including `Core`, `Algebra`, `Relations`, `Letters`, and `Greek` pages.
- Added a virtual keyboard registry, capability gating, duplicate-governance checks, and mode-aware layout filtering so future CAS keyboard milestones can be activated without exposing unfinished symbols.
- Added milestone lesson content in `docs/guides/` and tests for keyboard catalog visibility, deduplication, and layout generation, then re-verified unit tests, lint, build, and Rust compile checks.
- Activated the `Discrete Core` keyboard milestone with curated `Discrete` and `Combinatorics` pages for `âˆ‘`, `âˆ`, `!`, `nCr`, and `nPr`, while keeping unsupported future domains hidden behind capability gates.
- Added exact discrete evaluation fallback for factorial, `nCr`, and `nPr`, plus controlled domain errors for invalid negative or non-integer inputs in this milestone.
- Extended factoring with a symbolic fallback so common symbolic factors, difference of squares, and simple integer quadratics can factor even when the underlying engine leaves them unchanged.
- Activated the `Calculus Core` keyboard milestone with curated `Calculus` and `Functions` pages for derivatives, derivative-at-point, integrals, limits, and common functions.
- Added calculus evaluation handling so derivative-at-point works directly, definite integrals can fall back to numeric Simpson evaluation, and limits use controlled numeric approximation when they converge reliably.
- Added the first explicit roadmap note for a future in-app guidance page that can teach milestone symbols and workflows in one place instead of relying only on standalone guide files.
- Added a dedicated `Guide` app as launcher item `6`, with domain-driven handbook pages, symbol lookup, mode guidance, search, and example cards that can load content into the right tool without auto-running it.
- Extended shared frontend and Rust mode/launcher state to support `guide`, including launcher/menu hotkeys, persisted mode compatibility, and Guide-specific soft-key behavior.
- Added typed in-app Guide registries plus unit coverage for guide content, navigation, search, and symbol lookup, then re-verified unit tests, lint, web build, and Rust compile checks.
- Added context-sensitive Guide deep links from calculator work screens so `Calculate`, `Equation`, `Matrix`, `Vector`, and `Table` can jump directly into the most relevant handbook page or mode guide.
- Tightened Equation and Guide contrast by brightening breadcrumb chips, branch badges, hotkey pills, and selected-state accents in the dark workspace panels.
- Added a shared result-size guardrail so oversized or undersized numeric results return controlled messages instead of destabilizing the UI, with the safe display window set to roughly `1Ã—10^-150` through `1Ã—10^150`.
- Fixed the remaining Guide example-action contrast issue by giving article action buttons a dark-panel-specific style instead of reusing the LCD button treatment.
- Added a guided `Calculate > Calculus` workbench with menu-driven routes for derivative, derivative-at-point, integral, and limit while keeping the free-form calculate editor available.
- Replaced fixed-step definite-integral fallback with adaptive Simpson integration and added left/right/two-sided numeric limit handling with controlled divergence, mismatch, and unsafe-range errors.
- Routed calculus Guide examples directly into the new workbench screens and added unit coverage for calculate navigation, calculus workbench builders, adaptive Simpson integration, and directional limit behavior.
- Added a dedicated `Calculus` launcher destination that opens the existing calculate-family calculus workbench as a visible page instead of leaving it hidden behind the standard Calculate panel.
- Updated launcher selection, header labeling, and guide copy so calculus now reads as a first-class page while still reusing the `Calculate` engine and history model under the hood.
- Extended the calculus workbench with rule-based symbolic antiderivatives for a practical single-variable subset, including power, reciprocal, affine-power, simple trig-affine, and exponential-affine forms.
- Extended limit handling so the calculus workbench can target finite values or `Â±âˆž`, using heuristics and numeric sampling for common rational-at-infinity and directional unbounded cases while keeping controlled mismatch and instability errors.
- Improved calculus UX with stronger workbench cards, target-kind toggles, result badges, and signed-number draft inputs for derivative-point, definite-integral, and finite-limit numeric fields.
- Activated `Linear Algebra Core` in the virtual keyboard rollout with a curated `MatrixVec` page and guide content that aligns matrix/vector notation with the dedicated `Matrix` and `Vector` modes without pretending full free-form matrix CAS.
- Added matrix/vector notation pads plus Guide deep links in the `Matrix` and `Vector` workspaces, then re-verified unit tests, lint, web build, and Rust compile checks.
- Added a focused linear-algebra UX pass so Matrix and Vector screens now separate executable operations from notation-drafting more clearly, with scope badges, quick preset actions, and copy/clear ergonomics around the notation pads.
- Added a small linear-algebra workbench helper and tests so current Matrix A/B and Vector A/B values can be loaded into textbook notation snippets for reuse without pretending they are free-form CAS inputs.
- Added a new top-level `Advanced Calc` app with guided Integrals, Limits, Series, and ODE screens, plus launcher/runtime support across the frontend and Tauri shell.
- Strengthened advanced calculus math handling with inverse-trig and logarithmic antiderivative rules, improved improper-integral numeric evaluation, stronger finite/infinite limit handling, and series builders/tests for Maclaurin and Taylor workflows.
- Added Phase 2 ODE support inside `Advanced Calc`, including guided first-order and second-order symbolic flows plus a numeric IVP solver path backed by Rust commands in desktop runtime and an RK4 web fallback.
- Repaired the advanced calculus integration by fixing improper half-line integration, one-sided unbounded limit detection, stale launcher/guide/keyboard tests, and TypeScript/lint regressions, then re-verified unit tests, lint, web build, and Rust compile checks.
- Polished `Advanced Calc` and core `Calculus` so both layers now use clearer route-aware preview cards, explicit empty states, guide chips, copy/to-editor actions, and stronger visual separation between menu screens and tool screens.
- Upgraded the Guide content model to teaching-first articles with `What It Is`, optional `What It Means`, `How To Use It`, and step-based worked examples, then rewrote the advanced-calculus, touched calculus, basics, and linear-algebra guidance to match that structure.
- Repaired a batch of malformed fallback expressions and label regressions in `App.tsx` introduced during the UX pass, then re-verified unit tests, lint, web build, and Rust compile checks.
- Added a unified symbolic-engine layer with normalization, precedence tracing, factoring, differentiation, integration, limit, and partial-derivative modules under `src/lib/symbolic-engine/`.
- Routed factoring and textbook partial-derivative evaluation through the new symbolic engine, added advanced partial-derivative keyboard symbols, and expanded Guide teaching for BIDMAS, factoring order, derivative rules, stronger integrals, and stronger limits.
- Reconciled the new symbolic behavior with tests by validating stronger symbolic limit resolution and mathematically simplified log differentiation, then re-verified unit tests, lint, web build, and Rust compile checks.
- Added a formal desktop runtime-validation runbook at `docs/validation/symbolic-engine-runtime.md` so symbolic factoring, calculus, advanced-calculus, ODE, and guide-launch behavior can be checked repeatably in `tauri:dev`.
- Added `Advanced Calc > Partials > First Order`, including variable chips for `âˆ‚/âˆ‚x`, `âˆ‚/âˆ‚y`, and `âˆ‚/âˆ‚z`, seeded guide examples, symbol lookup wiring, and route metadata/tests.
- Broadened symbolic indefinite integration for stronger substitution and bounded integration-by-parts patterns, plus scaled inverse-trig families, and normalized common implicit-multiplication / `e^(...)` input forms before symbolic parsing.
- Tagged exact Compute Engine integral results with explicit provenance, then re-verified unit tests, lint, web build, and Rust compile checks after the symbolic-depth pass.
- Deferred broader pre-integration rewrite work such as trig-product identities, wider rewrite search, and broader substitution/by-parts search until after new `Trigonometry`, `Geometry`, and `Statistics` cores are scoped and delivered.
- Added a new top-level `Trigonometry` app as the first three-core expansion, with launcher/runtime wiring, guide content, capability-gated keyboard pages, and guided workflows for functions, identities, equations, triangle solving, angle conversion, and special angles.
- Added `src/lib/trigonometry/` engine modules and tests for exact-angle handling, conversions, identity transforms, bounded trig equation solving, and triangle solvers, then re-verified the full Vitest suite and production build.

- Added a focused Trigonometry UI polish pass with dedicated panel styling, preset buttons, a unit-swap action, and card-based special-angle references, then shipped the new top-level Geometry app with 2D, 3D, circle, triangle, and coordinate workbenches plus updated launcher, guide, keyboard, and runtime wiring.
- Re-verified the Geometry delivery with `npm test` and `npm run build`.

- Replaced the flat MENU launcher with a two-level category launcher (Core, Linear, Calculus, Shape Math, Data), moved Guide into the top strip with Ctrl+G, and updated the Tauri launcher payload plus guide copy to match the new navigation paths.
- Added the first Statistics mode pass with a real Data category entry, a home/data-entry/descriptive/frequency workspace, probability/regression placeholders, and full frontend routing/tests.
- Re-verified the launcher/statistics migration with 
pm test -- --run and 
pm run build.
- Re-verified the updated Tauri command surface with cargo check after renaming the launcher payload to get_launcher_categories.
- Added a reusable core-draft contract and migrated Geometry phase 1 onto it: the top display is now the Geometry draft/editor, 2D/circle/coordinate tools seed structured Geometry requests into that draft, and line-equation results can explicitly send into Equation.
- Kept Geometry triangles and 3D solids live as guided legacy tools for this release, with the top Geometry area marked preview-only on those screens instead of pretending they already share the executable core.
- Added Geometry parser/serializer/core tests plus App/UI wiring for Geometry-specific editor focus, menu-vs-editor EXE behavior, and explicit result transfer actions, then re-verified full tests and production build.
