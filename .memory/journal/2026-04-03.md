## Historical Attribution
- primary_agent: codex
- primary_agent_model: gpt-5.4
- attribution_basis: historical-user-confirmed
- scope: entries recorded before protocol enforcement and preserved through historical backfill

- 2026-04-03: Implemented `SX1.1` outboard side-surface correction. `Settings` and `History` now share a right-side rail outside the calculator shell when real gutter space is available, and fall back to a right overlay when it is not.
- 2026-04-03: Added a shared side-surface host in `src/AppMain.tsx`, extracted `src/components/HistoryPanel.tsx`, and updated shell CSS so opening settings/history no longer truncates the calculator workspace.
- 2026-04-03: Verified `SX1.1` with `npm run test:gate`, including new jsdom and Playwright checks for outboard-vs-overlay presentation and shell-width stability.
- 2026-04-03: Implemented `PRL1` as a display-only symbolic normalization layer for powers, roots, and light log notation cleanup on rendered math surfaces.
- 2026-04-03: Wired `Symbolic Display` settings from `SX1` into live exact-result rendering, supplement lines, exact-error math, transform preview math, and the settings preview without changing copy/editor/history payloads.
- 2026-04-03: Verified `PRL1` with `npm run test:gate`, including new unit coverage for bounded root/power/log display rewrites plus UI/E2E checks that raw exact LaTeX still drives Copy Result and To Editor.
- 2026-04-03: Implemented `PRL2` with a shared real-domain numeric evaluator for bounded powers, roots, and logs, then wired it into `Calculate` and `Table`.
- 2026-04-03: Added explicit-base `log_a(...)` keyboard support on `Functions` and exposed the `Algebra` page in `Table` so PRL2 numeric features are reachable from guided inserts.
- 2026-04-03: Verified `PRL2` with `npm run test:gate`, including unit coverage for real-domain power/root/log rules plus UI/E2E coverage for Calculate and Table behavior.
- 2026-04-03: Followed up PRL2 so numeric-only `Simplify` no longer leaks raw CE `NaN`/complex output for invalid power/root/log expressions like `\log(-8)`; the same real-domain guard now returns controlled errors on those paths too.
- 2026-04-03: Re-verified the PRL2 follow-up with focused unit/UI/E2E checks covering invalid log/root simplify behavior.
- 2026-04-03: Re-ran the full `npm run test:gate` after the PRL2 simplify-path hardening and it passed cleanly, with only the already-known Compute Engine stderr noise and Vite chunk-size warning remaining.
- 2026-04-03: Implemented `PRL3` with a bounded symbolic normalization layer for power/root/log exact output, condition tracking, same-base log combine under `Simplify`, and Equation preprocessing for supported notational variants.
- 2026-04-03: Added shared algebra-tray transforms for `Rewrite as Root`, `Rewrite as Power`, and `Change Base` across `Calculate` and `Equation`.
- 2026-04-03: Updated UI/E2E coverage to reflect the PRL3 contract that rendered output still follows `PRL1` display preferences while copy/editor flows now carry PRL3 canonical raw exact output.
- 2026-04-03: Verified `PRL3` with `npm run test:gate`, including new unit coverage for symbolic normalization plus UI/browser smoke for explicit transforms, same-base log combine, and Equation preprocess parity.
- 2026-04-03: Followed up PRL3 by preserving exact symbolic branch solutions after substitution validation, so preprocessed `\\log_e(...)` Equation solves no longer collapse back to decimal-only exact output.
- 2026-04-03: Added repeated-factor compaction to symbolic multiply serializers so user-facing outputs stop leaking forms like `xx` and `4xx^3`; repeated factors are folded back into powers before exact/display latex is emitted.
- 2026-04-03: Re-verified the PRL3 follow-up with `npm run test:gate`, plus new unit/UI/E2E coverage for exact `\\log_e` solve preservation and repeated-factor log-combine rendering.
- 2026-04-03: Implemented `PRL4` as the bounded solve expansion for exponentials, roots, and logs inside the guarded Equation backend, including same-base equality solving, same-base log quotient preprocessing, bounded mixed-base log normalization, and bounded rational-power/root lifting with candidate validation.
- 2026-04-03: Corrected explicit-base log inversion so guarded substitution now inverts `\\log_a(u)=c` to `u=a^c` instead of hardcoding base 10, and merged substitution-domain supplements into successful exact outcomes.
- 2026-04-03: Added PRL4 regression coverage across guarded/unit/UI/E2E layers for same-base equalities, bounded mixed-base exact solves, recognized unresolved mixed-base families with numeric guidance, and bounded rational-power isolation families.
- 2026-04-03: Tightened the PRL4 Playwright smoke assertions to target result badges and rendered supplement text robustly, then re-ran `npm run test:e2e -- e2e/qa1-smoke.spec.ts` and the full `npm run test:gate` successfully.
- 2026-04-03: Reworded guarded symbolic candidate-rejection messaging so preserved-domain failures in PRL4 log/root cases no longer mention denominator exclusions; the runtime now derives a more honest message from the actual rejected-candidate reasons.
