# Decisions

## 2026-07-20 - GRAPHING-THREE-RENDERER1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Three `0.185.1` is a Graph-private, dynamically loaded WebGL2 adapter. Only `src/lib/graphing/renderers/three/` may import Three runtime/types; document, sampler, scene, camera, React, and OOE boundaries remain renderer-neutral. SVG remains deterministic 2D and immediate fallback authority.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Real and Complex panes own independent tab-local 2D/3D, projection, camera, vertical-exaggeration, wireframe, and flythrough state through strict session V4/surface V3 migration. These are view-only changes: they do not alter Graph mathematics revisions or launch sampling.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] The 3D navigation contract follows Unity-compatible desktop gestures and explicit orientation/projection controls. Rendering is on demand with capped pixel ratio, no shadows/fog/bloom/continuous loop, selected-point/item pivot feedback, deterministic teardown, and non-destructive SVG recovery on WebGL2 loss or absence.

## 2026-07-19 - GRAPHING-PARAMETERS1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Graph parameters are document-local structured items with explicit `authored-definition` or `slider-created` provenance. Finite scalar authored definitions compile into the numeric environment; unresolved symbols expose opt-in slider actions. Graphing never captures calculator Variables and never rewrites dependent relation source.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Parameter interaction permits one sampling preview in flight and retains only one latest queued revision. Pointer release requests settled refinement; autoplay advances only when sampling can consume the next value and is disabled under reduced motion, while manual adjustment remains available.

## 2026-07-19 - GRAPHING-PIECEWISE1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Direct cases entry and guided piecewise editing share `GraphPiecewiseSpecV1`. Guided value and condition fields become structured expression/condition IR independently; regenerated LaTeX is presentation/provenance only and never becomes downstream authority.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Piecewise rendering has no hidden first-match semantics. Every matching branch renders, otherwise covers only unmatched input, conflicting overlap remains visible and ambiguous, impossible conditions warn, and endpoint inclusion is an explicit renderer-neutral scene marker.

## 2026-07-19 - GRAPHING-INTERACTION-SAMPLING-CORRECTION1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] The Graphing program has 20 moves: the interaction/sampling correction is Move 11, Piecewise/Parameters/Polar-Grid are Moves 12-14, and Three.js cannot begin before Move 15. SVG remains the reference/fallback renderer.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Graph polar and parametric routes use structured authored domains with bounded defaults; grid choice never changes automatically. Cartesian/Polar/None grid geometry and labels are renderer-neutral scene authority, while Unit Circle is an independent teaching overlay. Three.js remains blocked until the existing eager-main ratchet and packaged-app visual checkpoint are genuinely green.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Production geometry projection is imperative behind `GraphRenderFrameV1`; React owns shell/accessibility/document controls, while active gestures transform one last-complete scene without React or OOE sampling work. Fairness is per visible item, incomplete implicit refinements never publish, and ordinary preview slice exhaustion is not user-facing.

## 2026-07-19 - GRAPHING-IMPLICIT-REGIONS1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Implicit equality, inequality, and supported chained intersections consume only structured `GraphRelationIR`. Each expression leaf compiles through the existing revision-aware plan cache; sampling never reparses source LaTeX or creates a second worker/runtime lane.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] The bounded implicit sampler uses a CSS/quality-derived grid scaled down against remaining sample/time/vertex budgets. Marching-square boundary segments and clipped region triangles are separate scene buffers and identities; strictness is preserved per chain clause as dashed versus solid boundary style.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Cells with non-finite evaluation or unresolved topology are omitted, never presented as complete fill. Structured per-item warnings disclose topology and hard-budget stops while truthful bounded geometry remains renderable and traceable through the renderer-neutral scene.

## 2026-07-19 - GRAPHING-RELATION-ROUTES1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Explicit-x relations and finite point sets widen the existing structured Graph document and renderer-neutral scene; authored LaTeX remains provenance only. Point coordinates are evaluated from bounded standard MathJSON and cross the sampling seam as owned finite typed arrays.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Graph hit testing consumes the bounded scene directly. Active explicit-y traces follow x, explicit-x traces follow y, and point sets follow point identity. Pointer movement is coalesced to one imperative frame and never becomes OOE work; Enter, arrow keys, and Escape provide the keyboard trace path.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] The authored-row and trailing-blank editors are one flat keyed collection so promotion preserves the exact MathLive element. Graphing removes ambiguous `in` and `!in` word shortcuts while retaining explicit `\in` entry and full `sin`, `infty`, and `infinity` shortcuts; this profile does not alter other workspaces.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Workspace-tab running-state refresh subscribes to the audited active-job registry seam, not the private OOE event outbox. The registry publishes start, cancellation, and terminal changes; workspace ownership and OOE capability identity remain unchanged.

## 2026-07-19 - GRAPHING-MINIMUM-VISIBLE1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] The first visible Graph page is a non-singleton, full-width real-function SVG reference experience opened only through Workspace Tabs `New Graph`. It exposes only working controls: expression-rail collapse, document undo/redo, visibility, deletion, pan/zoom, and Auto-Fit. Analyze, Complex/Both, Export, Presentation, Three.js, and not-yet-renderable relation controls remain absent.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] MathLive supplies visual authoring source and provenance only. The visible plotter consumes classified explicit-y `GraphRelationIR`; bare x-based expressions plot without `y=`, while future recognized relations remain controlled drafts. The rail retains one trailing blank row, preserves focus when that row is promoted by its first character, and assigns a stable distinguishing palette without rewriting source.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Graph interaction stays off React's pointer hot path: drag and wheel gestures apply one imperative HTML-layer transform per animation frame, wheel bursts coalesce behind a 180ms settlement window, and viewport state commits only when the gesture settles. Native drag/selection is suppressed inside the plot. Sampling remains coalesced and latest-only; old geometry is visibly pending during typing grace, invalid settled drafts clear it, and inactive Graph tabs release live sampling/render work while retaining document/view state.

## 2026-07-19 - GRAPHING-SAMPLE-OOE1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] `graph.sample` is the sole production sampling traffic lane. It uses one retained application-level `graph-sampling-worker-runtime`, never a worker per tab, and a bounded cooperative `graph-sampling-runtime` fallback that yields between items and caps each item slice. Document, viewport, parameter, workspace, quality, and scene revisions participate in latest-only commit; switching or closing a Graph tab requests cancellation through the existing workspace job registry.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Graph sample results cross the host seam as uniquely owned transferable typed arrays. Cancelled, stale-revision, or closed-workspace results detach their buffers before returning to a consumer. Runtime snapshot hashes stream canonical scene tokens directly and never allocate JSON numeric-array copies on the interaction hot path. Graph sampling records OOE events/diagnostics but reserves no History ticket and adds no OOE diagnostics UI.

## 2026-07-19 - GRAPHING-SCENE-HEADLESS1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Scene assembly adopts sampler-created typed arrays without copying, gives every view unique `ArrayBuffer` ownership, and emits an exact transfer list. Stable path and label ordering occurs before the scene crosses a host boundary; malformed identities, offsets, indices, non-finite numbers, and aliased ownership fail closed.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Runtime scene assembly and structural validation never materialize JSON snapshots or calculate snapshot hashes. Bounded normalized snapshots and deterministic hashes are produced only by the explicit headless evidence path for tests, recovery evidence, and later export—not during interactive geometry assembly.

## 2026-07-19 - GRAPHING-EVALUATOR-SAMPLER1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Graph evaluation compiles validated `GraphExpressionIR` once per source revision into a frozen postfix plan and reuses a bounded plan cache plus one scratch stack per evaluator. Sampling receives only structured relation authority and parameter values; it never reparses authored LaTeX or imports a private solver.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Explicit-y/x sampling is screen-space adaptive with distinct preview and settled policies. Domain transitions, suspected discontinuities, curvature, steep spans, and viewport re-entry refine only inside hard time/sample/depth/vertex budgets; cancellation or exhaustion retains finite multi-vertex segments and reports a structured stop instead of presenting incomplete geometry as complete.

## 2026-07-19 - GRAPHING-PARSER-IR1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Graph authoring uses MathLive's visual editor and retains its LaTeX serialization only for round-trip source/provenance. A bounded structural Compute Engine parse is canonicalized into allowlisted standard MathJSON leaves, and only `GraphRelationIR`, `GraphConditionIR`, or the structured piecewise/point result may drive evaluation, sampling, or rendering. Classifier code is statically barred from reading `sourceLatex`.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Bare x-based expressions, unresolved scalar expressions, and constants normalize to explicit-y without requiring `y=`. Bare y or polar-coordinate expressions remain ambiguous drafts. Unknown/custom operators, assignments/evaluation constructs, non-monotone chains, scalar-only relations, coordinate-system mixing, cycles, non-finite values, and parser budget overflow fail closed with structured stop reasons.

## 2026-07-19 - GRAPHING-WORKSPACE-RUNTIME1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Graph workspace identity is non-singleton and session-owned. Each instance receives an independent Graph document/view slot, a monotonic default title, and the narrow Graph runtime context; ordinary app pages remain runtime-null. Graph cannot be reached by same-tab retarget, duplicate, or generic clear shortcuts before exact document semantics land.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] The Graph app runtime loads only after a Graph tab becomes active and is emitted as a separate production chunk. Switching away or disposing the tab shell requests cancellation for Graph-owned jobs; inactive tabs retain document/view state but do not gain visible render or sampling work in this gate. `New Graph` remains hidden until Move 8.

## 2026-07-19 - GRAPHING-CONTRACT-PERFORMANCE-FOUNDATION1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Graph authoring retains MathLive LaTeX only as source/provenance. Bounded standard MathJSON inside discriminated `GraphRelationIR` and `GraphConditionIR` is the sole downstream mathematical authority; Graph contracts forbid `exactLatex` authority and private solver/runtime imports.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Renderer-neutral scenes use stable path, region, and point-batch identities. Deterministic snapshot hashing canonicalizes both collection order and object-key order; region boundary references and later point hit-testing never depend on renderer state.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] The pre-Three checkpoint is fixed at 25 rows with 10 visible mixed geometry items, four-times Chromium CPU slowdown, p95 frame interval at most 24ms, no main-thread task above 50ms, editor feedback at most 50ms, first preview at most 150ms, settled scene at most one second, and bounded lifecycle/heap cleanup. Three.js remains forbidden outside its future private adapter and absent until the checkpoint passes.

## 2026-07-15 - CANONICAL-RESULT-V3-ANGLE-QUANTITY1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] V2 remains the default canonical-result authority. V3 is approved only for typed angle quantities that retain the existing compound presentation beside producer-proven magnitude and the unit `deg`, `rad`, or `grad`; no custom gradian MathJSON operator or redundant radians field is allowed.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Browser and desktop History treat result versions 1-3 as current and version 4+ as opaque future data. Canonical-only size fallback remains V1-only; oversized V2/V3 rows fail closed.

## 2026-07-13 - NOTEBOOK-RIBBON-ARCHITECTURE1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Notebook uses a labelled five-group ribbon as its primary authoring surface: Font, Paragraph, Structure, Math, and Edit. Daily prose controls remain visible; Math owns only In text and Separate equation, avoiding duplicate insertion paths.

## 2026-07-12 - NOTEBOOK-AUTHORING-POLISH1

- [agent: codex | model: gpt-5.6 | agent_family: terra | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: terra | attribution_basis: live] Notebook academic structure uses the fixed container catalog Theorem, Definition, Lemma, Corollary, Proof, Example, Solution, Exercise, Hint, Answer, Note, and Warning. Labels and numbers are optional and author-owned; only Hint and Answer may collapse.
- [agent: codex | model: gpt-5.6 | agent_family: terra | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: terra | attribution_basis: live] The Notebook outline indexes headings and academic containers rather than every paragraph or math node. It supports drag/drop plus Move Up/Down, while app-level Workspace Tabs remain the only document-tab system and a saved Notebook library waits for local persistence.
- [agent: codex | model: gpt-5.6 | agent_family: terra | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: terra | attribution_basis: live] The REZANOVA math-authoring keyboard is hosted inside the active Notebook canvas, keeps the active field above its dock, and never changes calculator MathLive layouts. Notebook page content honors UI scale and high contrast while inverse frame sizing prevents 80%/130% scale from shrinking or overflowing the app frame.

## 2026-07-12 - NOTEBOOK-INLINE-MATH-CANVAS1

- [agent: codex | model: gpt-5.6 | agent_family: terra | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: terra | attribution_basis: live] The live Notebook page uses one continuous Tiptap-backed authoring canvas, but the app-owned version-2 Notebook tree remains the durable state and future package contract. Tiptap and ProseMirror runtime objects and native JSON are internal implementation details.
- [agent: codex | model: gpt-5.6 | agent_family: terra | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: terra | attribution_basis: live] Prose-to-math conversion is explicit and non-destructive. Passive detection may suggest likely math, but only author acceptance creates an inline MathLive node with preserved source; display math is inserted explicitly, and placement conversion never gives Notebook solver or Order of Execution authority.

## 2026-07-12 - NOTEBOOK-AUTHORING-KEYBOARD1

- [agent: codex | model: gpt-5.6 | agent_family: terra | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: terra | attribution_basis: live] Notebook owns an adaptive authoring keyboard layered over its active MathLive field. The bottom dock and floating quick-template toolbar call only the Notebook coordinator's public insert/command surface; they do not alter shared MathEditor behavior, global MathLive layouts, calculator key support, or execution authority.
- [agent: codex | model: gpt-5.6 | agent_family: terra | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: terra | attribution_basis: live] Curated Notebook symbols are classified as `authoring-runnable`, `document-only`, or hidden. Document-only notation remains available for mathematical writing but disables `Open in Tool`; unsafe raw HTML/link commands stay hidden. Support classification is a launch-safety policy, not a claim that Notebook solves or validates authored mathematics.

## 2026-07-11 - NOTEBOOK-MATH-FIELD-ACTIVATION1

- [agent: codex | model: gpt-5.6 | agent_family: terra | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: terra | attribution_basis: live] Notebook owns its math-field activation and keyboard focus boundary. Notebook fields use MathLive with manual virtual-keyboard policy, hide the built-in keyboard toggle only under the Notebook page, and expose insertion/command operations through a local active-field coordinator without changing shared MathEditor or global MathLive layouts.
- [agent: codex | model: gpt-5.6 | agent_family: terra | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: terra | attribution_basis: live] The Notebook active field remains registered across ordinary focus transfer to custom authoring controls and is released only when its field unmounts or another Notebook math field activates. This supports focus-safe custom keyboard input without mounting a second execution authority.

## 2026-07-11 - NOTEBOOK-RICH-DOCUMENT-MODEL1

- [agent: codex | model: gpt-5.6 | agent_family: terra | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: terra | attribution_basis: live] Notebook rich authoring uses a versioned app-owned JSON document tree. Tiptap and ProseMirror remain replaceable editor implementation details; their runtime objects and package-native JSON types do not become Notebook package, persistence, Surface Protocol, or external contracts.
- [agent: codex | model: gpt-5.6 | agent_family: terra | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: terra | attribution_basis: live] Version-1 Notebook drafts migrate one way into version 2. Valid accepted inline spans become structured inline math, while stale or misaligned spans fall back to the unchanged original prose rather than losing or rewriting author text.
- [agent: codex | model: gpt-5.6 | agent_family: terra | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: terra | attribution_basis: live] Durable Notebook storage is a required later product capability, but the current program adds only an asynchronous persistence port and in-memory contract adapter. Production remains on workspace surface state until History persistence parity is committed and a separate Notebook local-persistence milestone owns its namespace and lifecycle.
- [agent: codex | model: gpt-5.6 | agent_family: terra | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: terra | attribution_basis: live] Notebook editor dependencies must be official open-source packages with MIT, ISC, BSD-2-Clause, BSD-3-Clause, or Apache-2.0 licensing. Tiptap Pro, Cloud, paid, source-available, copyleft, unidentified, collaboration, and unofficial extensions are excluded.

## 2026-07-06 - NOTEBOOK V1 Math-Aware Authoring

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Notebook V1 is a protected app-level page workspace with document tabs, not a singleton and not a calculator-shell mode. Each Notebook tab owns a session-only draft, has null Order of Execution runtime context, suppresses quick inspectors, and uses protected page tab actions.
- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Notebook math-aware prose is the flagship V1 behavior. Text blocks preserve original prose, store rich text marks separately, detect likely math spans for review, and accept inline MathLive-capable spans with normalized LaTeX without destructive rewriting.
- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Notebook math editor blocks are authoring/launch surfaces only. V1 may send supported math to Calculate or Equation, but Notebook does not run jobs, own solver authority, attach raw History, persist across restart, or implement import/export packages.
- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Future Notebook/import-export packages must use compact evidence snapshot contracts and must not serialize raw History, Variables, Display block trees, MathJSON trees, solver objects, Order of Execution envelopes, diagnostics, host commands, local paths, app-state schemas, or executable code.

## 2026-07-06 - CALCULUS-LIMITS-READBACK-SANITY2

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Limit conditional case answers render as direct `L = cases` math for up to six rows; larger case answers keep the compact Formula Viewer handoff. Limit-owned method cards should use structured text/math parts for rewrite evidence so prose such as common-denominator or conjugate rewrites does not appear as glued math text.

## 2026-07-06 - COMPLEX-EQUATION-POLYNOMIAL-HARDENING1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] The five-move Complex Equation hardening track stays TypeScript-only: no MPSolve, WASM, or GPL solver dependency is bundled or required. The Complex numeric polynomial path may describe `global-polynomial` scope with hardened residual, backward-error, root-slot, cluster, and pole evidence, but must not call those approximate results formal certificates.

## 2026-07-05 - COMPLEX-EQUATION-NUMERIC-CONTRACT1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Complex Equation benchmark support must now state its evidence scope in the corpus ledger. `global-polynomial` may claim all complex roots only through the polynomial evidence path; `bounded-region` may be marked `supported` only with contour-verified evidence and matching contour/candidate counts; `locus-deferred` records cannot be marked supported. These evidence fields are optional ledger metadata and do not expand public `DisplayOutcome`.

## 2026-07-05 - LINEAR-ALGEBRA-RUNTIME-SEAM-AUDIT0

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Linear Algebra must eventually expose a public runtime seam for Matrix/Vector paste naturalization, editor canonicalization, request construction, named-value snapshots, replay helpers, and typed Equation handoff. Validator hardening for `workspace-runtime-request-boundary` is deferred because app runtime currently imports private Linear Algebra internals and the public seam does not yet exist. This `0` milestone intentionally changes no runtime code, request schemas, capability ids, workers, or validators.

## 2026-07-03 - CALCULUS-LIMITS-ASYMPTOTIC-TERM-IR1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Limits frontier symbolic work starts with an internal asymptotic term/series IR instead of route-local mini-algorithms. V1 caps are Taylor order `10`, branch drivers `2`, and displayed conditional rows `12`; live evaluation remains unchanged until later orchestration gates adopt the IR.

## 2026-07-03 - CALCULUS-LIMITS-CONDITIONAL-CASE-SURFACE1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Limits conditional answers use a Limits-owned helper that emits capped `L\in\begin{cases}...\end{cases}` LaTeX plus normal detail-section proof rows. Conditional branching remains capped at `12` rows and `2` symbolic branch drivers, and live limit evaluation is not widened until later route adoption gates.

## 2026-07-03 - CALCULUS-LIMITS-FINITE-RECURSIVE-LEADING-TERMS1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Limits finite local symbolic evaluation may now use a recursive leading-term route that preserves target-free symbolic coefficients and exact finite answers without requiring numeric approximations. The route is Limits-owned and remains capped to local carriers, products, quotients, integer powers, sums/cancellations, and selected standard compositions; broader infinity scales, Piecewise, abs side behavior, MRV-lite, and Gruntz remain deferred.

## 2026-07-03 - CALCULUS-LIMITS-INFINITY-SCALE-TERMS1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Infinite-target Limits may now use a direct scale-comparison route before L'Hospital/numeric fallback. V1 compares numeric-coefficient powers/roots, logarithms, iterated logarithms, linear exponentials, products, quotients, and dominant sums; it is a capped pre-Gruntz scale engine, not full MRV/Gruntz or broad symbolic-coefficient branching.

## 2026-07-03 - CALCULUS-LIMITS-REWRITE-CANCELLATION-SPINE1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Limits cancellation rewrites now route through a single Limits-owned retry spine before algorithm-specific leading-term or scale comparison. V1 centralizes finite common-denominator rewrites, positive-infinity radical conjugates, and safe log/power transforms; it does not import Equation solving logic or become a broad algebra engine.

## 2026-07-02 - EQUATION-SOLVER-CARD-CREDIBILITY-CONSISTENCY1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Equation Real mode must not display complex-only roots; it should stop with no-real-root guidance and point to Complex On. Numeric/detail card taxonomy must keep hard domain facts, periodic structure, probe evidence, search diagnostics, breakpoints, extraneous candidates, and confidence wording in their own categories, with target-dependent branch guards scoped to branch/candidate details when safe.

## 2026-07-02 - PAGE-SURFACE-CHROME-SCALE-PREVIEW1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] App chrome remains outside page/workspace scaling: Workspace Tabs stay top-anchored and unscaled, while active page surfaces receive their own UI scale, math scale, result scale, and high-contrast styling. Settings symbolic rewrite previews should use a readable input/output preview card rather than a tiny strip.

## 2026-07-02 - HISTORY-ROW-NOTATION-PREFERENCES1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] History row notation is user-configurable separately for the quick inspector and full page. Quick inspector rows default to Rendered Math for readability under the existing 20-row cap; full page ledger rows default to LaTeX for responsiveness. Rendered Math row modes require an explicit warning confirmation and remain bounded to capped quick rows or visible virtualized full-page rows.

## 2026-07-01 - CALCULUS-LIMITS-AUDIT0

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Differentiation expansion is paused until Vector/Matrix are upgraded enough for symbolic vector/matrix outputs and multivariable readback. Gradient, Jacobian, Hessian, divergence, curl, and Laplacian should not be implemented as scalar/list-only Calculus features before that foundation exists.
- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Limits are the next active Calculus lane. The first Limits work should start from source/UX and request/variable parsing over the existing finite/infinite heuristic baseline, then add route preflight before widening L'Hospital, series, squeeze, or Gruntz-style algorithms.
- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] User-facing Limit UX should call the entered math a "limit expression"; "request" remains acceptable only for internal type/function names.
- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Limit variable mismatches are advisory stops, not auto-corrections. If the approach variable is absent from the expression body, Calculus must not evaluate by treating the expression as constant or substituting stored values; it should suggest a corrected expression when one body variable is clear.
- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Limit failure explanations belong in existing Display `detailSections`; the primary error card should stay concise, and no new public Display schema is needed for proof/explanation cards.
- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Limit side-behavior explanations are educational readback over existing finite-limit evidence. They must not change final Answer ownership, auto-correct user input, or claim broader theorem-prover coverage.
- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Natural Limit evaluation is classifier-planned: supported route kinds may run exact symbolic evaluation, malformed/too-complex/unsupported routes stop with diagnostics, and numeric fallback is route-owned rather than globally attempted. Direct-substitution and finite-pole side-evidence plans are the first routes allowed to fall back.
- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Exact local algebra for Limits is a Limits-owned adapter over existing symbolic simplification/local-equivalent/asymptotic primitives. It must not import Equation solving logic or become a separate algebra engine.
- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Indeterminate transforms for Limits are bounded pattern routes with explicit method details. They may use safe product/log and log-power rewrites, but must not become a broad theorem prover or a replacement for Taylor/Gruntz-style asymptotics.

## 2026-07-01 - EQUATION-EXACT-BRANCH-CORRECTNESS1

- [agent: codex | model: gpt-5.5] Real Exact `|F|=R` is a guarded branch family: positive comparisons preserve both branches, exact-zero comparisons collapse to the single branch, negative comparisons stop as no-real-solution cases, and `sqrt(F^2)` / `sqrt((F)^2)` must route through absolute-value semantics before generic power lifting.

## 2026-07-01 - SURFACE-SPEC-EXAMPLES1

- [agent: codex | model: gpt-5.5] Surface Protocol hostless v1 has an internal-agent contract reference backed by canonical fixtures. The old `SURFACE0` audit now reflects the landed hostless spine while keeping mounting, Graphing, event/query pagination and cursors, History, Variables, Model Context Protocol adapters, plugins, remote compute, and external software development kit behavior deferred until dedicated milestones.

## 2026-07-01 - SURFACE-CONTRACT-FIXTURES1

- [agent: codex | model: gpt-5.5] Surface Protocol v1 has canonical hostless contract fixtures for the current capability manifest, current-result query, safe-settings query, lifecycle event, and structured failure outputs. Fixtures must stay JSON-serializable and match live helpers, and they remain Surface contract fixtures rather than broad math golden cases.

## 2026-07-01 - SURFACE-POLICY-VOCAB-REGISTRY1

- [agent: codex | model: gpt-5.5] Surface Protocol field exposure and result-summary vocabulary classification now live as internal policy registries, not DTO response metadata. Current DTO fields are classified exactly once, blocked future/internal areas remain sensitive-gated or internal-forbidden, and existing fact/count vocabulary is cataloged without exposing richer facts, History, Variables, Graphing, mounting, host commands, plugins, remote compute, or external software development kit behavior.

## 2026-07-01 - SURFACE-CONFORMANCE-TESTS1

- [agent: codex | model: gpt-5.5] Surface Protocol has a dedicated conformance gate. `test:surface-protocol` must validate the production import boundary and runtime DTO serialization/no-leak behavior, and `test:gate` must include it before broader repo gates. The sanctioned Order of Execution dependency remains limited to the read-only event adapter; app-state schemas, React/DOM objects, Display block trees, solver objects, MathJSON trees, local paths, host commands, History payloads, Variables payloads, Graphing, mounting, plugins, remote compute, and external software development kit surfaces remain outside the boundary.

## 2026-07-01 - SURFACE-QUERY1

- [agent: codex | model: gpt-5.5] Surface Protocol queries are pure snapshot-input infrastructure, not app-global facades. The first query set is limited to current result summary, workspace info, and safe settings with optional angle unit for Calculate and Equation; History, Variables, host commands, mounting, Graphing, tabs, plugins, remote compute, raw Display schemas, and external software development kit behavior remain outside the query surface.

## 2026-07-01 - SURFACE-EVENT-ADAPTER1

- [agent: codex | model: gpt-5.5] Surface Protocol may observe the Order of Execution event outbox only through a read-only curated adapter. The adapter maps job started, result committed, stale result dropped, job cancelled, and job failed lifecycle facts for Calculate and Equation into Surface DTOs, while dropping host selection, preflight, unsupported workspaces, raw payloads, diagnostics, plan/host internals, host commands, mount behavior, Graphing, History, Variables, plugins, remote compute, and external software development kit surfaces.

## 2026-07-01 - SURFACE-VERSIONING-ERRORS1

- [agent: codex | model: gpt-5.5] Surface Protocol v1 is the only supported protocol version in the hostless spine. Version, workspace, query, and field validation must return structured Surface result/error DTOs instead of throwing, and unsupported Graphing, History, Variables, tabs, mount, commands, plugin, remote-compute, or external software development kit requests must fail closed until dedicated milestones authorize them.

## 2026-07-01 - SURFACE-CAPABILITY-MANIFEST1

- [agent: codex | model: gpt-5.5] Surface Protocol capability exposure is locked to Calculate and Equation for the first hostless spine. The manifest may advertise compact result summaries, lifecycle events, and current-result queries, while host commands, mounting, History, Variables, Graphing, tabs, plugins, remote compute, and external software development kit surfaces remain unavailable until later dedicated milestones.

## 2026-07-01 - SURFACE-DTO-FIREWALL1

- [agent: codex | model: gpt-5.5] Surface Protocol now begins as hostless infrastructure under `src/lib/surface-protocol/`. The first DTO firewall exposes versioned compact result summaries and wrapper DTOs for Calculate and Equation only, mapped from committed `DisplayOutcome` values without exposing Display block trees, solver/runtime objects, diagnostics, History, Variables, Graphing, mount contracts, host commands, plugins, remote compute, or an external software development kit.

## 2026-07-01 - EQUATION-NUMERIC-KERNEL-CONTRACT1

- [agent: codex | model: gpt-5.5] Equation real numeric root refinement now has an internal kernel contract before the ITP replacement. The contract reports method id, interval, candidate root, residual, termination reason, iteration count, and evaluation count while preserving the existing Brent-Dekker-style behavior for user-visible interval/manual/auto-search routes. Future numeric algorithms must plug into this contract instead of adding route-local refinement loops.

## 2026-07-01 - EQUATION-NUMERIC-ITP-KERNEL1

- [agent: codex | model: gpt-5.5] Equation numeric interval/manual/auto-search sign-change brackets now refine through ITP as the maintained real bracketed root kernel. TOMS 748 remains off the planned path unless future benchmarks prove a specific need, and route-local Brent-Dekker/TOMS variants should not be introduced outside the kernel contract.

## 2026-07-01 - EQUATION-NUMERIC-SEGMENTATION-HARDEN1

- [agent: codex | model: gpt-5.5] Equation numeric interval/manual/auto-search now uses bounded segmentation probes for solved denominator exclusions, log boundaries, even-root/fractional-power boundaries, affine trig poles, and sampled discontinuity hazards. Hard domain facts stay in `Domain and Exclusions`; sampled and segmentation evidence stays in diagnostic detail sections; excluded boundary candidates remain validation-visible extraneous evidence instead of accepted roots.

## 2026-07-01 - EQUATION-NUMERIC-POLYNOMIAL-ABERTH1

- [agent: codex | model: gpt-5.5] The shared numeric polynomial root helper now uses Aberth-Ehrlich for degree-3+ simultaneous complex roots while keeping direct linear/quadratic paths. Equation deterministic numeric fallback still filters visible output to validated real roots, preserves denominator validation, and keeps Complex numeric Equation display deferred.

## 2026-07-01 - EQUATION-NUMERIC-PRECISION-CONDITIONING1

- [agent: codex | model: gpt-5.5] Equation numeric precision v1 is diagnostics-only. Polynomial roots report coefficient scale, nearest-root separation, close/repeated-root signals, residuals, and higher-precision guidance when risk signals appear; interval/manual/auto-search reports search/segment complexity and narrower-interval or higher-precision guidance for discontinuity-heavy cases. Arbitrary-precision execution remains deferred.

## 2026-07-01 - EQUATION-NUMERIC-GOLDEN-TRACE-HARNESS1

- [agent: codex | model: gpt-5.5] Equation numeric route changes now require a golden trace harness to protect route/method/root/residual/detail evidence with soft timing and count guards. The harness is unit-level and should remain stable enough for CI; hard per-fixture performance budgets remain deferred.

## 2026-06-30 - PUBLIC-IDENTITY-LOCK1

- [agent: codex | model: gpt-5.5] The primary current-facing public identity is `REZANOVA CLASSWIZ CALCULATOR`. `Calcwiz` and `Classwiz` are allowed as friendly aliases, but they must not replace the primary identity in release/GitHub positioning. Technical slugs such as `calcwiz-desktop`, `calcwiz_desktop`, and `com.ahmed.calcwizdesktop` stay unchanged for compatibility, and historical memory keeps old names as evidence rather than being rewritten.

## 2026-06-30 - PUBLIC-OVERVIEW-REFRESH1

- [agent: codex | model: gpt-5.5] Public overview wording must separate current bounded capabilities from future roadmap claims. Current-facing docs may claim Linux-first preview status, guided workspaces, Workspace Tabs, live Order of Execution, Formula Viewer, bounded Equation exact/numeric progress, bounded Calculus integration progress, English-only Language infrastructure, bounded Risch-Norman/Rothstein-Lazard-Rioboo-Trager adoption, and the first `e^(quadratic)` non-elementary certificate family. They must keep Graphing, Spreadsheet, full Settings/History/Variables pages, Surface Protocol, plugins, external software development kit, Complex numeric roots, Complex locus/set output, higher-order/mixed derivative evaluation, full Risch certificates, named special-function readback, and Arabic/right-to-left localization future-only until dedicated milestones make them live.

## 2026-06-30 - SURFACE0

- [agent: codex | model: gpt-5.5] Surface Protocol is a future external embedding/integration contract, not internal workspace surface state, Order of Execution, Supercarrier, a plugin system, an external software development kit, network/remote compute, or Graphing. First implementation must be DTO-firewall-first and read-only over existing committed state and Order of Execution lifecycle facts. Graphing is deliberately excluded from early Surface work until numerical solving, domain facts, discontinuities, branch behavior, local/global completeness wording, and locus/set semantics are stable enough to support it honestly.

## 2026-06-30 - NUMERIC-METHODS-DOMAIN-POLICY-WIDENING1

- [agent: codex | model: gpt-5.5] Cross-domain numeric policy is locked as contracts plus neutral primitives, not shared orchestration. Equation owns equation root solving, branch/root validation, target protection, stored-value consent, and Equation numeric readback. Statistics owns simulation, resampling, confidence intervals, and Monte Carlo workflows. Limits own directional/local approach semantics and asymptote confidence. Differentiation owns derivative-at-point approximation, finite-difference policy, step-size adaptation, and error estimates. Calculus owns quadrature, improper-integral policy, and integration-specific safety/readback; the integration pipeline Layer 5 numeric fallback is adaptive quadrature for definite integrals and must not import Equation root-solving orchestration.

## 2026-06-30 - EQUATION-NUMERIC-SEARCH-UX-DISCIPLINE1

- Equation nonlinear numeric fallback should search progressively and stop after a wider pass adds no new accepted roots or unique extraneous values; it must still state bounded-search limits and never claim global completeness.
- Numeric fallback diagnostics should separate hard `Domain and Exclusions` facts from sampled `Domain Probe` evidence and `Search Diagnostics`; representative probe/search/extraneous values are capped by the existing Detailed Facts policy.
- Periodic numeric guidance should surface the existing Numeric Interval Solve panel without auto-running and without adding another solve action; normal Equation Solve/Run remains the only execution entry.

## 2026-06-29 - SHARED-ALGEBRA-COEFFICIENT-DOMAIN1

- The RN MathJSON coefficient field is now a domain-neutral symbolic primitive for exact-rational plus target-free symbolic coefficients, with RN wrappers kept for compatibility.
- This extraction does not create an Equation consumer and does not move LRT/Hermite/RN orchestration out of Integration. Future Equation use must come only through Equation-owned routes/readback.

## 2026-06-29 - SHARED-POLYNOMIAL-SQUAREFREE-RESULTANT1

- Shared symbolic-polynomial primitives may support bounded parse/build, derivative, exact division, squarefree readiness, and resultants over the shared coefficient domain.
- This does not widen Equation polynomial elimination or make resultants product-facing. LRT/RN consumers must opt in later from Integration-owned routes.

## 2026-06-29 - ALGEBRAIC-ROOT-DESCRIPTOR1

- Future LRT algebraic logarithm readback should use named root descriptors such as `\alpha_i` with local definitions, not raw `RootOf` in the main answer.
- The descriptor helper is primitive/readback infrastructure only. Equation must not consume or display these descriptors unless a later Equation-owned route explicitly opts into shared primitive use.

## 2026-06-29 - RN-HERMITE-REDUCTION-GENERALIZE1

- RN Hermite rational-correction adoption is capped at symbolic denominator degree `8` and denominator power `3` for this LRT-prep batch.
- This milestone may prove exact rational derivative corrections, but LRT logarithmic completion and algebraic-log descriptor use remain separate later milestones.

## 2026-06-29 - RN-LRT-LOG-PART-SUBSTRATE1

- The first LRT logarithmic-part substrate is behavior-invisible and may construct bounded Rothstein/Lazard-Rioboo-Trager resultants plus named-root/formal-gcd log evidence for squarefree rational residuals.
- Exact denominators and target-free symbolic numerator coefficients are in scope. Symbolic denominator coefficients stop before algebraic coefficient reduction; that requires a later explicit algebraic-extension/LRT widening step.

## 2026-06-29 - EQUATION-REAL-PERIODIC-INTERVAL-NUMERIC1

- Unsupported periodic/trig numeric fallback is interval-first. When exact symbolic periodic routes miss and no numeric interval is provided, Equation returns guidance asking for finite real bounds instead of searching default windows.
- Numeric interval results are local to the chosen interval and must say so; they must not claim all real roots for periodic families.
- Numeric interval solving is selected-target aware for Equation symbolic inputs, including non-`x` targets, and carries domain/exclusion evidence such as trig poles, denominator exclusions, and sampled discontinuities in detail sections.

## 2026-06-29 - EQUATION-REAL-NONLINEAR-NUMERIC-SEARCH1

- Equation Real nonlinear numeric fallback is allowed only after supported exact symbolic routes, Real formula fallbacks, and deterministic polynomial/rational numeric fallback miss.
- Automatic bounded windows are allowed for numeric-ready non-periodic nonlinear or discontinuity-heavy real equations; the route must state that the search is bounded and not a proof of all roots outside the searched range.
- Periodic/trig numeric fallback remains interval-first and must not use these automatic global-looking windows.
- Candidate output remains approximate real roots with residuals, domain/exclusion facts, searched-window evidence, and rejected-candidate details attached to result detail sections rather than global `Valid When`.

## 2026-06-29 - CALCULUS-DERIVATIVE-EDITOR-SOURCE1

- Guided Calculus derivative and derivative-at-point screens use the shared Calculus main editor as the editable body source. The editor value is only `f(x)`; fixed derivative context and generated `d/dx(...)` request LaTeX stay derived from workspace state.
- The compact Calculate derivative workbench keeps its separate lower math fields. This decision applies only to guided Calculus.
- Integration and Laplace main-editor ownership remains unchanged.

## 2026-06-29 - CALCULUS-DERIVATIVE-SINGLE-RESULT1

- Guided Calculus derivative and derivative-at-point results are owned by the structured Display `Answer` block. The Display expression preview and derivative `Resolved form` readback should not duplicate the derivative answer.
- This is a presentation ownership rule only. It does not change solver payloads, Display schemas, History, OOE, Tauri, persistence, or Calculate's compact derivative workbench.

## 2026-06-29 - SYMBOLIC-DIFFERENTIATION-PREFLIGHT1

- Symbolic differentiation now has an internal/test-facing preflight classifier before guided Calculus derivative evaluation. It may classify `direct-symbolic`, `compute-engine-fallback`, `unsupported`, `too-complex`, or `malformed`.
- Normal guided Calculus derivatives must stop with controlled messages for unsupported, malformed, or over-budget symbolic paths. Derivative-at-point may use bounded numeric central difference only after preflight blocks symbolic differentiation and numeric evaluation succeeds.
- Preflight route/cost evidence is not a public Display schema, History schema, OOE capability, worker-host, or visible strategy-metadata change.

## 2026-06-29 - RISCH-NORMAN-OUTPUT-HYGIENE1

- Risch-Norman generated exact antiderivative LaTeX must be notation-safe before Display: malformed sign sequences, negative fraction placement, and inline slash divisions such as `(2c)/a` are normalized at the producer/output-hygiene boundary.
- This is scoped readback hygiene only. It does not authorize broad algebraic simplification, public RN strategy metadata, Display schema changes, Calculus result schema changes, or new integration families.

## 2026-06-29 - RN-DEGENERACY-FACTS-BASELINE1

- Risch-Norman generic formulas may keep denominator and pivot requirements as visible `Valid When` facts until a dedicated branch milestone implements a special case. This includes symbolic trig product-to-sum denominators, exp-sincos pivots, positive-base exponential facts, affine-log domain/slope facts, and affine rational-correction slope facts.
- Milestones that add runtime tests or baseline assertions must not use an `AUDIT0` name; docs-only/readiness-only milestones may use `0`.

## 2026-06-29 - CALCWIZ-NUMERIC-METHODS-ROADMAP0 Refinement

- The future numeric implementation session is Equation-owned. It may define and enforce shared numeric policies/contracts, but it must not implement Statistics, Limits, Differentiation, or Calculus domain behavior.
- Cross-domain numeric work in this roadmap is limited to contract wording, policy handoff, and boundary enforcement so other agents can own their domain lanes without Equation-session interference.
- Shared numeric primitives may be planned only as toolbox pieces with explicit consumers and must not merge domain solvers, workspace runtimes, OOE capabilities, result schemas, or user-facing workflows.

## 2026-06-28 - CALCWIZ-NUMERIC-METHODS-ROADMAP0

- The numeric roadmap is a prerequisite artifact, not one of the future eight implementation moves. The eight-move session should be planned only after this policy is accepted.
- Equation numeric widening must be a guarded numeric route family, not a weakening of symbolic guarded solving and not silent substitution of unknown parameters. Numeric solving may use stored values only through explicit numeric consent, with the solve target protected and used values shown.
- Shared numeric work should create reusable primitives/toolbox pieces while leaving orchestration domain-owned: Equation owns roots, Calculus owns quadrature/local calculus numeric policy, Statistics owns simulation/resampling/Monte Carlo, Limits own approach semantics, and Differentiation owns derivative-approximation policy.
- Cardano/Ferrari remain symbolic exact formula/readback routes. Numeric degree-3/4 solving should use numeric polynomial/root methods or guided polynomial numeric fallback, and future step-by-step output should not default to Cardano/Ferrari derivations for simple numeric cubic/quartic cases.
- The future eighth implementation move is reserved for widening numeric policy to Statistics, Limits, Differentiation, and related Calculus numeric surfaces after the Equation real numeric foundation is planned.

## 2026-06-28 - RISCH-NORMAN-FOUNDATION-CHECKPOINT0

- The current Risch-Norman foundation is closed as a bounded internal proof-based layer for the approved target-free symbolic scope. It may continue using existing public strategy labels, visible facts, coefficient-field arithmetic, bounded linear solving, and guarded dispatch after Tier-I routes miss.
- This checkpoint does not claim full Risch, a complete literature-grade Risch-Norman implementation, non-elementary certificates, broad symbolic partial fractions, symbolic degeneracy case splitting, or public `risch-norman` metadata.
- Future RN widening should first add symbolic degeneracy facts and a shared ansatz orchestrator before broadening to harder rational/quadratic or tower families.

## 2026-06-28 - RISCH-NORMAN-AFFINE-RATIONAL-CORRECTION-LIFT1

- Affine rational correction is approved only for bounded `P(v)/(a*v+b)^k` with polynomial degree `<=6` and denominator powers `1..3`, using affine substitution rather than broad symbolic partial fractions.
- Existing direct affine reciprocal ownership must stay intact for simple `1/(a*v+b)` cases. Supported target-dependent affine-denominator corrections surface through existing public `partial-fractions`.
- Symbolic quadratic denominators, multiple symbolic factors, broad symbolic rational decomposition, public `risch-norman` strategy labels, and public result/schema/runtime changes remain out of scope.

## 2026-06-28 - RISCH-NORMAN-SYMBOLIC-TRIG-PRODUCTS1

- Symbolic affine product-to-sum adoption is approved only for two `Sin`/`Cos` factors with exact-rational or target-free symbolic affine arguments. Exact direct product-to-sum rules keep precedence.
- The route may emit denominator facts such as `a+c\ne0` and `a-c\ne0`, and structural same-argument products may use direct identities with the affine slope fact. Unresolved symbolic same-slope/degenerate products remain unsupported instead of guessing equality.
- Public behavior stays on existing `direct-rule`; no public `risch-norman` strategy, result schema, Display, History, OOE, Tauri, or persistence changes are introduced.

## 2026-06-28 - RISCH-NORMAN-EXP-SINCOS-MIXED1

- Mixed exponential-sine/cosine RN adoption is approved only for bounded polynomial factors times affine products `e^(a*v+b)sin(c*v+d)` or `e^(a*v+b)cos(c*v+d)` over exact-rational plus target-free symbolic coefficients.
- The internal solver may use the derivative-closed span `e^u(Q(v)sin(w)+R(v)cos(w))`, require visible pivot facts such as `a^2+c^2\ne0`, and surface through existing public `integration-by-parts`. No public `risch-norman` strategy, public result schema, Display, History, OOE, Tauri, or persistence changes are introduced.

## 2026-06-28 - RISCH-NORMAN-LINEAR-COMBINATION1

- Risch-Norman linear combination adoption is all-or-nothing and capped. Each top-level additive term must resolve through existing Tier-I/RN routes after normal whole-integrand routes miss; unsupported terms keep the whole sum unsupported.
- The combiner may preserve a homogeneous existing public strategy, but mixed-strategy sums surface as existing `integration-by-parts`. No public `risch-norman` strategy or result schema is introduced.

## 2026-06-28 - RISCH-NORMAN-LOG-CORRECTION1

- Risch-Norman affine-log adoption may use the affine substitution formula in powers of `u=a*v+b` instead of broad symbolic long division. This keeps symbolic coefficient readback bounded, exact, and fast while still requiring visible `a\ne0` and `u>0` facts.
- Live RN log correction remains an internal fallback surfaced as existing public `integration-by-parts`; it must not introduce a public `risch-norman` strategy or public Calculus result-schema change.

## 2026-06-28 - CALCULUS-INTEGRAND-EQUATION-GUARD1

- Calculus integral requests own expression inputs, not relation/equation inputs. Relation roots such as `Equal`, inequalities, and equation-like parsed trees must stop before symbolic integration routing with the controlled message that integrals expect `f(x)`.
- The controlled relation-input error may pass through the Calculus core, but generic unsupported antiderivatives must continue using the normal Calculus unsupported message. This is not a public result-schema or workspace handoff change.

## 2026-06-28 - EQUATION-COMPLEX-ABS-WRAPPER-POLICY0

- Complex absolute-value wrappers are magnitude/locus equations, not Real sign-split equations. Complex `|F|=R` must not be rewritten as `F=R` or `F=-R`.
- Complex abs wrappers remain unsupported until Calcwiz has set/locus output, especially for affine circle cases such as `|z-c|=r`.
- Complex abs policy tests must continue proving no Real formula sections, `RootOf`, or finite-branch readback leaks into Complex abs outcomes. Complex Off may continue using the existing real-domain absolute-value path.

## 2026-06-28 - EQUATION-COMPLEX-ROOT-WRAPPER-PRINCIPAL-IMAGE1

- Complex root wrappers are principal functions, not all-branch power relations. `sqrt(F)=R`, `root(F,n)=R`, and `A*root(F,n)+C=R` may solve only after isolating the root value, validating or guarding it against the principal-root image, and delegating `F=V^n` to compact Complex-capable routes.
- Exact isolated values outside the principal-root image return a controlled error instead of an empty-set success. Unknown symbolic values carry principal-image facts such as the square-root half-plane condition or the nth-root argument-sector condition.
- Generated Complex Cardano/Ferrari wrapper readback and visible `RootOf` remain blocked. Cubic/quartic root-wrapper carriers are not globally banned, but they need compact non-Cardano/Ferrari route/readback evidence before becoming live.

## 2026-06-27 - EQUATION-COMPLEX-PRINCIPAL-IMAGE-INEQUALITY-SUBSTRATE1

- Complex principal-root image validation uses a reusable fact substrate before visible root-wrapper solving: square roots use the half-plane condition `Re(R)>0 or (Re(R)=0 and Im(R)>=0)`, while nth roots use `R=0 or -pi/n < arg(R) <= pi/n`.
- The first classifier is intentionally exact and narrow. It may prove zero/positive-real constants inside, negative-real constants outside, square-root half-plane exact constants, and simple exact imaginary-axis cases; symbolic or non-obvious exact complex constants remain guarded as unknown.
- This milestone is behavior-invisible. It supplies detail/supplement readback facts for later Complex root-wrapper routes but does not enable `sqrt(F)=R`, `root(F,n)=R`, or affine root-wrapper solving.

## 2026-06-27 - RISCH-NORMAN-EXP-ANSATZ1

- Risch-Norman exponential ansatz solving may use generated positive-base logarithm constants such as `ln(q)` only inside the exponential extension derivative factor and only with visible `q>0` and `q\ne1` facts. User-parsed coefficient-side transcendental expressions remain rejected by the coefficient field.
- Exponential ansatz output remains behavior-invisible until an explicit dispatch probe. Existing Tier-I symbolic by-parts routes keep precedence and public strategy labels.

## 2026-06-27 - RISCH-NORMAN-LINEAR-SOLVER1

- Risch-Norman symbolic linear solving is internal and capped. It may solve bounded square systems over the MathJSON coefficient field and collect visible-ready nonzero pivot facts, but it must not become a determinant API, nonlinear solver, broad simplifier, integration dispatch route, public strategy label, or public result-schema change.

## 2026-06-27 - RISCH-NORMAN-COEFFICIENT-FIELD1

- Risch-Norman coefficient arithmetic is internal and MathJSON-based. It may operate on exact-rational plus target-free symbolic coefficient expressions, collect nonzero denominator facts, and reject decimals, branch-sensitive carriers, selected-variable-dependent coefficients, coefficient-side transcendentals, and zero denominators. It must not become a broad public CAS field or change integration dispatch/public schemas.

## 2026-06-27 - RISCH-NORMAN-SUBSTRATE1

- Risch-Norman Layer 2 starts as internal/test-facing readiness evidence only. The first substrate may profile exact-rational plus target-free symbolic affine exponential, positive-base exponential, sine/cosine-pair, and logarithm candidates, but it must not solve ansatz coefficients, adopt antiderivatives, add a public integration strategy, or change Calculus/Display/History/OOE/Tauri/persistence schemas.
- Approximate decimal coefficients are outside the first Risch-Norman substrate coefficient scope. The substrate must stop them instead of treating them as target-free symbolic assumptions.

## 2026-06-27 - RUBI-TIER1-CLOSEOUT-GATED1 Backend Gates

- Calculus indefinite integration must try Calcwiz symbolic rules before Compute Engine; CE indefinite fallback is lazy and guarded to low-risk selected-variable-only expressions.
- Parameter-heavy selected-variable integrals should return through Calcwiz rules or controlled unsupported output rather than synchronously invoking CE on broad symbolic parameter sets.
- Target-free polynomial direct integration may integrate `P(v)/D` where `D` is target-free with respect to the selected variable and must expose a nonzero-denominator `Valid When` fact.
- Trig-substitution radical adoption remains rule-proof/exact only. No numeric-confidence adoption is allowed for symbolic or branch-sensitive radical results.
- Copy Result in rendered/LaTeX notation modes should copy exact symbolic results as reusable LaTeX, not ASCII/plain visible text. Plain-text notation remains the explicit plain copy path.
- Combined safe relational `Valid When` facts should render as stacked math rows without changing public result or Display schemas.
- Agreed Rubi Tier I is closed for current exact-rational plus target-free symbolic scope. Risch/Risch-Norman, broader symbolic assumptions, full Euler substitutions, and research-grade CAS breadth remain separate future tracks.

## 2026-06-27 - EQUATION-COMPLEX-WRAPPER-ROLE-POWER-POLICY-LOCK1

- Wrapper index/exponent is not the same thing as inner carrier polynomial degree. Tests must keep `\sqrt[n]{F}=rhs` and `F^n=rhs` wrapper inversion separate from the method used to solve the generated carrier equation.
- Current Real wrapper formula behavior remains unchanged: generated inner cubics/quartics may still delegate to Real Cardano/Ferrari when that route is explicitly formula-backed.
- Complex compact higher-index power roots may remain valid through `PrincipalRoot_n(...)\omega_k` special-form readback. Explicit generated Complex Cardano/Ferrari wrapper formula output remains blocked.
- This milestone adds regression evidence only; it does not enable Complex power wrappers, root wrappers, or new solver routes.

## 2026-06-27 - RUBI-TIER1-SYMBOLIC-COEFFICIENT-CATCHUP1

- Tier-I symbolic coefficient integration is allowed only for target-free coefficients relative to the selected integration variable; the selected variable is carried as `integrationVariable` through Calculus integral state, preview, request building, parsing, dispatch, and replay.
- Symbolic coefficient antiderivatives must not be adopted through numeric-confidence verification. Accepted symbolic cases need route-local rule proof plus visible supplement facts, or a genuinely exact backcheck when available.
- Positive symbolic-base exponentials are allowed with visible facts such as `base>0`, `base-1\ne0`, and affine-slope nonzero. Negative, zero, complex-principal, and symbolic-base branch policies remain deferred.
- Calculus may reuse domain-neutral exact-supplement rendering concepts for `Valid When` facts, but must not import Equation-owned branch-domain wrappers directly.
- Decimal output settings apply to approximate/numeric output only; exact symbolic antiderivative lines must stay exact-rational or symbolic.

## 2026-06-27 - EQUATION-COMPLEX-WRAPPER-CATCHUP-POLICY0

- Real wrapper formula coverage is complete enough to begin Complex catchup planning, but Complex wrappers must not be enabled by reusing Real-only formula handoff flags.
- Complex wrapper catchup must be `Complex On + Exact` only, delegate first to existing Complex preimage/algebraic routes where those routes already own branch semantics, and preserve `complexExactForm` readback policy.
- Explicit generated Complex Cardano/Ferrari wrapper payloads are no longer a wrapper roadmap target because that readback would be too large and unreadable. This does not ban all degree-3/4 Complex wrapper solving; compact non-Cardano/Ferrari representations remain valid future candidates.
- Complex square-root/nth-root wrappers remain deferred until principal-root wrapper semantics are locked; Complex absolute-value wrappers remain deferred because they require magnitude/locus output rather than a Real sign split.
- Created the durable roadmap `.memory/research/roadmaps/equation-complex-wrapper-catchup-roadmap.md` for the Complex wrapper catchup track.

## 2026-06-27 - EQUATION-COMPLEX-WRAPPER-BASELINE-LOCK1 + EQUATION-COMPLEX-PREIMAGE-WRAPPER-CATCHUP1

- Complex Exact wrapper catchup now starts only with exact-constant affine preimage shells around one selected-target exp/log/trig carrier. The route isolates the carrier value and delegates the carrier equation itself to existing Complex preimage solving with a wrapper-local degree-2 guard for this first preimage slice, not to Real inverse generation or Real formula handoff.
- Enabled Complex preimage wrappers preserve Complex-owned branch semantics such as `2*pi*i*k`, principal-log nonzero facts, rational denominator exclusions, inverse-trig branch families, and `complexExactForm`; Real range facts and Real `Trig Formula Cases` / Cardano / Ferrari wrapper sections remain out of Complex output.
- Symbolic shells such as `a*ln(F)+c=d` and `a*sin(F)+c=d`, same-argument mixed sine/cosine, trig products, mismatched or nested trig wrappers, numeric-interval routes, and explicit generated Complex Cardano/Ferrari wrapper payloads remain deferred.

## 2026-06-27 - EQUATION-COMPLEX-WRAPPER-READBACK-POLICY-REFINE0

- User decision refined: do not expose explicit generated Complex Cardano/Ferrari wrapper formula readback. The issue is the Cardano/Ferrari formula/readback method, not degree 3/4 roots themselves.
- Degree-3/4 Complex wrapper solving may still be allowed when it can use compact, validated, non-Cardano/Ferrari representations such as factorable branches, special-form `PrincipalRoot`/omega branches, or a future approved compact algebraic-root policy.
- The Complex wrapper roadmap retires `EQUATION-COMPLEX-CARDANO-FERRARI-WRAPPER-CATCHUP1` as an explicit formula-readback milestone. It should not be interpreted as a ban on all higher-degree Complex wrapper solving.

## 2026-06-27 - EQUATION-WRAPPER-INPUT-MIXED-TRIG-AUDIT-BUNDLE1

- Shared MathEditor/app paste canonicalization may rewrite only explicitly grouped top-level quotients inside function arguments, such as `ln((F)/(G))`, into safe LaTeX fractions before MathLive insertion. This does not authorize broad `x/y` parsing or solver-side normalization.
- Real Exact mixed trig formula widening is live only for affine shells around one selected-target trig carrier and same-argument `A*sin(F)+B*cos(F)=C` forms. Generated nonlinear cubic/quartic arguments may delegate to Real Cardano/Ferrari under existing `Parameterized Trig Solve` / `Parameterized Mixed Trig Solve` plus grouped `Trig Formula Cases` readback.
- Complex trig wrappers, nested trig wrappers, trig products, mismatched arguments, target-dependent companions, broad trig identities, Display/OOE/History/Tauri/app-state/schema/copy contract changes, and new visible mixed-trig section labels remain deferred.

## 2026-06-26 - FORMULA-VIEWER-VIRTUALIZATION1

- Formula Viewer virtualization is viewer-only: inline Equation source cards stay compact-first, while the dedicated Formula Viewer tab owns block-level and case-row virtualization for huge formula artifacts.
- Virtualization consumes structured Display blocks, not raw LaTeX parsing as the primary source. Collapsed detail bodies stay unmounted, viewport-plus-overscan items render inside `.formula-viewer-scroll`, and over-budget rows remain per-row opt-in placeholders.
- This is Display/UI/tab infrastructure only; it must not become a solver, Copy Result, To Editor, History, OOE, app-state, Tauri, or persisted schema contract.

## 2026-06-25 - EQUATION-CARDANO-FERRARI-NUMERIC-READBACK-BUG

- Numeric-coefficient Cardano/Ferrari output should not show the symbolic derivation form as the primary answer when all polynomial coefficients are concrete exact rationals and actual exact/numeric roots can be computed. Formula derivation cards remain valuable for symbolic-coefficient cases.
- A follow-up readback/UI milestone should add an `Extraneous solutions` card for candidate-validation rejects, listing each rejected candidate, the original-equation substitution check, and a short rejection reason. This is intended for radical squaring, absolute-value branch splits, and any family where back-substitution eliminates generated candidates.
- No production Cardano/Ferrari readback code changed in the exact-zero wrapper fast-path bugfix.

## 2026-06-25 - EQUATION-NTH-ROOT-WRAPPER-POLICY0

- Future Real Exact nth-root wrapper formulas invert `\sqrt[n]{F(target)}=rhs` to `F(target)=rhs^n`; odd root wrappers allow exact negative RHS and need no nonnegative output fact, while even root wrappers require `rhs\ge0`, stop exact negative RHS as domain-empty, and collapse exact zero to `F=0`.
- `n=2` remains the existing square-root wrapper path. Future non-square nth-root wrapper implementation must add explicit `Root(F,n)` carrier detection, preserve root-output facts as wrapper facts, validate candidates against the original nth-root equation, preserve denominator exclusions from generated rational clearing, and reject target-bearing RHS expressions.
- Complex nth-root wrappers remain deferred until Calcwiz has a principal-branch wrapper policy; Real nth-root wrapper output must use real root/power notation and never Complex `PrincipalRoot`.

## 2026-06-25 - EQUATION-HIGHER-EVEN-POWER-WRAPPER-FORMULA1

- Real Exact one-layer higher even-power wrapper formulas are live only for even integer exponents `4,6,8,10,12`.
- Higher even-power wrappers split `F(target)^n=rhs` into grouped `F=\sqrt[n]{rhs}` and `F=-\sqrt[n]{rhs}` generated formula branches under target-free `rhs\ge0`, collapse exact `rhs=0` to one branch, stop exact `rhs<0` as domain-empty, and preserve denominator exclusions plus local Cardano/Ferrari case facts.
- Existing `n=2` square-power wording and section titles stay stable; Complex even-power wrappers, nth-root wrappers such as `\sqrt[n]{F}=rhs`, nested/mixed algebraic wrappers, and exp/log/trig formula handoff remain deferred.

## 2026-06-25 - EQUATION-ODD-POWER-WRAPPER-FORMULA1

- Real Exact one-layer odd-power wrapper formulas are live only for odd integer exponents `3,5,7,9,11`.
- Odd-power wrappers generate one real odd-root branch `F=\sqrt[n]{rhs}`; exact negative RHS is allowed, exact zero collapses to `F=0`, and symbolic or compound target-free RHS expressions do not receive an `rhs\ge0` fact.
- Complex odd-power wrappers, nth-root wrappers such as `\sqrt[n]{F}=rhs`, higher even powers beyond square-power, nested/mixed algebraic wrappers, and exp/log/trig formula handoff remain deferred.

## 2026-06-25 - EQUATION-ABS-WRAPPER-FORMULA-POLICY0

- Future Real Exact absolute-value wrapper formula solving should use the guarded split `|F(t)|=b -> F(t)=b` or `F(t)=-b` with global wrapper fact `b>=0`; exact negative right sides remain domain-empty and exact zero right sides should collapse duplicate generated branches.
- Absolute-value formula handoff must preserve wrapper branch provenance and formula case/fact scopes through grouped generated formula output. It must not reuse the current single-payload square-root formula promotion as a one-line flag change.
- Complex absolute-value formula wrappers remain deferred because Complex `Abs` is magnitude, not a real sign split.

## 2026-06-25 - EQUATION-GENERATED-FORMULA-VALIDATION1

- Structured generated formula payloads must be blocked at the generated-handoff boundary until a wrapper consumer supplies explicit validation evidence.
- The required evidence is wrapper back-substitution validation, candidate validation against the original generated-wrapper equation, and scoped fact handling for branch/case/wrapper facts.
- Real formula `caseMath` output must not be flattened into unconditional generated roots; case-local and branch-local formula facts remain attached evidence, not global supplements.

## 2026-06-25 - EQUATION-GENERATED-FORMULA-HANDOFF-PAYLOAD1

- Structured generated formula payloads belong in the shared generated branch handoff seam, not in a parallel formula-only lane.
- Legacy generated handoff remains the compatibility path: existing solvers still provide `exactLatex`, extracted solution expressions, and global supplements.
- Formula payloads are internal evidence only in this milestone. They may carry Complex branch rows, Real case rows, route-owned definitions, scoped facts, answer domain, and detail sections, but generated/wrapper Cardano and Ferrari remain non-live until validation is implemented.

## 2026-06-24 - EQUATION-CUBIC-CARDANO-POLYNOMIAL-NORMALIZATION2

- Top-level rational denominator clearing may hand a cleared degree-3 selected-target polynomial to Cardano in Real or Complex Exact.
- Existing rational degree-1/2 solving remains first and unchanged; the Cardano-owned rational helper exists only for cleared cubics and inspection stops.
- Generated-handoff plans must continue excluding `cubic-cardano` until case-local facts, wrapper back-substitution, and validation evidence are designed.

## 2026-06-24 - DISPLAY-CASE-MATH-LAYOUT1

- Real Cardano case answers should use producer-backed structured case rows in Display instead of relying on one large `\begin{cases}` math blob.
- The new `caseMath` render path is internal Display structure only; it is not a persisted `DisplayOutcome`, History, OOE, app-state, Tauri, or copy-contract schema change.
- Canonical `exactLatex` remains the source for Copy Result, To Editor, History, replay, and stored output, while promoted case rows are a readability layer.

## 2026-06-24 - Algorithm Prerequisite Gate

- Nontrivial algorithm milestones must declare their required infrastructure before implementation, including representation, symbolic primitives, facts/assumptions, validation, route evidence, readback/presentation, and tests.
- A future algorithm may proceed only when those prerequisites already exist, are built in a prior milestone, or are built in parallel as part of the same approved milestone.
- If the prerequisite logic is missing, the milestone should stop and record the gap instead of embedding route-local mini-infrastructure that later algorithms will duplicate.

## 2026-06-24 - ANSWER-PRESENTATION-IR1

- Clean Canonical finite-root output now belongs to the internal Equation presentation district when a migrated producer can provide a MathJSON-backed presentation item.
- The first approved implementation slice is carrier follow-on plus parameterized quadratic/polynomial finite branches; the older open question about whether to include parameterized quadratic/polynomial branches is resolved.
- Presentation IR remains producer-side and adapts back to current `exactLatex` and `branchReadback` compatibility surfaces. Display remains a renderer; History, OOE, app-state, Tauri, Calculate actions, periodic families, inequalities, facts, supplements, and detail sections stay out of scope.

## 2026-06-23 - ANSWER-READBACK-OVERRIDE-POLICY1

- Clean Canonical is the finite-root override policy: when a single-target `exactLatexOverride` can be safely decomposed into finite root branches, Equation normalizes those branches and rebuilds canonical `exactLatex` before Display, Copy Result, History, replay, and To Editor consume it.
- Unsafe overrides fail closed and remain raw, including systems, inequalities, periodic families, malformed sets, multiple targets, prose, and nested solution tuples.
- Complex rectangular exact-coordinate readability remains deferred; high-degree rectangular/polar Complex roots may still use exact trigonometric branch notation until a dedicated coordinate/readback milestone exists.

## 2026-06-23 - BRANCH-READBACK-POLISH1

- Finite standalone Equation branch arrays may be normalized producer-side before dedupe, set joining, and `DisplayBranchReadback` creation.
- `exactLatexOverride` remains preserved in v1, so branch rows may be cleaner than an override until a later explicit override policy milestone.
- Periodic `k` families, inequality sets, facts, supplements, detail sections, decimal/approx branch metadata, and Display-side parsing remain outside branch-readback polish.

## 2026-06-23 - COMPLEX-READBACK-POLICY1

- Complex exact-form settings are readback policy, not decoration: `cis` may render `\operatorname{cis}`, while `rectangular` and `polar` must not silently substitute `cis`.
- When compact exact rectangular radical coordinates are not implemented for high-degree Complex roots, exact trigonometric branch notation is the readable fallback rather than fake rectangular output.
- Symbolic principal-branch Complex roots remain deferred until a dedicated branch/root-representation policy can carry branch semantics honestly.

## 2026-06-23 - ANSWER-READBACK-SIGN-NORMALIZATION1

- Equation readback sign cleanup belongs in the producer-side normalization seam before Display, not in Display rendering or solver math.
- The v1 sign policy is intentionally syntactic and local: clean double signs, leading plus signs, exact numeric-fraction signs, and simple negative grouped terms without distributing, factoring, cancelling, extracting radicals, or normalizing `exactLatexOverride`.
- Symbolic fractions such as `\frac{-a}{b}` remain untouched until a broader, fact-aware simplification/readback policy proves them safe.

## 2026-06-22 - SYMBOLIC-SUBSTITUTION-PRIMITIVE1

- Structural substitution belongs in the private Symbolic Primitives district as reusable mechanics, while Equation keeps carrier-elimination solver judgment, route order, stop wording, facts, and readback.
- Substitution v1 is carrier-focused: protected symbols, exact subtree replacement, and carrier power-basis reduction. Stored-variable substitution remains in `algebra/variable-memory` until a later parity milestone proves matching protected-symbol semantics.
- Final-answer readback polishing and normalization should happen after all five Symbolic Primitives are established, not inside the substitution primitive.

## 2026-06-21 - EQUATION-SYMBOLIC-FACTOR-DISCOVERY-FRONTIER1

- Symbolic factor discovery may now extract only common selected-target power factors in zero-form expanded polynomials through degree 12.
- The approved first shape is `x^k * Q(x)=0` with `k > 0`, residual `Q` linear or quadratic, and target-free symbolic residual coefficients. Discovered factors must delegate to existing linear/quadratic selected-target solvers and merge roots through the existing factorable/root readback path.
- Residual degree >2, target-bearing coefficients/denominators/functions, no common target factor, broad symbolic factoring, sum/difference-of-powers factoring, Cardano/Ferrari, visible implicit roots, numeric Exact fallback, OOE/Display/History/app-state/Tauri changes, graphing, step-by-step, and DAG/search graphs remain deferred.

## 2026-06-21 - EQUATION-SYMBOLIC-CARRIER-COEFF-FRONTIER1

- Special-form carrier solving may now use target-free symbolic outer quadratic coefficients through the existing Equation `SymbolicTargetPolynomial` seam.
- The widened path applies only to Real Exact pure or affine carriers through total target degree 12, producing symbolic carrier-root branches plus discriminant and even-root nonnegativity facts through existing readback/fact surfaces.
- Target-bearing carrier coefficients, non-affine carrier discovery, Complex high-degree carrier roots, broad symbolic factoring, visible implicit roots, numeric Exact fallback, Display/History schema changes, OOE/app-state/Tauri changes, graphing, step-by-step, and DAG/search graphs remain deferred.

## 2026-06-21 - EQUATION-SPECIAL-FORM-ROOTS-FRONTIER1

- Real Exact affine selected-target powers may solve through degree 12; Complex selected-target power isolation remains capped at degree 4 until complex branch/readback policy is widened separately.
- Special-form carrier substitution starts exact-rational and pure-carrier only: quadratic in `u=x^n`, total degree `2n <= 12`, no symbolic carrier coefficients, and no affine/non-pure carrier substitution.
- The new `special-form-roots` route is an internal/test-traced selected-target family after factorable polynomial and before generic fallbacks; it is not broad automatic factoring, Cardano/Ferrari, visible implicit roots, numeric fallback, or schema/runtime work.

## 2026-06-21 - EQUATION-EXPANDED-FACTOR-FRONTIER1

- Expanded exact-rational factorable solving may use a 12 target-degree-slot frontier only through the Equation factorable path and only when Algebra can reduce the polynomial to supported real linear/quadratic pieces.
- The Algebra bounded factorization APIs keep degree 4 as their default for existing callers; wider degree settings are explicit opt-ins.
- Unsupported/unfactored expanded exact-rational degree 5-12 polynomials must stop honestly as unsupported expanded polynomials, while degree >12 remains a `degree-limit`. This is not broad automatic factoring, general high-degree formula solving, implicit-root display, or numeric fallback.

## 2026-06-21 - EQUATION-FACTORABLE-DECOMPOSITION-FRONTIER1

- Explicit zero-product factorable solving now has its own bounded frontier cap: 12 total target-degree slots after factor multiplicity.
- The widened cap applies only to explicit products whose target-containing factors each delegate through existing linear or quadratic selected-target solvers. Individual cubic/quartic factors remain unsupported in this path.
- Expanded and exact-rational factorable polynomial solving remains capped at the existing degree-4 behavior. This milestone is not broad automatic factoring, Cardano/Ferrari, implicit-root display, numeric fallback, or a DAG/search-graph step.

## 2026-06-21 - EQUATION-FRONTIER-SOLVER-ROADMAP0

- Equation moves from search-discipline/substrate preparation into frontier capability planning. The next frontier should expand algebraic solving capability through typed substrate and family-owned consumers, not by adding more router scaffolding.
- The first recommended implementation lane is `EQUATION-FACTORABLE-DECOMPOSITION-FRONTIER1`: use product decomposition, root representation, branch/domain facts, and compact readback to widen factored/product exact solving before broad automatic factoring or general cubic/quartic formulas.
- DAG/e-graph/search-graph work remains deferred until route/branch/factor/elimination traces show concrete repeated transformation-state pressure.

## 2026-06-21 - EQUATION-FRONTIER-HANDOFF-RESYNC0

- The external solver-phase handoff remains valuable for sequencing discipline, but current repo state supersedes stale pointers: search/profile/route/trace work is already implemented enough, and `semantic-planner.ts` is not the live selected-target family router.
- Cap tables in the handoff are context, not implementation approval. Current cap evidence still says most caps are algorithm/readback/semantic boundaries, so frontier work should add representation and algorithms before changing cap constants.
- Source mirrors stay context-only. Calcwiz should translate mirror lessons into Calcwiz-native seams, facts, readback, and tests; no source-mirror runtime dependency, copy, parity obligation, or hidden backend delegation is approved.

## 2026-06-20 - EQUATION-EXACT-ISOLATE-SEMANTICS-AUDIT0

- Superseded on 2026-06-21 by `EQUATION-ANSWER-MODE-SIMPLIFICATION0`: do not preserve `approximate` as an active persisted/user-facing answer mode; keep compatibility for legacy records/settings only.
- `Approximate` is the numeric/decimal lane only. It should not become a fallback bucket for ugly or large exact forms, and it should require numeric values for every non-target symbol before numeric root search.
- `Isolate` means bounded selected-target rearrangement/formula output. It may return an isolated formula or isolated equation without proving full exact root closure.
- `Exact` may use isolation helpers internally, but should claim success only when terminal exact evidence exists: finite roots, factor-derived roots, branch/principal/periodic families, guarded inequality sets, complex exact branches, symbolic-parameter formulas, domain/exclusion facts, or candidate validation. Irrational radicals such as `\sqrt{2}` are Exact when represented symbolically.
- Approximate missing-parameter stops should point to the missing numeric values and the Variables path, not merely list symbols as generic parameters.
- The next safe implementation is behavior-preserving answer semantics tagging through existing outcome metadata such as `solutionKind`, not a visible UI wording pass or solver rewrite.

## 2026-06-21 - EQUATION-ANSWER-MODE-SIMPLIFICATION0

- Active Equation answer modes should be simplified to `Exact` and `Isolate`; `Approx` should be removed as a persisted/user-facing answer mode in the next implementation.
- Numeric Interval Solve should remain as an explicit contextual numeric route/tool, not as ordinary answer-mode state. It should surface from exact-stop advisories, interval/periodic suggestions, or an explicit fallback affordance, not on every symbolic equation by default.
- Header `DECIMAL`, approximate digits, and numeric notation settings are display-output controls and stay separate from Equation answer-mode simplification.
- Legacy `equationAnswerMode: 'approximate'` settings/history need compatibility handling: settings should sanitize to `exact`, while old numeric interval history entries should remain readable/replayable as numeric-route entries.
- The uncommitted `EQUATION-ANSWER-SEMANTICS-TAGS1` source/test slice was removed from the worktree before commit; keep its Exact/Isolate produced-answer tagging idea and precise Variables guidance only after removing the Approximate answer-mode framing.

## 2026-06-21 - EQUATION-ANSWER-MODE-SIMPLIFICATION1

- `Approx` is no longer an active persisted/user-facing Equation answer mode. Active settings and workspace controls expose only `Exact` and `Isolate`.
- `Numeric Interval Solve` is a contextual numeric route/tool, not an answer-mode setting. New numeric launches use the route's `numericInterval` payload and never write `equationAnswerMode: 'approximate'`.
- Legacy `approximate` remains accepted only in compatibility seams: low-level request/history/display schemas can read it, while web and Tauri settings sanitize it to `exact`.
- Successful numeric interval results keep `solutionKind: 'approximate-numeric'` as route-result metadata for now. This describes numeric-route output, not a persisted Approx answer mode.
- Contextual Numeric Solve visibility is locked for this slice: the affordance is hidden on ordinary symbolic input, stays available when already open, appears for periodic suggested intervals, and auto-opens only for error advisories that explicitly suggest numeric solving.

## 2026-06-21 - EQUATION-NUMERIC-INTERVAL-ROUTE-REPAIR1

- Numeric Interval Solve eligibility and panel visibility are separate states. Eligible exact stops may show the `Numeric Solve` opener, but do not auto-arm numeric execution.
- Once the Numeric Interval panel is explicitly open, header Run, F1, and EXE launch the numeric interval route. When the panel is hidden, those controls launch symbolic Exact/Isolate solving.
- The Numeric Interval panel is configuration-only for interval fields and no longer owns a separate `Run Numeric Solve` button.

## 2026-06-21 - EQUATION-NUMERIC-INTERVAL-GUIDANCE1

- Exact periodic `suggestedIntervals` surface inside the Numeric Interval panel as click-to-fill Start/End actions only. They must not auto-run and must not change Subdivisions.
- Numeric Interval Solve copy should state that it searches local real roots in the chosen interval and validates candidates against the original equation; it must not promise all roots are found.
- No-root and rejected-candidate guidance should point users toward suggested intervals, shifted/widened bounds, more subdivisions for dense or nested periodic cases, and discontinuity/domain-hole awareness.

## 2026-06-21 - EQUATION-NUMERIC-INTERVAL-STABILITY1

- Numeric Interval Solve uses bounded adaptive refinement, not open recursion and not an all-roots guarantee.
- The first stability strategy is combined and deterministic: refine sign-change, near-zero, local-minimum, steep-jump, discontinuity-like, and trig/log dense-periodic cells under a fixed extra-sample cap.
- Original-equation candidate validation remains the authority for accepting roots; discontinuity sign changes may produce no accepted candidate and should be explained as domain/discontinuity evidence.

## 2026-06-20 - EQUATION-COMPACT-ROOT-READBACK1

- Compact root readback is an internal producer-side policy seam. It adapts Equation root representations back to existing `exactLatex`, `branchReadback`, `exactSupplementLatex`, `approxText`, and detail-line surfaces rather than adding Display/History schemas.
- Visible implicit-root / `RootOf` notation remains deferred; implicit algebraic and numeric validated roots do not become Exact-mode visible answers in this milestone.
- First adoption is intentionally limited to factorable root-set readback. Formula-size algebraic stops and selected-target isolation compact fallback remain visibly unchanged.

## 2026-06-20 - EQUATION-IMPLICIT-ROOT-READBACK-AUDIT0

- Visible implicit-root / `RootOf` notation is deferred. The internal implicit algebraic root and structured-stop variants are substrate only until notation, copy/editor, ordering, domain, and user-expectation policy are explicitly decided.
- Formula-size boundaries should be treated as readback/product-safety boundaries, not as proof that no mathematical root object exists.
- The recommended next implementation is a producer-side compact root/readback policy seam that adapts root representations into the existing `exactLatex`, `branchReadback`, supplements, and detail-section surfaces without changing DisplayOutcome or History schemas.

## 2026-06-20 - EQUATION-BRANCH-DOMAIN-FACTS1

- Equation branch/domain facts are internal semantic facts with attachment metadata, not a new Display/History/app-state schema and not a solver/planner authority.
- The first facts seam wraps existing `ExactSupplementEntry` and `SolveDomainConstraint` meanings, then renders current raw `exactSupplementLatex` compatibility strings such as `z-a\ne0` and `a\ge0`.
- First adoption is intentionally narrow: factorable delegated root groups and parameterized rational denominator exclusions. Guarded algebra, composition, periodic facts, candidate-validation facts, and richer UI/readback remain separate future milestones.

## 2026-06-20 - EQUATION-FACTS-SURFACE-AUDIT0

- Future Equation branch/domain facts should reuse the existing typed `SolveDomainConstraint` and `ExactSupplementEntry` / `mergeExactSupplementLatex` bridge rather than creating a parallel fact model from scratch.
- Treat `exactSupplementLatex` as rendered compatibility output, not canonical fact storage; treat `detailSections` as human readback/prose, not a parseable fact source.
- Root-level fact attachment should start narrowly in `EQUATION-BRANCH-DOMAIN-FACTS1`, likely with factorable root groups and rational denominator exclusions before broader guarded algebra/composition adoption.

## 2026-06-20 - EQUATION-ROOT-REPRESENTATION-SEAM1

- Root representation is now an internal Equation substrate seam, not a Display or History schema. V1 may model exact finite roots, factor-derived roots, exact-rational factor roots, numeric validated roots, implicit algebraic roots, and structured stops, but only exact/factorable adapters produce current visible surfaces.
- The first consumers are both factorable paths: explicit zero products and exact-rational expanded factorable output. Other Equation families must adopt the seam only through named parity milestones.
- Visible behavior stays unchanged in this milestone: no `RootOf`, no implicit-root display, no cap raise, no broad factoring, no numeric fallback, no app-state/Tauri/OOE/History/Display schema change, and no Exact/Isolate cleanup.

## 2026-06-20 - EQUATION-ROOT-REPRESENTATION-AUDIT0

- Equation needs a solver-owned internal root representation before widening factor/product, degree, formula-size, or implicit-root behavior. Current `exactLatex` and `branchReadback` are result/display surfaces, not a canonical root model.
- The first root representation seam should adapt back to existing `exactLatex`, `branchReadback`, `approxText`, and detail-section behavior. It must not change DisplayOutcome, History, app-state, Tauri, OOE, UI, or persistence schemas in v1.
- Do not raise formula-size, factorable-degree, algebraic-degree, or branch caps because of this audit. Visible implicit-root notation such as `RootOf` remains deferred until readback/product policy is explicitly decided.

## 2026-06-20 - EQUATION-FACTOR-PRODUCT-DECOMPOSITION1

- Product/factor decomposition is an internal Equation parameterized substrate seam, not a new solver authority and not broad automatic factoring.
- The first consumer is only `factorable-polynomial.ts` explicit zero-product handling. Factorable remains responsible for visible stops, caps, target-free symbolic factor policy, branch delegation, readback, supplements, and source labels.
- Rational denominator/fact adoption, algebraic isolation, composition, and exact-rational factoring remain separate lanes with dedicated parity tests. `src/lib/algebra/polynomial-factor/` stays Algebra-owned and separate from this explicit-product seam.

## 2026-06-20 - EQUATION-SUBSTRATE-ROADMAP0

- Equation moves from search-discipline/cap work into substrate work. The durable substrate order is factoring/product decomposition, higher-degree root representation policy, branch/domain/exclusion facts, compact readback/implicit-root policy, then Exact/Isolate semantics.
- `EQUATION-FACTOR-PRODUCT-DECOMPOSITION1` is the recommended first implementation milestone. It should be an internal pure seam first, not broad automatic factoring or a visible solver promise.
- DAG/search-graph work is deferred. Current Calcwiz has shape profiling and local memoized traversal, not a symbolic DAG substrate; a DAG/e-graph/search graph should wait for concrete repeated transformation-state pressure.

## 2026-06-20 - EQUATION-CAP-HIT-REAL-CASES0

- Real/default cap-hit evidence does not currently justify raising Equation caps. The default selected-target peel-depth probe solved after normalization, and no public default generated branch-count hit was found.
- Keep selected-target peel depth and generated branch counts on a watchlist only. Any future cap raise still needs user-real default-cap hits plus trace/readback evidence that a supported solution lies just beyond the current guard.
- Route current default stops such as symbolic degree, rational cleared degree, factorable/algebraic degree, formula-size, mixed carrier count, and composition depth to algorithm, readback, or semantic milestones instead of cap tuning.

## 2026-06-20 - EQUATION-SOURCE-MIRROR-CONTEXT-AUDIT0

- Source mirrors remain context-only research inputs. They must not become Calcwiz runtime dependencies, execution authorities, imported code, or parity obligations.
- Mature CAS/source mirrors validate substrate-first solver growth: conversion/domain gates, polynomial/rational representations, assumptions, branch/domain facts, elimination/factorization, exact/numeric separation, and explicit fallback objects matter more than deeper selected-target peeling.
- Calcwiz should keep selected-target peeling bounded as an isolation tactic. Any future cap raise needs real default-cap hit evidence and trace-backed proof; degree, formula-size, composition-depth, and periodic-family boundaries should route to algorithm/readback/semantic milestones instead.

## 2026-06-19 - EQUATION-GENERATED-HANDOFF-PERF1

- Exp/log generated-equation handoff is the only implementation surface for this milestone. It now owns a focused generated-handoff helper that route-gates the local supported family order `linear -> polynomial -> rational -> carrier` from target-shape evidence.
- Carrier, composition, and mixed-algebraic generated branch helpers remain deferred. They have different branch counts, angle-unit needs, fallback semantics, and failure-message precedence, so unifying them would be architecture drift rather than a safe performance slice.
- Generated-handoff trace evidence remains internal/test-facing only. It must not be attached to `DisplayOutcome`, History records, OOE diagnostics, app-state, Tauri persistence, or visible result cards.

## 2026-06-19 - LANGUAGE-ROADMAP-CLOSEOUT0

- The active Language roadmap is paused after `LANGUAGE-PANELS-PILOT1`. The foundation is complete enough for current Calcwiz work: typed English catalog, deterministic fallback, provider/settings seam, shell consumers, and core panel consumers are implemented and tested.
- Remaining Language roadmap items should not be treated as urgent translation work. Navigation metadata migration waits for concrete navigation-copy pressure, RTL waits for a real RTL language commitment, and language packs/content work waits for a real second-language decision.
- The DisplayTextPart / mixed prose-math seam remains important, but it belongs to future Display/readback/math-fidelity work after solver/result surfaces mature. It should not pull the Language roadmap forward by itself.

## 2026-06-19 - LANGUAGE-PANELS-PILOT1

- `SettingsPanel`, `HistoryPanel`, and `VariablesPanel` are the first post-shell Language migration slice. Panel-owned visible copy belongs in the English Language catalog, including helper text, empty states, reset/action labels, aria labels, History pending-row interpolation, History expanded metadata labels, and Variables stored/inserted feedback messages.
- Persisted settings, persisted History entries, variable storage policy, solver/readback text, Display math rendering, OOE authority, runtime tickets, and non-English/RTL behavior remain outside this panel pilot.
- History entries must continue storing math/result metadata, not localized display strings. User/runtime data may be passed into typed language functions only as interpolation input.

## 2026-06-19 - LANGUAGE-SETTINGS-SEAM1

- `languageCode` is the durable persisted settings field for Language. It stores supported language pack ids, not locale-style regional tags.
- English (`en`) remains the only supported language code. Missing, unknown, invalid, or unsupported persisted values resolve back to English in both TypeScript app-state parsing and Tauri desktop settings sanitization.
- `LanguageProvider` is mounted in `AppMain` from `settings.languageCode`; app shell observability uses `lang="en"` only. `dir`, RTL layout behavior, MathLive direction behavior, and math/display direction semantics are deferred.
- Settings exposes a visible English-only Language control so the seam is discoverable and testable before additional language packs exist.

## 2026-06-19 - LANGUAGE-SHELL-PILOT1

- Low-risk app-shell consumers may call `useLanguage()` directly before `LanguageProvider` is mounted because the language context has a safe English default.
- `LanguageProvider` mounting, a settings schema field, persistence, reset behavior, invalid persisted value handling, and any user-facing language picker stay deferred to `LANGUAGE-SETTINGS-SEAM1`.
- The shell pilot is limited to prose/labels/titles/aria text for `ModeStrip`, `LauncherWorkspace`, `WorkspaceTabs`, `MenuInspectorPanel`, and `DisplayPanel` runtime controls; Display result-card copy, side panels, Guide content, navigation metadata, solver/readback wording, and OOE/runtime semantics are not part of this decision.
- Custom tab titles remain user data and are passed into typed language functions only as interpolation input; they are not translated or normalized.

## 2026-06-19 - LANGUAGE-COMPARTMENT-FOUNDATION1

- `src/lib/language/` is the static Language compartment foundation for product text. It owns the English canonical catalog, typed contracts, deterministic fallback, metadata, validation, and typed dynamic string functions.
- Dynamic language entries use typed function interpolation, such as title/count functions, not placeholder strings or scattered template literals.
- The first public seams are the pure API at `src/lib/language/index.ts` and the React seam at `src/lib/language/language-context.ts`. The provider/hook is intentionally not mounted into the app yet.
- The compartment manifest records `language` as a static library compartment with no future UI surface candidate yet, private language packs under `src/lib/language/languages/`, and `library-no-app-ui` / `no-source-mirrors` policies.
- This foundation does not migrate UI strings, add a settings schema, add Arabic/RTL layout behavior, change solver/readback wording, change Display math rendering, or alter OOE/runtime authority.

## 2026-06-11 - REPO-HYGIENE0 Stale Surface Audit

- `REPO-HYGIENE0` is a read-only repo hygiene audit, not a cleanup/removal milestone.
- No prior `REPO-HYGIENE0` milestone name exists; `EQUATION-RESULT-HYGIENE1` is related only by word choice and remains a separate Equation result-surface milestone.
- Stale-looking route names such as `advancedCalculus`, hidden Trigonometry screens, guided Calculate calculus screens, `calculate.workbench`, and legacy guide launches are compatibility contracts unless migration tests prove otherwise.
- Shared math cores must not be deleted merely because a visible workspace no longer exposes that old surface.
- Future cleanup should proceed by named slices with proof: visible wording first, route vocabulary second, schemas third, and internal implementation deletion last.

## 2026-06-11 - DISPLAY-PRODUCER-METADATA2 Producer Metadata

- Equation selected-target trig routes now emit display-only `branchReadback` metadata when they already have real `solutionExpressions`.
- The metadata covers direct affine trig and same-argument mixed sine/cosine selected-target branches, including generic targets and preserved symbolic parameters inside each branch.
- `exactLatex` remains the canonical full-fidelity payload for Copy Result, To Editor, history, replay, and stored output.
- Display still fails closed for tuple systems, inequalities, fact lists, geometry measurements, statistics summaries, table rows, matrix/vector grids, triangle measurements, and opaque monolithic LaTeX.
- This is a producer metadata widening pass only; it does not change solver math, OOE behavior, history schemas, or display scheduling policy.

## 2026-06-11 - DISPLAY-RENDER-SCHEDULER1 Progressive Rendering

- Committed result rendering may be scheduled progressively inside DisplayPanel after OOE has already allowed the result commit.
- Reveal order is fixed: answer/error text first, Valid When second, approx/warnings third, periodic-family blocks fourth, and detail blocks last.
- Metadata chips and action buttons remain immediate because they are lightweight navigation/context controls.
- While committed blocks are still queued, the display status may show `Rendering result`; it must return to Ready or the existing runtime/editor status once the display queue is empty.
- Delayed blocks should show lightweight placeholders or collapsed headers until their turn, not block the first answer paint.
- This scheduler is explicitly not OOE. OOE owns launch, host routing, cancellation, stale gates, commit/drop decisions, diagnostics, and History tickets; Display owns already-committed render order and responsiveness.

## 2026-06-11 - DISPLAY-BRANCH-METADATA1 Branch Metadata

- Display producers that already own finite branch arrays may emit optional `branchReadback` metadata with target LaTeX, relation, branch rows, and optional source/label.
- `exactLatex` remains the authoritative result for Copy Result, To Editor, history, replay, and stored output. Branch metadata is display-only and must not replace canonical solver output.
- Display block construction prefers validated producer metadata, then safe LaTeX branch extraction, then the original single math block.
- Metadata migration is limited to real finite branch arrays already known to producers, especially Equation finite selected-target and algebraic branch helpers. Periodic families stay in `periodicFamily.branchesLatex` / piecewise fields.
- Invalid metadata, tuple/system answers, malformed multi-target output, and opaque monolithic LaTeX must fail closed without inventing branch rows.
- This milestone changes readback structure only. It does not change solver math, OOE behavior, history schema, or copy/editor semantics.

## 2026-06-11 - DISPLAY-BRANCH-READBACK1 Branch-Aware Result Readback

- Branch-heavy exact answers must not default to a count-only receipt such as `s in 4 exact branches`.
- Display may extract safe top-level finite branch sets from existing `exactLatex` and render them as vertical branch rows, but the original exact LaTeX remains the authoritative copy/history/replay/stored value.
- Generic selected targets are in scope for display extraction: `x`, `s`, `t`, `z`, `\theta`, and similar single-target symbols. Other variables in each branch are preserved as parameters, not treated as extra solve targets.
- Display shows up to 4 branch rows immediately. Larger finite branch sets show the first 4 rows plus an explicit `Show remaining branches` action.
- Ambiguous multi-target systems, tuples, nested solution tuples, malformed sets, and unsafe comma splitting must fail closed to the original single math block.
- `\imaginaryI` normalization to visible `i` is a render-only `MathStatic` guard. It must not alter canonical LaTeX, copy/editor output, history, or solver semantics.
- Branch readback is display policy. OOE still owns launch, host routing, cancellation, stale gates, commit/drop decisions, diagnostics, and history tickets; it does not own render layout or progressive rendering.

## 2026-06-10 - Memory Catch-Up For Calculate Runtime And Display Rendering

- Durable memory was missed at commit time for five already committed milestones: `CALCULATE-RUNTIME-SHELL1`, the worker/display bridge, `DISPLAY-PROFILING0`, `RESULT-SIZE-POLICY1`, and `DISPLAY-BLOCK-CONTRACT1`.
- The catch-up is memory-only and records existing behavior; it must not be treated as a product or code change.
- Future runtime-shell, launch-ticket, and display-policy commits should update durable memory before the code commit.

## 2026-06-10 - DISPLAY-BLOCK-CONTRACT1 Renderable Display Blocks

- Display now has an adapter-first internal `DisplayBlock` contract over existing `DisplayOutcome` fields.
- The adapter derives answer, approx, Valid When, periodic-family, detail, warning, and error-text blocks without requiring producers to emit a new schema field.
- `DisplayPanel` should render result-card surfaces through the shared block path and apply the same size policy to math and math-list blocks.
- Solver output, OOE envelopes, history schema, copy/editor data, and stored LaTeX remain unchanged.

## 2026-06-10 - RESULT-SIZE-POLICY1 Compact Result Policy

- Oversized committed result blocks may render a compact preview first with an explicit `Show full result` action.
- The policy is display-only. Full exact LaTeX remains available for copy, editor insertion, history, replay, and stored output.
- The first policy is intentionally conservative and based on simple length/line thresholds, not a structural cost estimator.

## 2026-06-10 - DISPLAY-PROFILING0 Render Profiling

- Display-side render profiling is dev-gated and exists to distinguish LaTeX conversion, DOM/layout, React reconciliation, and mixed render costs.
- The profiling lane is not an OOE responsibility; it studies how already-committed results render after OOE has allowed the commit.
- Findings should guide display policy and scheduling, not solver semantics.

## 2026-06-10 - Worker Startup And Large-Result Defer Bridge

- Worker clients now share startup and cancellation-polling timeout configuration rather than scattering timeout constants.
- The display layer gained a crude long-LaTeX defer path for answer and Valid-When blocks as a bridge before stronger result-size policy.
- This bridge does not change solver output, history, OOE commit legality, or result schemas.

## 2026-06-10 - CALCULATE-RUNTIME-SHELL1 Runtime Shell And Tickets

- Calculate now uses the shared OOE runtime-shell plus launch-ticket model.
- `calculate-worker-runtime` is the primary worker host and `calculate-runtime` is init/unavailable fallback.
- Existing capability IDs stay stable: `expression.evaluate`, `expression.simplify`, `expression.factor`, `expression.expand`, `calculate.algebraTransform`, and legacy `calculate.workbench`.
- Calculate remains the quickform evaluator surface. It may use shared math capabilities but does not own guided calculus/trig/equation workflows or future step-by-step experiences.
- Runtime worker failures after startup do not silently retry on the main thread; cancellation preserves no-commit cancelled behavior.

## 2026-06-10 - GEOMETRY-OOE-PILOT1 + GEOMETRY-RUNTIME-SHELL1

- Geometry now uses the shared OOE runtime-shell plus launch-ticket model for explicit evaluations.
- New Geometry OOE records use `geometry.evaluate` with `geometry-worker-runtime` as the primary isolated worker host and `geometry-runtime` as init/unavailable fallback.
- The Geometry worker dispatches existing typed `GeometryRequest` kinds through the current Geometry core and returns the same `DisplayOutcome`, parsed request, replay screen, and `geometrySeed` data as the main-thread path.
- Every explicit Geometry run gets a launch ticket. Pending rows reserve launch order, show Running/Stopping plus Stop, and finalize or disappear without persisted fake records.
- Runtime worker failure after startup records a controlled failure and does not silently retry on the main thread; cancellation hard-terminates the worker when possible and preserves no-commit cancelled behavior.
- Background Geometry completion may finalize History, but visible Geometry output commits only when the same launched Geometry request is still current.
- This milestone does not add Geometry math capability, graphing/scenes, theorem proving, Rust solver execution, scheduler changes, or Geometry UI changes.

## 2026-06-10 - GEOMETRY-REQUEST1 + GEOMETRY-HISTORY1 Typed Replay Contract

- Geometry completed history entries may now persist `geometrySeed: { screen, request }`, where `request` is the existing typed `GeometryRequest` union.
- New Geometry runs should prefer the parsed replay screen and typed request seed when the draft parses successfully. `inputLatex` remains the compact visible/copy source.
- Geometry replay should prefer `geometrySeed`, serialize the stored request back into structured draft text, and open the stored Geometry screen. Legacy `geometryScreen`-only entries remain compatible by reparsing `inputLatex`.
- This closes the request/history maturity gap identified by `GEOMETRY-BOUNDARY0`, but it does not itself migrate Geometry to launch tickets or a worker shell.
- The next Geometry OOE sequence remains `GEOMETRY-OOE-PILOT1`, then `GEOMETRY-RUNTIME-SHELL1`.

## 2026-06-10 - GEOMETRY-BOUNDARY0 Geometry Workspace Boundary

- Geometry remains a visible workspace. It is not currently redundant in the same way old Trigonometry was, because its visible surface is already organized around geometry-specific objects, measurements, coordinate workflows, and bounded solve-missing flows.
- Geometry owns guided geometric setup and measurement experiences: shapes, solids, circles/sectors, triangle area/Heron, coordinate distance/midpoint/slope/line equation, and future construction/measurement flows.
- Geometry does not own broad algebraic equation solving, quick scalar expression evaluation, or trigonometric triangle-relation workflows. Those remain Equation, Calculate, and Trigonometry responsibilities respectively.
- Geometry helper modules remain reusable capability code. Workspace boundaries describe product experience, not exclusive ownership of algebra, square roots, scalar math, readback, or validity facts.
- Geometry should not receive OOE runtime shell or launch tickets yet. The blocker is typed launch/replay maturity: completed records still mainly persist `geometryScreen`, not a full typed `geometrySeed` with the launched `GeometryRequest`.
- The preserved sequence is `GEOMETRY-BOUNDARY0 -> GEOMETRY-REQUEST1 -> GEOMETRY-HISTORY1 -> GEOMETRY-OOE-PILOT1 -> GEOMETRY-RUNTIME-SHELL1`.

## 2026-06-10 - TRIGONOMETRY-RUNTIME-SHELL1 Runtime Shell And Tickets

- Trigonometry now follows the shared OOE runtime-shell plus launch-ticket model, but still uses one shell per workspace rather than one shell per Trig workflow.
- New Trigonometry OOE records use `trigonometry.evaluate` with `trigonometry-worker-runtime` as the primary isolated worker host and `trigonometry-runtime` as init/unavailable fallback.
- The Trigonometry worker dispatches by typed `TrigRequest` kind (`identities`, `triangles`, `angleConvert`, `periodPhase`) and returns the same `DisplayOutcome`, replay screen, and `trigSeed` data as the existing main-thread path.
- Every explicit Trigonometry run gets a launch ticket, including fast runs. Pending rows reserve launch order, show Running/Stopping plus Stop, and finalize or disappear without persisted fake records.
- Background Trigonometry completion may finalize History, but visible Trig output commits only when the same launched Trigonometry request is still current.
- Runtime shell evidence and launch-ticket evidence remain separate code concepts. Duplicate Run/Enter handling remains deferred to a future shared OOE duplicate-launch policy.
- This milestone does not add new Trigonometry math capability, does not migrate Geometry, and does not alter legacy hidden Trig Function/Equation/Special-Angle replay routing.

## 2026-06-10 - TRIGONOMETRY-PERIOD-PHASE1 + TRIGONOMETRY-RUNTIME-SEED1

- Trigonometry keeps the stricter workspace boundary from `TRIGONOMETRY-SURFACE1`, but now has four visible guided workflows: `Identities`, `Triangles`, `Angle Convert`, and `Period & Phase`.
- `Period & Phase` is a guided expression-analysis workflow, not a relation solver. It accepts expression-only affine `sin`/`cos`/`tan` waves in fixed variable `x`, such as `2sin(3x-pi)+1`, and stops cleanly for equations, inequalities, nested trig, mixed carriers, symbolic parameters, non-affine arguments, absolute value, piecewise forms, and powers like `sin^2(x)`.
- Period/phase readback must honor the active angle unit and keep math display clean; escaped artifacts such as `\\pi` in rendered Period & Phase output are regressions.
- Trigonometry now writes typed replay data through `trigSeed: { screen, request }` for new records, while legacy hidden `functions`, `equationSolve` / `equationsHome`, and `specialAngles` records keep their forward-compatible Calculate/Equation/Guide routing.
- Guide owns the teaching material for Period & Phase alongside the Unit Circle reference. The Guide article should explain amplitude, period, phase shift, vertical shift, tangent asymptotes, and first-cycle landmarks without turning Trigonometry back into a general equation solver.
- Geometry remains audit-only for now. It is cleaner than old Trigonometry, but should not receive OOE runtime shell/tickets until request/history cleanup milestones such as `GEOMETRY-BOUNDARY0`, `GEOMETRY-REQUEST1`, and `GEOMETRY-HISTORY1`.
- The concurrent `CSS-DECOMP1 + CSS-DECOMP2` work is CSS relocation only for `shell.css`, `keypad.css`, and `guide.css`; it should not be conflated with Trigonometry capability, replay, or guide-content changes.

## 2026-06-09 - TRIGONOMETRY-BOUNDARIES0 Trigonometry Surface Boundary

- Trigonometry remains a visible workspace, but its product role is guided angle, unit-circle, identity, triangle, and periodic-structure understanding rather than generic expression evaluation or broad equation solving.
- Reusable trig cores stay reusable across Calculate, Equation, Trigonometry, and future workspaces; surface cleanup must not delete core capability modules just because a visible card is redundant.
- `TRIGONOMETRY-SURFACE1` locks the visible Trigonometry home to exactly `Identities`, `Triangles`, and `Angle Convert`.
- Visible `Functions` is removed from Trigonometry because quick direct trig-value evaluation belongs to Calculate.
- Visible `Equations` is removed from Trigonometry because broad trig relation solving belongs to Equation.
- Visible `Special Angles` is removed from Trigonometry because reference material belongs in the Guide; the Trigonometry guide now owns a visual Unit Circle article with special-angle notes beneath it.
- Internal `TrigScreen` values and trig helpers remain for compatibility. Legacy `functions` / expression `specialAngles` replay routes forward to Calculate, while legacy `equationsHome` / `equationSolve` replay routes forward to Equation symbolic.
- OOE widening for Trigonometry stays deferred until the surface cleanup is complete and the MathLive live-snapshot rule is applied.

## 2026-06-08 - OOE-RS34 Linear Algebra Runtime Shell And Launch Tickets

- Matrix and Vector widen onto OOE through one shared Linear Algebra runtime-shell host pair, not separate Matrix/Vector worker hosts.
- New Matrix/Vector OOE records keep separate capabilities, `linearAlgebra.matrix` and `linearAlgebra.vector`, while selecting `linear-algebra-worker-runtime` as the primary isolated worker host and `linear-algebra-runtime` as init/unavailable fallback.
- Runtime worker failure after startup records a controlled failure and must not silently retry on the main thread; cancellation hard-terminates the worker and preserves the no-commit cancelled behavior.
- Launch-ticket ordering applies to explicit Matrix/Vector operations, but remains a separate concept from runtime-shell host evidence.
- New Matrix history entries may carry `matrixSeed`; new Vector history entries may carry `vectorSeed` and angle unit. Legacy seedless Matrix/Vector records remain loadable/replayable.
- Matrix and Vector remain separate visible workspaces; RS34 does not add exact linear algebra capability, UI taxonomy changes, Rust solver execution, or broader workspace migration.

## 2026-06-08 - PRE-RS34 Live Snapshot Gate

- The RS33-deferred same-base Equation UI failure was not a symbolic solver bug. It was an OOE launch handoff bug where the visible MathLive field could contain the new LaTeX while `runEquationAction()` still launched from an older React state/ref snapshot.
- Equation launch, pending History ticket input, active-revision resolution, and OOE route snapshots must use the same live/canonical request shape. OOE stale gates remain strict; the fix is to make the execution source current and canonical, not to weaken stale-drop behavior.
- Statistics received the same live/canonical snapshot treatment because RS33 made it an OOE/ticketed MathLive workspace too.
- Future OOE widening must audit whether a workspace is MathLive-backed. If it is, launch-time live snapshot and revision canonicalization are required before adopting worker shells or launch tickets.

## 2026-06-07 - OOE-RS33 Statistics Runtime Shell And Launch Tickets

- Statistics is the next OOE widening lane after Equation, Table, and Calculus.
- New Statistics OOE records use `statistics.evaluate`, with `statistics-worker-runtime` as the primary isolated worker host and `statistics-runtime` as init/unavailable fallback.
- New completed Statistics history entries write typed `statisticsSeed` replay data while keeping legacy `statisticsScreen` records loadable through `inputLatex` reparsing.
- Explicit Statistics evaluations adopt pending launch tickets that reserve launch-order position, show Running/Stopping plus Stop, and finalize or disappear without persisted fake records.
- Runtime shell contract and launch-ticket ordering remain separate concepts.
- Calculate, Trigonometry, Matrix/Vector, Geometry, and product taxonomy cleanup remain deferred.
- The AppMain PRL4 same-base Equation UI regression was initially postponed to `OOE-RS34`, then resolved before RS34 by `PRE-RS34-LIVE-SNAPSHOT-GATE`.

## 2026-06-07 - OOE-RS32 Canonical Calculus Runtime Shell And Tickets

- New Calculus runtime, History, and OOE records use canonical `calculus` / `calculus.evaluate` identity.
- Legacy `advancedCalculus` mode/history/guide/replay records remain accepted and map forward into the unified visible Calculus workspace.
- `calculus-worker-runtime` is the primary isolated worker host for explicit Calculus evaluation; `calculus-runtime` is init/unavailable fallback only.
- Worker runtime failures do not silently retry on the main thread after execution has started.
- Calculus now adopts shared launch tickets: pending rows reserve launch-order position, show Running/Stopping plus Stop, and finalize or disappear without persisting fake records.
- Background Calculus completion may finalize History without switching the user back to Calculus or overwriting another active workspace.
- Runtime shell contract and launch-ticket manager remain separate concepts.
- Statistics, Matrix, Vector, Trigonometry, Geometry, Calculate, and Editor analysis remain deferred for later OOE widening.
- Physical implementation folders such as `src/lib/advanced-calc/*` remain unchanged in RS32.

## 2026-06-11 - CALCULUS-CANONICAL-ID1 Live Identity Cleanup

- New live Calculus actions must emit `mode: calculus`, `capabilityId: calculus.evaluate`, and `routeLabel: calculus.<screen>`.
- New Calculus commits must write `mode: calculus`; `advancedCalculus` is retained only for read/replay compatibility and internal implementation naming.
- Workspace-pilot current capability metadata must not advertise `advancedCalculus.evaluate` as an active runtime capability.
- Guide launches and visible guide mode references should target `calculus` where safe, while old article IDs and compatibility fields remain stable until a dedicated retirement slice proves they have no consumers.
- Do not delete `src/lib/advanced-calc/*`, hidden Trigonometry compatibility screens, guided Calculate compatibility, `calculate.workbench`, or old schema fields in this cleanup slice.

## 2026-06-07 - CALCULUS-WORKSPACE-MERGE1 Unified Calculus Surface

- User-facing `Calculus` and `Advanced Calc` are merged into one visible `Calculus` workspace.
- The visible launcher must show one `Calculus` entry and no visible `Advanced Calc` entry.
- The unified Calculus hub must not have a duplicate `Basics` section; former Basics tools are routed into `Derivatives`, `Integrals`, and `Limits`.
- `src/lib/advanced-calc/*` remains the implementation backend for advanced calculus tools for now.
- The internal `advancedCalculus` mode/schema/history identifiers remain for legacy replay and guide compatibility.
- OOE worker-shell migration, launch-ticket adoption, engine deletion, and broad file renaming are deferred.

## 2026-06-04 - COMPLEX-INPUT1 Imaginary Input Contract

- Equation input accepts standalone `i` and `\imaginaryI` as the imaginary unit.
- `j` is deferred and remains a normal symbol.
- Imaginary-unit normalization is Equation-first and does not apply to Calculate/Table in this milestone.
- Complex Off stays real-first and returns guidance for explicit imaginary input.
- `ImaginaryUnit` is a reserved identifier for target discovery and must not appear as a solve target or symbolic parameter.
- No stored complex values, Approximate complex search, Isolate complex solving, or non-Equation adoption is included.

## Historical Attribution
- primary_agent: codex
- ownership_model_rule: before `2026-03-12` use `gpt-5.3-codex`; on or after `2026-03-12` use `gpt-5.4`
- attribution_basis: historical-user-confirmed

- 2026-03-02: Primary stack is `Rust + Tauri + React + TypeScript`.
- 2026-03-02: User-facing math input and rendering must prefer textbook notation through MathLive.
- 2026-03-02: Symbolic algebra for v1 is handled in the TypeScript adapter with Compute Engine.
- 2026-03-02: Rust owns app shell state, persistence, and the stable command boundary.
- 2026-03-02: Version 1 targets Windows first and ships as an installable desktop app.
- 2026-03-02: Mode fidelity is enforced so `Calculate` never solves equations; `Equation` owns symbolic solving and solve only occurs through an explicit solve action.
- 2026-03-02: Domain-level regression coverage uses a dedicated `Vitest` unit harness.
- 2026-03-02: `Auto-switch to Equation` is an opt-in convenience setting; it may move the user from `Calculate` to `Equation`, but it must not auto-solve.
- 2026-03-02: A dedicated Casio-style `MENU` launcher is the primary navigation model; top tabs are hidden in normal use and only shown behind `VITE_SHOW_MODE_TABS=1` in development.
- 2026-03-02: Polynomial solvers remain deferred until after the launcher milestone so they can be integrated under `Equation` instead of bolted onto the old tab flow.
- 2026-03-02: Polynomial solving is now part of the `Equation` app, using guided coefficient entry for quadratic, cubic, and quartic forms while keeping the existing symbolic and linear-system branches intact.
- 2026-03-02: Polynomial solves reuse the existing explicit `solve` engine path and store the generated equation in history so replay returns to a valid symbolic equation instead of an opaque placeholder.
- 2026-03-02: Guided polynomial flows use a symbolic-first strategy and then fall back to in-repo numeric complex roots for degrees 2 through 4 when symbolic solve fails.
- 2026-03-02: Numeric fallback currently applies only to the guided `Polynomial` branch; free-form `Equation > Symbolic` remains symbolic-only in this version.
- 2026-03-02: The `Equation` app now uses a routed Casio-style tree with `home`, `Polynomial`, and `Simultaneous` menus instead of a flat segmented selector.
- 2026-03-03: Equation UX now uses breadcrumb chips, branch badges, and route-aware help text, while history replay restores polynomial or simultaneous branch context only when it can be inferred conservatively from the existing saved data.
- 2026-03-03: Numeric editing uses signed text inputs with deferred parsing instead of raw `type="number"` controls so intermediate `+` and `-` entry works across calculator data-entry screens.
- 2026-03-03: The virtual keyboard is now product-controlled through custom MathLive layouts and capability-gated page specs instead of the default MathLive keyboard.
- 2026-03-03: The active CAS keyboard rollout starts with `Keyboard Foundation + Algebra Core`; future domains remain hidden until their milestone is explicitly activated and documented.
- 2026-03-03: `Discrete Core` is now part of the active keyboard rollout, exposing only curated `Discrete` and `Combinatorics` pages while later CAS domains remain hidden until their milestone.
- 2026-03-03: Factoring now uses a custom symbolic AST fallback when Compute Engine leaves symbolic expressions unchanged, covering common-factor extraction, difference of squares, and simple integer quadratics.
- 2026-03-03: `Calculus Core` is now part of the active keyboard rollout, exposing only curated `Calculus` and `Functions` pages while later linear-algebra keyboard content remains hidden.
- 2026-03-03: A future in-app guidance page should consolidate milestone lessons so new symbol families can be taught inside the calculator as they are activated.
- 2026-03-03: The guidance experience is now a dedicated top-level `Guide` app in launcher slot `6`, not an overlay or markdown viewer.
- 2026-03-03: Runtime Guide behavior is driven by typed TypeScript registries for domains, articles, symbols, mode guidance, search, and example launch actions rather than rendering markdown docs at runtime.
- 2026-03-03: Calculator screens should provide context-sensitive deep links into the Guide app so help stays one jump away from the active workflow.
- 2026-03-03: Guided calculus stays inside `Calculate` as a route-based workbench for single-variable `x` workflows instead of becoming a separate top-level app.
- 2026-03-03: In this calculus pass, plain derivatives and indefinite integrals remain symbolic-only, while definite integrals and limits may use clearly labeled numeric fallback.
- 2026-03-03: `Calculus` is now launcher-visible as a dedicated destination, but it is still implemented as a calculate-family surface rather than a separate persisted mode.
- 2026-03-03: Stronger antiderivative coverage and broader symbolic or generalized limit forms are deferred to a later advanced-calculus milestone instead of being bundled into the visibility pass.
- 2026-03-03: Numeric display results are guarded to an approximate safe magnitude window of `1x10^-150` through `1x10^150`; out-of-range values return controlled errors instead of rendering huge or tiny outputs.
- 2026-03-03: The advanced single-variable calculus pass now uses app-owned symbolic antiderivative rules before failing indefinite integrals, while still keeping indefinite integration symbolic-only overall.
- 2026-03-03: Calculus limits now support finite targets and `Â±âˆž` through lightweight heuristics plus numeric fallback, but they remain scoped to single-variable `x` and controlled error messaging rather than broad symbolic asymptotics.
- 2026-03-03: `Linear Algebra Core` is now active in the keyboard rollout through a curated `MatrixVec` page, with matrix/vector notation treated as dedicated-mode-aligned templates rather than a promise of full free-form matrix CAS.
- 2026-03-03: Matrix and Vector UX should explicitly separate executable numeric operations from notation drafting; the notation pads may load current A/B values and operator snippets for reuse, but they remain template-only surfaces rather than general-purpose matrix CAS editors.
- 2026-03-03: Advanced single-variable calculus now lives in a dedicated top-level `Advanced Calc` app, while the existing `Calculus` page remains the approachable entry-level workbench.
- 2026-03-03: ODE capability is now part of `Advanced Calc` rather than a separate top-level app, using a hybrid strategy of guided symbolic flows plus Rust-backed numeric IVP solving in desktop runtime.
- 2026-03-03: Guide articles in touched domains now follow a teaching-first structure with required `What It Is` and `How To Use It` sections, while `What It Means` is shown only for real mathematical concepts and omitted for UI-only topics.
- 2026-03-03: `Advanced Calc` and core `Calculus` now share a consistent preview-card and empty-state language, but `Advanced Calc` keeps a visually stronger menu/tool treatment so it reads as the heavier-duty calculus surface.
- 2026-03-03: The symbolic engine is now a first-class app-owned layer with explicit normalization and precedence handling before rule dispatch, instead of relying on scattered heuristics around Compute Engine calls.
- 2026-03-03: Factoring is locked to a symbolic-first policy: symbolic grouping/common-factor extraction before numeric GCD factoring, with unchanged output when no simpler factorization is found.
- 2026-03-03: For symbolic operations, app-owned rules lead for factoring, derivatives, limits, and partial derivatives, while indefinite integrals still try Compute Engine exact resolution before the app-owned rule engine.
- 2026-03-03: Desktop symbolic-engine validation is now tracked through a repo-local runbook in `docs/validation/symbolic-engine-runtime.md` instead of ad hoc spot-checking.
- 2026-03-03: First-order partial-derivative tooling lives under `Advanced Calc > Partials`, not under the simpler `Calculus` page.
- 2026-03-03: Symbolic integral input now tolerates a small set of calculator-style implicit multiplication forms such as grouped expressions followed by `e^(...)`, while keeping indefinite integration symbolic-only.
- 2026-03-03: Broader pre-integration rewrite work is deferred; the next major product expansion will scope `Trigonometry`, `Geometry`, and `Statistics` cores before returning to wider trig-product and rewrite-search integration work.
- 2026-03-03: The three-core expansion will ship incrementally in the plan's recommended order, starting with `Trigonometry` as a real top-level app that combines bounded symbolic identity work with guided numeric triangle and angle tools.
- 2026-03-03: On user request, `Geometry` ships immediately after `Trigonometry` and occupies launcher hotkey `0`, with the development shortcut on `Ctrl+Shift+2` until `Statistics` is slotted.
- 2026-03-03: The primary launcher now uses a two-level numeric category model (Core, Linear, Calculus, Shape Math, Data) so growth reuses the same menu footprint instead of adding more permanent UI.
- 2026-03-03: Guide is now a top-panel utility with Ctrl+G, not a launcher app; numeric launcher slots are reserved for calculator families.
- 2026-03-03: Statistics enters as the first app under the Data launcher category, with descriptive/frequency workflows usable now and probability/regression routes kept visible as placeholders.
- 2026-03-03: Domain-heavy modes will converge on a reusable core-mode contract with one top draft/editor plus guided builders below it; Geometry is the first implementation, and it borrows shared scalar math semantics instead of redefining them per core.
- 2026-03-04: Geometry is now fully on the shared core-mode contract across all leaf tools, and Geometry history entries persist an optional `geometryScreen` hint so shorthand drafts replay into the correct Geometry screen.
- 2026-03-04: Trigonometry now follows the same shared core-mode contract as Geometry: one top trig draft/editor, guided builders beneath it, explicit cross-mode send actions, and per-entry `trigScreen` history hints for replay.
- 2026-03-04: Statistics now follows the shared core-mode contract too, using one top Statistics draft/editor, hybrid dataset vs frequency-table workflows, bounded probability and point-set tools, and per-entry `statisticsScreen` history hints for replay.
- 2026-03-04: Calculate angle consistency is handled with a narrow direct-trig MathJSON rewrite in Calculate evaluation only, not with the global Compute Engine `angularUnit` switch, so explicit radian expressions keep their meaning.

- 2026-03-04: Shared action controls on dark panels now use a higher-contrast button treatment by default, while buttons inside the light LCD display stay on a darker text treatment through scoped CSS overrides.
- 2026-03-04: Harmonized core workbenches now use tighter card spacing, clearer section labels, and consistent high-contrast interactive states, with shared hover/active/focus-visible/disabled treatments applied across dark-panel controls and scoped overrides kept for the light LCD display.
- 2026-03-04: Calcwiz now uses a commit-first workflow by default: durable `.memory` files are tracked in git, `.task_tmp/` is the ignored gate-tracking workspace, verified app-state checkpoints live under `docs/checkpoints/`, and worktrees are documented as exception tools rather than the standard workflow.
- 2026-03-04: Canonical math-input normalization is now a shared pre-execution layer across MathLive-backed editors; live typing assist is helpful but not authoritative, and execution paths must canonicalize supported reserved tokens before parse/planning.
- 2026-03-04: Equation now owns general `x` solving after safe pre-solve reductions, while bounded trig equation solving is a shared fallback backend rather than a separate long-term Trigonometry-only grammar.
- 2026-03-04: Supported derivative reduction now uses the app-owned symbolic differentiation engine as the primary exact reducer for embedded and direct derivative workflows; broad trig rewrites and indefinite-integral reduction stay outside the automatic pre-solve set for this milestone.
- 2026-03-04: Shared MathLive editors should keep native inline shortcuts enabled and layer app-specific overrides on top; pre-execution canonicalization remains the safety net, but live typing should use the editor’s structured shortcut path instead of a disabled-shortcut fallback.
- 2026-03-04: Exact bounded trig solving expands through the shared Equation-owned backend with selected double-angle rewrites and chosen `sin^2` / `cos^2` split families first; broader trig identity search, `tan^2`, and polynomial-in-trig solving remain deferred.
- 2026-03-04: `Equation > Symbolic` is the unified guarded solve surface for single-variable real equations: it owns symbolic-first solving, bounded substitution families, domain/candidate validation, and explicit inline interval numeric solving, while `Trigonometry > Equation Solve` stays guided and hands off there for interval work.
- 2026-03-04: A conservative solve-only real-range impossibility check now runs after planner resolution and before bounded family expansion, so provably impossible real equations hard-stop with `Range Guard` instead of falling through to unsupported-family or numeric-fallback messaging.
- 2026-03-05: Forward planning now uses dedicated focus tracks instead of one whole-app roadmap: Track A (Solver/Equation), Track B (Trigonometry), Track C (Geometry), Track D (Statistics), with Track E (Error Semantics & UX Consistency) maintained as a parallel quality lane.
- 2026-03-05: Track A implementation is locked to a guarded solve stack: direct symbolic first, then range guard, bounded trig/rewrite/substitution families, then explicit interval numeric solving with bracket-first bisection plus residual-validated local-minimum recovery.
- 2026-03-05: Before starting any new roadmap track, we ship a user-facing manual verification checklist for the just-finished track (achieved-now + app steps + expected outcomes) and store it in memory.
- 2026-03-05: Track A4 exp/log completion stays bounded: selected `ln` + common `log` inverse-isolation and exp-polynomial families are in scope, while mixed log-combination transforms (for example `ln(x)+ln(x+1)=2`) remain intentionally unsupported for this milestone.
- 2026-03-05: Track B1 solve expansion is bounded to affine trig arguments and wrappers (`kx+b`, `a*f(kx+b)+d=c`) with Trigonometry still using `Send to Equation` for interval numeric fallback instead of adding a local numeric panel.
- 2026-03-05: Track A5 now includes bounded log-combine sum transforms only (`ln(u)+ln(v)`, `log(u)+log(v)`), while log difference/ratio/power families remain deferred.
- 2026-03-05: Solve provenance summaries shown in result cards should be human-readable and must not expose raw wrapper LaTeX control tokens like `\left` and `\right`; solver internals stay in equations, while summaries stay plain.
- 2026-04-09: `POLY-RAD4` keeps `exactSupplementLatex` as the stable visible output surface, but supplement assembly now flows through one small internal structured model that groups informational notes separately from merged `Exclusions:` and `Conditions:` lines.
- 2026-03-05: Track B4 extends bounded log solving to same-base and mixed constant-base normalization (`ln/log/log_a` with constant numeric bases only), with invalid-base hard stops and explicit interval guidance when mixed-base exact bounded solving is unavailable.
- 2026-03-05: Track B5 adds bounded two-term trig sum-to-product solving (`sin/cos` sums and differences), including exact zero-target branch splits and recognized unresolved messaging for non-zero families outside the current exact bounded set.
- 2026-03-05: Refactor Track R is executed as strict no-behavior-change decomposition with a mandatory tests-first R0 gate before moving any large structural extraction.
- 2026-03-05: In Refactor Track R routing extractions, decision logic is moved into `src/app/logic/*` modules while operational side effects remain executed in `App.tsx` through dependency-driven router calls.
- 2026-03-05: Refactor Track R is now closed with `App.tsx` as the composition root, workspace rendering split into `src/app/workspaces/*`, app orchestration helpers split into `src/app/logic/*`, styles split under `src/styles/app/*`, and solver/guide/type entrypoints preserved through compatibility barrels instead of breaking import paths.
- 2026-03-06: Track R closure verification is anchored to the stable runtime shell shape `src/App.tsx` (import shell) -> `src/AppMain.tsx` (orchestration), with `src/App.css` as the manifest over `src/styles/app/*`; further app-shell decomposition is treated as optional follow-up, not required for Track R completion.
- 2026-03-06: Track C P1 geometry solve-missing is locked to explicit `?` marker with strict one-unknown policy; distance returns both real branches with warnings, and equation handoff is shown only for unresolved-but-eligible coordinate constraints.
- 2026-03-06: Track C P2 extends Geometry with bounded inverse solve-missing families (`cone`, `cuboid`, `arcSector`, `triangleHeron`) and locks `lineEquation(...)` solve-missing routing to exactly one unknown + one constraint, reusing slope/distance/midpoint engines with explicit coordinate-handoff mapping warnings.
- 2026-03-06: Track D D1+D2 locks Statistics to explicit dataset vs frequency-table source-sync UX with no live bidirectional sync, and adds a dedicated `Inference` branch whose first bounded tool is structured, `t`-first, two-sided one-sample mean inference.
- 2026-03-07: Track D3 keeps Statistics regression/correlation inside the existing screens and uses one bounded `Quality Summary` result section plus balanced warnings (`n < 5`, weak/moderate fit) instead of adding prediction UI, residual tables, or inferential regression.
- 2026-03-07: After the current guarded-solve/trig/geometry/statistics passes, the next major CAS-grade capability gap is exact algebra normalization for rational and radical math; later planning should treat it as a dedicated roadmap rather than scattered simplify fixes.
- 2026-03-07: Exact Algebra Core starts with bounded rational normalization first: `Calculate > Simplify` and `Calculate > Factor` may combine/factor supported exact rational forms, `Equation > Symbolic` may normalize rational structure before solve without automatic LCD clearing, and denominator exclusions must be shown as a second exact line in the result area.
- 2026-03-09: Exact Algebra Core `R2` is bounded radical normalization plus solve-prep only: `Calculate` may normalize/rationalize supported radicals, while `Equation > Symbolic` uses a more conservative radical pre-solve normalization that preserves domain conditions and avoids introducing new radical-equation solve families.
- 2026-03-09: The repo now defines line endings in `.gitattributes` instead of relying on `core.autocrlf`; source/docs use LF, while Windows-native scripts (`.ps1`, `.bat`, `.cmd`) use CRLF.
- 2026-03-09: Primary milestone verification is now repo-owned automation. `npm run test:gate` is the main close-out check, browser-first Playwright replaces MCP as the first E2E path, and manual verification is optional final confirmation after automation passes.
- 2026-03-09: Exact Algebra Core `R3` extends `Equation > Symbolic` with bounded rational LCD-clearing solves, bounded radical isolation / nth-power solves, and bounded square-root-binomial conjugate transforms; transformed equations recurse only through the existing guarded solver, preserve original validation/domain constraints, and must pass the full `npm run test:gate` automation close-out.
- 2026-03-09: Standardized cloud/local handoff on a single operator checklist in `docs/workflow/cloud-local-sync-reference.md` (fetch/status/branch check, pull --rebase, then commit+push publish).
- 2026-03-10: Exact Algebra Core `R4` exposes bounded algebra transforms explicitly instead of hiding them behind generic simplify/solve flows: `Calculate` standard uses `F4 Algebra`, `Equation > Symbolic` gets the same inline transform tray, and equation transforms are transform-only rather than transform-and-solve.
- 2026-03-10: Exact Algebra Core `R5` broadens bounded algebra solving to one-variable monomial/binomial rational factors and monomial/binomial radical radicands, while capping chained radical transforms at depth 2 and keeping all candidates subject to original-equation and preserved-constraint validation.
- 2026-03-10: The next preferred post-`R5` roadmap is settings + power/root/log expansion in this order: `SX1` settings foundation, `PRL1` display normalization, `PRL2` broad numeric support, `PRL3` bounded symbolic support, then `PRL4` bounded solve expansion.
- 2026-03-10: Settings is treated as a canonical app surface, not a `Menu` entry; visibility/readability controls and symbolic display preferences should live there as the app grows.
- 2026-03-10: `SX1` implements settings as a canonical shell-owned hybrid surface: docked right inspector at `>= 1180px`, right slide-over overlay below `1180px`, with a dedicated top-bar `Settings` entry point next to `Guide`.
- 2026-03-10: `SX1` keeps top-row quick toggles as shortcuts over the same persisted settings state and makes Settings/History mutually exclusive side surfaces.
- 2026-03-10: `SX1` persists `Display` and `Symbolic Display` preferences now, but broad symbolic output changes remain deferred to `PRL1`; in `SX1` those symbolic settings affect only the preview and saved preference state.
- 2026-04-03: `SX1.1` corrects the settings/history shell model: on wide windows both surfaces use a shared outboard right rail only when real spare gutter space exists, the calculator shell never shrinks to make room, and overlay remains the fallback when that space is unavailable.
- 2026-04-03: `PRL1` is display-only. Rendered exact math may be normalized through a bounded power/root/log display layer, but engine output, copy/editor flows, replay, and persisted history keep the raw exact LaTeX unchanged.
- 2026-04-03: `PRL1` makes `Symbolic Display` settings live across result rendering, with `Auto` power-leaning for awkward/nontrivial forms while still preserving familiar plain roots like `\sqrt{x}` and `\sqrt[3]{x}`.
- 2026-04-03: `PRL2` is numeric-only and real-domain-only: broad power/root/log evaluation ships first for `Calculate` and `Table`, while symbolic transforms and solve-family expansion stay deferred to `PRL3` and `PRL4`.
- 2026-04-03: Negative-base non-integer powers are accepted in `PRL2` only when the exponent is a true exact rational with odd denominator; decimal approximations that merely resemble those rationals are rejected.
- 2026-04-03: `Table` now keeps mixed-domain samples by marking invalid rows `undefined` and surfacing one table-level warning instead of failing the whole table.
- 2026-04-03: `PRL3` is symbolic normalization plus explicit transforms only; it may preprocess notational variants into already-supported Equation carriers, but it does not widen solve-family scope beyond the existing bounded backend.
- 2026-04-03: `PRL3` raw exact output is canonical power-leaning for awkward/nontrivial root-power forms, while simple `\sqrt{x}` and `\sqrt[3]{x}` remain roots; rendered style still layers through `PRL1` display preferences on top of that raw exact output.
- 2026-04-03: In `PRL3`, same-base log combine is automatic only under `Simplify`, while `Rewrite as Root`, `Rewrite as Power`, and `Change Base` are explicit algebra-tray transforms shared by `Calculate` and `Equation`.
- 2026-04-03: Symbolic substitution validation must preserve exact accepted branch LaTeX when a bounded solve produced it; post-validation rebuilds should not downgrade exact symbolic solutions into rounded decimal-only `exactLatex`.
- 2026-04-03: Repeated identical multiplicative factors in user-facing symbolic serializers must compact back into powers before emission (`xx -> x^2`, `4xx^3 -> 4x^4`); this is formatter debt, not a deferred `PRL4` solve-family concern.
- 2026-04-03: `PRL4` is Equation-first: broader log quotient/difference solving, bounded mixed-base normalization, and bounded radical/rational-power lifting ship only through `Equation > Symbolic`, while `Calculate` keeps its narrower PRL3 simplify contract for now.
- 2026-04-03: Recognized mixed-base log equations that leave bounded exact symbolic support should stop with explicit numeric guidance instead of silently falling through to numeric fallback or fake symbolic success.
- 2026-04-04: Numeric output is user-controlled through Settings with a typed `Approximate digits` input (`0..20`, default `6`), notation modes `decimal/scientific/auto`, and scientific styles `×10^n` or `e`; these controls are display-only and do not change solver effort or CE precision.
- 2026-04-04: Numeric output `Auto` notation switches to scientific form when `abs(x) >= 1e6` or `0 < abs(x) < 1e-4`; otherwise it stays in decimal form.
- 2026-04-04: Decimal-only symbolic Equation outcomes must not populate the exact line; symbolic provenance may remain, but decimal values belong only in approximate output.
- 2026-04-04: The next major Equation-solver roadmap candidate after `PRL4`/`NP1` is bounded composition and nested-function solving, starting with safe outer-function inversion plus range/image impossibility checks before any broader multi-layer search.
- 2026-04-04: Selected angle mode now applies to direct numeric trig input in `Calculate` even when the numeric argument contains `\pi`; `sin(\pi/2)` is treated as radians only in `RAD`, not silently preserved across `DEG` and `GRAD`.
- 2026-04-04: `COMP1` is Equation-first and one-layer only: Calcwiz now solves bounded `f(g(x))=c` families through safe outer inversion or image-aware trig reasoning with at most one guarded recursive handoff, and recognized trig compositions outside that bounded symbolic depth must stop with explicit numeric guidance instead of generic unsupported-family messaging.
- 2026-04-09: Calcwiz now treats `AGENTS.md` as the authoritative cross-agent workflow file, requires durable memory attribution through `.memory/PROTOCOL.md`, validates that protocol with `npm run test:memory-protocol`, and backfills historical ownership as `codex` with the user-approved model split at `2026-03-12`.
- 2026-04-04: `COMP2` keeps composition Equation-first and real-domain-only, but promotes nesting to an explicit policy: at most two successful outer inversions per solve attempt, followed by honest handoff into already-shipped bounded trig/PRL/algebra families or explicit numeric guidance when the remaining branch set is periodic, infinite, or outside supported exact scope.
- 2026-04-04: `COMP3` upgrades bounded trig follow-on from guidance-only to symbolic periodic families when the remaining carrier can still finish through bounded exact handoff; Equation now surfaces unit-aware family formulas, representative branches, and interval suggestions, while nonlinear-in-`k` carriers still stop honestly with structured periodic guidance.
- 2026-04-04: Explicit Equation numeric interval solving is now authoritative when a numeric interval is supplied: it runs before later symbolic-family stops, and all direct trig evaluation, residual checks, and candidate validation in that path must honor the selected angle unit (`rad`, `deg`, or `grad`).
- 2026-04-04: When Equation numeric interval solving misses a direct trig root and the solver can infer the sampled inner image, the failure message should explain that image and the current-unit branch family to target next instead of only suggesting wider intervals.
- 2026-04-04: Shared signed-number inputs should accept both plain decimals and scientific notation so large numeric-interval suggestions (for example `3e19`) are directly enterable across the app instead of being trapped behind giant expanded literals.
- 2026-04-05: `COMP4` extends composition solving in two bounded directions only: parameterized periodic follow-on may solve affine and pure power-form carriers `(ax+b)^n` for integer `n` from `2` to `6`, and inverse-trig outers (`arcsin/arccos/arctan`) may invert once and hand off once into already-supported guarded families; broader polynomial-in-`k` carriers and open-ended inverse-trig nesting remain deferred.
- 2026-04-05: The next planned composition milestone is `COMP5`, and it stays in the Equation-composition lane: it should deepen bounded periodic nesting and broaden bounded inverse-trig follow-on rather than folding calculus composition into the `COMP` sequence. Calculus composition is now treated as a separate future roadmap lane.
- 2026-04-05: `COMP5` stays Equation-first and still caps exact symbolic closure at finite branches or single-parameter periodic families; one additional structured periodic reduction is allowed beyond `COMP4`, but any path that would introduce a second independent periodic parameter must stop with structured guidance and suggested intervals instead of fake symbolic success.
- 2026-04-05: After `COMP5`, the composition roadmap is explicitly split into two named lanes instead of a vague future fork: `COMP6` continues bounded Equation composition (reciprocal trig, stronger periodic pruning, cleaner inverse/direct trig reductions), while calculus composition begins as `CALC-COMP1`/`CALC-COMP2`/`CALC-COMP3` for substitution antiderivatives, chain-rule structure, and composition-aware limits.
- 2026-04-05: `COMP6` stays bounded and Equation-only: reciprocal trig (`sec/csc/cot`) is implemented by rewrite into the existing `sin/cos/tan` composition machinery, while inverse/direct trig identities only collapse canonically when the inner carrier is provably inside the selected unit's principal range; anything beyond a single periodic parameter must stop with structured guidance instead of fake exact closure.
- 2026-04-07: `COMP7` keeps the Equation-composition lane periodic-heavy and honest: one additional structured periodic reduction is allowed through explicit `periodicReductionDepth`, discovered family trails must be preserved in the result surface, and any path that would require a second independent periodic parameter must stop with structured guidance instead of fake exact closure.
- 2026-04-07: Read-only math notation is now a persisted user setting (`rendered`, `plainText`, `latex`); `Copy Result` follows the visible notation, `To Editor` always uses canonical LaTeX, and the top-row `EXACT/DECIMAL/BOTH` control remains a synced shortcut over the canonical `outputStyle` setting.
- 2026-04-07: `COMP8` is a closure-quality Equation milestone, not a raw depth bump: bounded inverse/direct trig sawtooth closure is allowed only when the reduced carrier is affine (`x`, `ax+b`, or trivially sign-flipped/reordered affine equivalents), with at most one safe outer-inversion handoff and no expansion into nonlinear or multi-parameter sawtooth families.
- 2026-04-07: `COMP9` broadens sawtooth closure across the near-maximal current single-parameter carrier surface: after inverse/direct trig branch windows are generated, the solver may reuse bounded power, log, exponential, and selected root/rational-power carrier resolution, but it must still stop with structured guidance on general polynomial carriers, multi-parameter periodic families, or broader sawtooth normalization.
- 2026-04-07: `COMP10` extends the bounded single-parameter carrier resolver to normalized quadratics `ax^2+bx+c` and shifted powers `(ax+b)^n+c` for integer `n=2..4` across both plain periodic and sawtooth paths; broader polynomial solving, unrestricted sawtooth normalization, and multi-parameter periodic exact closure remain out of scope.
- 2026-04-07: The Rust numeric ODE backend now uses `mathexpr` instead of `meval`; maintainability and future Rust compatibility take priority over keeping the older parser, and the supported RHS surface remains the current frontend-emitted math subset with variables `x` and `y`.
- 2026-04-07: `POLY1` establishes `src/lib/algebra/polynomial-core.ts` as the canonical app-owned exact polynomial substrate for the bounded one-variable surface (degree `<= 4`, exact rational coefficients only); composition, factoring, and polynomial-pattern helpers should reuse that core instead of maintaining private exact polynomial parsers/arithmetic.
- 2026-04-08: `POLY2` uses one shared factor-first bounded exact engine for cubic/quartic polynomial support across guided `Equation > Polynomial`, free-form `Equation > Symbolic`, and `Calculate > Factor`; exact success is limited to rational-root, biquadratic, and bounded quadratic-pair families, while unsupported cubic/quartic families keep the existing numeric fallback or unsupported behavior instead of widening into general closed forms.
- 2026-04-08: Shared reusable math cores are now an explicit architecture principle for Calcwiz: when bounded algebra/solver logic is needed across multiple surfaces, we should centralize it in one maintained substrate instead of letting feature-local engines drift into duplicated logic. `POLY1`/`POLY2` are the first concrete application of that rule, and later algebra/radical/composition work should follow the same pattern.
- 2026-04-08: `RAD1` establishes `src/lib/algebra/radical-core.ts` as the canonical bounded radical-recognition substrate; symbolic radical normalization and guarded Equation algebra should share that one core for supported-radicand checks, condition synthesis, perfect-square radicand recognition, and bounded conjugate/rationalization eligibility instead of carrying duplicated radical-local logic.
- 2026-04-08: `RAD1` keeps Equation solve preprocess conservative even while simplify/factor normalization gets stronger: visible perfect-square square-root collapse into absolute-value form and bounded two-radical denominator rationalization are allowed on read-only normalization surfaces, but Equation solve must not auto-introduce new `|...|` carriers or widen radical-family solving in this milestone.
- 2026-04-08: `RAD2` widens radical Equation solving only through a bounded sequential pipeline: at most two radical-specific transform steps per solve path, bounded absolute-value branching only when produced by an exact supported radical step such as `\sqrt{(u)^2}`, and every surviving branch must still pass original-equation candidate validation plus merged domain/exclusion constraints.
- 2026-04-08: `POLY-RAD1` reuses one bounded algebraic follow-on bridge across both radical solve paths (`RAD2` sequential isolation and outer-inversion/composition handoff): exact algebraic-root follow-ons are limited to real one-variable biquadratic / quadratic-in-even-power families, `Factor` may surface the same algebraic quadratic factors, and `Simplify` may use the bridge only for direct radical normalization wins rather than general factor-style output.
- 2026-04-08: Calcwiz's architecture direction is now explicitly kernel-first: prefer a kernel-centered modular monolith with stable internal contracts, shared runtime/result/job/capability semantics, and clear pillar boundaries before attempting a true microkernel/plugin platform. External/plugin-style extensibility and Rust-authoritative runtime migration remain later phases, not immediate prerequisites.
- 2026-04-08: `ARCH1` is implemented as a narrow, parity-safe ownership-and-seams pass: runtime/kernel-adjacent types are split compatibility-first, kernel capability metadata stays internal and limited to six real execution seams, and app-shell execution now routes through typed Calculate/Equation controllers instead of growing new dependency-bag orchestration.
- 2026-04-08: `ARCH2` promotes `ARCH1` seams into static internal runtime hosts rather than inventing a broader platform: Equation stage order and Calculate action dispatch now live in minimal descriptor-backed hosts, kernel capabilities point to owning hosts, `table.build` stays metadata-linked only, and internal expression `solve` remains non-public.
- 2026-04-09: `ARCH3` keeps runtime envelopes and advisories intentionally narrow: `Calculate` and `Equation` now share one internal `DisplayOutcome` envelope helper, Equation numeric-solve controller gating is driven by tiny internal advisories instead of error-string matching, and those advisories must stay non-rendered and non-persisted.
- 2026-04-09: `ARCH4` adds a minimal shared runtime-policy layer for `Calculate`, `Equation`, and the planner boundary: `RuntimeAdvisories` now carries a small structured stop-reason taxonomy (`invalid-request`, `planner-hard-stop`, `range-guard`, `unsupported-family`) plus the existing tiny Equation numeric-solve advisory, all kept internal-only, non-rendered, and non-persisted.
- 2026-04-09: `ARCH5` adds one explicit internal default runtime execution profile for `Equation` and `Calculate`; Equation recursion/transform caps and Calculate numeric-fallback permissions are now owned by a shared kernel profile/budget layer, while planner behavior and all user-facing runtime controls remain unchanged.
- 2026-04-09: After `ARCH5`, the next algebra lane remains Poly-Rad, and `POLY-RAD2` is the next intended milestone. `POLY-RAD3` and any abs-bridge follow-up must be framed as bounded expansions on top of already-shipped `RAD2` behavior, not as if repeated-radical handling or radical-to-abs follow-on solving were starting from zero.
- 2026-04-09: `POLY-RAD2` is Equation-first and reuses one shared bounded polynomial-in-carrier bridge across both shipped radical entry paths. Supported carrier families are limited to affine, even-power affine, shifted even-power, and direct quadratic carriers; the bridge solves `P(u)=0` through the existing bounded polynomial engine, back-solves `u(x)=c` through shared carrier logic, then validates final `x` candidates against the original equation. No intentional new visible `Factor` / `Simplify` scope is included in this milestone.
- 2026-04-09: `POLY-RAD3` is solver-first and Equation-first: ordinary `RAD2` stays capped at two radical transform steps, while one dedicated repeated-clearing allowance may be spent only on recognized families that predictably land in existing bounded sinks. Visible non-Equation scope is limited to exact constant two-level square-root denesting in `Calculate > Simplify`.
- 2026-04-09: `POLY-RAD5` is a staged bounded transform-breadth milestone, not a new solver-depth or factorization milestone: Stage A widens shared square-root conjugate/rationalization coverage for stronger two-term families across Calculate and Equation, while Stage B is limited to selected three-term families that reduce to the Stage A surface after one canonical first multiply and at most one residual shared cleanup step.
- 2026-04-09: `POLY-RAD6` is the factor-first breadth milestone: Calcwiz recognizes one-pass mixed expressions that normalize to a bounded polynomial in a single supported carrier `u` (square roots and same-base sibling rational powers only), exposes the win primarily through `Calculate > Factor`, and allows Equation reuse only when the factor result deterministically improves an already-supported bounded sink.
- 2026-04-09: `ABS1` is the canonical repo milestone name for the roadmap's `ABS-BRIDGE1`.
- 2026-04-09: `ABS1` introduces one shared internal absolute-value core across direct abs equalities, transform-produced abs follow-ons, Calculate simplify normalization, and Equation numeric follow-up; visible exact solving remains bounded to deterministic branch families (`|u|=c`, `|u|=v`, `|u|=|v|`) and does not introduce a general-purpose piecewise engine or new visible `Factor` scope.
- 2026-04-10: `ABS2` broadens the abs lane around the same bounded `u = \pm v` branch model instead of adding case depth: direct affine-wrapped families (`a|u|+b=c`, `a|u|+b=v`, `a|u|+b=|v|`) and transform-produced wrapped closures are supported only when generated branches land in already-shipped bounded sinks, numeric follow-up becomes more branch-aware, `Calculate` stays Simplify-only, and nested abs towers, abs sums, inequalities, and general piecewise search remain out of scope.
- 2026-04-10: `ABS3` keeps the shared absolute-value branch model fixed at `u = \pm v` and broadens exact closure by admitting stronger bounded carrier families only when every branch lands in already-shipped sinks. Stronger unresolved families now surface stronger-carrier guidance, and transformed branch solving must not leak non-real direct-symbolic branch roots into merged exact real-output lines.
- 2026-04-10: `ABS4` stays in the abs lane and broadens outer algebra around one abs target instead of introducing `BRANCH1`: bounded one-placeholder outer-polynomial closure `P(|u|)=0` is allowed on the existing exact polynomial surface, trig/composition carriers are included only as reuse of already-shipped sinks after the same `u = \pm v` split, and any branch that reaches only guided periodic/composition output keeps the overall abs family unresolved rather than partially exact.
- 2026-04-10: `COMP11` is a deliberately broad composition milestone: exact composition closure now extends to reduced polynomial carriers on the existing bounded one-variable polynomial surface, shared Equation composition/periodic budgets are raised to a bounded depth of `3`, and selected exact two-parameter periodic families are allowed only when the first reduction yields exact numeric branch constants and the remaining continuation is direct trig on affine `x`.
- 2026-04-10: Large math-lane milestones should default to a two-slice structure when practical: slice `A` ships the real capability expansion, and slice `B` follows immediately as the polish/trust/readback/guidance pass for the same lane. This is now the preferred pattern for heavier roadmap steps such as `COMP12A`/`COMP12B` and later `ABS5A`/`ABS5B`.
- 2026-04-10: `COMP12A` broadens the composition lane by carrier breadth, not by adding more periodic parameters: reduced-carrier periodic and sawtooth exact success is now allowed for already-shipped bounded single-family carriers beyond reduced polynomials, explicit `x` closure remains preferred whenever it already exists, and mixed poly-rad carriers stay out of scope for this slice.
- 2026-04-10: `COMP12B` is polish-only for the `COMP12A` surface: admitted reduced-carrier periodic and sawtooth exact outcomes should render as intentional exact successes with canonical reduced-carrier summaries, guided composition stops should explain their specific blocker instead of reusing generic unsupported wording, exact reduced-carrier context should render separately from stop-reason messaging in the result card, and numeric follow-up may gain composition-aware guidance wording but not a new solving algorithm or broader exact surface.
- 2026-04-10: `ABS5A` makes `t = |u|` a first-class bounded placeholder across shipped non-periodic outer sinks: the abs lane may now reuse bounded logarithmic, exponential/same-base, radical/root, and rational-power follow-ons over one exact abs target, allow one additional non-periodic outer layer before returning to the existing abs sink path, keep the same `u = \pm c` branch model, and leave periodic outer families composition-owned.
- 2026-04-11: `ABS5B` keeps abs polish on the existing generic `DisplayOutcome.detailSections` surface instead of adding an abs-specific result card; `solveSummaryText` stays short and outcome-level, while reduction context, generated branches, and exact-closure boundaries render as separate detail sections.
- 2026-04-10: Architecture direction for future algebra growth is now explicit: keep `src/lib/kernel/*` as the single runtime kernel, continue extracting reusable bounded algebra cores when needed, and avoid introducing multiple per-engine microkernels for piecewise/case handling, transforms, or other math families unless a later real runtime/plugin boundary appears. If architecture resumes in this lane, the preferred next extractions are shared `branch-core` / `case-core` and `transform-core`, with a thin algebra registry only as a later optional coordination layer.
- 2026-04-10: Commit metadata recording is now part of the same intended checkpoint: when a task is committed, session `commit-log.md` and related `commit_hash` fields should be updated within that same commit flow whenever possible, rather than creating a second metadata-only commit afterward.
- 2026-04-10: Transform handling and piecewise/case handling should not be merged into one new subsystem. If Calcwiz extracts more algebra cores here, they should stay separate: `transform-core` owns deterministic transform recognition, exact application, preserved-constraint inputs, and sink-prediction hints; `branch-core` owns bounded branch generation, branch constraints, and branch metadata for abs/principal-range/periodic-style families. Use `branch-core` rather than `piecewise-core` unless the repo later commits to broader piecewise algebra. Default architecture preference is `transform-core` first because it is lower-risk, unless the active roadmap becomes branch-heavy enough to justify `branch-core` first.
- 2026-04-10: `ARCH6A` is a pure extraction milestone: `src/lib/algebra/transform-core.ts` now owns deterministic transform descriptors, exact transform application, eligibility checks, normalized output comparison, and shared expression/equation transform mechanics, while `src/lib/algebra/algebra-transform.ts` stays the stable public facade and no visible transform behavior changes in this pass.
- 2026-04-10: `ARCH6B` is a pure extraction milestone: `src/lib/algebra/branch-core.ts` now owns normalized branch sets, branch-constraint merging, branch-pair adapters, and periodic/principal-range metadata merge behavior for the Equation/abs/rewrite/substitution lane, while public result-facing types stay unchanged and no visible solver/UI behavior changes in this pass.
- 2026-04-11: Calcwiz's future Playground should be treated as a level-based incubation system, not as a production core or hidden solver stack. Stable app code must never import from Playground code, and any successful experiment must graduate into stable architecture by extraction or rewrite rather than by direct reuse.
- 2026-04-11: `PGL1` establishes the first implementation boundary for Playground without turning it into a platform: keep Playground as a top-level tree outside `src/`, enforce the one-way dependency law only for stable TypeScript product code in `src/`, and include only thin starter records/templates rather than schema validation, workflow automation, or experiment-execution infrastructure.
- 2026-04-11: `PGL2` makes Playground records canonical without adding platform machinery: Markdown records are authoritative, YAML manifests are tiny companion summaries only, every real experiment should have a record + manifest + index entry, and the first seeded starter experiment is `sym-search-planner-ordering` at `level-0-research` in the symbolic-search lane.
- 2026-04-11: `PGL3` symbolic-search experiments use a dedicated `test:playground` Vitest harness plus an export-only guarded-stage replay seam; the stable guarded solver default order remains unchanged.
- 2026-04-11: The first `sym-search-planner-ordering` PGL3 run stays at `level-0-research`: both alternate whole-stage orderings produced no exact improvements and each introduced the same honesty regression on the guided abs/composition boundary case `2^{|sin(x^5+x)|}=2^{1/2}`.
- 2026-04-14: Calculator-visible Playground work is now separated from the core incubation ladder as a follow-on roadmap family named `PGL-VIS`; it begins only after the main `PGL` roadmap is sufficiently complete and should start with an internal developer/operator console rather than a normal-user-facing Playground mode.
- 2026-04-14: `PGL4` is now explicitly an external-compute foundations milestone rather than a first provider pilot: it adds provider-neutral runner/job/artifact contracts, checked-in JSON templates, ignored local SSH-shaped profiles, and a local harness over one real existing Playground workload while keeping real SSH/provider execution deferred.
- 2026-04-14: Future external-compute config should use checked-in template JSON plus ignored repo-local `*.local.json` profiles. SSH is the first expected future transport, but `PGL4` keeps SSH structurally validated and intentionally non-executable.
- 2026-04-14: `PGL5` is a singular VM-first external-compute milestone, not a `PGL5A/PGL5B` split. The first real SSH target is the user-owned `<user-ssh-target>` VM, while provider/rented-host execution remains a later sequencing decision.
- 2026-04-14: `PGL5` turns the `PGL4` SSH contract into one bounded remote pilot only: upload JSON inputs, run one dedicated remote Playground entrypoint for `sym-search-planner-ordering`, pull back the manifest and summary, and require a local parity report before calling the pilot successful.
- 2026-04-14: `PGL5+` is the explicit pre-adoption hardening gate between `PGL5` and any later provider-host or `PGL6` discussion. The canonical operator path is now `npm run playground:ssh-vm -- --profile <path> --job <path>`, and the gate requires preflight checks, classified failure modes, provenance, and one live success path plus induced failure-path proofs before any adoption review.
- 2026-04-24: Version 1 platform direction is now Linux-first, superseding the earlier Windows-first v1 target while preserving cross-platform ground for Windows/macOS through Tauri, TypeScript, Rust, and validation gates.
- 2026-04-24: External compute remains a valid Playground lane but is postponed after `PGL5+`; the VM-first SSH proof is preserved, provider-host expansion is deferred, and the next incubation work should improve the Playground system and non-remote experiment discipline until the core calculator and solver roadmap are more stable.
- 2026-04-24: Near-term product sequencing now pauses broad algebra breadth and moves to a calculus roadmap: first `CALC-AUDIT0` verifies current calculus and Advanced Calc status plus safe algebra-core reuse, then bounded calculus-composition milestones should proceed through `CALC-CORE1`, `CALC-COMP1`, `CALC-COMP2`, `CALC-LIM1`, `CALC-INT1`, and `CALC-POLISH1` unless the audit changes the order.
- 2026-04-24: `CALC-AUDIT0` locks `CALC-CORE1` before `CALC-COMP1`. The audit found enough duplication in basic Calculus vs Advanced Calc result origins, warning policy, and numeric fallback handling that a narrow shared calculus evaluation/reporting boundary should land before new substitution-antiderivative or composition capability work.
- 2026-04-24: After `CALC-CORE1`, `CALC-*` milestones are dependency-gated product-calculus milestones. Each one must state the algebra, differentiation, domain, formatting, and result-surface substrates it needs; if a needed substrate is missing or unstable, calculus work pauses and the prerequisite is planned explicitly instead of adding calculus-local algebra workarounds.
- 2026-04-24: `CALC-CORE2` permits `CALC-COMP1` to proceed only as a narrow derivative-backed substitution/composition milestone. Broader integration-by-parts hardening, partial fractions, rational integration, radical/rationalizing substitutions, and definite-integral trust remain deferred or blocked until their algebra/domain prerequisites are explicit.
- 2026-04-25: `CALC-CORE3` locks Basic Calculus and Advanced Calc indefinite integrals to one shared app-owned symbolic backend for all shared behavior. Separate UI sections and Advanced-only workflows may remain, but private Basic-vs-Advanced symbolic integral rule stacks are disallowed unless a future milestone documents a real product boundary.
- 2026-04-25: `CALC-COMP1` exposes integration strategy badges for symbolic indefinite-integral wins while keeping antiderivative verification status internal. `ResultOrigin` values remain stable; visible badges describe strategy, not proof strength.
- 2026-04-25: `CALC-DIFF1` exposes derivative strategy badges for supported derivative wins while keeping proof/check status and step-by-step derivations internal. Known inverse trig and parser-recognized inverse hyperbolic families are supported; arbitrary generic inverse-function theorem handling for `f^{-1}` remains deferred, and explicit reciprocals such as `(sin(x))^{-1}` must stay reciprocal expressions.
- 2026-04-25: The original `CALC-COMP2` derivative-structure milestone is now parked as mostly absorbed by `CALC-DIFF1`. The next active calculus milestone should be `CALC-LIM1` unless a specific derivative readback/domain blocker is found first.
- 2026-04-25: `CALC-LIM1` strengthens finite limits through bounded known-form and removable-hole rules only. It does not introduce a general asymptotic engine, multivariable limits, new `ResultOrigin` values, or visible limit strategy badges; numeric fallback must remain visibly labeled.
- 2026-04-25: `CALC-LIM2` intentionally treats trustworthy signed infinities as successful finite-limit results when direction/sign evidence is clear, while preserving mismatch/domain stops for unsafe cases. Typed directional targets such as `0^+` and `0^-` are normalized at shared input boundaries, and rational-hole expansion remains limited to existing rational/factor/cancel substrates without adding a general asymptotic or series engine.
- 2026-04-25: Tracked durable memory is public-promotable by default: exact local filesystem prefixes, private operator names, and local SSH target aliases should be represented with placeholders such as `<local-source>`, `<local-repo-path>`, and `<user-ssh-target>`. Exact environment mappings may be kept only in ignored scratchpads or local config, not in tracked memory.
- 2026-04-25: `CALC-LIM3` may combine bounded local-order reasoning, existing rational normalization/cancellation, and listed elementary equivalents into rule-based symbolic limit wins, but it must not become a general series/asymptotic engine. User-facing limit explanation should use accurate `DisplayOutcome.detailSections` notes rather than new limit strategy badges, and `ResultOrigin` values remain stable.
- 2026-04-26: `domain-range-CORE1` is a shared bounded real-domain/range substrate before `CALC-INT1`, not a calculus-local workaround and not an expansion of `branch-core`. It may collect common real-domain constraints, prove simple ranges, and provide point/one-sided/interval safety checks, but full inequality solving, broad domain display, piecewise engines, and definite-integral trust changes remain out of scope for this milestone.
- 2026-04-26: `CALC-INT1` may use verified antiderivatives for exact finite definite integrals only after bounded interval-safety checks find no concrete real-domain violation. Detected unsafe intervals must stop before numeric fallback; safe unsupported finite definite integrals may still use clearly labeled adaptive Simpson fallback. Definite-integral UX uses existing detail sections for method and safety notes, while broad exact improper integration and new definite-integral strategy badges remain deferred.
- 2026-04-26: `REL0` establishes public-repo guardrails before broader promotion: `main` should require PR review, Code Owner review, conversation resolution, no force pushes/deletions, restricted direct pushes, and status check `ci-linux`. Linux preview releases are manual/tag-triggered only, draft/prerelease by default, and must not include Playground/external compute, signing keys, private paths, provider-host compute, or claims of full CAS parity.
- 2026-04-27: The next clean-base sequence should finish public-release foundations before new math breadth: `REL1`, `PILLARS0`, and `MATH-GOLDEN0` are preferred before returning to `CALC-POLISH1` unless public usage exposes an urgent calculus UX blocker. `FRICAS-CTX0` is allowed only as an isolated context-mirror research lane after a further incubation-system strengthening pass: FriCAS is context, not authority; Calcwiz must not depend on it, submodule it, copy code by default, or inherit its identity, and any useful idea must be translated into a Calcwiz-native bounded form through Playground/incubation before stable adoption.
- 2026-04-28: `.memory/sources/` is the canonical location for preserving external roadmaps, research files, and exported discussion sources as verbatim snapshots. Snapshot files should not be wrapped or edited; capture metadata, hashes, original placeholder paths, and downstream interpretation links belong in `.memory/sources/INDEX.md`. `.memory/research/references/sources.md` remains a legacy/reference note rather than the as-is preservation location.
- 2026-04-28: `REL1` preview packaging should run a fuller release gate before Tauri bundle creation: launch preflight, memory protocol, unit tests, UI tests, lint, frontend build, Rust `cargo check`, and `tauri:build`. First Linux preview releases remain draft/prerelease by default and must not imply production stability, full CAS parity, external compute, auto-updating, signing, or Windows/macOS artifact guarantees.
- 2026-04-28: `PILLARS0` establishes eight small public project pillars as governance docs guarded by `npm run test:pillars`, not as a runtime architecture layer. `MATH-GOLDEN0` remains the next correctness milestone; telemetry, crash uploaders, config migrations, plugin APIs, FriCAS research, incubation changes, and solver/math behavior changes remain out of scope for `PILLARS0`.
- 2026-04-28: `MATH-GOLDEN0` protects shipped behavior with a small typed golden corpus over stable mode boundaries, not UI snapshots or aspirational examples. Golden cases should prefer stable substrings, origins, strategies, warnings, detail titles, and controlled-stop metadata; broad benchmark corpora and future challenge sets belong outside this gate.
- 2026-04-28: `CALC-POLISH1` makes typed calculus replay context the durable history policy for new guided Calculus and Advanced Calc entries. The fields remain optional, old history entries must keep replaying through LaTeX inference fallback, and replay metadata may preserve UI/tool state but must not imply new math capability, new `ResultOrigin` values, or new strategy labels.
- 2026-04-30: `INCUBATION-LABS0` establishes the only allowed first calculator-visible incubation surface as a generated, read-only Labs catalog: tooling may read Playground manifests/records at generation time, stable app runtime may import only the committed `src/lib/labs` snapshot, `playground/...` paths are inert display text, and the visible Labs mode is developer-only behind `VITE_SHOW_LABS=1` with no experiment execution, remote controls, result delegation, or history mixing.
- 2026-05-01: `INCUBATION-SOURCES0` establishes `playground/sources/` as the controlled registry for external CAS/math source mirrors. Cloned repos must live only under ignored `playground/sources/mirrors/<mirror-id>/`, committed metadata is context-only, stable `src/` code must not import or read source mirrors, and FriCAS/SymPy/Maxima/SageMath/Giac-XCAS/SymEngine may inform Calcwiz only through translated Playground/incubation work, not direct dependency, submodule, code copying, or identity inheritance.
- 2026-05-01: `FRICAS-CTX0` makes FriCAS an active context mirror only, not an authority or backend. Its outputs are research notes, a Playground `source-context` record, and a typed challenge/reference corpus; future use must choose bounded Calcwiz-native prototypes from the idea ledger, and the corpus is evidence for research, not a product parity contract.
- 2026-05-01: The post-`FRICAS-CTX0` native roadmap starts with `ALG-CAPS0` capability facts before heavier polynomial, integration, series, Grobner, or matrix work. FriCAS-derived work must first establish shared readiness language so later milestones do not invent private local capability checks.
- 2026-05-01: Post-FriCAS work follows a core-first rule: strengthen existing Calcwiz-owned cores before jumping into advanced fields, unless a concrete blocker proves the advanced field is required. Grobner, regular chains, broad asymptotics, and full Risch-style integration remain Playground-only or deferred until current algebra/domain/calculus substrates are explicitly ready.
- 2026-05-01: Current Matrix and Vector support is treated as numeric product workspaces plus notation pads, not reusable algebra cores. Exact linear algebra work such as `MATRIX-EXACT0` is postponed behind `VEC-MAT-AUDIT0` and likely a small `VEC-MAT-CORE0` boundary so future determinant/rank/echelon/inverse/system work has explicit coefficient-domain and result-envelope ownership.
- 2026-05-20: `VEC-MAT-AUDIT0` confirms Matrix and Vector are current numeric product workspaces with notation helpers, not shared symbolic/exact algebra substrates. Future exact linear algebra must start with `VEC-MAT-CORE0` ownership over exact scalar support, vector/matrix data models, coefficient-domain gates, and result envelopes before `MATRIX-EXACT0` can reopen.
- 2026-05-20: `ALG-CAPS0` establishes a code-backed math-substrate readiness table in `src/lib/algebra/capability-readiness.ts`, separate from runtime execution capabilities in `src/lib/kernel/capabilities.ts`. The post-FriCAS sequence is now `ALG-CAPS0 -> VEC-MAT-CORE0 -> POLY-CORE-AUDIT1 -> INT-CANDIDATE2`; vector/matrix core is `blocked` until `VEC-MAT-CORE0`, and exact linear algebra remains `defer`.
- 2026-05-20: `VEC-MAT-CORE0` creates two sibling reusable numeric cores, `matrix-core` and `vector-core`, rather than one fused linear-algebra engine. Product Matrix/Vector adapters must preserve shipped numeric behavior and UI result wording; exact linear algebra remains deferred until exact scalar readiness and coefficient-domain gates are explicit. The next native milestone is `POLY-CORE-AUDIT1`.
- 2026-05-20: `POLY-CORE-AUDIT1` confirms `polynomial-core` is `ready-with-adapter`, not a broad polynomial algebra engine. Shipped support covers bounded one-variable exact rational polynomial parsing/arithmetic/readback and current factor/rational/numeric-root/solve consumers; polynomial gcd/division, square-free factorization, resultants, partial fractions, Grobner/elimination, broad rational integration, and exact matrix algebra remain blocked or deferred. The next native milestone is `INT-CANDIDATE2`.
- 2026-05-20: `POLY-RAT-CORE0` promotes exact polynomial division/GCD, primitive normalization, rational-function normalization, and bounded distinct-linear partial-fraction readiness into shared internal substrates. This unlocks planning for bounded `INT-RAT1`, but rational integration is not adopted yet; repeated factors, irreducible quadratic decomposition, square-free factorization, resultants, Grobner/elimination, and exact matrix algebra remain blocked or deferred.
- 2026-05-20: `INT-CANDIDATE2` adds internal integration candidate metadata without changing visible integration behavior. Integration attempts may now record method, prerequisites, blocked prerequisites, verification status, controlled failure class, readiness notes, and domain hazards; rational integration gaps must remain blocked on polynomial gcd/division/partial-fraction readiness rather than becoming calculus-local workarounds.
- 2026-05-20: `APPMAIN-SLIM0` locks the near-term app-shell organization rule: `src/AppMain.tsx` remains the orchestration root for global state, refs, routing, history, keyboard handling, execution handlers, and shell ownership, while `src/app/workspaces/*Workspace.tsx` components own render/view boundaries. Further state extraction is deferred to a later `APPMAIN-SLIM1`-style milestone rather than bundled into this no-behavior refactor.
- 2026-05-21: `MEMORY-ORG0` makes calendarized durable-memory layout canonical for journals and sessions. New journals must use `.memory/journal/YYYY-MM/YYYY-MM-DD.md`, new session dossiers must use `.memory/sessions/YYYY-MM/YYYY-MM-DD/YYYY-MM-DD__slug/`, and the memory protocol validator should reject deprecated flat entries.
- 2026-05-21: `MEMORY-ORG1` makes the typed `.memory/research/` taxonomy canonical. New interpreted research artifacts should go under `roadmaps/`, `checklists/YYYY-MM/YYYY-MM-DD/`, `readiness/`, `audits/`, `source-context/<mirror>/`, `architecture/`, or `references/`; root-level research artifacts are disallowed except `README.md` and `INDEX.md`.
- 2026-06-04: Manual verification checklists now use day-level calendarization under `.memory/research/checklists/YYYY-MM/YYYY-MM-DD/`. Month-level checklist files are considered clutter and are rejected by `npm run test:memory-protocol`.
- 2026-05-21: `LIB-ORG0` locks the `src/lib` organization pass to clean import rewrites with no root-level compatibility shims. Each `LIB-ORG` phase must commit separately after verification and record what moved in durable memory.
- 2026-05-21: Journal months with completed milestone work must have corresponding session-month coverage under `.memory/sessions/YYYY-MM/`. Closely related slices may share one dossier to keep memory navigable, but the dossier must point back to the relevant journal, checklist, research, and commit evidence.
- 2026-05-21: `PGL-VIS1` allows interactive Labs execution only through an explicitly enabled dev-server runner bridge. Stable `src` runtime code must still not import Playground code, approved runners must declare supported input kinds, outputs must be labeled experimental/developer-only, release builds must not expose runner execution, and Labs runs must not enter normal calculator history or stable result provenance.
- 2026-05-21: `PGL-VIS1-POLISH` makes Labs mode own the top display as a live developer-only preview surface. Labs should suppress stale normal calculator outcomes, render comparison math through stable display components, and keep raw LaTeX in details/accessibility surfaces rather than the primary visual table.
- 2026-05-21: Incubation Infrastructure is now the preferred framing for the research-to-product pipeline. The next infrastructure milestone should formalize source-mirror security, Labs runner policy, area-study synthesis modes, missing-capability gates, and truth-first math output taxonomy before starting a new major cross-engine area study.
- 2026-05-21: `FRICAS-CTX0` is closed as a single-source context milestone. Future work should not be organized around FriCAS alone; ideas first noticed through FriCAS should become multi-source `AREA-*` capability studies where FriCAS is one evidence source alongside other relevant systems.
- 2026-05-21: `INCUBATION-INFRA1` makes source mirrors, Labs runners, and area studies explicit guardrails before new cross-engine research. Source mirrors have security tiers and no-execution policies; Labs runners are dev-only, no-history, no-remote, and no-source-mirror-execution; area studies are the required structure for future multi-source capability synthesis.
- 2026-05-21: `SOURCE-CAPTURE1` uses shallow static captures as the default for local source mirrors. Captured mirrors remain context-only under ignored `playground/sources/mirrors/<mirror-id>/`, use `static-only` plus `no-execute`, and may be deepened later only when a concrete area study needs history. Closed/proprietary calculator installations are excluded from the source-mirror registry.
- 2026-05-21: `AREA-POLY-RAT0` establishes the first multi-source capability-area study over polynomial and rational substrates. Its exact next recommendation is bounded `INT-RAT1`, limited to one-variable exact rational functions with distinct rational linear partial fractions and derivative-backed verification. Repeated factors, irreducible quadratics, square-free factorization, resultants, Grobner/elimination, exact linear algebra, source execution, and code copying remain out of scope or deferred.
- 2026-05-21: `INT-RAT1` adopts only the distinct-rational-linear partial-fraction slice into stable calculus. The `partial-fractions` strategy is allowed for verified app-owned one-variable exact rational integrations over shared `POLY-RAT-CORE0` substrates; repeated factors, irreducible quadratic partial fractions, square-free factorization, resultants, Grobner/elimination, broad rational integration, source-mirror execution, and Risch/Liouville-style work remain deferred.
- 2026-05-22: `AREA-POLY-RAT1` makes full-domain polynomial/rational atlas work the planning source for future POLY/RAT milestones. The exact next recommendation is `POLY-RAT-CORE1`, not `INT-RAT2`: repeated linear factors, irreducible quadratic readiness, square-free/factor-multiplicity facts, and stronger rational stop metadata should be added to the algebra substrate before calculus widens again. Future polynomial/rational implementation slices should come from the multi-source atlas, not single-engine imitation.
- 2026-05-22: `.memory/research/roadmaps/poly-rat-native-roadmap.md` is the planning bridge from `AREA-POLY-RAT1` to implementation. `POLY-RAT-CORE1` should be a substrate leap, `INT-RAT2` should be a later consumer, and simplification or elimination work should reopen through separate `AREA-SIMPLIFY0` / `AREA-POLY-ELIM0` studies rather than being smuggled into rational integration.
- 2026-05-22: `POLY-RAT-CORE1` promotes repeated-rational-linear and irreducible-quadratic denominator family readiness into the shared rational substrate without widening calculus. `INT-RAT1` distinct-linear behavior remains stable; `INT-RAT2` may later consume the new typed readiness envelopes only through derivative-backed verification and existing domain-safety gates.
- 2026-05-22: `AREA-SIMPLIFY0` chooses `SIMPLIFY-CORE0` before `INT-RAT2`. The blocker is broader than rational-integration result wording: Calcwiz needs a bounded shared normal-form/readback/equivalence policy that preserves denominator/domain facts before visible rational-calculus widening resumes. This is not approval for a broad simplifier or single-engine imitation.
- 2026-05-22: `SIMPLIFY-CORE0` is an internal policy substrate only. It defines form intent, equivalence trust, and preserved facts for readback/adoption gates, but it does not add new simplification rules, visible result labels, UI badges, solver behavior, or calculus behavior by itself.
- 2026-05-22: `INT-RAT2` may widen the existing `partial-fractions` integration strategy to bounded repeated rational linear and irreducible quadratic denominator families only when the candidate antiderivative passes the shared verification/readback policy. It must preserve `inverse-trig` and `derivative-ratio` priority, add no new `ResultOrigin` values or strategy labels, and keep broad rational integration, Risch/Liouville work, high-degree/algebraic-root factor families, and source-mirror execution out of scope.
- 2026-05-22: `CALC-RAT-READBACK0` treats rational integral display/readback as a separate polish layer from integration-family adoption. It may clean app-owned `partial-fractions` LaTeX, add explanatory detail sections, and update Guide examples, but it must not add new calculus capability, `ResultOrigin` values, strategy labels, source-mirror execution, or Playground runner behavior.
- 2026-05-22: `AREA-ASSUMPTIONS0` selects `ASSUMPTIONS-CORE0` as the next bounded internal substrate. The need is broader than denominator exclusions or branch policy alone: Calcwiz needs request-scoped, result-attached facts for domain constraints, exclusions, branch/principal-range choices, interval hazards, candidate rejection, and equivalence trust before more algebra/calculus/table/graphing-readiness widening. This is not approval for global mutable assumptions, broad inequality solving, graphing behavior changes, or source-mirror execution.
- 2026-05-22: `ASSUMPTIONS-CORE0` defines assumptions as scoped internal facts, not global runtime assumptions. The bounded vocabulary covers domain exclusions, domain constraints, interval hazards, branch/principal-range facts, candidate rejection, and equivalence trust, with source and trust metadata. It does not add a public `assume(...)` feature, broad inequality solving, graphing behavior, branch-cut theorem proving, or visible result changes.
- 2026-05-22: `ASSUMPTIONS-ADOPT1` adopts assumption facts internally only. Existing rational-function, domain-range, branch, candidate-rejection, and simplify/readback facts may map into `AssumptionFact[]`, but visible result text, detail sections, badges, result origins, history/provenance, source-mirror boundaries, and Labs runner behavior must remain unchanged until a later explicit readback or product milestone.
- 2026-05-22: `ASSUMPTIONS-READBACK0` is the explicit visible-readback milestone for the scoped assumption fact spine. It may add explanatory `DisplayOutcome.detailSections` for existing domain exclusions, interval hazards, candidate checking, branch facts, and trust notes, but it must not change primary answer LaTeX, solver/calculus/table behavior, strategy labels, result origins, history/provenance, source-mirror boundaries, or Labs runner behavior.
- 2026-05-22: `ASSUMPTIONS-POLISH1` makes concise fact readback the default product posture. Full source/trust/check detail remains available through the opt-in `Detailed Facts` setting, but backend fact generation and primary answers remain unchanged.
- 2026-05-22: `DOMAIN-GRAPH-READY0` creates shared domain sampling readiness for Table and future graphing work. It may produce reusable sampled-hazard and assumption facts, but it is not plotting, graph UI, interval arithmetic, or a graph correctness proof.
- 2026-05-23: Future milestone numbering should reserve `0` for audit, study, surveillance, and readiness work. Actual implementation work should start at `1` unless an older historical milestone name already exists; for example, the study is `AREA-POLY-ELIM0` and the first implementation slice would be `POLY-ELIM1`.
- 2026-05-23: Graphing remains an endgame product area, not a near-term recommendation. Existing graph-readiness infrastructure such as `DOMAIN-GRAPH-READY0` remains historical and reusable, but future planning should not propose graphing milestones again until the calculator is much closer to broadly stabilized/completed.
- 2026-05-23: `AREA-POLY-ELIM0` selects `AREA-EXACT-LINEAR-ALGEBRA0` as the exact next move before `POLY-ELIM1`. The study found that resultants, Grobner bases, and elimination require exact coefficient-domain and exact linear-algebra readiness; Calcwiz should not invent exact row-reduction or matrix algebra locally inside polynomial-elimination code.
- 2026-05-23: `AREA-EXACT-LINEAR-ALGEBRA0` selects `EXACT-LINEAR-ALGEBRA1` as the next implementation slice. The first exact-linear-algebra pass should be an internal bounded exact rational matrix core using current number-backed `ExactScalar` with strict size/coefficient/denominator growth stops; product-facing `MATRIX-EXACT1`, `POLY-ELIM1`, and bigint-focused `EXACT-SCALAR1` remain later decisions.
- 2026-05-23: `EXACT-LINEAR-ALGEBRA1` establishes exact linear algebra as an internal reusable substrate, not a product Matrix mode or solver feature. The core may support determinant, RREF/rank, square solve, and inverse over capped number-backed exact rationals; product `MATRIX-EXACT1`, symbolic linear-system solving, `POLY-ELIM1`, bigint scalar work, graphing, source-mirror execution, and Labs runner work remain separate future milestones.
- 2026-05-23: `POLY-ELIM1` establishes polynomial elimination only as a bounded internal scalar univariate resultant substrate. Resultants are computed from Sylvester matrices through the exact matrix determinant core; bivariate elimination, Grobner bases, multivariate representation, Equation solver adoption, product Matrix exact mode, graphing, source-mirror execution, copied source, and Labs runner work remain separate future milestones.
- 2026-05-23: Multivariable work is an app-wide variable-semantics roadmap, not a POLY/RAT subtask. Calcwiz must distinguish solve targets, stored numeric variables, symbolic parameters, active variables, and bound variables before `POLY-ELIM2` or product-facing bivariate elimination can proceed; stored variables must never silently override solve targets.
- 2026-05-23: `AREA-MULTIVAR0` selects `VARIABLE-CORE1` as the next implementation milestone. Equation target selection, variable memory, calculus variable widening, polynomial systems, and `POLY-ELIM2` must wait until a shared internal symbol-discovery and variable-role classification core exists.
- 2026-05-23: Variable identifiers are case-sensitive, but coding-style multi-character string variables are deferred. `K` and `k` are distinct symbols; raw adjacent letters such as `hello` should not be silently accepted as one named variable and should instead be treated as multiplied single-character symbols where the parser supports that, or stopped as ambiguous/unsupported until a future explicit named-variable milestone exists.
- 2026-05-23: Reserved-token highlighting is future visible UX work, not part of `VARIABLE-CORE1`. The variable core should classify reserved functions, reserved constants, variables, and ambiguous identifiers so a later `EDITOR-VARIABLE-HINTS1` or `VARIABLE-READBACK1` milestone can color or annotate them without inventing a separate parser policy.
- 2026-05-23: `VARIABLE-CORE1` is internal metadata only. It may expose shared symbol discovery, reserved-name filtering, identifier classification, variable-role facts, and structured stops to internal consumers, but it must not add Equation target UI, variable memory, named string variables, visible semantic highlighting, multivariable solving, `POLY-ELIM2`, graphing, source-mirror execution, or Labs runner behavior.
- 2026-05-23: `EQUATION-TARGET1` is the first visible solve-target foundation, not parameterized solving. Equation may auto-retarget safe single-variable non-`x` equations through the existing `x` backend and show a `Solve for` selector for multi-symbol equations, but multi-symbol parameterized solving, variable memory, named string variables, multivariable solving, `POLY-ELIM2`, graphing, source-mirror execution, and Labs runner work remain separate future milestones.
- 2026-05-24: OOE (Order Of Execution) is accepted as a valid Rust-first kernel execution-order direction, not a new solver or UI controller. The first OOE gates should be `OOE-RS0` readiness/docs and `OOE-RS1` Rust schema plus pure validation. Progressive and atomic solver ideas are future metadata/roadmap work only; do not bundle chunk scheduling, checkpoint ledgers, streaming, cancellation wiring, remote routing, or solver rewrites into the first OOE milestones.
- 2026-05-27: OOE is also the long-term execution traffic-control and observability spine. Future OOE design should include execution priority, host selection, budget enforcement, cancellability, stale-result commit policy, main-thread safety, trace events, diagnostic buffers, and developer/agent observability. The first OOE gates remain conservative (`OOE-RS0`/`OOE-RS1` only); do not bundle runtime routing, solver behavior changes, MCP servers, chunk scheduling, checkpoint ledgers, or real cancellation into the first gates.
- 2026-05-27: `OOE-RS1` adopts typed string newtypes, a small Rust module tree under `src-tauri/src/ooe/`, and structured serde validation errors as the first OOE code foundation. The module may define schema/versioned plan, node, policy, trace-event, and pure validation types, but it must not expose Tauri commands, add a TypeScript bridge, route runtime work, schedule jobs, cancel solvers, change UI behavior, or alter solver outputs.
- 2026-05-27: `OOE-RS2` adopts a Rust-only built-in plan registry with one validated plan per current kernel capability and lightweight descriptors for future command listing. The registry is internal Rust data only; no Tauri commands, TypeScript bridge, runtime routing, scheduling, solver behavior, UI behavior, trace buffer, MCP bridge, or cancellation behavior is introduced.
- 2026-05-27: `OOE-RS3` exposes only narrow Tauri diagnostic commands for listing built-in OOE plan descriptors, fetching a built-in plan, and validating a submitted OOE plan. Invalid plans return a validation report object rather than a command failure; unknown built-in lookups return `null`. RS3 must not add frontend invoke wrappers, route calculator execution, change UI behavior, execute solvers, add trace buffers, introduce MCP diagnostics, or implement scheduler/cancellation behavior.
- 2026-05-27: `OOE-RS4` adds a thin TypeScript diagnostics bridge over the RS3 commands with zod response parsing and explicit unavailable web-preview fallbacks. Rust remains canonical; TypeScript mirrors are adapter convenience only. RS4 must not add UI consumers, runtime routing, solver execution, scheduler/cancellation, trace buffers, history/schema changes, MCP diagnostics, or a TypeScript OOE registry/validator.
- 2026-05-23: `EQUATION-PARAM5` extends selected-target parameterized Equation solving only through bounded exp/log inverse-pair isolation and one safe handoff to existing PARAM1-4 solvers. Supported bases are `e`, common log base `10`, and explicit positive numeric bases not equal to `1`; symbolic bases, log-combine sums/quotients, Lambert W, nested exp/log towers, broad transcendental algebra, variable memory, named string variables, `POLY-ELIM2`, graphing, source-mirror execution, and Labs runner work remain out of scope.
- 2026-05-23: Parameterized Equation solving is now its own `EQUATION-PARAM*` roadmap, not a hidden extension of `EQUATION-TARGET1` or `POLY-ELIM2`. The lane must solve for one explicitly selected target while preserving other symbols as symbolic parameters, family by family: affine/linear first, then bounded polynomial, rational, absolute/radical/power, exponential/logarithmic, and trigonometric slices. This is not variable memory, named string variables, broad transcendental algebra, bivariate elimination, graphing, source-mirror execution, or CAS parity.
- 2026-05-23: `EQUATION-PARAM1` adopts only affine/linear selected-target parameterized solving. Non-target symbols are preserved as symbolic parameters, symbolic coefficients produce explicit nonzero facts, and raw adjacent-letter products such as `xz` remain unsupported unless multiplication is explicit. Polynomial-in-target, rational-target, absolute/radical/power, exponential/logarithmic, and trigonometric parameterized solving remain later `EQUATION-PARAM*` slices.
- 2026-05-23: `EQUATION-PARAM2` adopts only real-guarded quadratic selected-target parameterized solving. Non-target symbols remain symbolic parameters, symbolic leading coefficients produce nonzero facts, discriminants produce real-domain facts, and raw adjacent-letter products remain unsupported unless multiplication is explicit. Higher-degree parameterized polynomial solving, rational target-denominator solving, variable memory, named string variables, bivariate elimination, `POLY-ELIM2`, graphing, source-mirror execution, and Labs runner work remain later milestones.
- 2026-05-23: `EQUATION-PARAM3` adopts only bounded rational selected-target solving by LCD clearing and delegation to existing `EQUATION-PARAM1`/`EQUATION-PARAM2` cleared equations. Original denominator exclusions and simple derived nonzero facts must be preserved. Broad rational simplification, higher-degree cleared equations, nested target denominators, variable memory, named string variables, bivariate elimination, `POLY-ELIM2`, graphing, source-mirror execution, and Labs runner work remain later milestones.
- 2026-05-23: `EQUATION-PARAM4` adopts only bounded nonperiodic selected-target carrier solving. It may isolate absolute-value, square-root, and square-power carriers and delegate generated branch equations to existing `EQUATION-PARAM1`/`EQUATION-PARAM2`/`EQUATION-PARAM3`, but it must not reopen deep or periodic `COMP` composition. Branch/domain facts and delegated denominator facts must be preserved; broader inverse-function composition, variable memory, named string variables, bivariate elimination, `POLY-ELIM2`, graphing, source-mirror execution, and Labs runner work remain later milestones.
- 2026-05-23: Restriction/readback formatting such as `(...)^{-1}` versus `1/(...)` is deferred polish, likely under `EQUATION-PARAM-READBACK1` or `EQUATION-PARAM7`; capability milestones should not expand scope to fix display-only notation unless explicitly planned.
- 2026-05-23: `EQUATION-PARAM6` adopts only direct affine selected-target trigonometric carrier solving. It may isolate one `sin`, `cos`, or `tan` carrier with a target-free affine shell and affine selected-target argument, emit periodic families with integer parameter `n`, honor the active angle unit through explicit inverse-trig conversion factors, and preserve range/nonzero/periodic-family facts. It must not add trig identity solving, multiple-carrier solving, nonlinear trig arguments, periodic/deep `COMP` reopening, variable memory, named string variables, `POLY-ELIM2`, graphing, source-mirror execution, or Labs runner work.
- 2026-05-23: `EQUATION-PARAM7` is readback, Guide, replay, and roadmap polish only. It may normalize selected-target detail wording, improve safe restriction readback, preserve selected targets in history/Guide launches, and add Guide examples for PARAM1 through PARAM6. It must not add solver families, result origins, badges, variable memory, named string variables, `POLY-ELIM2`, graphing, source-mirror execution, or broad CAS behavior.
- 2026-05-23: The post-PARAM7 Equation roadmap reserves `EQUATION-PARAM8` for stronger rational parameterized handling, `EQUATION-PARAM9` for higher-degree/factorable polynomial selected-target solving, `EQUATION-PARAM10` for symbolic-base exp/log policy, `EQUATION-PARAM11` for bounded one-layer composition handoff, and `EQUATION-PARAM12` for mixed-carrier/composition handling. Composition and mixed-carrier work must not be pulled into PARAM7.
- 2026-05-24: `EQUATION-PARAM8` adopts rational selected-target normalization only. It may flatten nested rational structures, rational quotients, parameter-only denominators, and bounded rational sums before LCD clearing, preserve original and easy derived nonzero facts, and return parameter-condition results when the selected target cancels. It must not add higher-degree cleared solving, mixed-carrier/composition solving, symbolic-base exp/log solving, Guide content, variable memory, named string variables, `POLY-ELIM2`, graphing, source-mirror execution, Labs runner work, result origins, or badges.
- 2026-05-24: `EQUATION-PARAM9` adopts factorable selected-target polynomial solving only. It may solve explicit zero-products up to degree 4 by delegating supported linear/quadratic factors to existing selected-target solvers and may reuse the existing exact-rational bounded cubic/quartic factor core. It must not add general symbolic cubic/quartic formulas, degree greater than 4, partial solution sets from unsupported factors, Guide content, rational widening, composition, symbolic-base exp/log solving, variable memory, named string variables, `POLY-ELIM2`, graphing, source-mirror execution, Labs runner work, result origins, or badges.
- 2026-05-24: `EQUATION-PARAM10` adopts symbolic-base selected-target exp/log solving only under strict real-domain facts. Target-free symbolic bases may isolate through log-base or power notation, same-base symbolic reductions may reduce directly, and target-in-base power/log-base cases use the principal positive real branch with explicit positivity/nonzero/base-not-one facts. Lambert W, log-combine search, arbitrary mixed exponential-polynomial solving, target in both base and exponent/argument, extra integer/rational exponent branches, Guide content, composition widening, variable memory, named string variables, `POLY-ELIM2`, graphing, source-mirror execution, Labs runner work, result origins, and badges remain out of scope.
- 2026-05-24: `EQUATION-PARAM11` adopts bounded one-layer selected-target composition handoff only. It may invert one outer nonperiodic, exp/log, or direct trig carrier, generate branch equations, and delegate those branches to existing selected-target helper files; it also fixes the PARAM10 generated-power readback bug by parenthesizing nested powers. Nested/two-layer composition, mixed-carrier equations, broad/deep `COMP` reopening, new result origins, badges, history schema changes, variable memory, named string variables, `POLY-ELIM2`, graphing, source-mirror execution, and Labs runner work remain out of scope.
- 2026-05-24: `COMP13A` resumes the existing composition roadmap by refactoring the old guarded composition engine from inside, not by replacing it. Shared carrier/path detection, one-layer branch generation, branch-set provenance, and composition-depth policy may live in `composition-core`, while `composition-stage.ts` remains the guarded `x` runtime owner for recursion, candidate validation, periodic-family rendering, and legacy result formatting. `EQUATION-PARAM12` should consume that shared seam for future bounded selected-target two-layer/mixed-carrier solving rather than growing a parallel composition engine.
- 2026-05-24: `EQUATION-PARAM12` adopts bounded two-layer nested selected-target composition over the shared `composition-core` seam. It may invert one nested carrier chain of depth 2, preserve facts from both layers, and allow at most two independent integer-family parameters under strict branch caps. Additive mixed-carrier equations such as `sin(z)+sqrt(z)=a`, depth-three chains, variable memory, named string variables, `POLY-ELIM2`, graphing, source-mirror execution, Labs runner work, result-origin changes, badge changes, and history schema changes remain out of scope.
- 2026-05-24: `EQUATION-PARAM13` is reserved as a selected-target Equation error/boundary/readback polish pass, not a new solving-family milestone. User-facing messages should not mention PARAM milestone names or generic "outside this milestone" wording; they should explain the actual mathematical blocker such as range failure, branch cap, ambiguous adjacent symbols, target outside the carrier, or unsupported mixed carriers. Additive mixed-carrier capability moves to `EQUATION-PARAM14`.
- 2026-05-24: `EQUATION-PARAM13` implements the selected-target Equation error/boundary/readback polish pass through a boundary translator plus minimal helper-message cleanup. It preserves internal stop reasons and solver behavior while rendering user-facing `Why It Stopped` and selective `What To Try` details. It must not add new solving families, result origins, badges, history schema changes, variable memory, named string variables, `POLY-ELIM2`, or graphing.
- 2026-05-24: `EQUATION-PARAM14` adopts bounded algebraic additive mixed-carrier selected-target solving only. It may collect at most two abs/sqrt/square-power carriers, generate capped branch equations, preserve explicit branch/domain/denominator facts even when those facts still mention the selected target, and delegate generated equations to existing selected-target solvers. Direct trig mixed identities are reserved for `EQUATION-PARAM15`; broader transcendental algebra remains deferred for later discussion. Additive exp/log, Lambert W-style solving, variable memory, named string variables, `POLY-ELIM2`, graphing, result-origin changes, badge changes, and history schema changes remain out of scope.
- 2026-05-24: `EQUATION-PARAM15` adopts direct same-argument mixed sine/cosine selected-target solving only. It may solve bounded `A sin(u)+B cos(u)+Q=R` forms with affine selected-target argument `u`, symbolic target-free coefficients, `atan2` phase readback, active angle-unit scaling, and explicit coefficient/range/nonzero/periodic-family facts. Mismatched trig arguments, tangent mixtures, products, nonlinear arguments, additive algebraic/exp/log/trig mixtures outside this direct identity, broad trig identity search, variable memory, named string variables, `POLY-ELIM2`, graphing, result-origin changes, badge changes, and history schema changes remain out of scope. `EQUATION-PARAM15` closes the current parameterized Equation sequence; do not create `PARAM16` for now, and resume with `VARIABLE-MEMORY1`.
- 2026-05-24: Variable values and substitution now have a dedicated roadmap at `.memory/research/roadmaps/variable-values-and-substitution-roadmap.md`. `VARIABLE-MEMORY1` is the next implementation lane: it should add finite real numeric stored values, a dedicated Variables side panel, visible standard-Calculate substitution, and snapshot-based history replay. Equation symbolic solve must not substitute stored values in this slice; an explicit Equation stored-value action is deferred.
- 2026-05-24: `VARIABLE-MEMORY1` adopts stored numeric variables only for standard Calculate `evaluate`. Stored names are case-sensitive single-letter variables; values are finite real numeric values; substitutions are visible in `Stored Values` details and recorded as Calculate history snapshots. Equation symbolic solve, Table, Calculus, simplify/factor/expand, guided-calculus workflows, named-string variables, `POLY-ELIM2`, and graphing remain out of scope.
- 2026-05-25: `VARIABLE-MEMORY2` adopts stored numeric variables across Table, Basic/Advanced Calculus non-bound parameters, and Equation numeric solve only. Stored substitutions must be structured MathJSON replacements with protected names for active variables, bound variables, ODE `x/y`, partial derivative variables, and selected Equation targets. Equation symbolic solve, algebra transforms, named-string variables, broad multivariable solving, `POLY-ELIM2`, graphing, and symbolic stored values remain out of scope.
- 2026-05-25: `VARIABLE-MEMORY3` centralizes stored-value mode policy as apply/ignore/unsupported decisions. Ignored stored values may be explained through detailed-only `Variable Policy` notes, but Equation symbolic solve still does not substitute stored values and symbolic transforms keep variables symbolic.
- 2026-05-25: `EDITOR-VARIABLE-HINTS1` uses a hybrid visible-hint policy. Stable hint chips are the required product surface for variable semantics; inline MathLive token styling remains best-effort future polish only if it can be implemented without fragile DOM hacks. This milestone must not change parsing, solving, substitution, named-string variable policy, result origins, badges, or history behavior.
- 2026-05-25: `VARIABLE-READBACK2` is unsupported-guidance polish only. It may improve selected-target and variable-boundary wording, including cube-root/power isolation target-choice guidance, but it must not add algebraic isolation, named-string variable support, Equation symbolic stored-value substitution, solver-priority changes, result origins, badges, or history schema changes. `EQUATION-ALGEBRAIC-ISOLATION1` remains the next capability milestone.
- 2026-05-25: `NAMED-VARIABLES1` adds explicit named variables only through `@name`, `var(name)`, or their internal upright token form. Raw adjacent letters remain multiplication and should be hinted as such, not silently promoted to one variable. Variables panel accepts raw single-letter names but requires explicit syntax for multi-character names. Existing stored-value mode policies apply to named variables; Equation symbolic stored-value substitution, broad named symbolic solving, algebraic isolation, graphing, `POLY-ELIM2`, source-mirror work, and Labs runner changes remain out of scope.
- 2026-05-25: `NAMED-VARIABLES2` is UX, replay/readback, and target-boundary polish only. Variables panel insertion should use raw single-letter text for single-letter variables and canonical `@name` text for named variables; raw adjacent letters remain multiplication with guidance; explicit named variables may be symbolic parameters beside single-letter solve targets, but explicit named variables are not Equation solve targets yet. Equation symbolic stored-value substitution, raw multi-letter parsing, named symbolic target solving, algebraic isolation, graphing, `POLY-ELIM2`, source-mirror work, and Labs runner changes remain out of scope.
- 2026-05-25: `NAMED-VARIABLES3` enables explicit named variables from `@name` and `var(name)` as Equation symbolic solve targets, but only through existing bounded selected-target families. Raw adjacent letters remain multiplication, not one named variable; when ComputeEngine parses raw adjacent words as products, Equation may expose the parsed single-letter targets. Stored values remain ignored/protected in Equation symbolic solve, including stored named values.
- 2026-05-25: `EDITOR-KEYFLOW1` is editor UX/runtime polish only. MathLive `smartSuperscript` should be disabled so exponent entry stays intentional; physical Space should insert visible spacing and execution should trim only harmless trailing spacing; left/right wrapping is allowed only after MathLive fails to move at the true whole-field boundary; MathLive keypress/plonk sounds are disabled rather than re-routed.
- 2026-05-25: `EQUATION-ISOLATION1` adopts one-island selected-target isolation only. It may peel target-free add/subtract/multiply/divide shells around exactly one selected-target expression, preserve target-free denominator/nonzero facts, and delegate the generated equation to existing selected-target helper files. It must not add two-island algebra, broad simplification, Lambert W, symbolic cube-root/power isolation, Equation symbolic stored-value substitution, graphing, `POLY-ELIM2`, source-mirror work, Labs runner work, result-origin changes, or history-schema changes.
- 2026-05-25: `EDITOR-PERF1` adopts an analysis-boundary model rather than parent input debouncing. MathLive editor values and Run/EXE execution stay current, while preview rendering, variable hints, Equation target discovery, and algebra-transform eligibility may be debounced, huge-input guarded, and locally error-contained with last-safe output preservation. Solver cancellation, job infrastructure, and OOE wiring remain deferred.
- 2026-05-25: `EDITOR-RUNTIME1` makes display-header `Run`, `Stop`, and `Restart Editor` controls operate only over the editor-analysis boundary. `Stop` cancels queued/deferred editor analysis and preserves last-safe output; it must not claim solver cancellation or job interruption. `Run` still uses the current draft through the existing EXE/F1 execution path, while `Restart Editor` clears the active editor draft and stale editor-analysis cards before restarting analysis. True job/cancellation infrastructure remains deferred to OOE.
- 2026-05-25: App editor/input contrast is surface-aware: dark workspace editors, hint chips, and form controls use editor contrast tokens, while LCD display panels keep LCD ink. Equation Home must provide Back/F5/Esc escape parity to the launcher instead of trapping the user in the menu.
- 2026-05-25: Result cards use shared display hierarchy: exact output is labeled `Answer`, supplement restrictions are labeled `Valid when` and stay visible by default, and solver facts/method/context remain below as details. Display-only cleanup may remove redundant `Conditions:`/`Exclusions:` prefixes, but raw result data, copy, history, parser, and solver contracts stay unchanged.
- 2026-05-25: `APP-MEMORY1` adopts autosaved calculator memory rather than manual save prompts. Memory is enabled by default, settled-change autosave is the default, interval autosave must clamp to at least 20 seconds, and Reset History remains separate from Reset Calculator Memory. Desktop persistence extends the existing `calculator-state.json` without changing the Tauri identifier; browser preview uses versioned localStorage fallback.
- 2026-05-27: `APP-MEMORY1` calculator memory is core-only, not session restore. Settings, history, stored variables, and `Ans` may persist, but startup must intentionally open a clean empty Calculate editor and must not restore heavy drafts, result cards, mode/screen routes, or workbench state. Heavy previous work should be reachable through History replay rather than automatic startup restore.
- 2026-05-27: The visible app identity is `REZANOVA CLASSWIZ CALCULATOR` for browser title and Tauri product/window title; the existing Tauri identifier remains unchanged to preserve desktop storage continuity.
- 2026-05-27: `EQUATION-ALGEBRAIC-ISOLATION1` is the post-`EQUATION-ISOLATION1` selected-target power/root leap. It supports bounded affine selected-target cube/fourth-power isolation and algebraic composition handoff up to degree 4 with real even-root conditions, while broad symbolic cubic/quartic formulas remain guarded by a readback cap and stop cleanly when too large. No Equation symbolic stored-value substitution, bivariate elimination, graphing, source-mirror, Labs runner, result schema, history schema, or OOE behavior changes are included.
- 2026-05-27: `POLY-ELIM2` is backend-only bivariate resultant projection. It may represent two-variable exact rational polynomial inputs as polynomials in an explicitly eliminated variable with univariate coefficients in the retained variable, substitute finite stored numeric constants while protecting retained/eliminated variables, and return a normalized primitive univariate projection. Product-facing polynomial systems, Equation adoption, Grobner bases, graphing, complex symbolic solving, inequality solving, source-mirror execution, Labs runner work, result schema changes, and history schema changes remain out of scope.
- 2026-05-27: `POLY-SYSTEM1` adopts only bounded real 2x2 polynomial systems through Equation > Simultaneous > `Polynomial 2x2`. The fixed variables are `x` and `y`; stored finite numeric constants may substitute into coefficients while `x` and `y` remain protected; results must be validated candidate pairs from the original equations. Existing linear 2x2/3x3 solving remains unchanged, and Grobner bases, complex symbolic pairs, inequalities, 3x3 polynomial systems, graphing, source-mirror execution, Labs runner work, result schema changes, and history schema changes remain out of scope.
- 2026-05-27: MathLive spacing commands adjacent to operators are execution/display noise and should be normalized before preview/execution. Nonzero constant resultants in `POLY-SYSTEM1` represent inconsistent systems and should read back as no real solution pairs, while zero/dependent projections remain projection ambiguity.
- 2026-05-27: `BUNDLE-SPLIT1` is a frontend startup-size pass before `OOE-RS0`, not an OOE runtime-scheduling milestone. It may add build analysis, named vendor chunks, lazy non-initial workspaces/side surfaces, and dynamic imports for heavy runtime modules, but it must not change solver behavior, result semantics, history schema, variable policy, or cancellation/scheduling behavior. MathLive remains eager for the initial Calculate editor; Compute Engine may remain a named vendor chunk if it is not in the eager startup graph and app/eager budgets are enforced by a custom script.
- 2026-05-27: `OOE-RS0` is implemented as architecture/readiness only. It audits current execution seams and locks OOE as a Rust-first traffic-control contract, but it does not add Rust modules, Tauri commands, TypeScript bridge code, runtime routing, scheduling, cancellation, trace buffers, MCP servers, solver behavior, UI behavior, Progressive Solver, chunk scheduling, checkpoint ledgers, source-mirror execution, Playground runners, or remote execution. `OOE-RS1` is the first code milestone and should add Rust schema plus pure validation only under `src-tauri/src/ooe/`.
- 2026-05-27: `OOE-RS5` is the first fail-open runtime pilot and is intentionally invisible to normal users. It may validate the Rust built-in `plan.equation.solve` through the RS4 bridge, capture the existing guarded Equation stage attempts through the registered stage order, and return internal/test-visible metadata. It must still commit the exact same `DisplayOutcome` as `runEquationMode`, and it must not add scheduling, cancellation, stale-result control, trace buffers, MCP diagnostics, Rust solver execution, solver migration, result wording, badges, history schema, or UI changes.
- 2026-05-27: `OOE-RS6` adopts the internal trace/stability vocabulary while keeping Rust canonical and TypeScript mirrored only at the bridge boundary. Trace events may model plan validation, guarded stage attempts, provisional readiness, final stable outcomes, stale drops, cancellation, slow phases, failure, and commit decisions, but RS6 must not add a trace buffer, UI/debug panel, MCP diagnostics, scheduler, cancellation behavior, stale-result commit control, solver migration, result schema, history schema, badges, or user-facing result changes.
- 2026-05-27: `OOE-RS7` adopts OOE wrapping for standard Calculate expression actions only: `evaluate`, `simplify`, `factor`, and `expand`. It may validate matching built-in expression plans, emit coarse internal lifecycle trace events, and return test-visible metadata, but it must not wrap calculus workbench routes, advanced calculus, table, or algebra-tray transforms. It must not change visible output, history/result schemas, stored-value behavior, planner behavior, scheduling, cancellation, stale-result commit control, UI, or Rust execution.
- 2026-05-27: `OOE-RS8` adopts OOE wrapping for the active Table build route only. It may validate `plan.table.build`, emit coarse internal lifecycle trace events, and return test-visible metadata, but it must preserve exact `TableResponse` and `DisplayOutcome` behavior. It must not change visible output, history/result schemas, stored-value/replay/domain/row behavior, legacy `modeActionHandlers.ts`, scheduling, cancellation, stale-result commit control, UI, or Rust execution.
- 2026-05-27: `OOE-RS9` unifies the existing Equation, Expression, and Table OOE pilots behind a shared internal runtime envelope `{ payload, ooe }`. Runtime consumers may unwrap and commit only payloads, while OOE metadata remains internal/test-visible sidecar data. RS9 must not put OOE metadata into `DisplayOutcome`, add trace buffers, change visible results/history/schema/stored-value behavior, schedule/cancel work, enforce stale-result commits, or execute solvers in Rust.
- 2026-05-27: `OOE-RS10` is a tooling-only boundary validator. It may enforce OOE Rust and TypeScript dependency tiers through repo scripts and tests, but it must not change runtime routing, solver behavior, UI, result/history schemas, scheduling, cancellation, trace buffers, MCP diagnostics, stale-result commit control, or Rust solver execution. OOE production code must stay clear of UI, app-controller, Playground, source-mirror, `.memory`, Labs runner, tool-script, and broad solver/runtime dependencies outside the explicit pilot allowlist.
- 2026-05-27: Future solver execution modes are `classic`, `progressive`, and `atomic`. `classic` is the current one-shot calculator/CAS-style solve UX and may later use local compute or one external SSH/compute source. `progressive` is chunked asynchronous execution with a governor, search-first materialization, output streaming, and idempotent checkpoint/resume through a lightweight ledger plus persistent output storage; it may also use local compute or one external source. `atomic` is not the opposite of progressive and is not one-shot: it uses the same chunked/checkpointable/search-first architecture as progressive, but with a more aggressive resource policy and the ability to fan out across multiple external SSH/compute hosts. `OOE-RS11` should be reframed as solver execution policy metadata only; it must not implement chunk scheduling, checkpoint ledgers, streaming output, resumability, cancellation wiring, remote execution, or solver rewrites.
- 2026-05-27: Atomic solver mode is deferred for now because the aggressive resource policy and multi-external-host SSH/compute fanout make it harder than Classic or Progressive. Near-term OOE/PSE planning should actively handle only `classic` and `progressive`; `atomic` may remain in metadata as a future reserved mode but must not be implemented, exposed, or used to drive near-term execution behavior until Progressive is proven locally and the external/multi-host compute boundary is deliberately reopened.
- 2026-05-27: `OOE-RS11` adds only Classic/Progressive solver execution policy metadata to the Rust-owned OOE node schema. Current built-in capabilities remain `classic`, unchunked, final-only, full-materialization, local, and normal-resource. `progressive` metadata may validate only when chunked and remains future readiness; `singleExternal` is metadata-only and does not implement remote execution. `atomic`, multi-external-host fanout, aggressive resource policy, scheduler/cancellation wiring, checkpoint ledgers, streaming renderers, resumability, MCP diagnostics, UI controls, and Rust solver execution remain out of scope.
- 2026-05-27: `OOE-RS12` adds only job identity, input revision, and stale-commit assessment contract/helpers. `AlwaysCommit` commits unconditionally; `CommitLatestOnly` commits only matching input revisions and otherwise stale-drops; `CommitIfCurrent` commits matching revisions, stale-drops mismatches, and skips when no active revision exists; no-job routes are marked not-applicable. Current Equation, Calculate, and Table pilots/controllers must not enforce this contract yet. RS12 is a prerequisite for later stale-result gating, cancellation, editor containment, and Progressive Solver work, but it must not add runtime routing, scheduler behavior, cancellation, UI, history/result schema changes, trace buffers, MCP diagnostics, remote execution, or Rust solver execution.

- 2026-05-27: `OOE-RS13` threads deterministic job identity and RS12 commit-assessment metadata through the existing standard Calculate, shared Equation, and active Table OOE pilots only. Job IDs and input revision IDs are derived from stable canonical route snapshots; default active revisions equal the job revision so current pilots record would-commit metadata without enforcing it. Runtime controllers/hooks must still commit only payloads. RS13 must not add stale-result enforcement, cancellation, scheduling, UI, history/result schema changes, solver behavior changes, Rust execution, trace buffers, MCP diagnostics, remote execution, or Progressive Solver implementation.

- 2026-05-27: `OOE-RS14` enforces stale-result commit legality only for standard Calculate expression actions (`evaluate`, `simplify`, `factor`, `expand`). Completed jobs compare their deterministic input revision against the current active Calculate input revision before committing; stale results are silently dropped and replay substitution snapshots are not cleared. OOE bridge/preflight failures remain fail-open when revisions still match. Calculate workbench routes, algebra transforms, Equation, and Table remain outside stale enforcement; no cancellation, scheduler, UI, history/result schema, solver behavior, Rust execution, MCP diagnostics, or Progressive Solver behavior is added.

- 2026-05-28: `OOE-RS15` enforces stale-result commit legality only for existing OOE-covered Equation routes: symbolic Equation solve and Equation numeric-interval solve. Completed Equation jobs compare deterministic input revisions against the current active Equation request before committing; stale results are silently dropped, and numeric replay substitution snapshots are not cleared on stale drop. OOE bridge/preflight failures remain fail-open when revisions still match. Non-symbolic Equation screens, coefficient polynomial tools, polynomial systems, linear systems, algebra transforms, Table, scheduler, cancellation, active job registry, UI, history/result schema changes, solver behavior changes, Rust execution, MCP diagnostics, and Progressive Solver behavior remain out of scope.

- 2026-05-28: The next OOE traffic-controller sequence is locked as `OOE-RS16` active job registry, `OOE-RS17` cancellation contract, and `OOE-RS18` editor runtime containment and control lane. Table stale gating remains the next enforcement candidate if Equation stale gating works cleanly, but the registry/cancellation/editor path is the strategic route toward OOE as a real app traffic controller.

- 2026-05-28: `OOE-RS16` adds an internal in-memory active job registry for existing OOE-covered pilots only. It tracks active jobs and retains a bounded recent lifecycle buffer for `completed`, `staleDropped`, `skipped`, and `failed` records. Expression, Equation, and active Table pilots register jobs, but runtime consumers still commit only payloads. Table remains metadata-only with no stale-commit gate. Cancellation, scheduling, UI diagnostics, trace panels, MCP endpoints, history/result schema changes, solver behavior changes, Rust execution, remote execution, and Progressive Solver behavior remain out of scope.
- 2026-05-28: `OOE-RS17` adds only the cancellation contract over the active OOE job registry. It may record `cancelRequested` active state, terminal `cancelled` state, request metadata, and helper APIs for requesting/querying/marking cancellation. Current Expression, Equation, and Table pilots must not stop, skip, interrupt, or visibly change runtime work; cancellation requests on current one-shot TypeScript jobs are advisory metadata until RS18 containment and later cooperative/isolated runtimes. UI Stop controls, scheduler, worker isolation, hard interruption, Rust solver migration, trace buffer, MCP diagnostics, history/result schema changes, result wording changes, and solver behavior changes remain out of scope.
- 2026-05-28: `OOE-RS18` is the first visible OOE control-lane milestone. Display-header `Run`, `Stop`, and `Restart Editor` may coordinate editor analysis, MathEditor remount/crash containment, and current-lane RS17 cancellation requests for standard Calculate, Equation symbolic, and active Table jobs. Current one-shot TypeScript solvers remain advisory-only and are not interrupted; RS14/RS15 stale gates remain the real commit protection. Scheduler, worker/iframe sandboxing, Rust solver execution, trace panels, MCP diagnostics, history/result schema changes, solver output changes, and Progressive Solver behavior remain out of scope.

- 2026-05-28: `EQUATION-RESULT-HYGIENE1` is a result-surface polish milestone before resuming OOE RS work. It fails closed on internal symbolic artifacts such as `\mathtip`, `\blacksquare`, `\error`, and `tuple<...>`; preserves canonical exact LaTeX for copy/editor flows; normalizes only safe multiplication-shaped readback such as raw adjacent-letter products and monomial parenthesized products; and contains large Answer/Valid When blocks with scrolling. It must not add a new solver family, broad simplification engine, OOE behavior, history schema change, or fake exact answer. The likely next product milestone is `EQUATION-ANSWER-MODES1` so Equation jobs can distinguish Exact, Approximate, and Isolate/Rearrange intent before `OOE-RS19`.

- 2026-05-28: `EQUATION-ANSWER-MODES1` adopts three explicit Equation answer modes. `Exact` is the strict symbolic selected-target path and must stop numeric-only fallback output with guidance; `Approx` means the existing numeric interval solve lane, must not invent automatic intervals, and only runs when stored-value substitution leaves no non-target symbolic parameters; `Isolate` is a first-class textbook formula-rearrangement mode that peels target-free shells and may apply direct inverse operations such as real root branches for simple powers, showing even-power formulas with `\pm`, but it must not delegate to broad Exact-mode solving or chase monster exact expansion. The selected answer mode is persisted, shown on symbolic Equation result cards, stored in history where applicable, and included in OOE Equation snapshots as intent metadata only. No broad solver family, simplification engine, target-containing denominator/radical capability, Equation symbolic stored-value substitution, fake exact answer, OOE scheduling/cancellation behavior, or Rust solver execution is added.
- 2026-05-29: `OOE-RS19` enforces stale-result commit legality for the active Table build path only. Completed Table jobs compare their deterministic input revision against the latest active Table request before committing `TableResponse`, `DisplayOutcome`, or replay-snapshot clearing; stale completions are silently dropped and the previous visible table/result remains. The legacy `modeActionHandlers.ts` Table path, Table math, row/warning behavior, history schema, result wording, UI layout, central coordinator, scheduler, cancellation enforcement, trace UI, MCP endpoint, worker/Rust host migration, Progressive Solver, and broad OOE routing remain out of scope.
- 2026-05-29: Post-RS19 OOE sequencing is extended as a traffic-controller roadmap, not a Progressive Solver roadmap: `OOE-RS20` central runtime coordinator, `OOE-RS21` editor analysis budget lane, `OOE-RS22` diagnostics trace buffer, `OOE-RS23` host adapter contract, `OOE-RS24` cooperative budget/cancellation pilot, and `OOE-RS25` first isolated runtime pilot. OOE is the app traffic controller for ordering, priority, budgets, stale commits, cancellation contracts, host routing, and diagnostics. Progressive Solver remains a separate future execution strategy that can depend on OOE; it is not required for OOE to be useful and should not be blended into RS20 unless explicitly planned.
- 2026-05-29: `OOE-RS20` adds the central internal runtime coordinator for existing OOE-covered lanes: standard Calculate expression actions, Equation symbolic/numeric interval, and active Table builds. The coordinator owns job identity start, active registry start, plan preflight, runtime execution, post-run commit assessment, registry completion/failure, and envelope return. Active input revision is resolved after the runtime executes so edits during a job can still stale-drop. Route wrappers remain public-compatible, visible output and stale gates are unchanged, and no scheduler, budget policy, trace buffer, MCP diagnostics, Rust solver execution, worker isolation, Progressive Solver behavior, UI change, result schema change, or math capability is added.

- 2026-05-29: `OOE-RS21` adds the first editor-analysis budget lane. Rust built-in OOE plans now include editor variable hints, Equation target discovery, Calculate transform eligibility, Equation transform eligibility, and preview render handoff under `editor-analysis-runtime`. TypeScript editor-analysis helpers run only after existing debounce and huge-input guards, resolve active input revisions after analysis, stale-drop or skip outdated/stopped/restarted analysis, and preserve the last safe hints/target/eligibility/preview value. RS21 does not change typing, canonical editor state, solver behavior, visible math output, history/result schemas, scheduler behavior, worker isolation, Rust solver execution, Progressive Solver behavior, MCP diagnostics, trace UI, or math capability. `OOE-RS22` remains the diagnostics trace buffer and solver provenance milestone.

- 2026-05-29: `OOE-RS22` adds the first internal OOE diagnostics trace buffer and app-wide executable-workspace provenance. Diagnostics records are bounded in memory and include job identity, route label, plan/capability/host IDs, terminal status, commit assessment, trace events, timing, and compact provenance. Rust/bridge built-ins now cover existing expression/equation/table/editor lanes plus provenance-only Calculate workbench/algebra transform, Advanced Calc, Trigonometry, Statistics, Geometry, Matrix, and Vector plans; Guide and Labs remain excluded. Equation diagnostics are the richest route and may include answer mode, selected target, guarded stage attempts, winning stage/helper, stop summary, generated isolation/rewrite details, and output hygiene/readback status. Table diagnostics must not store table rows. RS22 must not add a public diagnostics UI, Tauri trace command, MCP endpoint, scheduler, cancellation enforcement, worker/Rust solver migration, Progressive Solver behavior, history/result schema changes, or solver behavior changes. `OOE-RS23` remains the host adapter contract milestone.

- 2026-05-29: `OOE-RS23` adds the host adapter contract layer. Rust owns built-in host descriptors for the current active hosts (`expression-runtime`, `equation-runtime`, `table-runtime`, `editor-analysis-runtime`, `advanced-calculus-runtime`, `trigonometry-runtime`, `statistics-runtime`, `geometry-runtime`, and `linear-algebra-runtime`), while TypeScript mirrors them through the OOE bridge. Host descriptors capture host kind, thread safety, supported task classes, budget policy, cancellation policy, default result stability, and diagnostic description. Future worker, iframe, Rust/Tauri command, and progressive runner host kinds are schema-only in RS23. The coordinator attaches fail-open host adapter metadata to internal envelopes and diagnostics records; host metadata failures must not block execution. RS23 must not add scheduler behavior, budget enforcement, cancellation enforcement, public diagnostics UI, MCP endpoint, worker/iframe/Rust host migration, Progressive Solver behavior, history/result schema changes, or solver behavior changes. `OOE-RS24` is the first cooperative budget/cancellation pilot.

- 2026-05-29: `OOE-RS24` is the first cooperative OOE traffic-control pilot and is limited to active Table builds. Table remains TypeScript-executed on the main thread, but the OOE wrapper may generate rows cooperatively, yield between row batches, check the active job registry for cancellation requests, and stop with a controlled visible cancellation note. Cancelled Table jobs are terminal `cancelled` in registry/diagnostics, use `notApplicable` commit decision metadata, do not replace the previous `TableResponse`, and do not clear replay substitution snapshots. Synchronous `runTableMode` remains behavior-stable for direct callers. `OOE-RS25` remains the first isolated runtime pilot. Equation cancellation is explicitly post-RS25 expansion work, not part of RS24 or a redefinition of RS25.
- 2026-05-29: `OOE-RS25` is the first isolated runtime pilot and is limited to active Table builds. The active `table.build` OOE plan now targets `table-worker-runtime`, a Web Worker host with isolated budget policy, hard-stop cancellation, and worker-safe thread-safety metadata. `table-runtime` remains the cooperative main-thread fallback. The Table wrapper attempts the worker first, falls back when the worker is unavailable or fails, terminates the worker on cancellation, and records host execution metadata in OOE diagnostics without storing table rows. Completed payloads must match `runTableMode`; stale drops and cancelled visible-state rules remain unchanged. RS25 does not add Equation cancellation, Progressive Solver behavior, Rust solver execution, public diagnostics UI, MCP endpoint, history/result schema changes, row-limit changes, or Table math semantic changes.
- 2026-05-29: After `OOE-RS25`, the OOE roadmap is intentionally paused before deeper Equation cancellation/isolation. The post-RS25 OOE upgrade notes remain valid and must not be renamed: `OOE-RS26` is Equation guarded-stage cancellation checkpoints, `OOE-RS27` is an Equation heavy-helper isolation pilot, and `OOE-RS28` broadens Equation cancellation coverage. The pause exists because inequalities and complex-number foundations will expand solver semantics, domain facts, answer modes, and provenance; those foundations should be established before OOE wraps more Equation internals.
- 2026-05-29: Inequalities and complex support should be planned together under `.memory/research/roadmaps/inequalities-complex-foundation-roadmap.md`. The preferred strategy is simultaneous but coordinated: first define shared domain/value/fact contracts, then advance bounded inequalities and complex-number rails in parallel. Do not let the inequality rail and complex rail independently redefine "real solution", "no real solution", complex roots, validity conditions, or stored-value policy.
- 2026-06-02: Complex support should be opt-in through a top-header `Complex`/complex-domain toggle rather than silently appearing in ordinary real-first workflows. When disabled, users should continue seeing real-domain behavior and "no real solution" style outcomes where appropriate; when enabled, bounded complex-capable routes may return visibly marked complex-domain answers. Inequality and complex foundations must be reusable cores, not one-off Equation hacks, and should reuse existing branch/domain/assumptions cores where relevant for branch facts, principal/range facts, denominator exclusions, radicand requirements, and validity readback instead of inventing parallel fact systems.
- 2026-06-02: Inequality and complex product adoption is Equation-first until the semantics stabilize. Shared value/domain/fact, branch, interval, and complex-number cores should be reusable across the app, but visible solver expansion into Calculate, Table, Calculus, Advanced Calc, Trigonometry, Geometry, Statistics, Matrix, or Vector is deferred. The first product-facing complex and inequality routes should live in Equation so answer modes, selected targets, validity facts, and "no real solution" versus complex-enabled behavior can be hardened in one workspace before wider adoption.
- 2026-06-02: `INEQ-COMPLEX-FOUNDATION0` freezes inequality/complex readiness as a documentation-only audit. The shared answer-domain vocabulary is `real`, `complex`, `conditional-real`, and `unknown-domain`; solution kind remains separate as exact symbolic, approximate numeric, isolate/rearranged formula, inequality solution set, or condition/fact-only stop. `VALUE-DOMAIN-CORE1` is the next implementation substrate and should extend/reuse `assumptions-core`, `branch-core`, `domain-range-core`, and the existing `numeric/complex` primitive instead of adding visible solver behavior directly.
- 2026-06-02: `VALUE-DOMAIN-CORE1` implements the shared internal TypeScript value/domain/fact contract only. It extends the existing assumption fact spine with inequality and complex-domain fact vocabulary and adds value-domain metadata builders, but deliberately does not thread metadata into `DisplayOutcome`, add the top-header `Complex` toggle, change OOE diagnostics, alter history/app-state/Rust schemas, or expose inequality/complex solving. `COMPLEX-CORE1` and `INEQUALITY-CORE1` remain separate future cores that must consume this shared contract.
- 2026-06-02: `COMPLEX-CORE1` implements only the bounded internal complex-number primitive and readback helpers. It adds complex conjugate, argument, polar construction, integer powers, principal nth roots, deterministic all nth roots, and principal/all-branches readback helpers under `src/lib/numeric/complex.ts`. It deliberately does not add a complex parser, the top-header `Complex` toggle, Equation adoption, stored complex variables, complex Approximate search, `DisplayOutcome`, history, app-state, OOE, Rust, Tauri schema, or visible UI behavior changes.
- 2026-06-02: `INEQUALITY-CORE1` implements only the bounded internal inequality interval/fact core. It adds typed one-variable real interval unions, typed constructors, canonical finite-union normalization, intersection, containment, empty-set detection, equality, and readback helpers under `src/lib/algebra/inequality-core.ts`, plus conversion into `inequality-core` assumption facts and `ValueDomainMetadata`. It deliberately does not add inequality parsing, Equation adoption, broad nonlinear solving, piecewise/graphing behavior, public assumptions UI, `DisplayOutcome`, history, app-state, OOE, Rust, Tauri schema, or visible UI behavior changes.
- 2026-06-02: `EQUATION-DOMAIN-INTENT1` adds only the product-facing complex-domain intent toggle and request/provenance threading. The top header exposes persisted `Complex Off` / `Complex On`, stored as `settings.equationDomainIntent` with `real` default and desktop/web sanitization. Equation symbolic requests, OOE input revisions, active request refs, history replay, and rich OOE provenance now carry the intent, and Complex-enabled Equation results show a visible `Domain intent: Complex` badge. Solvers remain real-first; no complex answers, complex parser, stored complex values, inequality solving, non-Equation adoption, history schema change beyond the optional history field, OOE behavior change, Rust solver execution, or result semantics change is included. `COMPLEX-EQUATION1` remains the first milestone allowed to return bounded complex Equation answers.
- 2026-06-02: `COMPLEX-EQUATION1` is the first product-facing complex Equation adoption slice. Complex answers are opt-in and limited to `Complex On` plus Equation answer mode `Exact`; bounded quadratics with negative discriminants and simple selected-target power equations may return marked complex-domain branches. `Complex Off` keeps real-first stops and successes, `Approximate` stays real numeric interval solving only, and `Isolate` stays textbook rearrangement. Optional `answerDomain` metadata may appear on outcomes/history and OOE provenance. The milestone deliberately does not add a complex parser, stored complex variables, complex Approximate search, inequality solving, non-Equation adoption, broad transcendental complex solving, OOE runtime behavior changes, or Rust solver execution.
- 2026-06-03: `INEQUALITY-EQUATION1` is the first product-facing inequality adoption slice and is Equation-only. It supports only top-level ordered relations `<`, `<=`, `>`, and `>=` in Equation answer mode `Exact` when they reduce to one-variable numeric-coefficient linear inequalities `a*x+b relation 0`; negative coefficients flip the direction, and constant true/false reductions may return all-real or empty-set results. Results use `INEQUALITY-CORE1`, `answerDomain: conditional-real`, and `solutionKind: inequality-solution-set`; history/replay and Equation OOE provenance may carry `solutionKind`. `Approximate` and `Isolate` return controlled inequality guidance, `Complex On` adds a real-order note without changing inequality math, and `!=`, symbolic-parameter, multivariable, quadratic, rational sign-chart, absolute-value, trig/log/exp, chained, non-Equation, broad parser, stored-value policy, OOE behavior, and Rust solver expansion remain out of scope.
- 2026-06-03: `ANSWER-DOMAIN-READBACK1` is readback/layout polish only. It fixes persisted History bootstrap scanability by making collapsed cards non-shrinking input previews and expanded cards contained readbacks; it also consolidates Equation domain/solution chips, keeps ordered-inequality Complex intent notes distinct from actual complex-domain badges, fixes prose readback for ASCII inequality operators such as `<=`, and normalizes simple bounded complex branch display where safe. It must not add solver capability, parsers, stored complex values, history schema changes, OOE runtime behavior changes, or non-Equation inequality/complex adoption.
- 2026-06-03: After `ANSWER-DOMAIN-READBACK1`, the next larger inequality/complex leap should not use the vague milestone name `EQUATION-ADVANCED-DOMAIN1`. Keep the shared-substrate idea as `POLYNOMIAL-DOMAIN-CORE1` only if it stays narrow: classifier/extractor/fact metadata for one-variable polynomial/rational shapes, degrees, coefficients/factors, and domain exclusions, with no solving or visible behavior by itself. Product-facing expansion should remain explicit and separate: `INEQUALITY-EQUATION2` for bounded quadratic/factored real inequality sets and `COMPLEX-EQUATION2` for broader bounded polynomial/power complex answers.
- 2026-06-03: `POLYNOMIAL-DOMAIN-CORE1` implements only a shared pure-core classifier over existing exact polynomial and rational-function primitives. It may classify one-variable polynomial/rational metadata, expose degree/shape/coefficient/domain-exclusion facts, add `polynomial-domain-core` as an assumption fact source, and provide value-domain metadata helpers for future consumers. It must not solve, change Equation routes, change OOE, alter history/UI/result schemas, or implement `INEQUALITY-EQUATION2` / `COMPLEX-EQUATION2`.
- 2026-06-03: `INEQUALITY-EQUATION2` and `COMPLEX-EQUATION2` were implemented in one coordinated session but remain separate product rails. `INEQUALITY-EQUATION2` owns real ordered polynomial inequality solution sets in Equation `Exact`; `COMPLEX-EQUATION2` owns opt-in complex exact branches in Equation `Exact + Complex On`. They may share the Equation polynomial-domain helper, but they must not merge contracts, answer-mode wording, or future scope. Ordered inequalities remain real-domain-only even when Complex intent is enabled.
- 2026-06-03: `INEQUALITY-EQUATION2` supports one-variable numeric-coefficient polynomial inequalities up to degree 4 only when exact real roots are available through the bounded path. It may use sign charts over exact roots and return finite interval unions via `INEQUALITY-CORE1`. It must not add rational sign charts, symbolic-parameter inequality solving, multivariable inequality solving, chained inequalities, `!=`, Approximate inequality sampling, Isolate inequality rearrangement, non-Equation adoption, OOE behavior changes, or Rust solver execution.
- 2026-06-03: `COMPLEX-EQUATION2` supports factorable one-variable polynomial equations up to degree 4 only when `Complex On` and `Exact` are selected, factorization reduces to linear/quadratic factors, and at least one branch is non-real. It must not add a complex parser, stored complex values, complex Approximate search, Isolate complex solving, unfactorable cubic/quartic formula expansion, numeric-only fake exact roots, non-Equation adoption, OOE behavior changes, or Rust solver execution.
- 2026-06-03: `INEQUALITY-EQUATION3` is the major Equation-only guarded real inequality engine. It may use exact roots/critical points plus numeric sign-cell sampling for proof classification, but numeric-only roots must not become Exact answers. It may cover factorable rationals, textbook abs/radical forms, monotone log/exp forms, bounded supported composition, and direct affine periodic trig inequalities in Equation `Exact`; it must keep `Approximate` and `Isolate` as guidance-only for inequality inputs, keep ordered inequalities real-domain-only under `Complex On`, and avoid non-Equation adoption, graphing, chained inequalities, symbolic-parameter/multivariable solving, OOE behavior changes, or Rust solver execution.
- 2026-06-03: `INEQUALITY-READBACK-COMPOSITION1` moves inequality validity restrictions into the existing `Valid when` card and raises guarded composition without changing answer-mode boundaries. Main answers remain solution sets only; denominator exclusions, radicand/log domains, monotone-base facts, tangent singularities, period/family facts, and real-order notes belong in `exactSupplementLatex`, not duplicated proof details. Finite guarded wrappers may compose up to 4 layers. Trig composition supports representable two-trig-layer cases where an outer `sin`/`cos`/`tan` reduces to an inner affine `sin`/`cos` range; inner `tan` support is limited to all/empty outer-range cases with tangent singularities in `Valid when`, while nontrivial tangent-branch subfamilies remain guarded stops until the periodic-set model can represent them honestly. Ordered inequalities stay real-domain-only even under `Complex On`.
- 2026-06-03: Inequality wrapper polish for `INEQUALITY-READBACK-COMPOSITION1` is narrow shell isolation, not a broad rearrangement engine. Exact numeric target-free additive/multiplicative shells around a supported inequality carrier may be peeled before routing, including direction flips for negative numeric factors; symbolic factor signs and deeper algebra still stop. Verbose `Valid when` and proof/detail result blocks may collapse/expand like History cards, but without deletion and without hiding the main `Answer`.
- 2026-06-03: `INEQUALITY-PREIMAGE-READBACK2` completes the pending inequality preimage/readback work as a single combined milestone with `INEQUALITY-PREIMAGE1`. Abs-affine periodic inequalities may flatten through `|ax+b|` into `x`-alone branch families when the split into `ax+b` and `-(ax+b)` is safe; distance-family notation remains proof/fallback only. Periodic readback should prefer calculator-style symbolic shifts (`k\pi`, `2k\pi`, `\frac{k\pi}{5}`) and keep tangent singularities, branch-index facts, period facts, and real-order notes in `Valid when`. The milestone may respect `EXACT` / `DECIMAL` / `BOTH` output style for inverse-trig thresholds, and may extend finite rational/nested preimages through the existing bounded helper. It must still stop periodic-over-rational, periodic-over-nonlinear-abs, symbolic-threshold, chained, multivariable, and Tier 4 nonlinear periodic cases.
- 2026-06-03: `INEQUALITY-STABILITY1` is an Equation-only audit/fix stability gate after `INEQUALITY-PREIMAGE-READBACK2`, not a capability expansion. It may harden relation operator normalization for typed, pasted, copied, and replayed inequalities; add regression coverage for already-supported polynomial, rational, abs/radical/log/exp, finite composition, trig, and preimage families; and polish guidance/readback only where defects are exposed. It must not add new inequality families, Approximate inequality sampling, Isolate inequality solving, graphing, chained/multivariable/symbolic-parameter solving, complex ordered inequalities, non-Equation adoption, OOE behavior changes, or Rust solver execution.
- 2026-06-04: `COMPLEX-EQUATION3` is the major algebraic complex Equation milestone after `COMPLEX-INPUT1`. It may solve bounded algebraic `Exact + Complex On` routes with explicit imaginary constants, factorable one-variable polynomial equations through degree 4 when branches are exact/readable, selected-target power carriers already covered by the bounded complex route, and supported rational equations by numerator roots with denominator exclusions. It must not add stored complex values, non-Equation adoption, Approximate complex search, Isolate complex solving, complex trig/log/exp solving, Cardano/Ferrari unfactorable cubic/quartic formulas, numeric-only fake exact roots, OOE behavior changes, or Rust solver execution.
- 2026-06-04: `COMPLEX-READBACK-STABILITY1` is stabilization/readback polish only. Equation analysis treats `i` and `\imaginaryI` as reserved imaginary units that must display as `i reserved unit` and must not appear as solve targets, symbolic parameters, stored ignored values, or ambiguity hints. `j` and `k` remain ordinary symbols. Awkward exact complex branch construction may read back with clean polar/cis notation, while simple rectangular complex values should stay rectangular. The milestone must not add a new complex solver family, complex trig/log/exp solving, stored complex values, complex Approximate search, Isolate complex solving, reserved-symbol override syntax, non-Equation adoption, OOE behavior changes, or Rust solver execution.
- 2026-06-04: `COMPLEX-DISPLAY-SETTINGS1` is readback/settings stabilization only. Persist `complexExactForm` as `rectangular`, `polar`, or `cis`, defaulting to `rectangular`; do not include an `auto` mode. `rectangular` uses exact rectangular branches, `polar` uses expanded `r(cos(theta)+i sin(theta))`, and `cis` uses compact `r cis(theta)`. The top-header `Complex On/Off` button remains only the domain-intent toggle. `ImaginaryUnit` / `\imaginaryI` must display as the imaginary unit, never numeric `1`. No new complex solver family, complex Approximate search, Isolate complex solving, stored complex values, reserved-symbol override syntax, non-Equation adoption, OOE runtime behavior change, or Rust solver execution is included.
- 2026-06-04: `COMPLEX-PREIMAGE-EQUATION1` is the guarded complex function-preimage milestone after `COMPLEX-DISPLAY-SETTINGS1`. In Equation `Exact + Complex On`, it may reduce supported `ln` / `log`, `exp`, direct `sin`/`cos`/`tan`, affine shells, rational-linear inners, bounded powers, and selected quadratic-over-linear rational equations into exact inner equations, using principal complex log plus explicit integer-periodic families where needed. Periodic-power answers should use concise root-family main readback with expanded branches in detail sections. It must not add complex Approximate, complex Isolate, stored complex values, non-Equation adoption, absolute-value complex locus solving, broad unfactorable cubic/quartic formulas, OOE behavior changes, or Rust solver execution.
- 2026-06-04: `COMPLEX-PREIMAGE-EQUATION2` is the broader rational and true two-trig-layer complex preimage milestone before the paired complex preimage readback/stability pass. In Equation `Exact + Complex On`, it may clear supported rational denominators when the resulting exact equation stays inside bounded linear/quadratic/polynomial/power routes, and it may solve outer `sin`/`cos`/`tan` over inner `sin`/`cos`/`tan` with independent integer branch parameters such as `k,n in Z`. Inner trig arguments may be affine or bounded selected-target powers through degree 4. It must still stop periodic-over-rational, degree > 4, multivariable, absolute-value complex locus, complex Approximate, complex Isolate, stored complex values, non-Equation adoption, OOE behavior changes, and Rust solver execution.
- 2026-06-04: `DISPLAY-DETAIL-MATH-RENDER1` is the app-wide detail-card render contract paired with `COMPLEX-PREIMAGE-READBACK1` and `COMPLEX-PREIMAGE-STABILITY1`. `DisplayDetailSection.lines: string[]` remains the compatibility surface, while route producers may add whole-line math metadata or mixed prose/math parts. The display layer should render explicit math through `MathStatic`, keep ordinary prose through `NotationText`, and only apply conservative known-route prose splitting; it must not force arbitrary prose through math parsing. This pass is readback/stability only and must not add new complex solving capability, complex Approximate, complex Isolate, stored complex values, OOE behavior changes, non-Equation adoption, or Rust solver execution.
- 2026-06-05: `OOE-RS26` resumes the paused OOE roadmap as Equation guarded-stage cancellation checkpoints only. Stop requests for active `equation.solve` jobs may be observed at guarded-stage boundaries, after no-outcome stage exits, before recursive guarded handoffs, and before direct symbolic fallback. Cancelled Equation envelopes must be terminal `cancelled` with `notApplicable` commit assessment, preserve previous visible result/history/replay substitutions, and show only transient stopped status. This milestone must not interrupt a currently executing heavy helper mid-call, migrate Equation to a worker, execute solvers in Rust, add a scheduler rewrite, expose public diagnostics UI/MCP, change result/history schemas, or add solver capability.
- 2026-06-05: `OOE-RS27` is the first Equation heavy-helper isolation pilot and is scoped only to the terminal guarded `direct-symbolic` helper. The `equation.solve` OOE plan remains hosted by `equation-runtime`; `equation-direct-symbolic-worker-runtime` is recorded as helper-level host evidence, not as a route migration. Worker completion must preserve main-thread direct-symbolic payload parity; worker unavailable/init/runtime failure may fallback to the main-thread helper with evidence; Stop cancellation must hard-terminate the worker and must not fallback. RS27 must not migrate the full Equation solver, interrupt other helper families, execute solvers in Rust, expose public diagnostics UI/MCP, change result/history schemas, or add solver capability.
- 2026-06-05: `OOE-RS28` broadens Equation cancellation coverage cooperatively inside the guarded async path. The Equation-local control contract may adapt OOE `yieldIfBudgetExceeded`, and helper evidence may include helper/family/branch/candidate metadata. RS28 may add async checkpoint/yield coverage for guarded stages, recursive guarded handoffs, substitution branches, and candidate-validation preparation, but it must preserve RS26 cancelled visible-state behavior and RS27 direct-symbolic worker isolation. RS28 must not add solver capability, migrate the full Equation solver to a worker, add another isolated host, execute solvers in Rust, expose public diagnostics UI/MCP, or change result/history schemas.
- 2026-06-05: `OOE-RS29` chooses the in-app developer diagnostics inspector path, not MCP. The inspector is dev-gated by `VITE_SHOW_OOE_DIAGNOSTICS === '1'`, is read-only over diagnostics except for clearing in-memory diagnostics/recent-job buffers and copying one selected record as JSON, and must not add public user UI, persistence, export files, Tauri diagnostics commands, MCP endpoints, solver behavior changes, scheduler changes, result/history schema changes, or full result/table-row storage.
- 2026-06-06: `OOE-RS30` makes `equation.solve` the first full Equation worker runtime-shell pilot and adds transient History launch-order tickets. The built-in Equation plan may prefer `equation-worker-runtime` with `equation-runtime` as init/unavailable fallback only; worker cancellation hard-terminates and preserves RS26/RS28 no-commit visible-state behavior. Pending History tickets reserve launch-order positions and may expose Stop while running, but only finalized completed entries persist an optional launch-order key. RS30 must not add solver capability, non-Equation worker migration, scheduler rewrite, public diagnostics expansion, Rust solver execution, persisted fake pending records, or retry on main thread after a worker runtime failure has started.
- 2026-06-06: `OOE-RS31` locks the widening strategy as one central runtime-shell contract with multiple per-workspace shells, and keeps that contract separate from launch-ticket History ordering. Equation and Table are the only RS31 implementation lanes: Equation keeps RS30 behavior after extraction, while Table adopts pending History tickets and active Computing/Stopping status. Calculate, editor analysis, Advanced Calc, Trigonometry, Statistics, Geometry, Matrix, and Vector are audited and deferred rather than migrated blindly. RS31 must not add new solver capability, non-Equation/Table worker migration, scheduler rewrite, Rust solver execution, public diagnostics expansion, result schema changes, or persisted fake pending records.
- 2026-06-10: `CALCULATE-BOUNDARY0` locks Calculate as Calcwiz's quickform evaluator, not a guided topic workbench. Calculate may call shared algebra, calculus, trigonometry, numeric, domain, and readback capabilities for compact one-shot answers and transformations, including essential validity facts such as denominator exclusions. Rich guided derivatives, integrals, limits, periodic/composition analysis, method panels, proof trees, and future step-by-step flows belong to topic workspaces or reusable step capabilities. Because Calculate still has visible guided calculus/workbench remnants, do `CALCULATE-SURFACE1` and `CALCULATE-RESTRICTIONS1` before `CALCULATE-RUNTIME-SHELL1`; do not widen Calculate OOE/tickets while its product boundary is still blurry.
- 2026-06-10: `CALCULATE-RESTRICTIONS1` preserves validity facts already emitted by Calculate transform cores, such as denominator exclusions after removable-factor cancellation, through the existing `Valid when` path. It does not add a broad domain analyzer, guided proof tree, step-by-step engine, OOE runtime shell, or launch-ticket behavior.
- 2026-06-11: Branch-heavy finite answers should render as separate row-level math islands, not one giant horizontal math object and not a count-only receipt. Display may split a safe branch row into a prefix island (`target relation`) plus a branch-expression island for responsiveness, but the full row LaTeX and full `exactLatex` remain available for accessibility, tests, Copy Result, To Editor, history, replay, and stored output. This remains a Display responsibility; OOE still owns only compute traffic control, commit/drop legality, cancellation, diagnostics, and history-ticket ordering.
- 2026-06-11: OOE consistency fixes should use existing OOE systems only. Do not introduce an event bus, Surface Protocol, Supercarrier compartments, public SDK, plugin architecture, or remote-compute protocol as part of host/plan cleanup. Host descriptors are the source of truth for plan-node cancellation policy, thread-safety, and default result stability; compatibility workspace pilots should mirror active worker-primary hosts so diagnostics/provenance do not drift.
- 2026-06-12: `DISPLAY-PRODUCER-METADATA3` widens only producer-owned finite branch metadata where the producer already has real branch arrays before formatting. Display should prefer validated `branchReadback`, fall back to safe extraction, and fail closed to the original math block for ambiguous output. Full `exactLatex` remains canonical for copy, editor, history, replay, and storage. Do not add branch metadata to facts, grids, tables, tuple systems, inequalities, periodic families, or ordinary measurement summaries.
- 2026-06-12: `DISPLAY-SCHEDULER-POLISH1` keeps committed-result render scheduling in Display, not OOE. Answer/error content should mount first, `Valid when` second, then approx/warnings, periodic-family blocks, and details. Later blocks may mount one display turn/frame at a time, collapsed math-heavy bodies may lazy-mount only when opened, and the display status may show `Rendering result` while committed blocks remain queued. OOE still owns launch, cancellation, stale gates, commit/drop, diagnostics, and history tickets.
- 2026-06-12: `FILE-SIZE-RATCHET1` adds an enforced file-size ratchet to `test:gate`: tracked `src/**/*.ts|tsx` files are capped at 900 lines by default, pre-existing larger files are capped at their introduction size +5% in `tools/file-size-baseline.json`, and `--update-baseline` may only lower or remove caps. Raising any cap requires a deliberate, review-visible edit to the baseline file with justification. Growth past a cap should be answered by extraction, not appending; this guards the post-APPMAIN-SLIM shape against regrowth.
- 2026-06-12: `MEMORY-STATE-SLIM1` fixes `.memory/current-state.md` as a bounded current operating snapshot: at most 500 lines, enforced by the memory-protocol validator. Finished-milestone history moves verbatim to `.memory/research/milestones/` archive files instead of accumulating in current-state. Future posture updates should replace or trim existing bullets rather than append history; the journal, sessions, and decisions files remain the chronological record.
- 2026-06-12: Agent workflow guardrail decision: meaningful code/tooling/UX/workflow commits must include durable memory updates or an explicit no-memory-needed note; internal milestone slices are verification gates/checkpoints rather than automatic `MILESTONE1a/b/c` commit boundaries unless the user requests split commits; file-size ratchet caps must lower to exact current line counts after slimming with no percentage headroom.
- 2026-06-12: Repo structure reorganization decision: the next organization lane starts with `REPO-STRUCTURE-AUDIT1`, followed by scoped AppMain extraction and move-only district splits for Equation/Algebra before any cleanup/deletion. Structure work must preserve solver behavior, OOE behavior, display policy, schemas, worker hosts, capability IDs, history/replay compatibility, and current product boundaries unless a later named milestone explicitly changes one of those contracts.
- 2026-06-12: `REPO-STRUCTURE-AUDIT1` locks the first recommended source move as `EQUATION-DISTRICT-SPLIT1` for the `equation-parameterized-*` selected-target family. The split should be move-only, keep paired tests beside implementation, preserve exports/behavior/history/OOE/display contracts, and avoid splitting `composition-stage.ts` in the same slice. `APPMAIN-SLIM6` remains the next orchestration-risk slice, but Equation district visibility is the immediate Explorer/navigation pain.
- 2026-06-12: `EQUATION-DISTRICT-SPLIT1` completed the move-only selected-target parameterized district. The `equation-parameterized-*` implementation/test family now lives under `src/lib/equation/parameterized/` with short filenames, while `equation-selected-target-isolation.ts` stays at the Equation root as the dispatcher/seam. Preserve emitted runtime/source strings like `equation-parameterized-trig`; path-migrate file-size ratchet entries manually for moved over-cap files because `--update-baseline` lowers/removes existing caps but does not add new moved-path caps automatically.
- 2026-06-12: `APPMAIN-SLIM6` completed the Trigonometry-only hook extraction. `src/app/runtime/useTrigonometryRuntime.ts` owns Trig screen/menu state, draft state, refs, Guide seed/example loading, history replay restoration, current/full reset helpers, and `runTrigAction` while preserving `trigonometry.evaluate`, worker/fallback host IDs, launch-ticket/stale-gate behavior, history/replay payloads, schemas, and Trigonometry workspace props. `AppMain` remains the cross-mode orchestration root; do not fold Equation districts, Algebra moves, or a global reducer/event bus into this slice.
- 2026-06-13: `APPMAIN-SLIM7` completed the Statistics-only hook extraction. `src/app/runtime/useStatisticsRuntime.ts` owns Statistics screen/menu state, dataset/frequency/source-sync state, probability/inference/point-set state, draft state, refs, Guide example loading, history replay restoration, current/full reset helpers, and `runStatisticsAction` while preserving `statistics.evaluate`, worker/fallback host IDs, launch-ticket/stale-gate behavior, history/replay payloads, schemas, and Statistics workspace props. `AppMain` remains the cross-mode orchestration root; continue `APPMAIN-SLIM8` as Geometry-only rather than a global reducer/event-bus slice.
- 2026-06-13: `APPMAIN-SLIM8` completed the Geometry-only hook extraction. `src/app/runtime/useGeometryRuntime.ts` owns Geometry screen/menu state, all shape/coordinate form state, draft state, refs, route/menu derived values, solve-missing templates, Guide seed/example loading, history replay restoration, current/full reset helpers, and `runGeometryAction` while preserving `geometry.evaluate`, worker/fallback host IDs, launch-ticket/stale-gate behavior, history/replay payloads, schemas, and Geometry workspace props. `AppMain` remains the cross-mode orchestration root; future slimming should be planned as a fresh slice rather than introducing a global reducer/event bus.
- 2026-06-13: `APPMAIN-SLIM9` completed the Guide route/content hook extraction. `src/app/runtime/useGuideRuntime.ts` owns Guide route state, per-route selection state, route/list/article/mode-ref/search derived values, Guide soft-menu entries, parent/back navigation, search query updates, Guide open helpers, and reset behavior. `launchGuideExample` and mode-specific Guide shortcuts remain in AppMain because they are cross-mode orchestration. `APPMAIN-CALCULUS-RUNTIME1` should land as one real commit after diff-only gates, not as public a/b/c/d commits.
- 2026-06-13: `APPMAIN-CALCULUS-RUNTIME1` completed the Calculus runtime hook extraction as one final commit after internal diff-only gates `1a-d`. `src/app/runtime/useCalculusRuntime.ts` owns Calculus navigation/menu state, Calculus form state, shared derivative/derivative-at-point workbench state, refs, preview generation, seed loading, reset helpers, history context/replay restoration, and `runAdvancedCalcAction`. Preserve canonical `calculus` identity, legacy `advancedCalculus` read/replay compatibility, `calculus.evaluate`, existing worker/fallback hosts, request/generator input-revision parity, stale-drop/cancellation semantics, and variable-substitution snapshot policy. Future AppMain slimming should continue as bounded hook extractions, not a global reducer/event-bus/framework slice.
- 2026-06-12: `EQUATION-COMPOSITION-DISTRICT1` completed the move-only composition district. `composition-stage.ts`, `composition-core.ts`, and the core test now live under `src/lib/equation/composition/` as `stage.ts`, `core.ts`, and `core.test.ts`, while root `composition-stage.ts` and `composition-core.ts` remain compatibility re-export wrappers. Preserve existing composition source strings, badges, error text, detail-section titles, branch metadata sources, exact output, guarded runtime ownership, and file-size ratchet caps. `COMPOSITION-STAGE-SPLIT1` may extract stable helpers next but must not change solver behavior.
- 2026-06-12: `COMPOSITION-STAGE-SPLIT1` completed the conservative helper extraction inside the composition district. `composition/stage.ts` remains the guarded composition runtime owner and re-exports the moved public carrier helpers for compatibility. `composition/carriers.ts` owns symbolic branch helpers, affine/power/shifted/quadratic carrier matching, and branch builders; `composition/periodic-family.ts` owns periodic-family formatting, supplement/range/metadata merge helpers, badge helpers, and solve-summary helpers. Keep future composition work behavior-gated: no solver output, OOE, schema, history/replay, capability, or display contract changes in structure-only slices.
- 2026-06-13: `GEOMETRY-DISTRICT-SPLIT1` is a move-only reorganization of the `src/lib/geometry/` solver district. Per-shape inverse `solve*Missing` solvers live under `solve-missing/` by family; the shared numeric/coordinate resolution layer lives in a district-level `resolvers.ts` (shared by forward and inverse solvers); the two parser strategies live under `parser/` with helpers in `parser/shared.ts`. `core.ts` and `parser.ts` remain the public dispatcher files (no import churn for external callers, tests unchanged). No solver/geometry math, request/evaluation/seed/history/OOE/display, or mode-surface change. Both files dropped out of the file-size baseline.
- 2026-06-14: `ARCHITECTURE-DOCS-GROUPING1` makes domain grouping canonical for `docs/architecture/`. Architecture notes live under ownership-area folders (`algebra`, `app-shell`, `calculus`, `display`, `engine`, `equation`, `modes`, `ooe`, `overview`, and `symbolic-engine`) with filenames preserved, `docs/architecture/README.md` as the detailed map, and `docs/README.md` as a slim top-level pointer. This is documentation organization only, not a runtime, solver, OOE, display, or workflow architecture change.
- 2026-06-18: `WORKSPACE-TABS-LAUNCHER-ACTIONS1` locks launcher tab actions as explicit workflow UI: primary launcher open remains current-tab retarget, while hosted workspace leaves expose `Open in new tab` through a visible row action and leaf context menu. Labs stays open-here only, Guide is not a launcher leaf, and `WORKSPACE-TABS-DEFAULTS1` is deferred so the `+` tab button remains blank Calculate.
- 2026-06-18: `WORKSPACE-TABS-PAGE-FOUNDATION-AUDIT0` locks tabs V1 as functionally complete for current session workspace use and ready as a future full-surface layer. Future management/page tabs must add a thin tab-surface/page-kind descriptor and per-surface action policy instead of expanding calculator `ModeId` with pseudo pages. Quick side panels stay as fast companions, and Graphing/Spreadsheet artifact or saved-work records remain separate future storage/history audits.
- 2026-06-18: `WORKSPACE-RUNTIME-ELAPSED-TIMER1` locks runtime elapsed timing as per-workspace UI/history metadata. Running runtime jobs show calm whole-second elapsed status; finished jobs show a two-decimal final elapsed `Ready` status until that workspace starts another runtime job. Finalized committed History entries may persist optional `runtimeElapsedMs`, but OOE remains the sole runtime traffic authority for launch, stale gates, cancellation, host routing, diagnostics, and commit/drop legality.
- 2026-06-19: `WORKSPACE-RUNTIME-STOP-CONTROL-FIX1` locks Display header Stop as an active workspace runtime-ticket cancellation surface when a hosted runtime job is pending. Header Stop and Restart must prefer the active scoped pending ticket before legacy editor-analysis cancellation, preserving OOE as the cancellation authority and avoiding unscoped latest-job cancellation across workspace tabs.
- 2026-06-19: Calcwiz should eventually have a first-class Language compartment, not a single loose translation file. English is the safe default/fallback for reset, invalid persisted language settings, missing packs/keys, and failed validation. Language owns prose, labels, commands, interpolation, and `ltr`/`rtl` metadata; Display/notation owns math rendering, and RTL metadata must not blindly change math semantics. The dedicated roadmap is `.memory/research/roadmaps/language-roadmap.md`, and the first path is `LANGUAGE-SURFACE-AUDIT0`, `LANGUAGE-COMPARTMENT-FOUNDATION1`, and a narrow shell pilot before settings, Display-text, Guide, solver/readback, Arabic/RTL, distro, plugin/pack, Graphing, or Spreadsheet work.
- 2026-06-19: Solver capability expansion resumes with search discipline before new algorithms. Preserve the corrected Claude handoff and dedicated roadmap, but treat live repo evidence as authoritative: the next Equation work targets selected-target/exp-log generated-equation routing and target-shape profiling, not a semantic-planner-only rewrite, a missing flat polynomial core, infinite recursion, graphing, step-by-step, or premature Rust migration.
- 2026-06-19: Defer the Exact/Isolate answer-mode boundary cleanup until after Equation search discipline stabilizes. The working future differentiator is that `Isolate` owns rearrangement/isolation intent, while `Exact` may build on isolation only when it adds roots, branch families, principal ranges, domain/exclusion facts, and candidate validation. Do not fold this UX/semantics cleanup into target-shape profiling or selected-target router performance work.
- 2026-06-19: `EQUATION-TARGET-SHAPE-PROFILE1` creates the target-shape owner/path/API as a pure Equation seam at `src/lib/equation/target-shape/` with root facade `src/lib/equation/equation-target-shape.ts`. It is profile-only: no live route threading, route pruning, solver behavior change, OOE/runtime change, Display/History/app-state change, or Exact/Isolate cleanup.
- 2026-06-19: `EQUATION-SELECTED-TARGET-ROUTER-PERF1` makes the target-shape profile the conservative route-pruning input for selected-target Equation routing. It may skip impossible family attempts in top-level parameterized routing and generated-equation handoffs, but mixed/unknown shapes must fall back to current route order and the helper must not become a broad semantic planner or new solver authority.
- 2026-06-19: `EQUATION-SEARCH-TRACE-EVIDENCE1` keeps selected-target search evidence internal and test-facing. Route traces may record profiles, skipped/attempted/success families, and final stop reasons, but they must not be stored in `DisplayOutcome`, History, app-state, Tauri settings, OOE diagnostics, or user-visible result cards without a later explicit milestone.
- 2026-06-20: `POLYNOMIAL-SYMBOLIC-COEFF-SEAM1` locks the coefficient-ordering decision for Equation search discipline: extract the shared Equation-owned MathJson-symbolic polynomial/rational coefficient seam before revisiting carrier, composition, and mixed-algebraic generated branch helpers. `polynomial-core` remains the exact-rational sibling/inspiration, not the dependency target for symbolic parameterized coefficients.
- 2026-06-20: `EQUATION-GENERATED-BRANCH-HANDOFF-SEAM1` locks generated branch handoff as a reusable delegation mechanic, not a solver authority. The shared seam may profile generated equations, route-gate caller-supported families, record internal trace evidence, and aggregate branch solutions/supplements, while carrier, composition, and mixed-algebraic solvers keep ownership of branch generation, facts, caps, fallback wording, readback, and source labels.
- 2026-06-20: `EQUATION-MATHJSON-ARITHMETIC-ADOPTION1` adopts shared `math-json.ts` arithmetic only where parity is already proven: carrier and mixed-algebraic. Linear arithmetic remains local because its raw-node behavior needs a separate parity audit before adoption.
- 2026-06-20: `EQUATION-SEARCH-DISCIPLINE-CLOSEOUT0` closes the search-discipline foundation as complete enough for future solver work to consume. Do not add another planner, trace layer, generated-handoff path, or symbolic coefficient representation without a concrete unsupported case proving the existing seams are insufficient.
- 2026-06-20: `EQUATION-CAP-RECALIBRATION-AUDIT0` locks cap expansion as evidence-first. Peel-depth and generated-branch caps may be recalibration candidates, but degree, formula-size, composition-depth, and independent-periodic-parameter caps protect correctness/readback/algorithm boundaries and must not be raised as mere performance tuning.
- 2026-06-20: `EQUATION-CAP-HIT-EVIDENCE1` locks configured cap-path sentinels as evidence of guard behavior, not evidence that default caps are too low. Any future cap raise needs real default-cap hit examples plus trace/readback evidence; algorithm/readback/semantic caps must route to capability or display-readback milestones instead.
- 2026-06-20/21: `EQUATION-ANSWER-SEMANTICS-TAGS1` was verified but then removed from the worktree before commit because `EQUATION-ANSWER-MODE-SIMPLIFICATION0` supersedes the Approximate answer-mode framing. Preserve the idea that `solutionKind` describes produced answers and that missing numeric values should point to exact Variables entries, but rework the implementation around active `Exact`/`Isolate` modes plus a contextual numeric interval route.
- 2026-06-21: Numeric Interval Solve revival should proceed as route/tool work after `EQUATION-ANSWER-MODE-SIMPLIFICATION1`, not by restoring `Approx` as an active Equation answer mode. First repair routing/launch clarity, then guidance/suggested-interval handoff, then sampling stability.
- 2026-06-21: Numeric Interval Solve results are numeric-route answers, so approximate roots must remain visible as the primary answer/readback even when symbolic output style is Exact. Do not fake them as `exactLatex`; Display should adapt `approxText`/numeric branch metadata for visible readback, and Copy Result should copy the numeric roots.
- 2026-06-21: Equation live input should normalize unbraced multi-digit numeric powers such as `^12` to braced LaTeX `^{12}` before app-state solving. This is an input-canonicalization fix, not a solver promise to accept malformed direct internal LaTeX everywhere.
- 2026-06-21: Real Exact special-form carrier frontiers may route before the shared symbolic fallback to avoid partial roots, but Complex high-degree special forms remain capped and must stop honestly until a dedicated complex frontier widens them.
- 2026-06-21: `EQUATION-AFFINE-CARRIER-SPECIAL-FORM-FRONTIER1` widens special-form roots only from pure `x^n` carriers to affine `(q*x+r)^n` carriers where `q` is nonzero exact-rational and `r` is target-free symbolic. Direct affine powers stay with algebraic isolation, symbolic outer carrier coefficients wait for the next frontier milestone, and Complex high-degree carrier roots remain deferred rather than partially solved as real-only roots.
- 2026-06-21: `EQUATION-SYMBOLIC-FACTOR-PATTERNS-FRONTIER1` locks symbolic factor-pattern discovery as an internal factorable-route seam, not broad symbolic factoring. It may recognize affine common-carrier powers and safe real `U^n-V^n` difference-of-powers patterns through degree 12, then delegate only linear/quadratic factors through existing solvers. Sum-of-powers, residual degree >2, target-bearing coefficients, denominator/function factors, Complex widening, Cardano/Ferrari, implicit visible roots, numeric Exact fallback, and DAG/search graphs remain separate future decisions.
- 2026-06-21: `EQUATION-COMPLEX-SPECIAL-FORM-FRONTIER1` widens Complex Exact special-form roots only for exact-rational direct and carrier-quadratic pure/affine cases through 12 visible branches. High-degree Complex branches must honor the selected `complexExactForm`: `cis` uses compact `cis` notation, while `rectangular`/`polar` use exact trigonometric branch notation until rectangular radical coordinates are implemented. Older low-degree Complex routes keep their existing readback. Symbolic-coefficient Complex carrier roots, non-real carrier-quadratic roots, branch counts above 12, broad Complex solving, visible implicit roots, and numeric Exact fallback remain deferred.
- 2026-06-21: `EQUATION-ELIMINATION-FRONTIER1` widens only the product-facing Polynomial 2x2 system route to 12-degree retained resultant projections. Algebra's elimination defaults stay conservative unless a caller opts in, candidate-pair validation remains mandatory, stored exact-rational constants may be substituted while `x`/`y` stay protected, and unsupported free symbolic parameters remain guidance instead of becoming guessed constants.
- 2026-06-22: `EQUATION-COMPLEX-SPECIAL-FORM-NOTATION-FIX1` locks high-degree Complex special-form readability as correctness: honor `complexExactForm`, render `cis` only when selected, use exact trigonometric branch notation for `rectangular`/`polar` until compact rectangular radical coordinates are implemented, and never silently replace a user-selected notation with misleading forced `cis`.
- 2026-06-22: `EQUATION-SYMBOLIC-FACTOR-DISCOVERY-FRONTIER2` keeps symbolic factor discovery as a bounded Equation factorable-route adapter. It may add explicit shared-carrier grouping and monic grouped affine-carrier quadratics through degree 12, but broad symbolic factoring, residual degree >2, target-bearing coefficients, and reusable factoring/expansion primitive promotion remain separate future work.
- 2026-06-22: `EQUATION-CARRIER-ELIMINATION-FRONTIER1` keeps carrier elimination as a bounded Equation-local adapter over explicit algebraic carrier equations. V1 may solve exact-rational linear/quadratic reduced equations in known carriers and back-substitute through existing branch solvers, but symbolic reduced-carrier coefficients, periodic/transcendental carriers, arbitrary auxiliary-variable inference, general Groebner/resultant solving, and reusable substitution/elimination primitives remain separate future work.
- 2026-06-22: `EQUATION-COMPLEX-SYMBOLIC-SPECIAL-FORM-FRONTIER1` keeps symbolic Complex special-form roots deferred until Calcwiz has a formal principal-branch root policy and root/readback representation for it. Exact-rational Complex special forms stay supported, but symbolic coefficients/constants must stop honestly rather than emitting visible `RootOf`, fake rectangular output, or informal `u^(1/n)` branch notation.
- 2026-06-22: The five reusable algebraic building blocks are named Symbolic Primitives and should be implemented as dedicated folders under `src/lib/symbolic-engine/primitives/`: `expansion`, `substitution`, `factorization`, `simplification`, and `elimination`. Future solver work should consume the relevant primitive when it exists, add the primitive first when repeated mechanics are emerging, or document why route-local logic remains semantic and owned.
- 2026-06-22: `SYMBOLIC-PRIMITIVES-COMPARTMENT-ROADMAP0` keeps Symbolic Primitives inside the existing `symbolic-engine` compartment for now instead of creating a new top-level compartment. Primitive APIs should be private-first, public facades should wait for proven consumers, and app-wide primitive-surveillance tooling is deferred until real primitive APIs and repeated bypasses exist.
- 2026-06-22: `SYMBOLIC-EXPANSION-PRIMITIVE1` makes expansion the first private Symbolic Primitive under `src/lib/symbolic-engine/primitives/expansion/`. Its v1 contract is bounded MathJSON expansion plus a narrow Equation polynomial-carrier follow-on consumer, including real Exact top-level quadratic-carrier handoff and a similarly narrow Complex Exact quadratic-carrier follow-on. It is not broad simplification, solver judgment, non-quadratic Complex carrier closure, or a public primitive facade.
- 2026-06-22: Final-answer readback polishing/normalization is a dedicated future milestone, not part of `SYMBOLIC-EXPANSION-PRIMITIVE1`. Local correctness leaks such as double-appended imaginary units may be fixed immediately, but broader presentation cleanup like `0 + ...`, reducible arithmetic fragments, equivalent radical spelling, and final sign/fraction normalization should be handled by a later readback/simplification pass.
- 2026-06-22: `SYMBOLIC-FACTORIZATION-PRIMITIVE1` keeps factorization primitives structural and private-first. The primitive may own explicit product decomposition, selected-carrier factor extraction, safe real difference-of-powers, shared-carrier grouping, grouped affine-carrier quadratic mechanics, multiplicity, and bounded unsupported reasons, but Equation keeps solver routing, stop wording, LaTeX/readback, branch/domain facts, root construction, validation, and current degree-12 frontier boundaries. `src/lib/symbolic-engine/factoring.ts` remains a later parity consumer.
- 2026-06-22: `SYMBOLIC-SIMPLIFICATION-SURFACE-AUDIT0` locks the first simplification primitive shape as structural and private-first, not final-answer prettification or a universal reducer. V1 should begin with bounded MathJSON simplification/arithmetic helpers and use `src/lib/symbolic-engine/primitives/factorization/node-helpers.ts` as the first consumer; radical, power/log, Algebra exact-rational, Complex branch/readback, route priority, and final-answer polish stay in separate later parity milestones.
- 2026-06-22: [agent: codex | model: gpt-5.5] `RELEASE-V0.2.0-LINUX` locks `v0.2.0` as a Linux-first early preview release target. Current release artifacts are AppImage, Debian/Ubuntu `.deb`, and RPM; Calcwiz must not claim native Arch packaging or full CAS parity for this preview, and publication/tag push remains a separate explicit action.
- 2026-06-22: [agent: codex | model: gpt-5.5] `SYMBOLIC-SIMPLIFICATION-PRIMITIVE1` keeps simplification structural and private-first. The primitive may own bounded MathJSON add/multiply/negate/subtract/divide/square helpers, additive-term splitting, exact scalar folding, stable structural keys, and node-limit stops, but it must not become final-answer readback polish, a route-priority policy, a theorem prover, or a broad ComputeEngine replacement.
- 2026-06-22: [agent: codex | model: gpt-5.5] `SYMBOLIC-ELIMINATION-PRIMITIVE1` completes the initial five private Symbolic Primitives. Elimination v1 is a MathJSON-first bridge over Algebra-owned exact resultant arithmetic and has Polynomial 2x2 as its only production consumer; it must not become Groebner-first solving, broad multivariable CAS behavior, single-equation auxiliary inference, or carrier-elimination migration without a later dedicated milestone.
- 2026-06-23: [agent: codex | model: gpt-5.5] `SYMBOLIC-PRIMITIVES-CONSUMER-AUDIT0` locks consumer expansion as parity-gated, not sweep-driven. Do not add a hard app-wide primitive-surveillance validator yet; expand each primitive only through focused consumer milestones that prove visible behavior, solver semantics, facts, and readback remain unchanged, or document why route-local logic stays owned.
- 2026-06-23: [agent: codex | model: gpt-5.5] `CALCULATE-ALGEBRA-ACTIONS-SURFACE-AUDIT0` locks Calculate `Simplify`, `Factor`, and `Expand` as public user-facing algebra actions rather than private Symbolic Primitive calls. Do not silently swap these soft keys to primitives during consumer parity; any future bridge needs explicit visible-output parity tests.
- 2026-06-23: [agent: codex | model: gpt-5.5] `SYMBOLIC-ELIMINATION-CONSUMER-READINESS0` locks elimination consumer expansion as product-pressure-driven. Polynomial 2x2 remains the only safe production consumer for now; carrier elimination, broader polynomial systems, higher-dimensional resultants, and auxiliary-variable inference require dedicated milestones before consuming or extending the elimination primitive.
- 2026-06-23: [agent: codex | model: gpt-5.5] `SYMBOLIC-PRIMITIVES-PARITY-CLOSEOUT0` pauses the Symbolic Primitives lane as complete enough for now. Remaining local primitive-like helpers are not automatic debt; Algebra exact-domain helpers, Equation route/fact/readback semantics, Calculate public actions, inequality/complex/variable-memory paths, and guarded transforms retain local ownership unless a focused parity milestone proves a primitive match.
- 2026-06-23: [agent: codex | model: gpt-5.5] `ANSWER-READBACK-POLICY-AUDIT0` locks final-answer readability as producer-side readback policy, not Display-side algebra cleanup. Equation readback should normalize safe exact root/branch expressions before `DisplayOutcome`; Display remains responsible for rendering, layout, notation, and fail-closed branch extraction. Symbolic Primitives may supply mechanics but must not own presentation semantics.
- 2026-06-23: [agent: codex | model: gpt-5.5] `ANSWER-READBACK-NORMALIZATION1` locks the first readback-normalization consumer as validated Equation root-set exact finite roots only. The normalizer may clean safe presentation noise with solve-target, variable-role, domain-intent, and reserved-identifier context, but it must not perform broad symbolic cancellation, radical extraction, `RootOf` rendering, Display-side algebra, or schema changes.
- 2026-06-23: [agent: codex | model: gpt-5.5] `ANSWER-READBACK-MATHJSON-BRANCHES1` locks finite Equation root/branch readback onto the existing MathJSON AST when branch nodes are available. Node-backed branches should be simplified recursively through the private Symbolic Simplification primitive, rendered to LaTeX, and then passed through safe final LaTeX polish; raw LaTeX readback remains fallback only.
- 2026-06-23: [agent: codex | model: gpt-5.5] Rust is explicitly not a target for MathJSON ASTs, expression trees, readback normalization, symbolic dispatch, simplification rewriting, rule matching, ODE/PDE/integration matching, or current target-size linear algebra. Future Rust candidates are limited to measured bottlenecks in homogeneous flat-array inner loops such as Groebner S-polynomial reduction or the Risch rational-function reduction inner loop; never pass expression trees, MathJSON, or AST fragments over Tauri IPC.
- 2026-06-24: [agent: codex | model: gpt-5.5] `ANSWER-PRESENTATION-SOURCE-MIRROR-AUDIT0` locks the next readback strategy: Calcwiz should converge on a mandatory producer-side answer presentation pipeline over MathJSON-backed presentation items, closer to source-mirror OutputForm/visitor-printer models. LaTeX string normalization remains final polish and compatibility fallback only; solver routes should not keep inventing finished visual LaTeX when expression structure is available.
- 2026-06-24: [agent: codex | model: gpt-5.5] `ANSWER-PRESENTATION-PIPELINE-ROADMAP0` locks the first implementation sequence: introduce an internal Equation presentation IR in `ANSWER-PRESENTATION-IR1`, adapt it back to current `DisplayOutcome` compatibility fields, and migrate finite root/set producers one family at a time. Display must remain a renderer, and broad LaTeX-to-MathJSON parsing, Rust AST migration, and all-at-once producer rewrites remain out of scope.
- 2026-06-24: [agent: codex | model: gpt-5.5] The external Equation algorithms handoff is preserved verbatim as source context, not direct implementation approval. Future algorithm work must interpret it through the live Calcwiz prerequisites gate: representation, symbolic primitives, facts/assumptions, validation, route evidence, readback/presentation, and tests must already exist, be built in the same milestone, or the algorithm must stop and record the missing prerequisite.
- 2026-06-24: [agent: codex | model: gpt-5.5] `EQUATION-POLYNOMIAL-ALGORITHM-PREREQ-AUDIT0` blocks Cardano/Ferrari formula implementation for now. Preserve the existing degree-2 `SymbolicTargetPolynomial` seam for current quadratic/rational/special-form consumers, add a separate degree-3/4 symbolic coefficient substrate first, require Cardano/Ferrari roots to enter through node-backed `EquationRootSet`/finite-root presentation, and do not expose symbolic complex intermediate radicals until a formal principal-branch/root policy or policy-approved real-form output exists.
- 2026-06-24: [agent: codex | model: gpt-5.5] `EQUATION-POLYNOMIAL-N_DEGREE-SUBSTRATE1` is internal substrate only. It may add degree-3/4 coefficient collection, a non-live Cardano/Ferrari policy inspector, internal `family-stop` route evidence, and cubic/quartic-sized presentation tests, but it must not add formula construction, inert formula templates, a live cubic/quartic route, user-visible stop changes, or replacement of the degree-2 `SymbolicTargetPolynomial` compatibility seam.
- 2026-06-24: [agent: codex | model: gpt-5.5] `EQUATION-COMPLEX-INTERMEDIATE-POLICY1` locks PrincipalRoot as Calcwiz's explicit symbolic Complex principal-root policy for new high-degree direct special-form output. Direct symbolic Complex powers with exact-rational carrier coefficients may solve through degree 12 using `\operatorname{PrincipalRoot}_{n}` plus `cis` or trigonometric branch multipliers. Existing low-degree symbolic Complex power output remains compatible; Cardano/Ferrari formulas, symbolic carrier quadratics, visible `RootOf`, implicit-root display, and Display/History/OOE/app-state/Tauri/schema changes remain out of scope.
- 2026-06-24: [agent: codex | model: gpt-5.5] `EQUATION-CUBIC-CARDANO-ROUTE1` makes Cardano live only as a Complex Exact selected-target route for direct degree-3 symbolic polynomials. The route uses the n-degree symbolic polynomial substrate, emits structured `PrincipalRoot_3` branch nodes through finite-root presentation, records `cubic-cardano` route evidence, and exposes nonzero supplements for symbolic leading coefficients and Cardano denominators. Real Exact Cardano, Ferrari/quartics, generated-handoff Cardano, symbolic carrier quadratics, visible `RootOf`, implicit-root notation, schema changes, OOE, Display, History, app-state, and Tauri remain out of scope.
- 2026-06-24: [agent: codex | model: gpt-5.5] `EQUATION-CUBIC-CARDANO-READBACK-POLISH1` locks live Complex Cardano readback to compact auxiliary definitions instead of expanded branch formulas. The visible answer rows may use declared `A/B/C/p/q/Delta/R/U_k` symbols, with definitions in Equation detail readback; Copy Result remains scoped to the compact `exactLatex` compatibility string. General-form Valid When uses compact `R\ne0` plus the leading coefficient fact rather than expanding the Cardano radicand.
- 2026-06-24: [agent: codex | model: gpt-5.5] `EQUATION-CUBIC-CARDANO-REAL-POLICY0` keeps Real Cardano audit-only for now. A future Real route must use real cube-root notation for `Delta>0`, split `Delta=0` into triple-root versus double-root cases, avoid Complex `PrincipalRoot` in `Delta<0`, and wait for approved trig/arccos or equivalent real-branch readback plus exact validation before going live.
- 2026-06-24: [agent: codex | model: gpt-5.5] `EQUATION-COMPLEX-POWER-READBACK-UNIFICATION1` revises the earlier low-degree compatibility boundary: symbolic direct Complex powers with exact-rational pure/affine carriers now use compact `\operatorname{PrincipalRoot}_{n}(r)\omega_k` rows for degrees 2 through 12, with `cis` or trigonometric multiplier meaning in a definitions card. Exact-rational and explicit-imaginary low-degree Complex powers keep concrete exact output, and Real Exact odd-power output keeps real radical notation.
- 2026-06-24: [agent: codex | model: gpt-5.5] `EQUATION-CUBIC-CARDANO-REAL-READBACK-POLICY1` locks future Real Cardano readback to case-local facts and real-only notation. Global Valid When may include `a\ne0`, but `\Delta>0`, `\Delta=0`, `\Delta<0`, `p=0`, `q=0`, `p\ne0`, and `p<0` must stay attached to their own case rows/details. Future `REAL-ROUTE1` should implement all three discriminant cases together unless Display cannot represent case-local facts clearly, in which case a presentation prerequisite must come first.
- 2026-06-24: [agent: codex | model: gpt-5.5] `EQUATION-CUBIC-CARDANO-REAL-ROUTE1` makes Real Exact Cardano live for direct degree-3 selected-target polynomials only, using the existing `cubic-cardano` family after simpler exact routes. Real output is a case-style math answer with compact `A/B/C/p/q/Delta` definitions, real cube-root/trig notation, case-local discriminant facts, and multiplicity in details; exact numeric specialization is limited to exact scalar coefficient signs. Complex Cardano remains the `U_k` branch-list route, while Ferrari/quartics, generated-handoff Cardano, symbolic carrier quadratics, `RootOf`, implicit-root display, Display/History/OOE/app-state/Tauri schemas, and broad CAS behavior remain out of scope.
- 2026-06-24: [agent: codex | model: gpt-5.5] `EQUATION-CUBIC-CARDANO-GENERATED-HANDOFF-AUDIT1` keeps generated/wrapper Cardano non-live. Do not add `cubic-cardano` to generated-handoff route order until Calcwiz has a structured generated-handoff payload that can preserve Real case rows, Complex branch definitions, scoped facts, wrapper back-substitution facts, and candidate validation without flattening them into unconditional finite branches.
- 2026-06-25: [agent: codex | model: gpt-5.5] `EQUATION-QUARTIC-FERRARI-ROUTE1` makes Ferrari live only as a top-level direct degree-4 selected-target route in Real Exact and Complex Exact. The route family is distinct as `quartic-ferrari`, runs after simpler exact routes and after Cardano, supports non-`x` targets, uses compact definitions and node-backed finite-root presentation, and keeps rational-cleared quartics plus generated/wrapper Ferrari deferred until the normalization/handoff infrastructure can carry four-root/case output with scoped facts and validation.
- 2026-06-25: [agent: codex | model: gpt-5.5] `EQUATION-CARDANO-FERRARI-RATIONAL-NORMALIZATION1` locks top-level rational formula normalization as a shared Cardano/Ferrari seam. Safe denominator clearing may feed Cardano for cleared degree 3 and Ferrari for cleared degree 4 while preserving denominator exclusions plus formula facts; generated/wrapper formula handoff remains excluded from both `cubic-cardano` and `quartic-ferrari` until a later structured handoff gate.
- 2026-06-25: [agent: codex | model: gpt-5.5] `EQUATION-CARDANO-FERRARI-GENERATED-HANDOFF-AUDIT1` blocks generated/wrapper Cardano and Ferrari until Calcwiz has a structured generated-formula payload plus candidate/fact validation. Do not add `cubic-cardano` or `quartic-ferrari` to generated-handoff route order or wrapper family lists while generated handoff still extracts raw solution strings and global supplements from `exactLatex`.
- 2026-06-25: [agent: codex | model: gpt-5.5] `EQUATION-ALGEBRAIC-WRAPPER-FORMULA1` locks the first live wrapper formula consumer as Real Exact one-layer square-root composition only. Square-root wrappers may opt into generated Cardano/Ferrari formula families when they preserve structured `caseMath`, wrapper nonnegative-output facts, denominator exclusions, and case-local formula conditions; default generated handoff and all other wrapper families still exclude formula routes. Complex square-root wrappers require a later principal complex square-root/back-substitution policy before going live.
- 2026-06-25: [agent: codex | model: gpt-5.5] `EQUATION-ABS-WRAPPER-FORMULA1` locks Real Exact one-layer absolute-value composition as a live generated formula consumer. Abs wrapper formulas must render as grouped sign-branch `caseMath`; helper symbols such as `A`, `p`, `q`, and `Delta` stay local to each visible generated branch group instead of being globally namespaced. Exact `b=0` collapses to one generated branch, exact `b<0` remains domain-empty, and mixed formula-plus-legacy grouped output is deferred rather than silently stitched.
- 2026-06-25: [agent: codex | model: gpt-5.5] `EQUATION-SQUARE-POWER-WRAPPER-FORMULA-POLICY0` locks the next square-power wrapper direction as Real Exact, one-layer, and square-only for the first live route. `F(target)^2=b` should branch to `F=\sqrt{b}` and `F=-\sqrt{b}` under `b\ge0`, collapse exact `b=0` to `F=0`, and stop exact `b<0` as domain-empty. Higher even powers, odd powers, Complex square powers, nested/mixed wrappers, and broad generated formula route widening remain separate decisions.
- 2026-06-25: [agent: codex | model: gpt-5.5] `EQUATION-SQUARE-POWER-WRAPPER-FORMULA1` makes Real Exact one-layer square-power composition the third live generated formula wrapper consumer. It remains square-only: `F(target)^2=rhs` may split into grouped `F=\sqrt{rhs}` and `F=-\sqrt{rhs}` generated formula branches under target-free `rhs\ge0`, collapse exact `rhs=0` to one generated branch, and stop exact `rhs<0` as domain-empty. Higher powers, Complex square powers, nested/mixed wrappers, exp/log/trig wrapper formula handoff, broad generated route widening, and schema/runtime changes remain deferred.
- 2026-06-25: [agent: codex | model: gpt-5.5] `EQUATION-GENERAL-POWER-WRAPPER-POLICY0` locks the next general-power wrapper direction as Real odd powers first, not a broad generalized power/root switch. Future `F(target)^{2k+1}=rhs` should use real odd-root notation and one generated branch with exact negative RHS allowed. Higher even powers, nth-root wrappers, and Complex power/root wrappers require separate notation, grouped readback, dedupe/multiplicity, root-carrier, and principal-branch policies before live implementation.
- 2026-06-25: [agent: codex | model: gpt-5.5] `EQUATION-CARDANO-FERRARI-COEFFICIENT-READBACK1` locks Cardano/Ferrari coefficient readback as a three-way presentation policy. Generic full-slot symbolic coefficient templates may use compact helper-symbol derivations as the primary exact answer; specialized symbolic or mixed equations must substitute collected coefficient values directly into the primary branch/case rows; fully concrete exact-rational cases should avoid generic helper-symbol primary output when Cardano/Ferrari is reached, while simpler factorable/special-form routes still retain priority.
- 2026-06-25: [agent: codex | model: gpt-5.5] `EQUATION-EXTRANEOUS-SOLUTIONS-CARD1` locks rejected-candidate evidence as an existing `detailSections` readback concern, not a new persisted DisplayOutcome field. Validation paths should preserve real rejected candidates as `Extraneous Solutions` rows when exact or approximate candidate evidence exists, keep the card visible by default, and avoid inventing rows for legacy paths that only know a count.
- 2026-06-25: [agent: codex | model: gpt-5.5] `EQUATION-NTH-ROOT-WRAPPER-FORMULA1` locks live nth-root wrapper formulas as Real Exact, one-layer, and exact-index-limited to `n=3..12`. Odd-index roots invert to one generated `F=rhs^n` branch without a nonnegative RHS fact; even-index roots require the root output condition `rhs\ge0` when symbolic/compound, stop exact negative RHS as domain-empty, and collapse exact zero RHS to `F=0`. Complex nth-root wrappers stay deferred until a principal-branch wrapper policy exists.
- 2026-06-25: [agent: codex | model: gpt-5.5] `EQUATION-FORMULA-READBACK-SIMPLIFICATION1` locks formula readback cleanup as producer-side Equation polish over Cardano/Ferrari formula rows and details. It may fold narrow exact arithmetic and neutral terms in visible formula readback, but it must not become broad algebraic simplification, Display-side math cleanup, radical denesting, case-condition proving, route widening, or a Copy/History schema change.
- 2026-06-25: [agent: codex | model: gpt-5.5] `FORMULA-PRESENTATION-PIPELINE1` locks Real formula case guards as row-local answer conditions. Display may promote producer-supplied `caseMath` rows into visible `formula when condition` layout, but `Valid When` remains only for whole-result facts such as leading coefficients, denominator exclusions, wrapper facts, and exp/log domain facts. This is an internal Display-block structure, not a persisted DisplayOutcome, History, OOE, app-state, Tauri, or copy-contract schema change.
- 2026-06-25: [agent: codex | model: gpt-5.5] `EXP-LOG-WRAPPER-FORMULA1` locks exp/log formula wrappers as Real Exact, one-layer, and existing-non-target-base only. Exp/log generated equations may opt into Cardano/Ferrari formula payloads only from Real Exact Equation mode with no numeric interval; Complex exp/log wrappers, target-in-base formulas such as `F(z)^a=b`, nested/mixed wrappers, trig handoff, broad generated route-order widening, and schema/runtime changes remain deferred.
- 2026-06-26: [agent: codex | model: gpt-5.5] `FORMULA-PRESENTATION-PIPELINE2` locks heavy formula case rendering as compact-first and opt-in. Large or grouped `caseMath` answers must not mount all formula and condition math immediately; they show a responsiveness summary first, preserve row-local guard semantics, keep global `Valid When` limited to whole-result facts, and leave Copy Result, To Editor, History, OOE, app-state, Tauri, and persisted Display schemas unchanged.
- 2026-06-26: [agent: codex | model: gpt-5.5] `EQUATION-TRIG-WRAPPER-FORMULA-POLICY0` keeps trig wrapper Cardano/Ferrari formula handoff non-live until periodic branch evidence is preserved. A future route must be Real Exact, one-layer, opt-in from trig/composition only, preserve integer-parameter and sine/cosine range facts, keep Cardano/Ferrari case guards row-local, validate generated candidates against the original trig equation, and handle sine/cosine endpoint dedupe. `tan(F)=rhs` is the safe fallback first slice if two-branch sine/cosine evidence is not stable.
- 2026-06-26: [agent: codex | model: gpt-5.5] `EQUATION-TRIG-WRAPPER-FORMULA1` locks the first live trig formula wrapper boundary as Real Exact, one-layer, and existing composition carriers only for `sin(F)=rhs`, `cos(F)=rhs`, and `tan(F)=rhs`. Generated degree-3/4 equations may opt into Real Cardano/Ferrari formula payloads, including safely rational-cleared generated equations, while preserving periodic integer facts, sine/cosine range facts, row-local formula guards, denominator exclusions, and exact endpoint dedupe. Complex trig wrappers, nested/mixed trig wrappers, and broad generated route-order widening remain deferred.
- 2026-06-26: [agent: codex | model: gpt-5.5] `FORMULA-PRESENTATION-RENDER-SCHEDULER-ROADMAP0` pauses new wrapper formula widening behind Display stabilization. Existing live algebraic, exp/log, and trig formula wrappers stay supported, but broader wrapper work should wait until heavy `caseMath` answers have progressive row rendering, stale-render cancellation, per-row budgets for giant formulas, and stronger lazy mounting for math-heavy details. This is a Display/UI performance boundary, not an OOE or solver correctness boundary.
- 2026-06-26: [agent: codex | model: gpt-5.5] `FORMULA-PRESENTATION-PIPELINE3` locks heavy formula-case compact summaries as metadata-only previews. Compact `caseMath` summaries may show row/group/character counts and the row-local-condition note, but must not carry raw LaTeX formula snippets or mount hidden formula-row math before explicit expansion. Non-case oversized result previews keep their existing snippet behavior.
- 2026-06-26: [agent: codex | model: gpt-5.5] `DISPLAY-CASE-ROW-SCHEDULER1` locks expanded heavy formula `caseMath` rendering as progressive and cancelable. Compact/heavy answers may reveal full rows only after explicit expansion; rows should mount in small scheduled batches, row-local guards stay attached to their rows, and stale scheduled reveals must cancel on result replacement or unmount. Per-row CPU budgets and virtualization remain future Display roadmap items.
- 2026-06-26: [agent: codex | model: gpt-5.5] `DISPLAY-CASE-ROW-BUDGETS1` locks giant formula rows as per-row opt-in after heavy `caseMath` expansion. Progressive row reveal may schedule cheap rows automatically, but rows over the render-cost budget must show lightweight placeholders first and must not mount formula MathStatic, condition MathStatic, raw LaTeX previews, or group-label math until the user explicitly chooses `Show formula row`. Virtualization remains a later Display roadmap item.
- 2026-06-26: [agent: codex | model: gpt-5.5] `DISPLAY-CASE-ROW-BUDGETS1` also locks formula-case detail sections onto the same budgeted `caseMath` renderer as answer blocks. Detail cards such as `Trig Formula Cases`, `Real Cardano Cases`, and `Real Ferrari Cases` must not mount hidden heavy formula math while collapsed or bypass per-row budgets when opened. History/replay compatibility may parse legacy raw `\substack` case LaTeX into non-persisted structured `caseMath` display blocks to avoid exposing raw `\substack` without changing Copy Result, History schema, OOE, app-state, or Tauri.
- 2026-06-26: [agent: codex | model: gpt-5.5] `INTEGRATION-FORM-CLASSIFIER1` locks classifier-first symbolic integration dispatch as an internal route-plan seam. The classifier may profile MathJSON forms, features, and route families for tests and future Rubi tier routing, but it must not widen public Calculus strategy/result metadata, import Rubi/SymPy code, add Rubi rules, or change visible integration outputs in this slice.
- 2026-06-26: [agent: codex | model: gpt-5.5] `RUBI-TIER1-SECTION1-AUDIT0` sets the first Rubi Section 1 implementation target as bounded positive-integer algebraic expansion, adopted through the existing direct polynomial integration strategy and original-integrand backcheck. Recurrence-heavy binomial/trinomial rules, fractional/non-integer polynomial powers, algebraic normalization, branch-sensitive transformations, public Rubi provenance metadata, and lazy Rubi tier imports remain deferred until separate prerequisite slices.
- 2026-06-26: [agent: codex | model: gpt-5.5] `RUBI-TIER1-SECTION1-POLY-EXPAND1` adopts Section 1 algebraic expansion only as an internal fallback of the existing `direct-rule` integration strategy. Target-free symbolic coefficients are allowed after bounded expansion, but no symbolic polynomial-core widening, public Rubi strategy/provenance metadata, recurrence machinery, or branch-sensitive normalization is approved.
- 2026-06-26: [agent: codex | model: gpt-5.5] `RUBI-TIER1-SECTION1-POLY-DEGREE-LIFT1` may lift only integration-local expansion and exact-backcheck caps for higher-degree monomials produced by bounded positive-integer polynomial expansion. Term/node limits, exact original-integrand verification, visible `direct-rule`, and branch-sensitive stop behavior remain mandatory.
- 2026-06-26: [agent: codex | model: gpt-5.5] `RUBI-TIER1-POLY-BYPARTS-FEEDER1` may expand only polynomial-like factors as an internal fallback after normal integration-by-parts fails. Successful feeder results must keep visible `integration-by-parts`, pass exact original-integrand backcheck, and avoid public Rubi metadata, broad recurrence machinery, and branch-sensitive transformations.
- 2026-06-26: [agent: codex | model: gpt-5.5] `RUBI-TIER1-SECTION1-QUADRATIC-RECIPROCAL-POWER1` is limited to `1/(c+u^2)^2` with positive exact-square scalar `c` and exact affine `u`, visible `partial-fractions`, and exact rational-function backcheck. Inverse-trig power-1 cases, derivative-numerator substitution cases, symbolic coefficients, higher powers, broad recurrence, and branch-sensitive forms remain out of scope.
- 2026-06-26: [agent: codex | model: gpt-5.5] `RUBI-TIER1-AFFINE-POWER1` adopts exact-rational affine powers only: `(m*x+n)^p` supports bounded integer powers, `p=-1` logarithms, and selected negative integer powers after exact derivative backcheck. Symbolic coefficients, branch-sensitive widening, public Rubi metadata, and public Calculus strategy/result schema changes remain out of scope; overlap precedence may still let substitution, derivative-ratio, or partial-fractions handle matching cases first.
- 2026-06-26: [agent: codex | model: gpt-5.5] `RUBI-TIER1-BINOMIAL-DERIVATIVE-SUB1` adds only exact-rational derivative-present binomial substitution as a `u-substitution` fallback: `C*x^(n-1)*(a+b*x^n)^p` may integrate for bounded integer `p`, logarithmic `p=-1`, and reciprocal cases when exact backcheck succeeds. It does not introduce symbolic coefficient widening, branch-sensitive transforms, public Rubi metadata, or public Calculus strategy/result schema changes, and it preserves the older numeric substitution route when that route already claims a case.
- 2026-06-26: [agent: codex | model: gpt-5.5] `RUBI-TIER1-REPEATED-LINEAR-RATIONAL1` keeps repeated linear rational integration under visible `partial-fractions`. Single exact affine reciprocals `1/(m*x+n)^k` may bypass the general rational-function degree cap through a narrow exact-backchecked path, while mixed repeated-linear products stay bounded by the rational-function core. Symbolic coefficient widening, broad factorization, public Rubi metadata, and public Calculus schema changes remain out of scope.
- 2026-06-26: [agent: codex | model: gpt-5.5] `RUBI-TIER1-QUADRATIC-RECIPROCAL-NUMERATOR1` handles `(A*u+B)/(c+u^2)^2` only for exact-rational affine `u`, exact-rational affine numerators, and positive exact-square `c`. Mixed and constant numerator cases use visible `partial-fractions`; pure derivative-numerator cases must remain with `u-substitution`. Non-square constants, higher powers, symbolic coefficients, broad recurrence, public Rubi metadata, and public Calculus schema changes remain deferred.
- 2026-06-26: [agent: codex | model: gpt-5.5] `RUBI-TIER1-QUADRATIC-RECIPROCAL-POWER-LIFT1` lifts constant repeated quadratic reciprocal powers only to `3` and `4`, using bounded recurrence-style native formulas with exact backcheck and visible `partial-fractions`. Power `5+`, non-square constants, symbolic coefficients, numerator widening for higher powers, and general recurrence machinery remain out of scope.
- 2026-06-26: [agent: codex | model: gpt-5.5] `RUBI-TIER1-RATIONAL-QUADRATIC-MIXED1` treats mixed exact-rational partial fractions with any bounded ordinary/repeated linear factors plus one irreducible quadratic factor as verified Tier-1 coverage through the existing rational-function core. Multiple quadratic factors, symbolic coefficients, broad factorization, public Rubi metadata, and public Calculus schema changes remain out of scope.
- 2026-06-26: [agent: codex | model: gpt-5.5] `FORMULA-VIEWER-TAB-FOUNDATION1` locks the Formula Viewer as a session-only tab/page surface, not a new `ModeId`, OOE runtime, or persisted result schema. Huge formula source cards may open one viewer tab per result signature using structured DisplayBlock artifacts; viewer tabs must not launch jobs, mutate calculator mode, change Equation runtime state, or alter Copy Result/History contracts. Row virtualization remains the next display-roadmap slice for extremely tall viewer content.
- 2026-06-26: [agent: codex | model: gpt-5.5] `CALCULUS-INTEGRALS-EDITOR-SOURCE1` locks guided Calculus integral body editing to the main editor for Indefinite, Definite, and Improper Integral screens. Lower workspace integral cards are controls/generated-request preview surfaces only; F2 means `Focus Editor` on those screens, Copy Expr copies the generated integral request, Copy Result copies the structured result, and the Answer block remains the only result rendering. No Display, History, OOE, Tauri, persistence, or public Calculus result schema changes are approved.
- 2026-06-27: [agent: codex | model: gpt-5.5] `RUBI-TIER1-QUADRATIC-RECIPROCAL-NONSQUARE1` extends literal repeated quadratic reciprocal powers to positive exact-rational nonsquare constants only for powers `2..4`, under visible `partial-fractions` and exact derivative backcheck. The verifier may normalize constant radical factors to prove these arctan forms exactly, but this is not broad radical simplification, symbolic coefficient support, completed-square parsing, public Rubi metadata, or a public Calculus schema change.
- 2026-06-27: [agent: codex | model: gpt-5.5] `RUBI-TIER1-QUADRATIC-COMPLETE-SQUARE1` permits exact-rational completed-square normalization for irreducible quadratic reciprocal powers only when the leading coefficient and residual constant are positive and powers stay within `2..4`. Scalar factors are folded into recurrence coefficients, reducible quadratics stay with linear partial fractions, and this does not approve symbolic coefficients, algebraic-root factorization, public Rubi metadata, or public Calculus schema changes.
- 2026-06-27: [agent: codex | model: gpt-5.5] `FORMULA-VIEWER-READABILITY1` locks Formula Viewer readability controls as viewer-local session UI: math size defaults to `125%`, options are `100%`, `125%`, `150%`, `175%`, and `200%`, and sizing is not persisted into FormulaViewerArtifact, workspace state, History, Copy Result, OOE, app-state, Tauri, or Display schemas. The focused row inspector is intentionally not part of v1 because it duplicated the virtualized row surface.
- 2026-06-27: [agent: codex | model: gpt-5.5] `RUBI-TIER1-QUADRATIC-NUMERATOR-POWER-LIFT1` extends exact-rational affine numerator handling for repeated quadratic reciprocal powers to powers `3` and `4`, including nonsquare and completed-square denominators, under visible `partial-fractions` with exact backcheck. Pure derivative-numerator overlaps stay owned by `u-substitution`; symbolic coefficients, powers `5+`, broad recurrence machinery, public Rubi metadata, and public Calculus schema changes remain out of scope.
- 2026-06-27: [agent: codex | model: gpt-5.5] `RUBI-TIER1-MULTI-QUADRATIC-PARTIAL-FRACTIONS1` permits the rational-function primitive to decompose exact-rational denominators with at most two irreducible quadratic factor groups, each multiplicity capped at `2`, under the existing degree cap. Quadratic-power terms may use the repeated quadratic recurrence internally, but this does not approve symbolic coefficients, arbitrary quartic factorization, multiplicity `3+`, public Rubi metadata, or public Calculus schema changes.
- 2026-06-27: [agent: codex | model: gpt-5.5] `RUBI-TIER1-AFFINE-LOG-BYPARTS1` permits polynomial times affine `ln(m*x+n)` and `log(m*x+n)` integration only for exact-rational affine arguments and bounded exact polynomial factors. Natural-log forms must keep exact original-integrand backcheck, expanded polynomial factors may use the existing by-parts feeder, and base-10 `log` may remain numeric-confidence verified until symbolic `ln(10)` cancellation is explicitly widened. Public Rubi metadata and Calculus result schemas remain unchanged.
- 2026-06-27: [agent: codex | model: gpt-5.5] `RUBI-TIER1-RECIPROCAL-BINOMIAL-SUB1` permits exact-rational reciprocal binomial substitution only when the derivative monomial is present: `C*x^(-n-1)*(a+b*x^(-n))^p`, with bounded integer `n,p` and exact backcheck. Derivative-present overlaps may preempt derivative-ratio and partial-fractions to keep visible `u-substitution`; missing derivative factors are not claimed by this route, although pre-existing rational routes may still solve them.
- 2026-06-27: [agent: codex | model: gpt-5.5] `CALCULUS-ANSWER-PREVIEW-AUTOSIZE1` is approved as a CSS/layout-only answer and generated-preview sizing fix. Large/nested calculus math should expand or scroll inside existing cards, but this does not authorize Display schema changes, Formula Viewer coupling, History/OOE/Tauri/persistence changes, public Calculus result-shape changes, or copy-contract changes.
- 2026-06-27: [agent: codex | model: gpt-5.5] `INTEGRATION-RATIONAL-PF-PERF-SPLIT-AUDIT0` locks the rational partial-fractions performance diagnosis: the slow integration suite path is verifier/equivalence cost for nonsquare and completed-square repeated-quadratic arctan forms, not bad classifier discipline. Follow-up optimization should target scoped backcheck normalization/caching for those forms before widening rational PF coverage further.
- 2026-06-27: [agent: codex | model: gpt-5.5] `INTEGRATION-RATIONAL-PF-BACKCHECK-CACHE1` locks the rational partial-fractions perf fix as verifier-local infrastructure, not route widening: AST-only differentiation must not render derivative LaTeX, exact backcheck may omit optional `derivativeLatex` on exact proofs, scoped per-backcheck rational caches stay non-global, and verifier-only raw exact-rational equivalence may prove equality without public rational-function normalization/readback.
- 2026-06-27: [agent: codex | model: gpt-5.5] The Display roadmap is no longer a blocker for Real wrapper work. Formula Viewer foundation, row budgets, detail-section budgeting, virtualization, replay cleanup, and viewer-local math sizing are sufficient for wrapper expansion; future Display polish returns to normal UX backlog unless a new blocker is proven.
- 2026-06-27: [agent: codex | model: gpt-5.5] The next Real wrapper sequence is locked as six milestones: `EQUATION-REAL-WRAPPER-COVERAGE-BUNDLE1`, `EQUATION-REAL-MIXED-RADICAL-WRAPPER-BUNDLE1`, `EQUATION-REAL-NESTED-WRAPPER-SUBSTRATE1`, `EQUATION-REAL-NESTED-ALGEBRAIC-WRAPPER-FORMULA1`, `EQUATION-REAL-MIXED-EXPLOG-WRAPPER-FORMULA1`, and `EQUATION-REAL-MIXED-TRIG-WRAPPER-FORMULA1`. Major wrapper milestones may have internal verified gates but should commit once at the end unless the user requests separate commits.
- 2026-06-27: [agent: codex | model: gpt-5.5] `EQUATION-REAL-WRAPPER-COVERAGE-BUNDLE1` locks coefficient self-ratio cleanup as producer-side Cardano/Ferrari readback policy only: known-nonzero equal coefficient numerators/denominators may become `1` inside formula `ratioLatex` before p/q/discriminant/radicand/case strings are built, but this does not authorize broad algebraic simplification, additive reordering, radical denesting, Display/global readback cleanup, Copy/History/schema changes, or wider formula routes. The same milestone also locks Real Exact affine single-root radical wrappers `A*root(F,n)+C=R` for exact indices `2..12`; Complex, mixed/two-radical, nested, mixed exp/log, and mixed trig wrappers remain deferred.
- 2026-06-27: [agent: codex | model: gpt-5.5] `RUBI-TIER1-LINEAR-QUADRATIC-PF-LIFT2` permits exact-rational partial fractions with one irreducible quadratic factor group of multiplicity up to `3` plus bounded exact-rational linear factors under the existing denominator-degree cap. Two-quadratic families remain capped at multiplicity `2`, over-cap mixed quadratic multiplicity stops explicitly, the PF coefficient solve may use an 8x8 exact matrix only in this bounded path, and verifier-local raw rational gcd reduction is allowed only to prove exact rational equality without public Calculus schema, strategy, or Rubi metadata changes.
- 2026-06-27: [agent: codex | model: gpt-5.5] `RUBI-TIER1-QUADRATIC-NUMERATOR-GENERAL1` confirms exact-rational affine numerator handling over completed-square irreducible quadratics as existing implemented coverage for powers `2..4`, including scaled quadratics with non-unit leading coefficient. Mixed or constant residual numerator cases stay visible `partial-fractions`; pure derivative-numerator overlaps stay visible `u-substitution`; no symbolic coefficients, powers `5+`, public Rubi metadata, or public Calculus schema changes are approved.
- 2026-06-27: [agent: codex | model: gpt-5.5] `EQUATION-REAL-MIXED-RADICAL-WRAPPER-BUNDLE1` locks mixed-radical formula widening as Real Exact and single-selected-target-square-root only. A mixed-algebraic equation may pass a generated branch to Real Cardano/Ferrari only when the live mixed-algebraic collector has exactly one selected-target square-root carrier and target-free or non-carrier additive companions; the route must preserve the square-root branch condition and formula `caseMath` structure. This does not approve Complex mixed radicals, true two-selected-target radical formula payloads, nested mixed radicals, broad radical normalization, route-order widening, Display/global readback cleanup, or persisted schema/runtime/copy changes.
- 2026-06-27: [agent: codex | model: gpt-5.5] `EQUATION-REAL-NESTED-WRAPPER-SUBSTRATE1` locks the nested wrapper substrate as internal/test-facing only. It may classify exact depth-2 selected-target algebraic wrapper chains and expose generated final branch equations, facts, and layer provenance, but it must not pass those branches to Real Cardano/Ferrari or change visible nested solve output until `EQUATION-REAL-NESTED-ALGEBRAIC-WRAPPER-FORMULA1`.
- 2026-06-27: [agent: codex | model: gpt-5.5] `RUBI-TIER1-TRIG-AFFINE-BASIC1` permits only exact-rational affine trig direct primitives for `sin(m*x+n)`, `cos(m*x+n)`, `tan(m*x+n)`, `cot(m*x+n)`, `sec(m*x+n)^2`, and `csc(m*x+n)^2`. The required `cot`, `sec`, `csc`, `ln(sin(u))`, and `ln(cos(u))` differentiation support is approved for exact backcheck, but broader trig powers, products, symbolic parameters, branch-sensitive transforms, public Rubi metadata, and public Calculus schema changes remain out of scope.
- 2026-06-27: [agent: codex | model: gpt-5.5] `RUBI-TIER1-TRIG-DERIVATIVE-PRODUCTS1` permits exact-rational affine derivative-present trig products only when exactly two same-argument trig factors remain after numeric-scalar stripping: `sec(u)tan(u)`, `csc(u)cot(u)`, and `sin(u)cos(u)`. Successful cases stay visible `u-substitution` with exact backcheck; extra symbolic factors, non-affine arguments, symbolic coefficients, broader trig products, public Rubi metadata, and public Calculus schema changes remain out of scope.
- 2026-06-27: [agent: codex | model: gpt-5.5] `RUBI-TIER1-TRIG-POWER-REDUCTION1` permits only the four exact-rational affine core square reductions `sin(u)^2`, `cos(u)^2`, `tan(u)^2`, and `cot(u)^2` under visible `direct-rule`. Exact proof may use verifier-local normalization of these identities, but higher powers, broad trig recurrence, symbolic coefficients, branch-sensitive trig analysis, public Rubi metadata, and public Calculus schema changes remain out of scope.
- 2026-06-27: [agent: codex | model: gpt-5.5] `RUBI-TIER1-TRIG-PRODUCT-TO-SUM1` permits only bounded two-factor exact-rational affine product-to-sum rules for `sin(u)cos(v)`, `sin(u)sin(v)`, and `cos(u)cos(v)` under visible `direct-rule`. Generated terms must delegate to existing affine trig direct primitives and exact proof may use scoped two-factor trig product identity normalization; symbolic coefficients, non-affine arguments, branch-sensitive carriers, higher products, broad trig recurrence, public Rubi metadata, and public Calculus schema changes remain out of scope.
- 2026-06-27: [agent: codex | model: gpt-5.5] `RUBI-TIER1-EXP-POLY-NUMERIC-BASE-BYPARTS1` permits polynomial-times-exponential integration by parts for positive exact-rational numeric bases `a^(m*x+n)` with exact-rational affine exponents and bounded polynomial factors. Expanded polynomial-factor adoption may use exact bounded expansion plus the finite by-parts recurrence proof; symbolic, zero, negative, decimal, exp-trig products, broad exp-log simplification, public Rubi metadata, and public Calculus schema changes remain out of scope.
- 2026-06-27: [agent: codex | model: gpt-5.5] `CALCULUS-LAPLACE-TABLE1` permits a bounded Calculus Laplace transform table screen with fixed source variable `t` and transform variable `s`. V1 may use exact-rational numeric parameters for constants, `t^n`, `e^(a t)`, `sin/cos/sinh/cosh(a t)`, and `e^(a t)sin/cos(b t)`; inverse Laplace, convolution, step functions, Dirac delta, symbolic parameters, broad transform simplification, Display schema changes, OOE/History/Tauri/persistence changes, and public Calculus result-shape changes remain out of scope.
- 2026-06-27: [agent: codex | model: gpt-5.5] `RUBI-TIER1-BYPARTS-CASCADE-POLY-EXP-TRIG1` confirms finite by-parts cascade coverage for exact-rational polynomial factors times affine `e`, positive exact-rational numeric-base exponentials, sine, and cosine under the existing polynomial degree cap `6`. This is a hardening slice over existing runtime behavior; it does not authorize symbolic coefficients, exp-trig products, public Rubi metadata, public Calculus schema changes, or Risch/Risch-Norman work.
- 2026-06-27: [agent: codex | model: gpt-5.5] `RUBI-TIER1-TRIG-SIN-COS-PARITY-POWER1` permits exact-rational affine `sin(u)^n` and `cos(u)^n` parity reductions only for positive integer exponents `3..12`. Adoption must stay visible `direct-rule` and pass exact verification through scoped identity normalization; symbolic coefficients, non-affine arguments, branch-sensitive transforms, exponent `13+`, broad trig recurrence, public Rubi metadata, and public Calculus schema changes remain out of scope.
- 2026-06-27: [agent: codex | model: gpt-5.5] `RUBI-TIER1-TRIG-TAN-SEC-COT-CSC-POWER1` permits exact-rational affine `tan/sec` and `cot/csc` power reductions with each exponent capped at `6`. Derivative-present products may remain visible `u-substitution`; other supported cases stay visible `direct-rule` and must verify exactly. Symbolic coefficients, non-affine arguments, exponent `7+`, odd standalone `sec/csc` recurrence logs, branch-sensitive transforms, broad trig recurrence, public Rubi metadata, and public Calculus schema changes remain out of scope.
- 2026-06-27: [agent: codex | model: gpt-5.5] `RUBI-TIER1-TRIG-PRODUCT-TO-SUM-LIFT1` permits exact-rational scalar multiples and either factor order for two-factor affine `sin/cos` product-to-sum rules under visible `direct-rule`. The route must still contain exactly two affine `Sin`/`Cos` factors after numeric-scalar stripping and must pass exact backcheck; symbolic scalars, non-affine arguments, branch-sensitive carriers, higher products, broad trig recurrence, public Rubi metadata, and public Calculus schema changes remain out of scope.
- 2026-06-27: [agent: codex | model: gpt-5.5] `RUBI-TIER1-INVERSE-TRIG-AFFINE-VARIANTS1` permits exact-rational affine inverse-trig widening for scalar numerators, positive completed-square arctan denominators, shifted arcsin square-root denominators, and rational square constants only when exact backcheck succeeds. Verifier-local reciprocal-square-root normalization may prove scaled arcsin identities. Arcsec-style reciprocal-root forms are explicitly deferred because they require branch/domain facts; no symbolic parameters, broad branch analysis, public Rubi metadata, or public Calculus schema changes are approved.
- 2026-06-27: [agent: codex | model: gpt-5.5] `RUBI-TIER1-TRIG-SUBSTITUTION-AUDIT0` locks trig-substitution runtime sequencing: implement `sqrt(a^2-(m*x+n)^2)` first if pursued, keep exact-rational affine coefficients only, require exact backcheck, and defer `sqrt(a^2+u^2)` logarithmic radical forms plus `sqrt(u^2-a^2)`/arcsec-style forms until branch/domain facts and readback proof are stronger. No public Rubi metadata or public Calculus schema changes are approved.
- 2026-06-27: [agent: codex | model: gpt-5.5] `RUBI-TIER1-EXP-AFFINE-BASIC1` permits exact-rational positive non-unit numeric-base affine exponential primitives `a^(m*x+n)` with exact-rational `a,m,n` and exact `ln(a)` backcheck. Existing `e^(m*x+n)` coverage may use exact affine slopes in the direct helper. Zero, negative, unit, decimal, symbolic bases, exponential products, broad exp-log simplification, public Rubi metadata, and public Calculus schema changes remain out of scope.
- 2026-06-27: [agent: codex | model: gpt-5.5] `RISCH-NORMAN-DISPATCH-PROBE1` permits Risch-Norman adoption only as a guarded internal fallback after existing Tier-I integration routes miss. Accepted exponential and sine/cosine ansatz results must carry rule-proof exactness and visible supplement facts, while the public Calculus strategy label remains `integration-by-parts`; no public `risch-norman` strategy, result schema, Display, History, OOE, Tauri, persistence, or log adoption is approved.
- 2026-06-27: [agent: codex | model: gpt-5.5] `EQUATION-REAL-NESTED-ALGEBRAIC-WRAPPER-FORMULA1` locks visible nested algebraic formula readback as generic `Nested Formula Cases` for substrate-approved Real Exact depth-2 algebraic wrapper pairs. Branches must preserve generated layer provenance and all branch facts; Complex, depth-3, non-algebraic nested, additive/mixed selected-target carrier widening, and Display/global readback/schema/runtime/copy changes remain out of scope.
- 2026-06-27: [agent: codex | model: gpt-5.5] `EQUATION-REAL-FERRARI-CLOSED-READBACK1` locks non-generic Real Ferrari primary readback as closed substituted `caseMath`: helper quantities `p/q/r/P/Q/Delta/t/Y/F_sigma` must be inlined in root rows and row-local conditions for specialized, generated, rational-cleared, wrapper, and concrete cases. Only generic full-slot Ferrari templates may keep helper-symbol primary rows; Complex Ferrari closed readback remains a later policy slice.
- 2026-06-27: [agent: codex | model: gpt-5.5] `EQUATION-REAL-MIXED-EXPLOG-WRAPPER-FORMULA1` locks mixed exp/log formula widening as Real Exact affine-shell-only: one selected-target exp/log carrier may have target-free affine/additive/multiplicative companions and still delegate generated degree-3/4 equations to Real Cardano/Ferrari. The producer must preserve symbolic coefficient nonzero facts plus target-free exp/log argument/base facts, keep existing `Parameterized Exp/Log Solve` readback, and leave Complex, target-dependent companions, multiple selected-target exp/log carriers, log-combination transforms, target-in-base formula widening, nested towers, Lambert W, and schema/runtime/copy changes deferred.
- 2026-06-27: [agent: codex | model: gpt-5.5] `EQUATION-WRAPPER-SAFETY-COUNT-POLISH1` locks formula/root count cues as Display metadata derived from structured answer blocks, not raw LaTeX parsing. Branch-list answers may show root counts, grouped formula `caseMath` answers may show branch-family plus guarded-row counts, and ungrouped formula `caseMath` answers may show guarded-row counts. The unsafe symbolic-output guard remains fail-closed; Copy Result, Formula Viewer `copyLatex`, History, OOE, app-state, Tauri, and persisted result schemas remain unchanged.
- 2026-06-27: [agent: codex | model: gpt-5.5] Generated formula handoffs that carry mixed-trig `atan2` coefficients must keep products explicit and use producer-safe `\operatorname{atan2}` tokens, not browser-fragile adjacent products or display-only `\mathrm{atan_2}` handoff strings. This is a producer-side readback/handoff safety policy and does not weaken the unsafe symbolic-output guard or add Display-side algebraic cleanup.
- 2026-06-27: [agent: codex | model: gpt-5.5] `EQUATION-COMPLEX-POWER-WRAPPER-CATCHUP1 + EQUATION-COMPLEX-ROOT-WRAPPER-POLICY1` locks Complex power wrappers as all-branch relations and Complex root wrappers as principal functions. Complex Exact `F^n=R` and `A*F^n+C=R` may generate `PrincipalRoot_n(...)\omega_k` carrier branches only when final branch equations stay inside compact Complex-capable routes and visible branch counts remain bounded; Complex `sqrt(F)=R` and `root(F,n)=R` must not use Real-style inversion until principal-image validation exists. Explicit generated Complex Cardano/Ferrari wrapper formula readback remains retired.
- 2026-06-28: [agent: codex | model: gpt-5.5] `EQUATION-COMPLEX-NESTED-WRAPPER-SUBSTRATE1` locks Complex nested wrapper readiness as internal/test-facing only. Exact depth-2 root/power algebraic chains may expose layer provenance, principal-image facts, all-branch power definitions, generated final equations, and compact-route eligibility, but they must not enable a new visible nested Complex wrapper route, visible `RootOf`, or generated Complex Cardano/Ferrari wrapper formulas.
- 2026-06-28: [agent: codex | model: gpt-5.5] `EQUATION-COMPLEX-MIXED-ALGEBRAIC-WRAPPER-CATCHUP1` permits Complex Exact mixed algebraic root wrappers only for one selected-target principal root carrier plus a bounded selected-target algebraic companion when the powered branch equation stays inside compact Complex-capable routes. The route must preserve target-dependent principal-image facts such as `b-z`; two-root carriers, nested mixed radicals, abs companions, over-cap branches, visible `RootOf`, and generated Complex Cardano/Ferrari wrapper formula readback remain deferred.
- 2026-06-29: [agent: codex | model: gpt-5.5] `EQUATION-SYMBOLIC-POLISH-CLOSEOUT1` keeps target-dependent Complex principal-image facts candidate-local: facts involving the solve target, such as `b-z`, must appear in `Complex Principal-Image Facts` or candidate-guard details rather than global `Valid When`. Finite branch-list answers whose validity depends on such candidate-local guards may display `candidate roots`; ordinary finite roots and copy/readback contracts remain unchanged.
- 2026-06-29: [agent: codex | model: gpt-5.5] `EQUATION-NUMERIC-ELIGIBILITY-VARIABLE-POLICY1` locks `Prepare Numeric Solve` as a prepare-only Equation Algebra/F4 action. It may apply stored non-target values with the solve target protected, report the effective equation, remaining non-target symbols, and numeric readiness, but it must not open the interval panel, launch OOE numeric solving, substitute the target, or alter symbolic solve behavior.
- 2026-06-29: [agent: codex | model: gpt-5.5] `EQUATION-NUMERIC-SHAPE-CLASSIFIER1` supersedes the visible prepare-only UX direction: `Prepare Numeric Solve` stays hidden from the Equation Algebra/F4 tray. Numeric solving stays inside the existing Equation Solve/Run path; Algebra/F4 is only the explicit stored-value substitution consent surface for parameterized equations with a selected target plus non-target variable parameters. Reserved functions/constants/units are not parameters. The substitution option should remain visible for parameterized equations even when values are missing, and clicking it must report missing stored values rather than disappearing or failing silently. The classifier substrate may internally prepare stored values, protect the solve target, classify target-aware numeric shapes, and collect denominator/log/root/periodic/discontinuity evidence, but it must not add new root-solving behavior or user-facing numeric Valid When facts yet.
- 2026-06-29: [agent: codex | model: gpt-5.5] `EQUATION-STORED-VALUE-SOLVE-CONSENT1` locks the visible Equation Algebra/F4 action as `Use Stored Values`. It is substitution consent, not a second solve button: show it only when editor-analysis variable truth has a selected target plus non-target parameter variables, report missing stored values when any parameter has no stored numeric value, protect the solve target, and otherwise resume the normal Equation Solve/Run path with a one-shot stored-value snapshot and target-protection evidence. No deterministic numeric root method is added by this slice.
- 2026-06-29: [agent: codex | model: gpt-5.5] `EQUATION-DETERMINISTIC-NUMERIC-ALGEBRAIC1 + EQUATION-NUMERIC-DOMAIN-SEGMENTATION1` locks exact-first deterministic numeric fallback for Equation: when supported exact symbolic solving misses and the input is numeric-ready with one target, polynomial/rational equations may return validated real approximate roots through degree `64`. Domain segmentation is bundled as the validation/facts substrate; exclusions, log/root/fractional-power/trig-pole facts, sampled hazards, residuals, and rejected candidates stay in detail sections, not global `Valid When`. Complex numeric roots, nonlinear transcendental search, and interval-free periodic enumeration remain deferred.

## 2026-06-29 - RN-ANSATZ-ORCHESTRATOR1

- [agent: codex | model: gpt-5.5] RN fallback adoption should enter through one internal orchestrator ordered as exponential, sine/cosine, exp-sincos, affine-log, then affine-rational correction. Dispatch may filter the orchestrator by existing route family to preserve public `integration-by-parts` and `partial-fractions` labels; no public `risch-norman` strategy or result schema is introduced.

## 2026-06-29 - RN-NODE-FIRST-READBACK1

- [agent: codex | model: gpt-5.5] Successful RN orchestrator results must carry an internal `antiderivativeNode`. Existing exact LaTeX readback may stay as a temporary family-specific override where generic node rendering is worse, but future RN widening should treat the MathJSON node as the proof/readback substrate.

## 2026-06-29 - RN-LOG-RATIONAL-CORRECTION-LIFT1

- [agent: codex | model: gpt-5.5] The first RN affine log/rational widening is limited to matching affine denominator powers `1..3` with polynomial numerator degree `<=6`. The route substitutes `u=ax+b`, integrates Laurent-log terms by rule proof, emits `a\ne0` and `ax+b>0`, and does not widen to non-affine logs, quadratic denominators, branch-sensitive carriers, decimals, or broad symbolic partial fractions.

## 2026-06-29 - SYMBOLIC-RATIONAL-QUADRATIC-AUDIT0

- [agent: codex | model: gpt-5.5] Symbolic quadratic rational widening should not jump to broad symbolic partial fractions. The first later implementation slice should be `RN-SYMBOLIC-QUADRATIC-RATIONAL-LINEAR-NUMERATOR1`: one degree-one numerator over one power-one symbolic quadratic denominator, generic facts `a\ne0` and `4ac-b^2>0`, proof-based adoption, and no repeated powers, reducible/negative-discriminant branch splitting, or multiple symbolic denominator factors.

## 2026-06-29 - RN-READBACK-MULTIPLICATION-STYLE1

- [agent: codex | model: gpt-5.5] Explicit multiplication-dot cleanup is scoped to RN-generated and symbolic-quadratic rational exact integration outputs for now. It may clarify nontrivial factor/function products such as `(...)\cdot\sin(...)`, `(...)\cdot\cos(...)`, and `...\cdot\arctan(...)`, but must not become a global Display/readback policy in this milestone.

## 2026-06-29 - RN-SYMBOLIC-QUADRATIC-RATIONAL-LINEAR-NUMERATOR1

- [agent: codex | model: gpt-5.5] The first live symbolic quadratic rational numerator slice is limited to one degree-one numerator over one power-one symbolic quadratic denominator under generic facts `a\ne0` and `4ac-b^2>0`. The route decomposes into derivative-log plus reciprocal-quadratic arctan terms, preserves existing reciprocal/exact-rational precedence, and does not implement repeated powers, reducible branches, negative-discriminant branches, or broad symbolic partial fractions.

## 2026-06-29 - RN-SYMBOLIC-QUADRATIC-BRANCH-FACTS-BASELINE1

- [agent: codex | model: gpt-5.5] Symbolic quadratic rational formulas are conditional generic formulas, not automatic branch splits. The only live symbolic quadratic branch is the `4ac-b^2>0` arctan branch with `a\ne0`; `4ac-b^2=0`, `4ac-b^2<0`, unknown-sign symbolic splitting, repeated powers, and reducible symbolic factorization remain deferred unless a later milestone explicitly implements them.

## 2026-06-29 - RN-SYMBOLIC-QUADRATIC-POWER-READINESS1

- [agent: codex | model: gpt-5.5] Repeated symbolic quadratic powers are readiness-only for now. The profiler may recognize `(A*v+B)/(a*v^2+b*v+c)^2` and power `3` under generic facts `a\ne0` and `4ac-b^2>0`, but live dispatch must keep them unsupported until a dedicated recurrence implementation proves and readbacks the derivative/residual reduction.

## 2026-06-29 - RN-READBACK-FACTOR-ORDER-AND-FUNCTION-CLARITY2

- [agent: codex | model: gpt-5.5] RN readback clarity remains producer-owned and scoped. Family overrides may polish exact LaTeX where generic node rendering is worse, but successful RN outputs must still carry `antiderivativeNode`; Display normalization may preserve explicit function-product dots without becoming a broad renderer migration.

## 2026-06-29 - RN-SYMBOLIC-QUADRATIC-CASEWISE-BRANCHES1

- [agent: codex | model: gpt-5.5] Symbolic quadratic rational power-one denominators now use casewise readback instead of a positive-only generic assumption. Exact supplements may preserve `=0` and `<0` relation facts, but symbolic quadratic branch sign facts live in the answer cases unless a fact applies globally.

## 2026-06-29 - RN-SYMBOLIC-QUADRATIC-REPEATED-POWER-ADOPTION1

- [agent: codex | model: gpt-5.5] Repeated symbolic quadratic rational powers are live only for powers `2` and `3` under the positive generic branch `4ac-b^2>0`. The route must split degree-one numerators into derivative and residual pieces, use the bounded recurrence for `1/Q^p`, and keep zero/negative repeated branches, powers `4+`, reducible symbolic splitting, multiple symbolic quadratic factors, and broad symbolic partial fractions deferred.

## 2026-06-29 - RN-LOG-DERIVATIVE-DETECTOR1

- [agent: codex | model: gpt-5.5] RN log-derivative adoption is limited to bounded symbolic rational shapes `k*D'(v)/D(v)` with denominator degree `2..6`, target-free symbolic/exact coefficients, and proof by coefficient cross-products. It should keep public strategy `partial-fractions`, leave exact-rational derivative-ratio and affine direct-rule overlaps with their existing owners, and avoid broad symbolic partial fractions or formal Risch behavior.

## 2026-06-29 - RN-SYMBOLIC-RATIONAL-HERMITE-REDUCTION1

- [agent: codex | model: gpt-5.5] RN Hermite-style rational correction is limited to target-free symbolic coefficient rational inputs with denominator powers `2..3`. It may adopt exact derivatives of `P(v)/Q(v)^m` and residuals reducible to the symbolic log-derivative detector, but must not preempt exact-rational substitution/rational routes, broaden to powers `4+`, multiple symbolic denominators, algebraic extensions, or a public RN strategy.

## 2026-06-29 - RN-DIFFERENTIAL-FIELD-TOWER-ORCHESTRATOR1

- [agent: codex | model: gpt-5.5] RN dispatch should enter through one internal bounded tower profile and attempt plan before invoking family solvers. The profile may order exponential, sine/cosine, exp-sincos, affine-log, affine-log-rational, symbolic log-derivative, Hermite correction, and affine-rational correction attempts, but public labels remain existing `integration-by-parts` or `partial-fractions`; no public `risch-norman` strategy or result schema is introduced.

## 2026-06-29 - CALCULUS-DERIVATIVE-TARGET-VARIABLE1

- [agent: codex | model: gpt-5.5] Guided Calculus derivative target variables are separate state from the function body. Valid committed targets are single symbols: one Latin letter or supported Greek ids such as `theta`; generated derivative requests own the `d/dtarget` wrapper while the main editor remains body-only.

## 2026-06-29 - CALCULUS-DERIVATIVE-TARGET-ROUNDTRIP1

- [agent: codex | model: gpt-5.5] Guided Calculus derivative targets persist as canonical single-symbol ids in Calculus seeds, history replay, workspace snapshots, OOE request/revision evidence, and commit context. Legacy derivative seeds without `variable` remain valid and default live runtime behavior to `x`.

## 2026-06-29 - CALCULUS-PARTIAL-DERIVATIVE1

- [agent: codex | model: gpt-5.5] Guided first-order Partial Derivative uses the same body-plus-target model as Derivative: the main editor stores only the function body, target variable state is canonical single-symbol state, and generated request/copy owns the `partial/partial target` wrapper. Higher-order and mixed partial derivatives remain separate future milestones.

## 2026-06-29 - RN-LRT-RATIONAL-INTEGRATION1

- [agent: codex | model: gpt-5.5] Live LRT rational adoption is limited to proper residuals over squarefree exact cubic denominators. The route may expose named `alpha_i` logarithmic terms and resultant/formal-gcd definitions through existing exact-supplement paths, but symbolic denominator coefficients, degree `4+` LRT adoption, repeated denominators, public LRT/RN strategy labels, and public Calculus/Display schema changes remain deferred.

## 2026-06-29 - RN-TOWER-BASIS-GENERATOR1

- [agent: codex | model: gpt-5.5] RN fallback planning should enter through the bounded tower-basis generator rather than each family adding independent inline probes. The generator may return ordered internal attempts with source evidence, but existing Tier-I precedence, public `integration-by-parts`/`partial-fractions` labels, and public result schemas remain unchanged.

## 2026-06-29 - RN-PRACTICAL-RISCH-NORMAN-CHECKPOINT0

- [agent: codex | model: gpt-5.5] The current RN/LRT layer is a practical bounded heuristic, roughly `70-80%` of Calcwiz's scoped Risch-Norman target, not full/general Risch-Norman. Next meaningful RN work should tackle nested towers, algebraic extensions beyond named-root descriptors, degeneracy branching, algebraic-log trace/readback, and eventually non-existence certificates as separate planned layers.

## 2026-06-30 - TRANSCENDENTAL-RISCH-CERTIFICATE-AUDIT0

- [agent: codex | model: gpt-5.5] After practical RN, the next high-value integration direction is certificate-first, not chasing unrestricted RN completeness. Transcendental Risch work must start with prerequisites for exact differentiation closure, tower/constant-field classification, Liouville/log-derivative proof evidence, visible facts, and node-first certificate readback; depth-2 tower widening remains a separate audit/implementation choice.

## 2026-06-30 - TRANSCENDENTAL-RISCH-DIFFERENTIATION-CLOSURE-AUDIT0

- [agent: codex | model: gpt-5.5] Certificate-grade non-elementary proofs must not use the convenience differentiation path that can fall back to Compute Engine or numeric-confidence equivalence. First certificate milestones should introduce a proof-local exact differentiation wrapper around the existing direct differentiator, with fallback denied, inexact scalar leakage rejected, tower-head normalization recorded, and focused closure tests required before live certificate adoption.

## 2026-06-30 - CALCULUS-DERIVATIVES-UX-TAXONOMY1

- [agent: codex | model: gpt-5.5] Partial Derivative is a guided Derivatives experience, not a top-level Calculus category; legacy `partialsHome` route compatibility remains for old records. The lower editor strip is the derivative operator rail, while the main editor stays function-body-only and visible derivative wording uses "with respect to" instead of "target".

## 2026-06-30 - CALCULUS-DERIVATIVE-OPERATOR-RAIL1

- [agent: codex | model: gpt-5.5] The derivative operator rail owns compact operator syntax separately from the function body. New replay state may carry `operatorLatex`; legacy `bodyLatex + variable` seeds still replay as first-order operators. Mixed partial readback follows the standard compact convention where the rightmost written factor applies first, and evaluation for higher/mixed operators stays gated until dedicated capability milestones.

## 2026-06-30 - EQUATION-PERIODIC-PREIMAGE-SUBSTRATE1

- [agent: codex | model: gpt-5.5] Equation periodic solving is exact-first when a compact symbolic family can be produced. Quotient-zero forms such as `sin(x)/x=0` reduce to the numerator family with denominator exclusions preserved, and zero trig targets may collapse to single compact periodic families such as `x=\pi n`; numeric enumeration is a fallback/guidance path, not the preferred answer for supported periodic preimages.

## 2026-06-30 - EQUATION-NUMERIC-INTERVAL-ACCESS-DISCIPLINE1

- [agent: codex | model: gpt-5.5] Numeric Interval Solve remains the existing Equation panel surface, not a second solve action or Algebra/F4 action. It should be manually accessible for selected-target symbolic Equation inputs, while periodic and dense-root numeric fallbacks must ask for a finite real window instead of using fixed auto-search windows. Interval results are local to the chosen window and may cap visible roots with narrowing guidance.

## 2026-06-30 - CALCULUS-HIGHER-ORDER-DERIVATIVES1

- [agent: codex | model: gpt-5.5] Guided Calculus ordinary derivatives now evaluate repeated single-variable operators from the derivative rail up to order `10`. Derivative at Point for higher-order operators differentiates symbolically first and then substitutes the numeric point; it does not use numeric fallback in this milestone.

## 2026-06-30 - CALCULUS-MIXED-PARTIALS1

- [agent: codex | model: gpt-5.5] Guided Partial Derivative now evaluates compact higher/mixed partial operators from the derivative rail up to order `10`. The typed/written operator order is preserved for readback, while computation follows the existing rightmost-first applied path exactly; v1 does not reorder by Clairaut/symmetry assumptions.

## 2026-06-30 - EQUATION-NUMERIC-INTERVAL-VALIDATION-DISCIPLINE1

- [agent: codex | model: gpt-5.5] Numeric Interval Solve is a permanent compact Equation control, not an auto-open panel and not a second solve action. Run/F1/EXE uses interval solving only after `Enable Numeric Interval` is active. Interval runs are explicit numeric intent: they may substitute stored non-target parameter values, must protect the selected target, and must stop with missing-value guidance when non-target parameters lack stored numeric values.

## 2026-06-30 - EQUATION-NUMERIC-POLYNOMIAL-ENGINE-LIFT1

- [agent: codex | model: gpt-5.5] Deterministic Equation polynomial/rational numeric fallback stays exact-first and real-output-only, but now consumes the shared polynomial root engine with multi-start Durand-Kerner refinement, Newton polishing, residual diagnostics, coefficient-scale conditioning evidence, and repeated/clustered-root warnings. Recognized quotient-zero rational boundaries may enter this numeric fallback after exact symbolic solving stops, preserving denominator exclusions and candidate validation; degree-3/4 fallback remains numeric-only and does not use Cardano/Ferrari readback.

## 2026-06-30 - EQUATION-NUMERIC-INTERVAL-ROOT-FINDER-LIFT1

- [agent: codex | model: gpt-5.5] Manual numeric interval solving and bounded nonlinear auto-search share the same bracket-first Brent-Dekker refinement with bisection fallback and local-minimum recovery. A tiny bracket is not accepted unless the residual is root-small; failed sign-change brackets near discontinuities may still produce validation-visible extraneous seeds so users can see which pole-like candidates were dropped.

## 2026-06-30 - EQUATION-NUMERIC-TRANSCENDENTAL-SEARCH-LIFT1

- [agent: codex | model: gpt-5.5] Real nonlinear numeric auto-search is allowed for numeric-ready smooth non-periodic transcendental/radical equations after exact and deterministic numeric routes miss. Multi-carrier expressions whose selected-target occurrences are only inside periodic carriers remain interval-first instead of falling into bounded auto-search.

## 2026-06-30 - EQUATION-NUMERIC-ROUTE-ORCHESTRATION-CLOSEOUT1

- [agent: codex | model: gpt-5.5] Equation Real numeric route order is locked as exact symbolic first, deterministic algebraic numeric fallback second, periodic/dense cases through exact symbolic families or explicit Numeric Interval Solve only, non-periodic bounded nonlinear auto-search after those routes miss, and controlled unsupported guidance last. Numeric degree-3/4 fallback remains numeric-method readback, not Cardano/Ferrari formula readback.

## 2026-06-30 - EQUATION-IMPLICIT-DERIVATIVE-SOLVE-SEAM1

- [agent: codex | model: gpt-5.5] Calculus implicit differentiation must consume the Equation-owned `implicit-derivative-solve` seam rather than importing Equation isolation internals or adding a private solver. The seam delegates to selected-target isolation for an internal single-letter derivative placeholder, maps only one clean placeholder equality to display `dy/dx`-style output, and returns controlled unsupported stops for missing placeholders, unsupported Equation routes, or multi-branch derivative outputs.

## 2026-07-01 - WORKSPACE-DESTINATION-ROUTING1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Destination routing in Workspace Tabs must settle the active workspace retarget or explicit new-tab activation before applying per-mode payloads. History replay, Auto Equation handoff, launcher/guide sends, and display action sends use this bridge so the first user action lands on the intended workspace destination instead of a default menu page.

## 2026-07-01 - EQUATION-NUMERIC-DECIMAL-PRECISION-SUBSTRATE1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Equation numeric precision v1 uses `decimal.js` as an internal diagnostics substrate for polynomial residual revalidation when existing scale, residual, repeated/clustered-root, or close-separation diagnostics signal risk. This does not make Equation fallback arbitrary-precision by default and does not widen supported numeric routes.

## 2026-07-01 - EQUATION-NUMERIC-POLYNOMIAL-CONDITIONED-SOLVE1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] The shared Equation numeric polynomial engine keeps Aberth-Ehrlich as the degree-3+ solver and adds producer-side conditioning polish plus residual-aware dedupe before readback/validation. Degree-3/4 numeric fallback remains numeric-method output, not Cardano/Ferrari formula readback.

## 2026-07-01 - EQUATION-NUMERIC-SMOOTH-ACCELERATION1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] ITP remains the maintained real bracketed root-refinement kernel. Smooth Newton/secant acceleration is allowed only as a guarded in-bracket accelerator under that kernel, with unsafe probes falling back to ITP/bisection and local-minimum recovery.

## 2026-07-01 - EQUATION-NUMERIC-PERIODIC-FAMILY-INTERVAL-LIFT1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Numeric Interval results may show reliable affine trig carrier period summaries as diagnostic interval evidence. These summaries do not claim global completeness or that the whole equation is periodic; all displayed roots remain local to the chosen interval and validated against the original equation.

## 2026-07-01 - CALCULUS-DERIVATIVE-NATURAL-EDITOR1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Guided derivative UX source is natural main-editor entry: users type the complete derivative request, while the operator/body split remains internal parser evidence rather than a separate editable rail control.

## 2026-07-01 - CALCULUS-DERIVATIVE-READBACK-STRIP1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] The derivative operator rail is read-only parse feedback. It may display written operator, applied order, and parsed body, but must not be the primary editable source for derivative or partial-derivative operators.

## 2026-07-01 - CALCULUS-DERIVATIVE-SHORTCUTS1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Derivative text shortcuts are scoped to guided derivative screens. `pd` normalizes to the partial symbol, `ddx`-style shortcuts normalize to ordinary derivative operators, and `pdx`-style shortcuts normalize to partial derivative operators before evaluation state consumes the input.

## 2026-07-01 - CALCULUS-WORKSPACE-KEYBOARD-OVERLAY1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] The keypad remains one shared component. Derivative-family Calculus screens apply a workspace-aware row overlay for derivative operator templates; non-derivative screens keep the base keypad unchanged.

## 2026-07-01 - SYMBOLIC-DERIVATIVE-NORMALIZATION1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Derivative display normalization is output-side and bounded: compact repeated factors, distribute only small products over sums, and collect identical terms before rendering final derivative answers and step lines. The differentiation rule engine itself remains unchanged.

## 2026-07-01 - CALCULUS-DERIVATIVE-NATURAL-REQUEST-SOURCE1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Guided Calculus derivative screens use the full natural editor derivative request as the active request source for new History/replay/runtime evidence. `variable` and `operatorLatex` remain optional derived fallback evidence, not the primary editing or replay contract.

## 2026-07-01 - EQUATION-COMPLEX-NUMERIC-POLYNOMIAL-ROOTS1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Complex On numeric polynomial/rational fallback is live after exact symbolic routes miss. It uses the shared Aberth-Ehrlich polynomial engine through degree `64`, preserves real and non-real roots, honors `complexExactForm` for approximate rectangular/polar/cis readback, validates residuals and denominator exclusions, and keeps Complex nonlinear numeric solving plus generated Complex Cardano/Ferrari formula output deferred.

## 2026-07-01 - EQUATION-COMPLEX-NUMERIC-NONLINEAR-POLICY0

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Complex nonlinear numeric solving is a separate future track from Real ITP interval solving. It must not adapt one-dimensional real interval search directly; prerequisites include branch-cut policy, seed-grid or contour/argument-principle boundaries, duplicate and cluster handling, two-real-variable residual validation, and explicit confidence/completeness wording before visible Complex nonlinear roots are enabled.

## 2026-07-01 - EQUATION-SOLVER-CONSISTENCY-HARNESS1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Equation exact/fallback and explicit Numeric Interval routes must agree on validated roots inside a chosen interval. Numeric interval repeated-root candidates are clustered after original-equation validation so repeated factors display one representative root instead of duplicate nearby approximations.

## 2026-07-01 - WORKSPACE-APP-FRAME-AUDIT0

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Workspace Tabs are app chrome, not calculator-shell chrome. The next tab layout milestone should lift `WorkspaceTabs` out of `.calculator-shell`; calculator-like workspaces may keep the calculator shell as their active renderer, while full Settings, full History/Records, Variables, Formula Viewer expansion, and future Graphing/Spreadsheet surfaces must be able to bypass it through an active surface host.

## 2026-07-01 - WORKSPACE-TABS-APP-CHROME1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Workspace Tabs now render as full-stage app chrome above `.calculator-shell`, not inside it. The shared app-frame owns UI scaling for both tabs and calculator body, while `.calculator-shell` keeps the side-surface geometry ref and remains the renderer for calculator-like workspaces.

## 2026-07-01 - WORKSPACE-ACTIVE-SURFACE-HOST1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Active workspace rendering now goes through an explicit `ActiveSurfaceHost`. Calculator-like workspaces still render through `.calculator-shell`, while Formula Viewer renders as a page surface outside the calculator chassis without gaining an Order of Execution runtime context or changing `currentMode`.

## 2026-07-01 - WORKSPACE-PAGE-SURFACE-MODEL1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Page surfaces are separate from calculator `ModeId` values. Formula Viewer is the only live page surface and uses a protected tab-action policy; future Settings and History are documented as singleton page surfaces, while future Graphing must arrive later as a canvas/page-class surface rather than a calculator-shell panel.

## 2026-07-01 - TRANSCENDENTAL-RISCH-CHECKPOINT0

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] The next formal transcendental Risch push should be method-level infrastructure rather than more isolated special-function family patches. Textbook elementary catchup should wait for the Stewart/Thomas benchmark sweep unless a case is needed as a regression for tower normal forms, RDE/Liouville, or certificate orchestration.

## 2026-07-01 - TRANSCENDENTAL-ALGEBRAIC-CONSTANTS-AND-TRACE1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Algebraic constant and trace helpers are domain-neutral primitive readback/proof evidence; LRT orchestration and method naming remain Integration/RN-owned, and Equation must not expose these trace/LRT steps through Equation-owned workflows.

## 2026-07-01 - TRANSCENDENTAL-DEGENERACY-BRANCH-SOLVER1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Formal transcendental proof branching must stay bounded. Vanished pivots, slopes, discriminants, resultants, and special-parameter cases may produce direct-test branch rows up to the formal cap `12`; larger branch products must stop with proof-scope evidence instead of generating unreadable case explosions.

## 2026-07-01 - TRANSCENDENTAL-DEPTH2-GENERALIZATION1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Depth-2 tower compositions should be recognized as method-level proof candidates even when no live certificate route owns them yet. They now carry explicit readiness labels and defer at the reduced-equation layer; depth-3 remains over-cap evidence until an approved depth-3 milestone exists.

## 2026-07-01 - TRANSCENDENTAL-CERTIFICATE-ORCHESTRATOR1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Live transcendental certificate adoption should use one integration-owned proof chooser after Tier-I/RN misses. Calculus may render the resulting certificate evaluation, but it should not hand-pick individual Risch/special-function proof builders.

## 2026-07-01 - TRANSCENDENTAL-RISCH-PRACTICAL-FORMAL-CHECKPOINT0

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] After the 12-milestone formal push, Calcwiz should proceed with benchmark-driven validation and targeted formal gaps rather than claiming unrestricted formal Risch. Depth-3 towers, algebraic function-field Risch, broad RDE solving, and complex branch constants need separate prerequisite audits before live adoption.

## 2026-07-01 - CALCULUS-LIMITS-NATURAL-PARSER1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Limits natural requests are parsed into structured variable, target, direction, body, and canonical request evidence before UI adoption. V1 accepts numeric finite targets, one-sided finite targets, `+/-infinity`, and simple exact `pi`/`e` constants, while symbolic targets such as `a` remain deferred.

## 2026-07-01 - EQUATION-NUMERIC-FACT-TAXONOMY-POLISH1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Numeric result details must keep hard invalid-domain facts, piecewise breakpoints, periodic structure, probe samples, algorithm diagnostics, conditioning warnings, and extraneous candidate evidence in separate cards. Breakpoints are not domain exclusions, probe samples are not proofs, and clean breakpoint/domain-boundary cases must not imply higher precision risk without stronger rejection or instability evidence.

## 2026-07-01 - EQUATION-RESULT-TRUST-READBACK-POLISH1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Equation result trust wording is display/readback metadata derived from existing structured blocks and confidence sections. Main Display and Formula Viewer may show concise cues such as exact roots, certified polynomial roots, local interval roots, validated bounded-search roots, and region-local Complex roots, but copy output, History, OOE, app-state, Tauri, persisted schemas, and solver result meaning remain unchanged.

## 2026-07-01 - MATRIX-EXACT-READBACK1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Matrix exact readback is adopted only for determinant and inverse over safe-integer numeric grid inputs when the internal exact rational Matrix core succeeds. Decimal, unsafe, over-cap, unsupported, and Vector requests remain on the existing numeric readback path; the Matrix notation pad is still template-only, not executable symbolic matrix CAS.

## 2026-07-01 - CALCULUS-LIMITS-EDITOR-SOURCE1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Guided Calculus Limits now expose one visible `Limit` screen whose main editor stores the full natural limit request. Legacy `finiteLimit` and `infiniteLimit` routes/seeds remain loadable as hidden compatibility paths that convert into the canonical request source; the Answer card remains the sole result owner.

## 2026-07-01 - SETTINGS-HISTORY-PAGE-SURFACES1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Settings and History are live singleton app-page workspace kinds, not calculator `ModeId`s. Quick Settings and History inspectors remain first-class fast-access side panels; full pages are additive app surfaces outside `.calculator-shell`, use null Order of Execution runtime context, and protected page tab actions. History V1 stays over existing `HistoryEntry` plus pending-ticket data and must not expose Formula Viewer actions from persisted records.

## 2026-07-02 - SETTINGS-HISTORY-PAGE-POLISH1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Settings full-page categories must be functional scopes over the existing settings controls, not decorative labels over one long inspector clone. History full-page rows are selection-first: single click focuses/selects, Shift-click range-selects, Ctrl/Cmd-click toggles, and double-click is the direct-open/replay gesture; explicit inspector buttons remain the primary visible commands.

## 2026-07-02 - LINEAR-ALGEBRA-EDITOR-SOURCE1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Matrix and Vector editor drafts are owned by the main display editor, not secondary notation pads. Matrix surface state stores `matrixEditorLatex`, Vector surface state stores `vectorEditorLatex`, and existing Matrix/Vector OOE capability IDs plus replay seeds remain separate.

## 2026-07-02 - LINEAR-ALGEBRA-KEYPAD-OVERLAY1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Matrix and Vector keypad overlays are mode-local replacements over the global keypad, following the derivative-screen pattern. They must disappear outside Matrix/Vector and must not alter derivative keypad replacement behavior.

## 2026-07-02 - WORKSPACE-PAGE-SURFACE-GUARDRAILS1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Workspace surface descriptors own quick-inspector permission. Calculator-like workspaces allow quick Settings, History, Variables, and Order of Execution diagnostics; app/page surfaces such as Settings, History, Formula Viewer, and future page/canvas surfaces default to no global quick inspectors. App chrome and full page surfaces stay stable under calculator UI scale.

## 2026-07-02 - HISTORY-QUICK-PANEL-LIGHTWEIGHT1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] The quick History inspector is a recent-summary surface, not a rich ledger. It renders all pending tickets plus at most 20 committed entries, uses text previews, and must not mount row-level `MathStatic`, exact supplements, or facts; full History owns deep browsing and rich selected-entry rendering.

## 2026-07-02 - HISTORY-LEDGER-ROW-BUDGET1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Full History ledger rows are scanning rows, not rich math surfaces. The row model owns plain-text input/result previews, virtualized visible rows render those previews through memoized row components, and rich `MathStatic` stays in the selected inspector or pending/detail areas.

## 2026-07-02 - HISTORY-TICKER-ISOLATION1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Pending elapsed timers are local presentation concerns. History page/panel parents must not own 250ms elapsed state for committed rows; only pending rows and pending detail inspectors may subscribe to the elapsed ticker.

## 2026-07-02 - HISTORY-PERF-CONFORMANCE1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] History responsiveness is a tested UI contract. Quick History stays capped to pending rows plus 20 committed summaries, full History rows stay virtualized/plain-text for scanning, rich math remains in selected inspectors/details, pending elapsed updates stay local, and page surfaces must suppress quick inspector overlays.

## 2026-07-02 - MATHSTATIC-MARKUP-LRU1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Rendered `MathStatic` markup is cached as bounded strings only, keyed by display LaTeX plus block/inline mode. The cache is internal Display infrastructure and must not store React nodes, DOM objects, solver objects, Display block trees, History entries, or public schema payloads.

## 2026-07-02 - LINEAR-ALGEBRA-NAMED-INPUTS1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Vector's user-facing named vectors are lowercase `u` and `v` in the workspace, soft keys, MatrixVec keypad overlay, guide text, and result/error readback. Runtime state and replay compatibility continue to use the existing `vectorA`/`vectorB` fields.

## 2026-07-02 - SETTINGS-HISTORY-VISUAL-FIDELITY1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Settings and History full pages follow the supplied dark desktop-product mockups as the visual target while staying truthful to current capabilities. `lucide-react` is the app-shell/page icon library for this slice. Settings uses existing settings state only, History remains over existing `HistoryEntry` plus pending tickets, and deferred mock-only controls such as Export, import, fake pagination, Formula Viewer-from-records, Graphing, Variables, and new runtime behavior remain excluded.

## 2026-07-02 - LINEAR-ALGEBRA-LATEX-PARSER1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Matrix/Vector editor LaTeX parsing is a local linear-algebra AST parser only. It recognizes named values, inline `bmatrix` literals, and approved Matrix/Vector operators without importing Equation internals, running operations, solving equations, or routing unsupported forms.

## 2026-07-02 - LINEAR-ALGEBRA-EDITOR-DISPATCH1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Matrix/Vector main editor execution must dispatch parsed executable forms through the existing Matrix/Vector request runners, OOE capability IDs, history seeds, and result readback. F-keys remain current named-input shortcuts. Parsed forms without an existing operation, including rank/RREF for now, stop with controlled local errors instead of creating a second solver or importing Equation internals.

## 2026-07-02 - EQUATION-NUMERIC-CARD-CREDIBILITY-POLISH1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Numeric result detail taxonomy is solver-owned, not wrapper-owned: Numeric Interval wrappers may add scope only, while domain facts, periodic structure, probes, diagnostics, breakpoints, and extraneous candidates stay in their dedicated cards. Equivalent fact strings are canonicalized before display dedupe, and `Solve Note` cards collapse by default in main Display and Formula Viewer.

## 2026-07-02 - EQUATION-NUMERIC-INTERVAL-TRIG-SEMANTICS1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Numeric Interval trig evidence must state the active angle unit and must not blur periodic-carrier evidence with full-equation periodicity. Affine direct trig equations may say equation periodicity is established; mixed equations such as quotient or algebraic-plus-trig forms only expose carrier periodicity, and excluded branch candidates remain validation evidence rather than accepted roots.

## 2026-07-02 - EQUATION-ANALYSIS-EVIDENCE-CONTRACT1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Equation analysis evidence v1 is internal and symbol-backed, not a public graphing API or persisted result schema. It may travel with `DisplayOutcome` objects inside the app while remaining absent from JSON serialization; later graphing-foundation milestones should extend this DTO instead of scraping Display prose.

## 2026-07-02 - EQUATION-DOMAIN-FACT-EVIDENCE-EXPORT1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Equation hard domain facts are exported through the internal analysis evidence contract from the numeric classifier/fact model, not from Display text. Denominator, log/root/fractional-power, inverse-trig, and trig-pole facts may be consumed structurally by future Equation-owned consumers while periodic carrier, sample-probe, singularity, interval-validity, certified-feature, range, and trust evidence stay in their own planned categories.

## 2026-07-02 - EQUATION-DISCONTINUITY-SINGULARITY-CLASSIFIER1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Equation singularity evidence is conservative and internal: solved denominator exclusions may be classified as removable candidates or pole/asymptote candidates by local zero-form probing, log/root/fractional-power/inverse-trig facts become branch/domain boundary candidates, trig-pole facts become trig-pole candidates, and uncertain exclusions stay `unknown`. These labels are not Display prose, proof certificates, graphing API, or public schema.

## 2026-07-02 - EQUATION-INTERVAL-VALIDITY-EVIDENCE1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Numeric interval validity evidence is exported from the existing interval arithmetic and segmentation substrate as internal symbol-backed evidence. Consumers may read `safe`, `invalid`, `split-required`, `unknown`, and interval-local boundary evidence structurally, but Display cards remain the user-facing explanation and no public graphing API or persisted schema is introduced.

## 2026-07-02 - EQUATION-CERTIFIED-FEATURE-EVIDENCE1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Certified Equation features are exported through internal analysis evidence: validated roots, interval-local scope, extraneous candidates, Sturm-certified real-root intervals, and visible Complex numeric polynomial branch roots are structured facts for future Equation-owned consumers. The evidence remains symbol-backed and internal; it is not a public graphing API, persisted result schema, Copy Result, History, OOE, app-state, or Tauri contract.

## 2026-07-02 - EQUATION-RANGE-BEHAVIOR-HINTS1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Equation range behavior hints are conservative internal evidence only. The supported v1 hints are real sine/cosine boundedness, absolute-value nonnegativity, real principal square-root nonnegativity, and angle-unit-aware tangent pole spacing. Derivative-based monotonicity, graph shape inference, Graphing UI, and public schema contracts remain out of scope.

## 2026-07-02 - EQUATION-CONSUMER-TRUST-READBACK1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Equation trust labels are internal structured evidence first. Display and Formula Viewer should prefer symbol-backed trust evidence for exact roots, certified polynomial roots, local Numeric Interval roots, bounded-search approximate roots, and region-local Complex roots, with legacy confidence prose only as fallback. This is not a public graphing API, persisted result schema, Copy Result, History, OOE, app-state, or Tauri contract.

## 2026-07-02 - LINEAR-SYSTEM-OWNERSHIP-AUDIT0

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Matrix owns only structured finite-dimensional linear systems, rank/RREF facts, and Matrix-local solution-count classification. Equation remains the owner of free-form equation solving, selected targets, nonlinear symbolic solving, branch/domain facts, numeric intervals, and generated routes. Matrix/Vector handoff to Equation must be explicit and typed, never an import of Equation internals or automatic routing.

## 2026-07-02 - MATRIX-AX-B-SYSTEM1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Matrix structured systems are limited to editor-entered `Ax=b` and `Ax+b=0` forms with local vector placeholder `x` and inline RHS/offset vectors. Matrix classifies solution count with Matrix-owned exact rank/RREF facts and commits through the existing `linearAlgebra.matrix` OOE path/history seed. Unsupported equation-shaped Matrix/Vector input must expose an explicit `Open in Equation` action and must not import Equation solver internals or auto-route.

## 2026-07-02 - MATRIX-EXACT-SCALAR-SIDECAR1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Matrix exact scalar widening uses optional `ExactScalarWire` sidecars on Matrix requests/replay seeds instead of replacing numeric grids. Editor-entered inline Matrix literals and structured-system RHS vectors may carry exact fractions and finite decimals through OOE/replay for determinant, rank/RREF, and structured-system exact paths; numeric grids and legacy numeric seeds remain compatible, and the decimal numeric engine remains the owner of approximate/numeric workflows.

## 2026-07-02 - MATRIX-NULL-COLUMN-SPACE1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Matrix owns null-space and column-space basis readback for finite exact matrices through the existing Matrix RREF pivot substrate. `null(...)` and `col(...)` may run for named A/B or inline matrices and produce exact bases, dimensions, pivot-column facts, and rank-nullity proof details; this does not add Equation solving, symbolic matrix CAS, automatic Equation routing, or Vector-space merging.

## 2026-07-02 - MATRIX-ROW-REDUCTION-STEPS1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Matrix row-operation readback is educational detail attached to Matrix-owned exact RREF results. `rref(...)` and structured systems may show row-operation traces from the exact Matrix RREF substrate, but those trace cards default collapsed so primary answers, system proof cards, and rank/RREF facts remain the first readback layer.

## 2026-07-02 - VECTOR-PROJECTION-ORTHOGONALITY1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Vector projection, orthogonal-component, unit-vector, and orthogonality-check capability belongs in the existing Vector numeric operation path and replay seed, not in Matrix or Equation. These operations use visible `u`/`v` labels, accept inline editor vectors where the parser already supports them, and return controlled local errors for zero-vector and dimension cases.

## 2026-07-02 - VECTOR-GRAM-SCHMIDT1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Vector owns two-vector `gram(u,v)` as a Vector numeric operation over existing `u`/`v` and inline vector operands. V1 returns orthogonal basis readback, orthonormal basis details when finite, proof details, and dependency/zero-span notes; Matrix-column QR, matrix-column Gram-Schmidt, least squares, and Equation solving remain deferred.

## 2026-07-02 - MATRIX-INVERTIBILITY-THEOREM1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Matrix owns finite square-matrix invertibility theorem readback through determinant, rank, pivot, and nullity facts from the exact Matrix substrate. `invertible(A/B)` may run for named or inline matrices; rectangular inputs produce rank/nullity guidance instead of invertibility claims. This does not add Equation solving, symbolic Matrix CAS, or automatic Equation routing.

## 2026-07-02 - MATRIX-EIGEN-2X2-EQUATION-BOUNDARY1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Matrix owns 2x2 characteristic-polynomial construction and rational-eigenvalue eigenspace presentation for `eigen(A/B)`, while Equation owns the typed exact quadratic polynomial root boundary. Matrix may consume only the boundary's rational real eigenvalues; irrational or complex roots return controlled Matrix guidance with an explicit `Open in Equation` action instead of importing Equation solver internals or auto-routing workspaces.

## 2026-07-02 - CALCULUS-LIMITS-TAYLOR-LEADING-TERMS1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Limits may use a capped Taylor leading-term probe through derivative order `10` for local additive cancellations, exposed through existing method detail cards. This is not Gruntz, symbolic targets, or a general public series engine.

## 2026-07-02 - CALCULUS-LIMITS-SQUEEZE-OSCILLATION1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Limits may use a pattern-based `squeeze-oscillation` route for classic finite-target bounded-oscillation forms. Successful products expose squeeze-theorem method details, and unsupported pure oscillation examples expose proof cards; this remains educational pattern handling, not a broad theorem prover.

## 2026-07-03 - CALCULUS-LIMITS-PIECEWISE-LIMITS1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Limits owns a narrow Piecewise branch-selection layer for simple comparison conditions in friendly `piecewise(...)` and LaTeX `cases` input. Branches feed existing finite/infinity limit engines; Equation inequality solving, broad Boolean condition proving, and Piecewise theorem proving remain out of scope.

## 2026-07-03 - CALCULUS-LIMITS-ABS-SIDE-BEHAVIOR1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Limits owns a narrow absolute-value side-behavior route for affine absolute-value carriers at their zero. It may prove one-sided signs and two-sided disagreement through detail cards; broad Piecewise/inequality solving and non-affine absolute-value theorem proving remain out of scope.

## 2026-07-03 - CALCULUS-LIMITS-MRV-LITE-ENGINE1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Limits owns a capped MRV-lite route for positive-infinity exponential/logarithmic scale comparisons. V1 compares collected exponential factors by flattened exponent differences, converts plain logarithmic exponent differences into ordinary powers, and stops short of full MRV-set construction, symbolic targets, or Gruntz.

## 2026-07-03 - CALCULUS-LIMITS-COMPLEX-PRINCIPAL-PROOFS2

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Complex Limit mode remains proof-first and narrow. It may prove recognized principal square-root boundary carriers whose radicand tends to zero, plus finite companion terms; unsupported branch, quotient, and non-radical complex boundary forms must stop with controlled guidance rather than numeric guessing.

## 2026-07-03 - CALCULUS-LIMITS-ASYMPTOTIC-ORCHESTRATION-HARDENING1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Natural Limit numeric fallback is explicitly route-owned and narrow: direct substitution and finite-pole side-evidence routes may use controlled numeric fallback, while exact symbolic/proof routes such as local-equivalent, exact-local-algebra, infinity-asymptotic, Piecewise, absolute-value side behavior, squeeze/oscillation, and MRV-lite must resolve symbolically or stop with controlled proof/diagnostic details.

## 2026-07-03 - CALCULUS-LIMITS-CONDITIONAL-INFINITY-CASES1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Limits may branch on target-free symbolic leading coefficients for narrow infinity polynomial-scale cases, using the existing conditional case surface. This is limited to coefficient signs/zero fallthrough for powers of the limit variable and must not import Equation inequality solving or claim broad symbolic target/Gruntz behavior.

## 2026-07-02 - LINEAR-ALGEBRA-EDITOR-TRUST-MILESTONE1 gate A

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Matrix/Vector variable hints are mode-aware UI analysis: supported linear-algebra functions, LaTeX matrix environments, and structural operands (`A/B/x`, `u/v`) are editor syntax, not generic variables. Equation and Calculate keep their existing raw adjacent-letter ambiguity warnings.

## 2026-07-02 - LINEAR-ALGEBRA-EDITOR-TRUST-MILESTONE1 gate B

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Full-expression Linear Algebra readback metadata belongs on the existing Matrix/Vector request and replay shapes as optional transport fields (`editorExpressionLatex` plus operand display labels). This preserves F-key compatibility and avoids creating a parallel history, display, or Equation-owned expression mechanism.

## 2026-07-02 - LINEAR-ALGEBRA-EDITOR-TRUST-MILESTONE1 gate C

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Linear Algebra readback should consume full expression metadata only when it adds trust: inline operands and structured systems show the typed expression/operand in titles and math cards, while named A/B and u/v requests keep compact labels. Prose cards should avoid raw LaTeX labels by saying "the matrix" or equivalent learner-facing wording.

## 2026-07-02 - LINEAR-ALGEBRA-EDITOR-TRUST-MILESTONE1 gate D

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Equation-specific root/trust summaries must not be inferred from generic finite-set LaTeX for Matrix/Vector results. Linear Algebra outcomes may carry source-mode provenance so Display renders basis, vector-set, Gram-Schmidt, and eigenspace answers as Linear Algebra math, while Equation keeps branch-list root summaries through Equation-owned evidence and metadata.

## 2026-07-02 - LINEAR-ALGEBRA-EDITOR-TRUST-MILESTONE1 gate E

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Matrix eigen readback should use learner-facing card labels even when it relies on typed cross-owner seams. The visible eigen method card says `How Eigenvalues Were Found`; the internal Equation quadratic boundary remains the polynomial-solving owner, and Matrix keeps eigenspace presentation plus explicit Equation handoff for unsupported roots.

## 2026-07-02 - ALGEBRAIC-DETAIL-CARD-NOTATION-NORMALIZATION1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Algebraic/genus proof details that contain math must use structured `lineKind`/`lineParts` metadata and preserve it through detail-section merging. Display should render that structured metadata according to the active math notation mode rather than receiving plain prose-like LaTeX fragments.

## 2026-07-03 - MEMORY-HISTORY-HYGIENE1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] `.memory/current-state.md` is a short operating snapshot, not a milestone ledger. Stale or superseded current-state records should be moved to the monthly current-state milestone archive instead of accumulating in the live snapshot. Resolved or superseded questions should move from `.memory/open-questions.md` into `.memory/closed-questions.md`, with a narrower replacement open question only when future work remains genuinely unresolved.

## 2026-07-03 - EQUATION-CORPUS-LEDGER1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Equation benchmarks use one canonical unique case as the runnable source of truth per mathematical case. Duplicate textbook or reference sightings are recorded as duplicate ledger rows attached to that canonical `case_id` and must not become separate run targets in the same sweep.

## 2026-07-03 - EQUATION-CORPUS-ALGTRIG-FIX1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Fixed corpus findings stay as historical scan evidence. A later fix must append a new `run_id` result and mark the finding with `resolution_status`, `resolution_run_id`, and `resolution_notes` instead of deleting or rewriting the original failed run.

## 2026-07-03 - ALGEBRAIC-GENUS1-SECOND-KIND-ROW-COEFFICIENT-EXTRACTION1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Genus-1 second-kind row extraction keeps first-kind Legendre multipliers as named target-free coefficient atoms while row equations are populated. This keeps bounded coefficient parsing inside the named-root field and defers concrete multiplier substitution to the later solved antiderivative/proof-backcheck gate.

## 2026-07-03 - ALGEBRAIC-GENUS1-SECOND-KIND-POPULATED-MATRIX-SURFACE1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Genus-1 second-kind matrix population is a proof surface before it is a solver. Populated entries, RHS nodes, and row residual backchecks may be recorded behavior-invisibly, but live EllipticE/Pi adoption still requires a later bounded symbolic solve with pivot facts and an antiderivative backcheck.

## 2026-07-03 - ALGEBRAIC-GENUS1-SECOND-KIND-SOLVE-BACKCHECK-SURFACE1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Second-kind solve readiness may record pivot candidates and row residual backcheck payloads without producing a solved coefficient vector. Live adoption remains blocked until a later bounded symbolic elimination accepts pivot facts and differentiates the resulting antiderivative.

## 2026-07-03 - ALGEBRAIC-GENUS1-SECOND-KIND-BOUNDED-SOLVE-ATTEMPT1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Second-kind bounded solve attempts may stop under explicit coefficient-growth, operation, or pivot-boundary caps. These controlled stops are proof evidence only; live EllipticE/Pi adoption still requires a solved coefficient vector, accepted pivot facts, node-first antiderivative substitution, and exact derivative backcheck.

## 2026-07-03 - ALGEBRAIC-FUNCTION-FIELD-ORCHESTRATOR1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] The algebraic function-field orchestrator is an internal coordinator over existing genus-0, genus-1, and boundary routes. It may preserve current public `u-substitution`/`unsupported` labels and result schemas, but it must not claim new second-kind, third-kind, symbolic, or degeneration live coverage without the required solve/backcheck prerequisites.

## 2026-07-03 - ALGEBRAIC-GENUS1-BRANCH-CASEWISE-COVERAGE1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Genus-1 branch casewise coverage is evidence, not live adoption. Exact-rational branch rows may classify radicand signs, real-valued intervals, endpoint policies, and reciprocal endpoint exclusions under the fixed cap, but symbolic branch formulas and branch-sensitive live widening remain blocked until their row counts and assumptions are capped.

## 2026-07-03 - ALGEBRAIC-GENUS1-ELEMENTARITY-CERTIFICATE1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Genus-1 elementarity certificates are proof context for already accepted elliptic answers, not a new solve route. They may state non-elementarity only for the displayed non-degenerate genus-1 elementary field and must keep degeneration, branch-heavy cases, and coefficient-cancellation specializations on separate fallback or controlled-stop paths.

## 2026-07-03 - ALGEBRAIC-GENUS1-DEGENERATION-FALLBACK-LIVE1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Live genus-1 degeneration fallback is limited to perfect-square quartic radicals with a provably nonnegative square factor. It may collapse those to existing genus-0 polynomial or rational integration routes; branch-changing square factors, symbolic repeated roots, and non-square repeated-root cubics/quartics must remain on controlled boundary paths until casewise branch splitting is available.

## 2026-07-03 - ALGEBRAIC-RISCH-PRACTICAL-CLOSEOUT0

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] The algebraic frontier push is closed as practical but incomplete. Future widening should be benchmark-driven or start with a generic genus-1 coefficient-vector/backcheck milestone; do not claim full algebraic-function-field Risch, broad symbolic named-root formulas, or generic second/third-kind live adoption until those prerequisites are solved and branch-capped.

## 2026-07-03 - VISUAL-OUTPUT-VERIFICATION-POLICY1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] App-visible mathematical output gates require Playwright visual inspection of the real rendered result, including answer/error cards, visible facts or assumptions, detail or boundary cards, and readability risks. Unit tests and DOM assertions may support the gate but do not replace visual verification. Benchmark duplicate-run rules are intentionally not an `AGENTS.md` policy; keep them scoped to the relevant benchmark corpus docs/schemas unless explicitly promoted later.

## 2026-07-03 - CALCULUS-INTEGRATION-CORPUS-LEDGER1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Calculus benchmark ledgers are lane-scoped. The first Calculus corpus is `benchmarks/calculus-corpus/integration/`, and it is indefinite-only: unique cases are the only runnable targets, duplicate sightings attach to canonical `case_id` rows, and run results must carry visual verification status for app-visible output. Limits, Differentiation, definite integrals, improper integrals, ODE, and advanced genus-heavy batches remain separate future lanes or deferred source slices.

## 2026-07-03 - GUIDE-EDUCATION-PLATFORM-AUDIT0

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | contributors: claude, user | attribution_basis: mixed] Future Guide platform work should reject a universal generated step-by-step engine and instead center on authorable notebooks/guidance packs, teacher/community export, learner import, and compact computation/evidence snapshots produced by real solver routes. External content embedding is not a dependency; import/export is the platform boundary, and History remains a personal computation ledger rather than the notebook schema.

## 2026-07-04 - GUIDE-PAGE-SURFACE1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Guide is a protected singleton app-level page surface, not a calculator-shell mode surface. Opening Guide focuses or creates `guide-page`, preserves the active calculator workspace/mode, uses null Order of Execution runtime context, suppresses quick inspectors, and keeps Notebook/import/export/persistence work deferred to later milestones.

## 2026-07-04 - EQUATION-HISTORY-ROUTING1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Equation guided-route history replay should persist typed `equationSeed` payloads and trust them before result-text inference. Structured display metadata such as `systemReadback` should be persisted with the history entry when it is required to replay the same answer-card presentation, rather than reconstructing clean rows from flattened result LaTeX.

## 2026-07-04 - INPUT-PASTE-CANONICALIZATION1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Plain-text math paste normalization belongs in the input/editor canonicalization layer. Safe top-level ASCII `/` and `*` plus textbook function powers may be converted before insertion, while solver lanes still own mathematical adoption, route facts, and backchecks.

## 2026-07-04 - LINEAR-ALGEBRA-READBACK-TRUST-REPAIR1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Matrix/Vector DisplayOutcome readback reserves global `APPROX` cards for actual numeric approximations. Matrix app-mode suppresses worker summary `approxText`; Vector app-mode keeps numeric scalar approximation text but filters nonnumeric summaries. Active operand selection should use readable custom workspace controls instead of native light selects.

## 2026-07-04 - LINEAR-ALGEBRA-TEMPLATE-NATURAL-INPUT1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Plain list syntax in Matrix/Vector is an import convenience only. Successful editor parses canonicalize editor/request/preview/history/readback to natural LaTeX, while MathLive matrix/template parsing stays local to Linear Algebra with no Equation import or automatic Equation routing.

## 2026-07-04 - EQUATION-COMPLEX-COMPANION-BENCHMARK1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Equation corpus Complex-On coverage is companion run evidence on the same canonical `case_id`, not a duplicate unique case. Applicable benchmark rows may carry `complex_companion_policy`, and Complex companion run results must record `domain_intent:"complex"`, `companion_run_kind:"complex-companion"`, and the real-domain run that triggered the check.

## 2026-07-04 - CALCULUS-INTEGRATION-NEXT350-STUDY1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Calculus integration benchmark sweeps for app-visible output use the Calculus workspace evaluator as the backend source of truth, not only the lower-level symbolic dispatcher. The dispatcher-only result can be stricter than the app because the workspace evaluator includes guarded Compute Engine fallback plus backcheck; run ledgers should reflect the app path when paired with Playwright visual evidence.

## 2026-07-05 - EQUATION-COMPLEX-COMPANION-FIX1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Equation Complex-On may preserve validated Real periodic trig families only when integer-parameter evidence is present; broad non-periodic real fallback must remain a controlled Complex boundary instead of hiding missing Complex support.
- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Positive numeric-base complex exponential equations use the existing log/exp family surface: `a^u=b` with positive numeric `a != 1` and affine `u` renders the structural branch `u=(Log(b)+2πik)/ln(a)`, while Real mode keeps its prior exact behavior.
- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] The scoped benchmark case `abs(2x+1)=x-5` is a Complex magnitude boundary with controlled empty-set evidence, not a general locus/set-valued Complex solver. General locus output remains deferred.

## 2026-07-05 - CALCULUS-INTEGRATION-LOWRISK-UNLOCKS1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Low-risk textbook integration widening is bounded to exact positive-discriminant improper rational division, reciprocal `1/(a^2-u^2)^(3/2)` and `1/(u^2+a^2)^(3/2)` affine radical templates, and affine `sinh^2(u)`/`cosh^2(u)` table forms. These routes must remain indefinite-only, Calculus-owned, and accepted only through route proof plus derivative backcheck evidence; difference-root and IBP families remain separate gates.

## 2026-07-05 - CALCULUS-INTEGRATION-IBP-GAPS1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Textbook IBP widening is bounded to indefinite Calculus integration for exact polynomial degree up to 4 times `arctan(x)`/`arcsin(x)`, and affine polynomial factors times `sec^2(ax+b)` or `csc^2(ax+b)`. Arctan residuals reuse the rational integration route, arcsin residuals use a capped `A*arcsin(x)+B(x)*sqrt(1-x^2)` recurrence, and affine trig-derivative rows must expose branch/fact evidence and pass derivative backcheck before adoption. Broader inverse-trig composition, higher-degree trig-derivative IBP, and definite-integral widening remain out of scope.

## 2026-07-05 - CALCULUS-LIMITS-PIECEWISE-CONDITION-ENGINE2

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Piecewise limit branch conditions remain Limits-owned linear interval logic in this arc. Numeric simple comparisons and chained intervals such as `0 <= x < 5` are parsed into internal comparison lists for branch selection; Calculus must not import Equation inequality solving for this milestone family.

## 2026-07-05 - CALCULUS-LIMITS-PARAMETER-ASSUMPTIONS1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Limits parameter assumptions remain generated guarded cases, not a new assumptions UI. Symbolic infinity polynomial-scale cases may branch on target-free symbolic leading coefficients and, when those coefficients vanish, fall through to lower numeric-growth terms or target-free constants under the accumulated guard conditions.

## 2026-07-05 - CALCULUS-LIMITS-RECURSIVE-LEADING-TERMS2

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Finite symbolic cancellation limits use a Limits-owned capped local-series fallback through Taylor order 10 after existing recursive leading-term routes fail. This is not a broad Taylor/Gruntz solver; workspace real-domain guards still block symbolic-log forms when assumptions are insufficient.

## 2026-07-05 - CALCULUS-LIMITS-CORPUS-HARNESS1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] The Limits corpus seed is a runtime-free ledger under `benchmarks/calculus-corpus/limits/`: one canonical runnable row per unique case, duplicate source sightings in a separate ledger, one run result per case per sweep, route/proof-card expectations captured as benchmark assertions, and large PDF/site expansion deferred until after Gruntz foundations land.

## 2026-07-05 - CALCULUS-LIMITS-GRUNTZ-FOUNDATION1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Gruntz work is now represented by Limits-owned foundation contracts first: MRV-set extraction, comparability classes, rewrite-to-`w`, and sign/limit extraction fixtures. This milestone deliberately does not expose a broad Gruntz solver route or alter app-visible Limit evaluation.

## 2026-07-05 - LINEAR-ALGEBRA-PASTE-NATURALIZATION1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Matrix/Vector friendly list syntax is paste/import UX only: supported pasted text naturalizes immediately through Linear Algebra-owned canonicalizers, malformed text stays editable until Run, and `answerRows` may improve visible row layout while `exactLatex` remains the Copy Result/export source.

## 2026-07-05 - CALCULUS-INTEGRATION-PASTE-KEYPAD-FIX1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Calculus integration input canonicalization must treat supported textbook function words as function heads before solver dispatch, including full inverse-trig names and hyperbolic names. Safe implicit product splitting is limited to a one-letter prefix before a known grouped function call, so arbitrary glued identifiers remain editable text.
- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] On-screen keypad layer/key interactions should preserve the active editor without stealing focus or scrolling back to the editor; editor focus calls made for keypad insertions should use no-scroll focus where the browser supports it.

## 2026-07-05 - CALCULUS-INTEGRATION-PRINTING-LAYER1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Calculus indefinite integration presentation is Calculus-owned and runs only after verified antiderivative backcheck. `+C` is added as a verified presentation suffix for indefinite results, Copy Result and To Editor keep canonical parseable LaTeX, and visible answer rows may preserve order/readability without changing solver adoption.

## 2026-07-05 - CALCULUS-INTEGRATION-ONE-ANSWER-PRESENTATION1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Ordinary indefinite integration output must render as one antiderivative expression, not additive answer rows. `answerRows` may be used as a single-row display-order guard for the canonical expression, while multi-row structures are reserved for true casewise or branch-family integration results.

## 2026-07-05 - CALCULUS-LIMITS-ASYMPTOTIC-SCALE-ENGINE2

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Limits infinity-scale comparison may derive logarithmic scales from eventually positive unbounded inner scales, including `log(x^p)`, `log(sqrt(x))`, `log(c*x)`, and `log(e^x)`, before L'Hospital or numeric fallback. This remains a scale route, not full Gruntz.
- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Real square-root infinity comparisons may proceed at negative infinity when the leading radicand scale is eventually positive; invalid real-domain radicals still stop through domain/proof routes.

## 2026-07-05 - CALCULUS-LIMITS-MRV-PREGRUNTZ3

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] MRV-lite may convert exact-cancelled logarithmic exponent differences into residual scale facts, including nested logarithmic factors. This is a pre-Gruntz residual-cleanup route, not a full MRV-set or rewrite-to-w solver.

## 2026-07-05 - CALCULUS-LIMITS-BRANCH-DOMAIN-PROOFS2

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Limits proof cards remain compact evidence cards, not generated step scripts. Branch/domain routes should show form detected, key calculation, and conclusion; Real-mode domain stops append a Limits-owned `Domain Proof`, and log Taylor fallback must only expand around `log(1 + small)`.

## 2026-07-05 - GUIDE-CONTENT-REALITY-FIX1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Statistics is now an active Guide-visible capability. Guide domain lists, mode references, related links, search targets, and home entries must stay mutually reachable for active capabilities; unsupported or future Guide claims should be corrected in content rather than hidden behind stale taxonomy.

## 2026-07-05 - PUBLIC-OVERVIEW-CATCHUP1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Current public overview prose should treat full Settings and History pages, the singleton Guide page, Statistics Guide visibility, Formula Viewer for dense current-result formulas, and the hostless Surface Protocol spine as current. Graphing, Spreadsheet, Notebook, import/export packages, website mounting, plugins, external software development kit work, Surface Protocol mounting/adapters, full Variables page work, broad Complex numeric/locus output, and full Risch-style certificates remain future.

## 2026-07-05 - GUIDE-CONTENT-CAPABILITY-EVIDENCE1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Guide examples are contract-bearing launch payloads. Every current example must pass static launch-shape validation, Matrix/Vector Guide entries remain open-tool only, and app-visible Guide example claims should be proven with representative Guide-page Playwright evidence or corrected in Guide content rather than widened through solver/runtime expansion.

## 2026-07-06 - CALCULUS-INTEGRATION-EXPONENT-PASTE-PRESENTATION1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Grouped exponent bodies pasted into math editors should reuse the same safe canonicalization as base expressions, so forms such as `x^(1/3)` become parseable fractional exponents instead of leaving inline slash text inside the exponent.
- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Verified Calculus indefinite-integration presentation should prefer explicit fractional powers for simple power/root antiderivative terms, such as `x^{3/2}`, when that avoids root-versus-power ambiguity; Copy Result and To Editor remain parseable and derivative-backchecked.

## 2026-07-06 - CALCULUS-INTEGRATION-TRIG-SUB-ROOTS1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Calculus indefinite presentation may reuse an existing verified antiderivative result when the visible/copy base expression is unchanged and only the structured integration constant is appended; if presentation normalizes the base expression, it must still run the derivative backcheck before replacing the route output.

## 2026-07-06 - CALCULUS-INTEGRATION-TRIG-POWER-REDUCTION1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Calculus trig-power reduction remains bounded to textbook rows: mixed `sin^m cos^n`, `tan^m sec^n`, and `cot^m csc^n` are capped at individual exponent 8 and total degree 12, while over-cap rows must return a controlled boundary card with no partial antiderivative.

## 2026-07-06 - CALCULUS-INTEGRATION-NEXT400-PROMOTION1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] The promoted next400 integration ledger truth comes from the regenerated candidate/backend/Playwright pass, not the earlier ambiguous scratch run. Duplicate source sightings stay in `duplicate-cases.jsonl` and are not separate run targets.

## 2026-07-06 - LINEAR-ALGEBRA-MULTI-MATRIX-EDITOR1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Matrix typed editor expressions may use any named or inline matrix values and compose them locally before routing to existing Matrix operations. Soft F-keys stay intentionally two-operand and reflect the active Left/Right selections only.

## 2026-07-06 - LINEAR-ALGEBRA-NAMED-LIBRARY-POLISH2

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Named Matrix/Vector library cards may insert their single-letter names into the main editor, while active badges and F-key labels remain driven by the selected Left/Right or First/Second operands.

## 2026-07-06 - COMPLEX-EQUATION-BRANCH-PULLBACK2

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Complex Region branch-sensitive solving may proceed through direct or real-affine principal-branch pullbacks only when the mapped bounded region avoids the branch point and cut. Broader composed pullbacks that cannot be certified safely must fail closed with branch-unsafe evidence instead of returning numeric roots.

## 2026-07-06 - COMPLEX-EQUATION-CONTOUR-MOMENTS3

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Delves-Lyness contour moments are Equation-local candidate-seed machinery, not a proof surface by themselves. They may generate extra seeds only for safe one- or two-root holomorphic cells after contour count under-finds candidates; accepted roots still require residual validation and contour agreement, while higher-count, pole-aware, branch-unsafe, or derivative-missing cells remain controlled/inconclusive.

## 2026-07-06 - COMPLEX-EQUATION-LOCAL-BOX-VALIDATION4

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Complex Region local box validation is evidence for already accepted bounded roots, not a replacement solver. A Krawczyk-style contraction may mark simple local boxes as validated when derivative variation stays inside the box; derivative-missing, branch-unsafe, pole-aware, clustered, or multiple-root boxes must remain unsupported or inconclusive rather than claiming unique-root validation.

## 2026-07-06 - CALCULUS-LIMITS-GRUNTZ-MRV-SET2

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Gruntz exposure remains contract-first: MRV-set extraction now owns symbolic coefficient drivers, numeric-coefficient filtering, principal-complex branch assumptions, and internal evidence rows before rewrite-to-`w`, sign extraction, or public route orchestration consume them.

## 2026-07-06 - CALCULUS-LIMITS-GRUNTZ-REWRITE-W1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Gruntz rewrite-to-`w` remains internal and evidence-first: it records the chosen MRV atom, `w` substitution, basic residual-variable substitution, transformed expression, parameter conditions, and branch assumptions without exposing a public Gruntz Limit route.

## 2026-07-06 - CALCULUS-LIMITS-GRUNTZ-SERIES-IN-W1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Gruntz series-in-`w` extraction remains an internal contract: it computes the leading `w` order and coefficient from supported transformed scale expressions and passes parameter/branch evidence forward, but public Limit routing waits for sign extraction and recursive orchestration.

## 2026-07-06 - CALCULUS-LIMITS-GRUNTZ-SIGN-LIMIT-EXTRACTION1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Gruntz sign extraction remains internal and proof-first: leading `w` order determines zero/finite/divergent shape, known coefficient signs determine signed infinity, symbolic leading coefficients produce guarded cases only within the three-driver and 12-row caps, and complex-valued outcomes still require principal-branch evidence before future public exposure.

## 2026-07-06 - CALCULUS-LIMITS-GRUNTZ-RECURSIVE-EVALUATOR1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Recursive Gruntz evaluation remains an internal contract until route orchestration: direct sign extraction is tried first, then bounded exponential exponent and quotient cleanup may recursively reduce residual scales while preserving parameter and principal-branch proof evidence.

## 2026-07-06 - CALCULUS-LIMITS-GRUNTZ-FINITE-TARGET-BRIDGE1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Finite Gruntz bridging remains an internal contract until public orchestration: finite targets are converted to one-sided positive-infinity recursive contracts through `x=a+1/t` and `x=a-1/t`, and two-sided finite results require bridge-side agreement before exposure.

## 2026-07-06 - LINEAR-ALGEBRA-MULTI-VECTOR-EDITOR1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Vector typed editor expressions may use arbitrary named or inline vectors beyond the active First/Second operands. `proj(base,target)` means "project target onto base"; `proj_u(v)` and `proj_v(u)` remain aliases. Exact 3D cross product and scalar triple product are Vector-owned operations, while F-keys remain active-operand shortcuts only.

## 2026-07-06 - CALCULUS-LIMITS-GRUNTZ-ORCHESTRATION1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Gruntz is now a controlled public Limit route, not the first route: exact/domain/piecewise/abs/specialized symbolic routes keep precedence, Gruntz runs before numeric fallback for supported scale/indeterminate cases, and visible output stays compact through method/route evidence cards.
- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Limits plain-text natural input may canonicalize `exp(...)` to exponential notation for Limit parsing and history/copy stability, while unsupported full Gruntz cases must still stop cleanly instead of guessing numerically.

## 2026-07-06 - CALCULUS-LIMITS-GRUNTZ-CORPUS-HARDENING1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] The seeded Limits corpus may now use `gruntz` as an expected route label and should assert compact detail-card evidence for Gruntz seed cases. Large PDF/site ingestion remains deferred until after the public Gruntz route is broader.

## 2026-07-06 - CI-UNIT-REFOBJECT-CLEANUP1

- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Current React code should use `RefObject` for mutable refs under the installed React typings; deprecated `MutableRefObject` usage should not be reintroduced for app/runtime/test code.
- [agent: codex | model: gpt-5.5 | primary_agent: codex | primary_agent_model: gpt-5.5 | attribution_basis: live] Generated exp/log symbolic candidates must validate inherited real-domain facts before display. Same-base logarithmic reductions whose algebraic candidate violates the original log domain should fail closed with a domain-empty result instead of surfacing an impossible exact answer.

## 2026-07-09 - ATTRIBUTION-FAMILY-GOVERNANCE1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Exact attribution model values `gpt-5` and `gpt-5-codex` are user-confirmed aliases for `gpt-5.5`. Explicit `gpt-5.4`, `gpt-5.3-codex`, and lower/versioned historical values are protected, and historical family values must not be inferred.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Agent family provenance begins prospectively on `2026-07-09` with allowed values `sol`, `terra`, and `luna`; structured artifacts carry role-specific family fields beside each applicable agent/model role.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] The anti-regression program is locked as four Incident Closure moves, mandatory user review, then five Behavioral Ratchets. Statistics guided controls and the printer/detail/clipboard implementation arcs remain separate.

## 2026-07-10 - WORKSPACE-CANARY-SUITE1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] The computational canary registry is derived from launcher leaves excluding Labs, carries an initial committed floor of 19 settings-explicit cases across all nine workspaces, and uses direct structured Statistics requests until guided-control defects receive their separate milestone.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Exact inverse-trig special values are shared by Trigonometry and Calculate, while the Calculate evaluator intercepts only direct exact numeric inverse-trig specials and real-domain violations before ordinary numeric evaluation. Symbolic and nonspecial values retain their existing routes.

## 2026-07-10 - WORKSPACE-RUNTIME-PROBE-REGISTRY1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Runtime anti-regression coverage is a declarative registry over each workspace's native public OOE facade, not a new generic runtime. Every computational launcher leaf keeps its existing capability ID, primary/fallback host identity, request shape, shell evidence, commit/stale assessment, diagnostics, and History launch ticket.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Statistics and Table retain distinct worker-failure contracts: Statistics may fall back before worker startup but does not retry a failure after startup; Table may cooperatively fall back after a worker runtime failure. Both preserve hard-stop cancellation behavior.

## 2026-07-10 - CI-GATE-ALIGNMENT1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Pull requests and `main` run the required static anti-regression gates and an independent browser job so workspace canaries are not skipped when another CI job fails. Browser retries are explicitly zero; flaky canaries must be repaired rather than retried.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Linux release must run app identity, Surface Protocol, OOE, compartment, file-size, canary-registry, runtime-probe, and workspace-canary gates before Tauri packaging. The workflow validator ratchets trigger, command, ordering, independence, and retry policy.

## 2026-07-10 - LINEAR-ALGEBRA-VECTOR-MATRIX-ROADMAP0

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Linear Algebra expansion begins with a centralized dimension contract before new Vector or Matrix operations. The 8 by 8 matrix and length-8 vector editing limits are distinct from operation-specific exact-execution limits; unsupported exact work must stop explicitly rather than silently becoming approximate.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Vector span/independence must reuse Matrix exact RREF infrastructure, variadic Gram-Schmidt must share an orthogonalization core with Matrix QR, and future spectral Matrix work must use only the typed Equation polynomial boundary.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Matrix and Vector keep separate OOE capability identities while sharing the current worker runtime. A worker/shell split requires a dedicated metrics audit proving execution-risk divergence; feature count alone is not sufficient.

## 2026-07-10 - SEAM-IMPACT-SELECTOR1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Seam impact evidence is additive: the selector may require allowlisted focused contract suites, but it must never skip, replace, or claim completion of baseline CI gates. Executable commands are resolved from the committed registry and run without shell interpolation.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Matrix and Vector are separate seam-selector lanes. A Matrix-only path trips Matrix, a Vector-only path trips Vector, and shared Linear Algebra core, host, shell, and pilot paths trip both lanes while the current shared runtime remains in place.

## 2026-07-10 - INCIDENT-CLOSURE-REVIEW-CI-FIX1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Canonical planner normalization to `\operatorname{abs}` must not bypass an already approved bounded Complex boundary route. The compatibility matcher is widened only for the existing affine benchmark `|2x+1|=x-5` and its reversed equality; this does not claim general Complex absolute-value or locus solving.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Mandatory Incident Review evidence is taken from the current shared `main`: all nine workspace canaries must pass with full-page visual inspection, seam evidence remains additive, and Behavioral Ratchets stay blocked until the user accepts the review checkpoint.

## 2026-07-10 - STATISTICS-WORKSPACE-CONSOLIDATION-ROADMAP0

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] The approved guided Statistics information architecture has four visible sections: `Data & Summary`, `Probability`, `Inference`, and `Relationships`. Existing request kinds and `StatisticsScreen` values remain compatibility vocabulary for parser, Guide, History, and replay while the visible section model is introduced separately.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Statistics keeps `statistics.evaluate`, its current primary/fallback hosts, one OOE authority, and one committed result authority. A combined Relationships result requires a typed composite request/result and one OOE launch; it must not be assembled from hidden duplicate jobs or stale independent results.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Statistics curriculum targets school through introductory university. Professional and research-oriented modeling suites, statistical programming ecosystems, and publication workflows remain outside the product boundary.
- [agent: codex | model: gpt-5.5 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.5 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.5 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.5 | verified_by_agent_family: sol | attribution_basis: live] Statistics section results are one canonical outcome plus originating input revision per section, not four renderers. Switching sections restores that cache without invalidating unchanged work; hidden completions may update History and their origin cache but may not replace another section's visible result. Clear is section-scoped, and only `Data & Summary` may reset shared data.

## 2026-07-10 - LINEAR-ALGEBRA-DIMENSION-CONTRACT1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Linear Algebra execution limits have one reusable contract. Visual editing supports matrices through 8 by 8 and vectors through length 8, while exact expression, elimination, augmented-system, spectral, power, and scalar-growth routes keep explicit operation profiles rather than inheriting one misleading global cap.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Proof-grade exact Matrix and Vector work must stop with a controlled learner-facing dimension error when a profile is exceeded. This milestone does not silently fall back to decimal computation, raise exact-elimination limits, split the worker, rename capability IDs, or change request and replay schemas.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Editing caps and the shared clamp helper are exported through the public Linear Algebra runtime facade so workspace UI does not duplicate private constants. Exact-core callers consume the internal profile contract directly.

## 2026-07-10 - VECTOR-SCALAR-LINEAR-COMBINATION1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Vector typed expressions support exact numeric scalar atoms, negation, scalar multiplication, and vector-by-scalar division while preserving rational sidecars and the existing scalar-growth guard. Finite decimals are exact inputs; symbolic coefficients remain deferred and stop explicitly.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Type-directed dot and cross tokens scale when exactly one operand is scalar and keep their existing dot/cross meaning for two vectors. Scalar-by-vector division and vector-by-vector division remain controlled errors; F-key actions and OOE capability identity are unchanged.

## 2026-07-10 - VECTOR-SPAN-INDEPENDENCE1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Vector `span(...)` and `independent(...)` are variadic exact-numeric experiences over one through six equal-length vectors of length at most six. Vector owns parsing, request/readback, and dependence explanations; Matrix owns the reusable exact column-family/RREF analyzer used to compute rank, pivots, kernel witnesses, and original-column image bases.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Variadic Vector requests carry all numeric/exact operands and operand LaTeX while preserving the first two compatibility slots. F-keys remain First/Second shortcuts, no approximate fallback is allowed, span facts and dependence relations open by default, and dense RREF evidence starts collapsed.

## 2026-07-10 - MATRIX-LINEAR-MAP-PROFILE1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Matrix `profile(...)` is an exact-numeric linear-map summary over named, inline, or computed matrices through 6 by 6. It reuses Matrix-owned column-family/RREF analysis for rank, nullity, kernel, image, and pivots; it does not add a parallel elimination mechanism or change Matrix/Vector OOE ownership.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Profile readback shows domain/codomain, rank-nullity, one-to-one and onto reasons, and square-only determinant/invertibility facts. Rectangular maps say invertibility is not applicable, learner-facing facts open by default, dense RREF evidence starts collapsed, and nonnumeric `APPROX` output remains forbidden.

## 2026-07-10 - LINEAR-ALGEBRA-SHELL-SPLIT0

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] The user accepted the completed anti-regression Incident Review and chose to freeze Matrix/Vector capability expansion through Move 9 while auditing host topology before Move 5.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] The risk-based Matrix/Vector split gate is not met: loaded production-worker savings are 1.34% gzip for Matrix and 5.00% for Vector, browser lifecycle timing is effectively equal, serialization differences stay below 2x, and current fallback/cancel/failure/stale/History policies do not diverge.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] `MATRIX-VECTOR-RUNTIME-SHELL-SPLIT1` remains blocked unless the user explicitly replaces the current evidence rule with a different product-isolation decision. Feature count and future hypothetical pressure do not override current measured evidence.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] After reviewing the failed current-risk gate, the user explicitly replaced its implementation blocker with a prospective product-containment topology lock. Matrix numerical decomposition/conditioning and Vector exact orthogonalization/geometric growth will use independent worker/fallback hosts before those features land. This does not rewrite the audit as a pass.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Anti-Regression Moves 5-9 are approved scheduled blockers but are not yet completed Git history. Matrix/Vector capability work remains frozen until the split and all five ratchets are actually committed and the manual closeout is accepted.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Matrix and Vector runtime topology is now independent: `linearAlgebra.matrix` uses `matrix-worker-runtime` / `matrix-runtime` / `matrix-worker-shell`, and `linearAlgebra.vector` uses `vector-worker-runtime` / `vector-runtime` / `vector-worker-shell`. The old shared host pair and worker entrypoint are retired.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Separate thin clients and entrypoints own worker identity while one shared helper retains only the proven-identical startup, fallback-before-start, hard-stop, message, and cleanup ritual. Solver cores, request/replay schemas, History, stale/commit legality, and OOE authority remain shared only where previously intended.

## 2026-07-10 - FEATURE-PROBE-REGISTRY1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Every live `Settings` key must be classified in the exhaustive feature-probe registry and name at least one executable native, component, or persistence test. Adding, removing, or renaming a setting or its evidence must fail the dedicated gate until the registry is updated intentionally.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Feature probes are evidence routing, not a second settings runtime. Existing native and UI tests remain behavior owners; the registry records their policy class and executable location without making application code depend on anti-regression metadata.

## 2026-07-10 - GOLDEN-CORPUS-REGISTRY1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] The fast golden corpus uses direct native workspace adapters over the existing `src/lib/__golden__` union. It is not an OOE, worker, or browser execution layer; those remain independent evidence gates.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] The initial ratchet is exactly 43 unique cases with at least two per launcher-derived computational workspace. Table keeps structured row evidence beside its `DisplayOutcome`, while large Equation and Calculus benchmark ledgers stay outside this fast curated corpus.

## 2026-07-11 - PRINT-HYGIENE-BASELINE1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Print hygiene is a pure evidence collector over typed mathematical fields, not a printer, serializer, renderer, or clipboard policy. Prose remains outside token checks, and Table row values remain separately typed so a complete `undefined` cell is legitimate domain evidence.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] The initial print baseline is the normalized structure of all 43 golden executions. Updates require explicit `--accept` plus a committed non-empty reason; broad snapshot-update flags are forbidden, and normalization may collapse whitespace but must not remove pedagogical parentheses.

## 2026-07-11 - WORKSPACE-FRESHNESS-REPORT1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Workspace freshness is an attention signal derived from dated session-directory slugs, never a correctness, release, or health gate. Stale and missing results must exit successfully; only invalid arguments or unreadable repository state fail execution.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Freshness aliases have one tooling-owned registry. Shared anti-regression/canary evidence applies to all nine workspaces, shared Linear Algebra evidence applies to Matrix and Vector, and weekly generated reports are artifacts only rather than tracked timestamp churn.

## 2026-07-11 - HISTORY-REPLAY-RATCHET1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Deterministic History evidence freezes launch-time computation and print settings plus `Ans` at pending-ticket reservation. The snapshot is consumed only by the automated harness; visible replay continues to use existing route seeds and saved result data without mutating global settings.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Snapshot-less History records remain loadable and are classified internally as `legacy-nondeterministic`; old defaults are never guessed. Stable outcome identity and structural cardinalities are hard comparisons, while normalized LaTeX differences remain report-only until a later printer policy deliberately promotes them.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Table History replay preserves its existing saved-summary contract and does not regenerate row previews. Personal History is never read implicitly; the opt-in importer requires an explicit export and writes allowlisted sanitized candidates only beneath ignored `.task_tmp/history-replay-import/`.

## 2026-07-11 - PRINTER-DETAIL-CLIPBOARD-ROADMAP0

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Canonical math originates from producer-owned MathJSON or native domain values. Producers derive canonical LaTeX through pure printer adapters; Display may derive presentation-only variants but may not become a CAS or reconstruct unknown math.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Printer profiles are internal `compatibility-v1` and `pedagogical-v1` contracts. No Settings key or visible style control is added, and presentation normalization must not simplify or reorder the producer tree.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Clipboard uses a lossless internal envelope where the host supports it and canonical LaTeX as the exact fallback. Visible History remains unchanged, detail parts stay canonical-LaTeX-only, and structured History plus Display inversion are deferred to a separate roadmap.

## 2026-07-11 - PRINTER-SERIALIZATION-CONTRACT1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Serializable MathJSON is plain structured-clone-safe data bounded at 2,000 nodes, 64 levels, and 320,000 bytes. Invalid, cyclic, boxed, non-finite, or oversized values fail closed before host transport.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Canonical printer serialization boxes the validated producer tree in structural form with explicit options. It may render subtraction and precedence parentheses, but it must preserve zeros, identity factors, operand order, and other producer-owned structure.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Domain adapters remain generic typed seams rather than a runtime registry. The initial Equation adapter delegates to the existing finite-root presentation IR, and compatibility LaTeX remains the production-safe fallback until each producer opts in.

## 2026-07-11 - DISPLAY-MATH-PAYLOAD1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] `DisplayMathPayloadV1` is additive internal metadata: canonical LaTeX must equal the existing `exactLatex`, optional MathJSON must pass the bounded plain-data validator, and existing LaTeX fields remain compatibility authority during migration.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Producers may dual-write only from a proven answer node. Calculate must not repurpose `normalizedMathJson`, and Equation finite-root payloads require node-complete visible branches; any target rewrite that cannot rewrite the payload must discard it.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Canonical payloads do not persist to History and do not cross Surface Protocol. They are worker-safe internal display metadata for the upcoming canonical Clipboard and printer migration, while visible output remains on the compatibility profile.

## 2026-07-11 - PRINTER-MIGRATION-RATCHET1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Result-serialization debt is governed by a TypeScript compiler API inventory of known output properties and builders. Stable source fingerprints and exact baseline membership reject new, changed, or stale paths; per-lane and per-registration compatibility floors may only stay level or decrease.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Compatibility fallbacks require narrow path ownership plus a durable owner and rationale. Input syntax, presentation-only math, prose, Guide/Labs/reference content, canonical-printer internals, and downstream rendering are classified separately and do not inflate producer migration debt.

## 2026-07-11 - CLIPBOARD-CANONICAL1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Internal math copy uses `MathClipboardEnvelopeV1`: exact schema/version, canonical LaTeX, optional bounded validated MathJSON, and coarse surface/mode metadata. Record IDs, settings, personal data, and unknown fields are rejected; canonical text and total envelope bytes are independently capped.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Browser rich copy writes custom web MIME, escaped HTML with a base64url envelope attribute, and visible text together. If rich copy is unavailable or blocked, text falls back to canonical LaTeX. The Tauri host uses only the official plugin's read-text, write-text, and write-html permissions and always supplies canonical alternate text because it has no HTML-read or arbitrary custom-MIME API.

## 2026-07-11 - CLIPBOARD-PIPELINE-RATCHET1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Math copy surfaces use one canonical-envelope pipeline with explicit coarse surface metadata; diagnostics remain plain text through the same host adapters. Production UI and app code may not access browser Clipboard APIs, raw paste-event data, legacy copy commands, or the Tauri plugin outside `src/lib/clipboard/`.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] `expressionRouting.ts` is the sole app-level paste router. Validated custom-MIME or HTML envelopes insert their canonical LaTeX without reparsing; safe text-only or mismatch fallbacks continue through existing mode-aware canonicalization, including Matrix and Vector naturalization.

## 2026-07-11 - DETAIL-SEGMENT-CONTRACT1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Live detail rendering uses one shared component with strict precedence: nonempty typed parts, explicit math/prose intent, then legacy inference only for undeclared compatibility data. Empty `lineParts` placeholders are not declarations, and old History strings remain renderable without being rewritten.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Mixed detail and solve-summary producers should derive compatibility strings from canonical text/math parts. Detail math stays canonical-LaTeX-only in this program; typed summary parts are not persisted to History or exposed through Surface Protocol.

## 2026-07-11 - DETAIL-SEGMENT-EQUATION-CORE1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Live detail migration debt is governed by a TypeScript contextual-type inventory with declarative ownership lanes, stable source fingerprints, and nonincreasing accepted floors. A removed fingerprint requires an explicit accepted baseline update; unchanged counts cannot conceal replacement debt.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Explicit `lineKind: 'text'` is the behavior-preserving declaration for rows that previously fell through to plain prose. Rows that previously used mixed legacy inference must receive typed parts instead; historical undeclared History data keeps compatibility inference.

## 2026-07-11 - DETAIL-SEGMENT-EQUATION-PARAMETERIZED1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Established parameterized detail builders are live producer seams and belong in the AST inventory. The ratchet now counts all 34 `buildParameterizedDetailSections()` call sites instead of limiting governance to contextual object literals.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Live parameterized normalization may preserve explicit typed parts and normalize compatibility text, but it may not reconstruct math intent through legacy string inference. Generated equations and formula branches are authored as typed parts at their producer; old undeclared History retains renderer-side compatibility inference.

## 2026-07-11 - DETAIL-SEGMENT-SYMBOLIC-LIMITS1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Symbolic Limits route producers must construct explicit typed rows. Regex classification of completed prose strings is compatibility debt and may not be called by finite rules, local equivalents, recursive leading terms, indeterminate transforms, L'Hospital, or rewrite/cancellation routes.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Limits-specific typed builders are governed AST producer seams. The ratchet inventories `limitDetailSection()` and `limitMethodRowsSection()` calls, while legacy string helpers remain temporarily available only to Calculus-owned callers until `DETAIL-SEGMENT-CALCULUS1`.

## 2026-07-11 - DETAIL-SEGMENT-SYMBOLIC-INTEGRATION1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Symbolic Integration details must be authored as typed math/text rows or explicitly declared prose. Normal-form, rewrite, substitution, integration-by-parts, polynomial-division, genus-1, and certificate routes may not rely on renderer inference for live intent.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] The user removed the intermediate contract-review pause. Its verification evidence remains mandatory, but the program proceeds directly through printer profiles and presents one accumulated review at the end.

## 2026-07-11 - DETAIL-SEGMENT-CALCULUS1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Calculus details use producer-owned typed rows for variable checks, route diagnostics, limit conclusions, integral methods and safety, improper-integral stops, and Laplace readback. Production Calculus may not call the legacy Limits completed-string inference helpers.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] One-sided domain detail subjects are derived from structured constraints at the Calculus boundary. Existing typed domain math must not be lost merely to remove a compatibility inference helper.

## 2026-07-11 - DETAIL-SEGMENT-WORKSPACE-DOMAINS1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Statistics quality readback keeps metric labels as prose and owns SSE, MSE, and residual standard error as typed mathematical values. Correlation interpretation remains explicitly prose because it communicates a qualitative classification rather than a mathematical fragment.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Table cancellation remains an explicit OOE prose detail. Declaring detail intent must preserve existing collapsed state and card behavior; verification expands collapsed sections to inspect typed math rather than changing their default presentation.

## 2026-07-11 - DETAIL-SEGMENT-LINEAR-ALGEBRA1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] The LU zero-pivot proof is an explicit prose stop, not a mathematical fragment. Closing detail metadata debt must preserve its existing error and proof wording and cannot be used to widen Matrix/Vector capabilities or alter their independent runtime topology.

## 2026-07-11 - DETAIL-SEGMENT-COMPAT-CLOSEOUT1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Every live detail producer must own typed math or explicitly declared prose; the accepted live inference floor is zero. Compatibility inference remains a renderer-only path for legacy or snapshot-less stored History and may not be reused to classify new producer strings.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Structured domain, assumption, variable-memory, and absolute-value facts preserve their exact compatibility lines while exposing mathematical subfragments as typed evidence. Free-form policy, trust, and concise explanatory rows remain explicit prose rather than being promoted by punctuation heuristics.

## 2026-07-11 - PRINT-PROFILE-CALCULATE-EQUATION1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] A producer may satisfy `pedagogical-v1` through a proven native-domain serializer when generic MathJSON serialization would expose normalization scaffolding or reduce readability. Profile adoption does not require visible drift.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Migrated History replay families hard-compare normalized LaTeX; unmigrated families remain report-only. The first hard workspace is Calculate with 20 fixtures, while Equation waits for the advanced slice despite finite-root adapter adoption.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Mathematically equivalent printer output is not automatically pedagogically acceptable. Constant-first sums, reordered leading-negative derivatives, and weakened standard domain forms are rejected and remain recorded in the output ledger.

## 2026-07-11 - PRINT-PROFILE-EQUATION-ADVANCED1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Advanced Equation producers keep their specialized exact, numeric, parameterized, composition, target, and Complex serializers as native domain adapters under `pedagogical-v1`; generic MathJSON reparsing or normalization is not introduced.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] A named profile wrapper counts as migrated only when it directly encloses the authored result object. The ratchet does not allow wrapper imports, file-level calls, or broad registrations to erase unwrapped producer debt.

## 2026-07-11 - PRINT-PROFILE-SYMBOLIC-LIMITS1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Proof-aware Limits producers retain their native exact and conditional serializers as the `pedagogical-v1` domain adapter. Profile migration must preserve signed infinity, sided conditions, method evidence, and route-local fallback behavior rather than reparsing conclusions through generic MathJSON.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Replay LaTeX becomes hard by migrated family, not by an unfinished parent workspace. The five Calculus limit fixtures are hard evidence; unrelated Calculus families remain report-only until their owning profile slices migrate.

## 2026-07-11 - PRINT-PROFILE-SYMBOLIC-INTEGRATION1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Symbolic Integration retains native exact antiderivative, certificate, and controlled-stop serializers under `pedagogical-v1`; producer constants, verification evidence, and method readback must not be reconstructed through generic MathJSON.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Printer ownership is semantic, not merely file-based. Generic factor and derivative outcomes in `symbolic-engine` belong to a narrow symbolic-core profile and may not be counted as Integration debt simply because their orchestrator was historically listed in the Integration fallback registration.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] The six Calculus integral fixtures become hard LaTeX evidence after the Integration family migrates; other Calculus fixture families remain report-only until the Calculus profile milestone.

## 2026-07-11 - PRINT-PROFILE-CALCULUS1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Calculus derivatives, integrals, limits, Laplace transforms, ODEs, and series retain their native exact serializers under one internal `pedagogical-v1` domain profile; the migration does not normalize or rederive their output through generic MathJSON.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Generic profile wrappers must not weaken TypeScript result-union contracts. Wrapped helpers that callers narrow by optional success/error fields retain explicit existing result annotations.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Once all Calculus producer families migrate, the entire 25-fixture Calculus replay file becomes hard LaTeX evidence; family-level hard flags remain compatible but no longer define the workspace boundary.

## 2026-07-11 - PRINT-PROFILE-GUIDED-DOMAINS1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Trigonometry, Geometry, and Statistics retain their native exact result serializers under separate `pedagogical-v1` profiles. Shared profile mechanics do not merge workspace ownership, request shapes, workers, capability IDs, or guided controls.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] A zero compatibility count does not prove profile adoption. Table's forwarded `DisplayOutcome` is explicitly profiled, while its structured rows remain `TableResponse` evidence and legitimate undefined cells keep their narrow print-hygiene exception.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Trigonometry, Geometry, Statistics, and Table replay files become hard together only after each workspace has explicit native profile evidence; Statistics guided-control defects remain outside this migration.

## 2026-07-11 - PRINT-PROFILE-LINEAR-ALGEBRA1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Matrix and Vector may share one internal presentation profile over native exact serializers, but they retain separate workers, hosts, capability IDs, request shapes, replay seeds, cancellation rules, diagnostics, History tickets, and fallback semantics.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Linear Algebra profile adoption does not authorize numerical capability expansion, shared runtime topology, or replacement of exact `resultLatex` copy/export truth.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] After Matrix and Vector migrate, all 100 sanitized replay fixtures hard-compare normalized LaTeX; producer closeout must keep that global hard floor.

## 2026-07-11 - PRINT-PROFILE-PRODUCER-CLOSEOUT1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] The accepted production printer compatibility floor is zero across all 519 inventoried result paths. New result serialization must enter an explicit producer profile or fail the TypeScript-AST ratchet; broad fallback registrations may not be reintroduced to hide new debt.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Shared polynomial-factor and transform helpers own a narrow shared-algebra presentation profile because they serve multiple workspaces. That profile does not merge solver, workspace, worker, capability, History, replay, or OOE ownership.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Playwright collects only `*.spec.ts` under `e2e/`; Vitest-only registry tests may remain nearby but must not initialize inside Playwright. Browser assertions follow current typed-detail and provenance contracts without changing app output to preserve obsolete selectors.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] No visible printer change is accepted by this program. Existing producer serializers remain the pedagogical output, old History strings remain unchanged, and `HISTORY-STRUCTURED-RESULT2` plus `DISPLAY-CONTRACT-INVERSION1` remain deferred to a separate roadmap after final user acceptance.

## 2026-07-11 - HISTORY-DISPLAY-CONTRACT-ROADMAP0

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Structured History and live Display share one bounded producer-owned `CanonicalResultDocumentV1`; Display blocks remain presentation/scheduling projections and are never persisted as canonical truth.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] New History remains success-only, restores the original stored card without recomputation, renders with current presentation preferences, stores exact bounded Table rows, excludes executable actions, and falls back whole to legacy fields rather than truncating oversized structure.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Browser/Tauri persistence parity is a mandatory prerequisite. Rust preserves extension fields through a minimally validated envelope, and failed durable saves retain the current-session row with a truthful warning.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Equation receives a separate internal solve-result carrier before advanced Display inversion. All migration slices preserve exact visible mathematical output; legacy Display fields remain derived projections until a separate removal audit.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Newly discovered prerequisites pause for evidence, roadmap revision, and user approval. Named-roadmap standing approval does not authorize inserted moves or any push.

## 2026-07-11 - HISTORY-PERSISTENCE-PARITY-CLOSURE1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Browser History accepts the exact 35-field `HistoryEntry` key set but returns the original validated object so unknown versioned extensions are preserved. Calculator Memory reuses that preserved History array instead of a lossy parsed projection.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Native History is an extension-preserving `serde_json::Value` envelope. Rust requires nonempty `id`, supported `mode`, nonempty `inputLatex`, and nonempty `timestamp`, enforces the 2,000,000-byte limit only for new appends, retains the newest 80 valid rows, and does not reinterpret versioned payloads.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] A failed browser or Tauri append does not remove the already committed in-session row. The app reports `History is available this session only; it could not be saved.` as a non-blocking runtime status.

## 2026-07-12 - RESULT-INTENT-DECLARATION-CLOSURE1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Canonical-result compatibility projection may consume only explicitly typed math/text detail and solve-summary parts. It may not infer mathematics from a prose string, even when the string contains LaTeX-like text.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Live solve-summary builders derive compatibility text from typed parts and preserve exact legacy wording. Legacy History may retain inference at its read boundary, but new producer output must declare intent.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Sanitized replay fixtures must use current native request contracts. Obsolete interval shapes are corrected rather than preserving malformed `undefined` diagnostics as historical expected output.

## 2026-07-12 - CANONICAL-RESULT-DOCUMENT1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] `CanonicalResultDocumentV1` is the neutral producer-owned mathematical result contract. Display and History may project from it, but render scheduling, actions, OOE/runtime advisories, UI state, settings, record identity, and personal identifiers are prohibited from the document.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Compatibility projection is structural and no-reparse: canonical LaTeX and optional proven MathJSON are copied only from typed result fields. Prompts remain control flow, while math-bearing success and controlled-error outcomes may carry a validated document.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Whole documents are strict JSON-compatible structured-clone data bounded at 10,000 nodes, depth 64, and 640,000 UTF-8 bytes. Every nested MathJSON value also retains the existing 2,000-node, depth-64, and 320,000-byte bounds; invalid or oversized documents are rejected whole rather than truncated.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] The canonical-result-to-Display adapter is downstream presentation, not a mathematical producer. Its three legacy result-field projections add no printer fallback debt and must remain covered by the printer migration registry.

## 2026-07-12 - HISTORY-STRUCTURED-RESULT2

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] New successful History entries dual-write `CanonicalResultDocumentV1` and the existing legacy fields. Structured data is read first; legacy-only, malformed, or unsupported future extensions remain loadable through the legacy read path and are never reconstructed by parsing LaTeX.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Exact bounded Table headers, rows, and warnings are stored in the result document and restored directly without engine recomputation. `HistoryReplaySnapshotV1` remains a separate launch-time settings and `Ans` record.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Invalid or oversized structured documents fall back whole to untruncated legacy fields with `unavailable`, `invalid`, or `over-size`. New malformed structured appends are rejected, while persistence loading remains extension-preserving for forward compatibility.

## 2026-07-12 - DISPLAY-CONTRACT-INVERSION-RATCHET1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Display inversion debt is governed by a TypeScript-aware source inventory. Authored compatibility outcomes, native canonical adapter calls, canonical projection exits, forwarders, control-only outcomes, and canonical/legacy/control/transient reads remain separate evidence classes.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Accepted compatibility-producer and legacy-read counts may only decrease by lane, while native-document coverage may only stay level or rise. Fingerprint or topology changes require an explicit accepted baseline update with a durable reason; dynamic and rest reads fail closed.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Curated golden expected outcomes are reference fixtures, not live producers. The golden execution adapter remains inventory evidence, and structured History's canonical-to-Display call is native read-model coverage rather than computational producer coverage.

## 2026-07-12 - EQUATION-SOLVE-RESULT-CONTRACT1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Equation solver evidence uses a dedicated version-1 internal carrier over `CanonicalResultDocumentV1`; candidate validation, branch evidence, badges, diagnostics, analysis evidence, and controlled-stop identity are solver truth rather than Display authority.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] The carrier core cannot depend on `DisplayOutcome`. One explicit compatibility adapter may project current outputs without reparsing LaTeX until `EQUATION-OUTCOME-BOUNDARY1`; prompts remain control outcomes and inherited errors are labeled as compatibility-boundary stops.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Equation carrier data is strict JSON-compatible structured-clone-safe data bounded at 20,000 nodes, depth 64, and 1,280,000 UTF-8 bytes. Mirrored canonical evidence and controlled-stop messages must agree exactly; invalid data fails whole rather than being truncated.

## 2026-07-12 - EQUATION-OUTCOME-BOUNDARY1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Equation worker and fallback hosts exchange a validated `EquationResultOutcomeBoundaryV1`, not `DisplayOutcome`. One outer adapter projects canonical truth for the current Display/provenance compatibility surface; native computational producers remain a later migration.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Worker completion is a result-only channel. The client revalidates the returned carrier, rejects malformed or cancellation-shaped completed messages without post-start fallback, and keeps hard-stop cancellation in OOE/runtime control rather than mathematical result truth.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Runtime advisories remain explicit transient boundary policy and are not persisted in the canonical document. Prompts and executable result actions fail closed until they have separate control contracts.

## 2026-07-12 - RESULT-DOCUMENT-CALCULATE-EQUATION-CORE1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Native canonical result documents are built from typed producer-owned fields and proven MathJSON only. The migration may not parse authored LaTeX, infer detail intent, or treat a normalized input tree as result truth.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] A native Equation document is retained only when the storage resolver proves exact parity with the final compatibility projection. Any later target rewrite, wrapper enrichment, or legacy-field mutation removes stale native truth and remains compatibility-owned until that producer lane migrates.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Strict worker transport compacts optional runtime-advisory fields before validation. Canonical-to-Display projection restores uniform explicit row intent as `lineKind` or `lineKinds`, reserving `lineParts` for genuinely mixed rows so presentation components keep their established behavior.

## 2026-07-12 - RESULT-DOCUMENT-EQUATION-ADVANCED1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Every non-prompt Equation producer now builds its canonical document through the Equation-owned typed adapter. At the completed producer boundary, missing or stale native truth is a contract violation rather than permission to fall back to compatibility projection.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] OOE cancellation remains a control-only outcome and is not promoted into canonical mathematical truth. The inversion registry classifies that exact boundary explicitly instead of broadening a control exemption.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Cross-workspace canonical authority stays with the producer owner. Trigonometry removes Equation's document after changing title, actions, planner presentation, and route identity; its result remains compatibility-owned until the approved guided-domain migration.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Zero Equation compatibility producers proves producer migration only. Equation's 265 legacy consumer reads remain measured debt for the later read-model and consumer-inversion milestones and may not be described as already inverted.

## 2026-07-12 - RESULT-DOCUMENT-SYMBOLIC-LIMITS1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Proof-aware Limit rules remain domain-owned typed evidence and do not import Display contracts. Native canonical truth is assembled once by the Calculus owner at the final workspace boundary after stored-value enrichment.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] The `limit`, `finiteLimit`, and `infiniteLimit` screens migrate together because they share the same final owner boundary; unrelated Calculus families remain compatibility-owned until their named milestones and may not be counted as migrated.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Math-bearing controlled Limit errors carry live canonical documents, but History remains success-only. Absence of a controlled-stop History row is required behavior, not lost structured persistence.

## 2026-07-12 - RESULT-DOCUMENT-SYMBOLIC-INTEGRATION1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Symbolic Integration, transform, and partial-derivative engines remain typed mathematical districts without `DisplayOutcome` or result-document dependencies. Their Calculus owner assembles canonical truth once at the final workspace boundary.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] The completed native Calculus screen set is explicit: three Limit screens, three integral screens, Laplace transform, and partial derivative. Derivatives, implicit derivatives, series, ODE, and IVP remain deferred until `RESULT-DOCUMENT-CALCULUS1`.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Static native call counts measure architectural boundaries, not runtime-family breadth. Expanding the same final adapter is ratcheted through exact screen policy and executable golden/replay coverage rather than duplicate wrapper calls.

## 2026-07-12 - RESULT-DOCUMENT-CALCULUS1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Calculus canonical ownership is complete across its 16 result screens. Seven home and navigation screens remain control-only and must not acquire result documents merely because they share the workspace engine.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Native Calculus breadth is proven by exact screen policy, both golden executions, and all 25 sanitized Calculus replay fixtures. The unchanged two-call static owner boundary is intentional and must not be inflated with route-local wrappers.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Current route-visible title overrides and ODE presentation wording remain compatibility behavior during producer migration. Read-model inversion must reconcile canonical titles with visible card identity before it removes those overrides.

## 2026-07-12 - RESULT-DOCUMENT-GUIDED-DOMAINS1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Trigonometry, Geometry, and Statistics own canonical truth only after their route-specific presentation changes are complete. Their leaf math cores remain independent of Display and result-document contracts.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Table canonical truth includes its completed bounded structured response. Exact headers and rows are stored directly; the legitimate complete cell value `undefined` is data, not malformed Display math.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Prompts and Table cancellation remain control-only. Executable Geometry transfer actions remain transient and are excluded from stored canonical documents.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Matrix and Vector remain outside this gate so their independent workers, hosts, capability IDs, replay seeds, and failure policy can be verified in their dedicated Linear Algebra milestone.

## 2026-07-12 - NOTEBOOK-NESTED-SECTION-HIERARCHY1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Notebook document version 3 owns recursive visible `section` nodes with stable ids, editable titles, document-persisted collapse state, and arbitrary child blocks. Tiptap remains an internal editor projection rather than the package contract.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] User-facing section nesting has no fixed depth limit. Visual indentation stops increasing after four levels, while complete ancestry remains available through outline paths and tooltips.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Deleting a section defaults to removing only its wrapper and retaining its contents. Recursive deletion requires explicit confirmation, and structural moves reject cycles.

## 2026-07-12 - NOTEBOOK-TRANSIENT-LAYER-DISMISSAL1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Transient-layer coordination is Notebook-local. Root menus are mutually exclusive, parent-child layers form a stack, and one non-repeated Escape closes exactly the topmost layer with trigger-focus restoration.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Holding Escape is suppressed until keyup so it cannot cascade through multiple Notebook layers or into app-tab behavior. Permanent desktop outline and inspector panes are not transient layers unless opened as narrow-layout drawers.

## 2026-07-12 - NOTEBOOK-SELECTION-FORMATTING-PALETTES1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Notebook prose formatting is selection-scoped: Bold, Italic, Highlight, and Text Color restore the exact saved prose range, while embedded math nodes do not inherit prose marks unless separately selected through their own math controls.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Text Color and Highlight remain separate palette modes. Curated and custom colors are allowed, low contrast warns without blocking, and one Escape closes the palette before the contextual toolbar without clearing the document selection.

## 2026-07-12 - NOTEBOOK-MATH-AUTHORING-SURFACE1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Notebook owns one draggable Math Authoring surface per active field. Compact field tools, expanded symbols, and the matrix picker form one transient chain; one Escape closes one level and leaves the active math field selected.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] User-facing keys are symbol-first with accessible labels, while MathLive insertion recipes remain internal. MathLive's native menu is suppressed only for Notebook-owned fields; calculator MathLive behavior remains unchanged.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Notebook matrix authoring uses a keyboard-accessible 1 by 1 through 8 by 8 picker with square brackets by default. Floating position is per-Notebook-tab session UI state and remains outside document and package contracts.

## 2026-07-13 - NOTEBOOK-WORKBENCH-RESIZE-POLISH1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] In-text Notebook math is a seamless authoring element, not a selected object with boxed chrome; Separate Equation is the explicit framed math object. The Notebook toolbar activates the keyboard only after the requested math node is confirmed in the document.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Notebook prose is an explicit local input boundary for calculator keyboard routing, and possible-math recognition is review-only after an intentional selection. It must never interrupt normal authoring or require accepting a suggestion to keep typing.

## 2026-07-12 - RESULT-DOCUMENT-PRODUCER-CLOSEOUT1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] A final workspace success is legal only when its canonical document validates and exactly matches the typed compatibility projection. Prompts and OOE cancellation controls remain outside this success-only invariant; Equation keeps its stronger non-prompt invariant.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Pre-final `DisplayOutcome` assembly is measured as `owner-assembly`, not compatibility debt, only through exact owner/function registrations with durable rationale. Completed Table authority must be checked with its companion `TableResponse` because bounded row truth is intentionally not duplicated onto `DisplayOutcome`.

## 2026-07-12 - DISPLAY-READ-MODEL-INVERSION1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Display presentation policy must resolve through one canonical-derived read model before building blocks, trust summaries, render schedules, or Formula Viewer artifacts. Native canonical truth wins over contradictory compatibility fields.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Typed legacy outcomes may project once without LaTeX reparsing. Only old History may preserve raw undeclared detail sections when that is the projection's sole validation failure; live producers remain forbidden from relying on inference.

## 2026-07-12 - CANONICAL-RESULT-CONSUMER-INVERSION1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Canonical result consumers use one neutral resolver: a present native document must validate and is authoritative; invalid native truth fails closed, and typed compatibility projection is permitted only when the native document is absent.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Prompt/control fields, transient actions, runtime commit policy, and required dual-written legacy History fields remain outside canonical consumer authority. Surface Protocol keeps its compact DTO unchanged and allows only its exact DTO mapper to cross the result-consumer seam.

## 2026-07-12 - DISPLAY-CONTRACT-INVERSION1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Every final workspace success and every math-bearing controlled error must carry a validated native canonical result whose typed compatibility projection matches. Prompts and registered OOE cancellation/stopped-job outcomes remain control flow and do not acquire result truth.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Producer adapters derive compatibility values from the canonical document. Compatibility policy may preserve optional property presence, route-proven `canonicalMath` presence, executable actions, and runtime advisories, but those inputs cannot override canonical mathematical values.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] When typed detail parts and a dormant compatibility `lines` string disagree, typed parts define the live and canonical rendering. Derived compatibility lines follow those parts; old History remains unchanged and visible wording must not be altered merely to preserve a stale dormant string.

## 2026-07-12 - HISTORY-DISPLAY-CONTRACT-CLOSEOUT0

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] The Structured History and Display Contract Inversion roadmap is closed with canonical documents authoritative and legacy compatibility fields still present as derived projections. Physical compatibility-field removal requires a separate inventory and approval.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] A concurrent Notebook-only browser timeout does not reopen the result-contract program: all program-owned browser, runtime, persistence, replay, static, and visual evidence passes, while the dirty Notebook source and test remain owned by their separate lane.

## 2026-07-12 - CANONICAL-MATH-AUTHORITY-POLICY1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] New result producers must create validated canonical documents through their workspace-owned adapter, and new semantic consumers must use the canonical resolver or an established canonical-derived read model. New string-only producers, semantic reparsing of rendered or legacy LaTeX, and growth in registered compatibility debt are prohibited.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Optional answer MathJSON is evidence-bearing producer truth. It may be attached only when route-specific tests prove answer ownership, clone/bound compliance, and canonical-value parity; normalized input trees and reparsed formatted output are not acceptable substitutes. When proof is absent, canonical LaTeX remains authoritative without invented MathJSON.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Existing solver-native representations remain legitimate. A universal solver AST or shared interchange IR is not an incidental refactor and requires a dedicated approved architecture milestone with semantic-loss, assumptions, branches, proof evidence, conversion, performance, and domain migration gates.

## 2026-07-12 - CANONICAL-MATHJSON-LEGACY-ROADMAP0

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Comprehensive MathJSON coverage uses standard Compute Engine MathJSON inside the existing `CanonicalResultDocumentV1`; domain-native solver IRs remain independent and convert only at workspace-owned final adapters.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Every representable canonical math leaf requires producer-owned proof. LaTeX-only exemptions are bounded, executable, owner-specific, and non-growing; normalized input and reparsed output are prohibited substitutes.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Old and malformed V1 History rows will be removed with notice, while future-version rows remain opaque and survive V1 rewrites. The final History fallback preserves the complete canonical structure and LaTeX while stripping optional MathJSON only when storage bounds require it.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Physical compatibility removal targets duplicate live result fields, Display projection/resolver scaffolding, legacy detail inference, and legacy History result fields. Canonical LaTeX, input syntax, replay seeds, native solver IRs, printer profiles, and OOE/runtime ownership are not legacy.

## 2026-07-12 - MATHJSON-COVERAGE-REGISTRY1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] MathJSON coverage is governed by exact workspace operation families and canonical leaf paths, with all 100 sanitized replay fixtures serving as native executable probes. Golden evidence remains an additional closeout layer rather than the sole coverage source.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Coverage reports and ratchets are audit-only modules imported directly by tools and tests. They must not be exported from production result-contract facades or pull replay fixtures and solver engines into the app bundle.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] The initial debt floor is 236 missing producer proofs and zero exemptions across 262 observed canonical leaves. Missing debt and exemptions may not grow, and accepted payload growth requires an explicit reason.

## 2026-07-13 - PROVEN-ANSWER-MATHJSON-CONTRACT1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] A proven answer MathJSON value begins with an explicit producer-owned candidate carrying workspace, route, source, and canonical LaTeX identity. Workspace adapters may consume only the branded proof result, not an unverified structured value.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Validation may parse canonical LaTeX solely to compare semantic identity and printer parity. It must return the original bounded, clone-safe, standard Compute Engine producer tree and may never promote the parsed validation tree into result authority.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Private `Calcwiz*` and `Rezanova*` operator heads are rejected at the canonical MathJSON boundary. Domain-native IR remains legal behind workspace-owned final conversion and no universal solver AST is introduced.

## 2026-07-13 - CANONICAL-PRODUCER-MATH-VALUE1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Workspace owner adapters accept a deep canonical math-value shape alongside compatibility input. Every supplied `mathJson` is compile-time constrained to `ProvenAnswerMathJson`; raw structured values cannot enter this lane without passing the producer proof contract.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Direct canonical values are enrichment-only during compatibility migration. After removing optional MathJSON, primary values, rows, branches, systems, periodic families, supplements, details, summaries, substitutions, and Table structure must exactly equal the existing typed projection or the producer fails closed.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Native authority may contain proven MathJSON where the legacy projection has no tree. If compatibility already carries MathJSON for that leaf, exact tree equality remains mandatory; stale canonical strings and non-math structure are never tolerated.

## 2026-07-13 - EQUATION-STAGE-CARRIER-CLOSEOUT1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Equation guarded recursion, stage descriptors, cancellation checkpoints, and orchestration transport validated `EquationSolveResultContractV1` carriers. Public guarded-solve APIs derive their compatibility Display read model only at `guarded/run.ts`.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] The isolated direct-symbolic worker payload remains unchanged until `CANONICAL-RUNTIME-OUTCOME1`; its result is converted immediately on entry to the guarded stage corridor. This preserves worker topology and avoids preempting the approved runtime-envelope contract.

## 2026-07-13 - CANONICAL-RUNTIME-OUTCOME1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] The final runtime result union carries only `kind`, a validated `CanonicalResultDocumentV1`, optional math-valued `CanonicalRuntimeActionV1` entries, and optional runtime advisories. Prompt outcomes retain their current control fields and `carryLatex`.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Runtime action payloads use `CanonicalMathValueV1`; free-standing result LaTeX is rejected. The runtime validator requires document/outcome-kind parity, declared fields, standard bounded MathJSON, plain clone-safe data, at most 64 actions, 11,024 nodes, depth 65, and 704,000 serialized bytes.

## 2026-07-13 - CANONICAL-OUTCOME-CALCULATE-EQUATION1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Calculate and Equation worker and OOE result payloads use `CanonicalRuntimeOutcome`; Display is a derived compatibility read model only at the existing public mode boundary.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Equation analysis evidence remains private host/provenance metadata beside the canonical result rather than widening `CanonicalResultDocumentV1` with runtime diagnostics.

## 2026-07-13 - CANONICAL-OUTCOME-SYMBOLIC-CALCULUS1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Symbolic calculus does not gain a second runtime shell: all current derivative, integral, limit, series, transform, partial, ODE, and IVP routes retain their native IR and converge through the existing Calculus workspace-owned final adapter and `calculus-worker-runtime`.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Canonical-to-Display projection always restores canonical LaTeX as `exactLatex`. The optional compatibility `canonicalMath` payload remains conditional on producer-proven MathJSON; suppressing an unproven tree must never suppress canonical text.

## 2026-07-13 - MATHJSON-COVERAGE-CALCULATE-EQUATION1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Optional answer MathJSON is transactional enrichment: if a producer tree fails semantic, bounds, clone, or printer proof, omit that tree and retain unchanged canonical LaTeX. Do not turn an optional proof failure into a runtime failure and do not reconstruct the tree from formatted output.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Standard complex MathJSON and established visible complex notation are separate contracts. The producer may transport standard imaginary-unit structure while the Equation presentation adapter preserves route-established `i` or `\\imaginaryI` readback.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] A compatibility supplement that combines prose labels and mathematical notation remains LaTeX-only unless the producer supplies a structurally separate math leaf. Its exemption must stay fixture-scoped and non-growing; parsing the mixed display string is prohibited.

## 2026-07-13 - MATHJSON-COVERAGE-SYMBOLIC-CALCULUS1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Calculus MathJSON authority comes from native route evidence retained by each evaluator and converted only in the workspace-owned final adapter. The adapter may overlay proven trees on typed compatibility structure, but it may not reparse answer or detail LaTeX.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Existing mixed operator/applied labels and malformed derivative-at-point compatibility strings remain exact, fixture-scoped exemptions until their producer representation is corrected by a separately approved output-change milestone. Optional absence is safer than invented structure.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Replay and print-hygiene fixtures treat `canonicalMath.canonicalLatex` presence as proof-coverage evidence. Adding or removing that duplicate marker is acceptable only when canonical LaTeX, identity, cardinality, visible formatting, and mathematical content remain unchanged and the baseline reason records the coverage milestone.
## 2026-07-13 - NOTEBOOK-TYPOGRAPHY-ANNOTATION1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Notebook authoring typography is versioned in the app-owned document contract: prose supports exact `50–249%` font sizing and strikethrough, while selected MathLive terms use the nearest one of ten visible native size levels. Math size changes are local to the selected term, not a global document scale.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Notebook mathematical cancellation is an authoring-only annotation. Diagonal, reverse-diagonal, and cross variants require an explicit selected term and must block `Open in Tool` until a future calculator-owned capability explicitly supports their semantics.

## 2026-07-13 - MATHJSON-COVERAGE-GUIDED-DOMAINS1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Guided-domain MathJSON is derived from workspace-owned native evidence only at the final Trigonometry, Geometry, Statistics, or Table adapter. Shared Equation answer trees may cross into Trigonometry as explicit proven evidence; formatted output and normalized input remain prohibited sources.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] A sampled Table cell whose exact result is `undefined` is absence of a real mathematical value, and a retained guided request string is control syntax rather than result truth. These cases remain fixture-scoped, non-growing exemptions instead of receiving fabricated MathJSON.

## 2026-07-13 - MATHJSON-COVERAGE-LINEAR-ALGEBRA1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Matrix and Vector produce standard MathJSON independently from their own native exact evidence at their final adapters. Their workers, hosts, capabilities, requests, fallbacks, replay seeds, and solver cores remain separate even when they use shared proof constructors.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Augmented-rank, augmented-RREF, and row-operation narration that is not valid standalone mathematical syntax remains an exact route-scoped exemption. The producer must prove separately structured solution, rank, and count values rather than parse or invent a tree for the narration.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] The exhaustive 100-probe coverage test has a 120-second test-local timeout because full-suite contention can exceed 30 seconds. This does not change payload, structured-clone, semantic, printer-parity, or missing-proof ratchets.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] A Vector GRAD result remains canonical-LaTeX-only until a standard gradian-unit MathJSON representation can preserve semantics and canonical printer parity. The visible superscript `g` must not be encoded as a mathematical exponent merely to increase coverage.

## 2026-07-13 - RESOURCE-SAFE-VERIFICATION-POLICY1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Verification follows impact rather than repetition: ordinary repairs run affected tests and relevant ratchets, milestone commits add incremental TypeScript and cheap repository gates, and complete suites are reserved for closeout, release, or genuine cross-cutting invalidation.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Full Vitest runs use no more than four workers, run without another heavy gate concurrently, and are announced before launch. Both standard and UI Vitest configurations plus their package scripts carry the cap.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] A bounded correction after successful broad evidence requires targeted delta verification, not an automatic full rerun, unless it changes shared runtime contracts, worker topology, global test infrastructure, or similarly broad behavior. Lost reporter output must be investigated before any rerun.

## 2026-07-13 - MATHJSON-COVERAGE-CLOSEOUT1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] The MathJSON coverage authority is the exact union of native replay and golden evidence, not either corpus alone. Every replay fixture and golden case maps to one workspace-owned operation family, and additions must fail until registered.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Golden-only compound presentation and custom-label fragments may remain canonical-LaTeX-only only through exact fixture-and-leaf exemptions whose separately structured mathematical values are proven. The exemption list is non-growing and cannot become a broad route bypass.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Aggregate coverage closes with zero missing classifications, but optional MathJSON remains enrichment rather than a license to invent trees. Future transport removal must preserve workspace-native provenance and the same proof boundary.

## 2026-07-13 - EQUATION-STAGE-CARRIER-GUARDED1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Guarded Equation recursion carries the existing validated `EquationSolveResultContractV1`; it must not introduce a second Equation IR. Display projections are temporary read models at current stage exits and are removed by the following closeout gate.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] The Display inversion inventory treats registered canonical carrier-returning calls as native producer evidence. Compatibility floors cannot be preserved by hiding carrier authority from the AST scanner, and prompts remain outside result carriers.

## 2026-07-13 - CANONICAL-OUTCOME-GUIDED-DOMAINS1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Guided-domain parsed requests, replay screens, and replay seeds remain sibling host transport. They support replay and OOE provenance but are not mathematical result truth and do not enter `CanonicalResultDocumentV1`.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Trigonometry, Geometry, and Statistics derive their established public Display payload from the canonical runtime outcome with `canonicalMath` compatibility duplication disabled. Proven MathJSON remains authoritative inside the canonical document; this preserves the existing structured-clone-visible public shape until direct consumer inversion.

## 2026-07-13 - CANONICAL-OUTCOME-LINEAR-ALGEBRA-TABLE1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Matrix and Vector share only a payload-generic worker lifecycle helper. Their worker entrypoints, clients, hosts, fallback hosts, capabilities, shell identities, request shapes, cancellation, diagnostics, and replay ownership remain independent.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Table keeps its established cooperative fallback after worker startup, including malformed canonical completions. Matrix and Vector continue to fail closed after startup. Canonical transport does not erase workspace-specific failure containment.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Table response evidence remains sibling runtime transport until direct consumer inversion; bounded canonical Table headers and rows remain the result authority. Cancellation stores an empty canonical Table structure and never partial rows.

## 2026-07-13 - CANONICAL-CONSUMER-DIRECT1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] `resolveCanonicalResultForConsumer` is strict: semantic consumers require a validated native canonical document and never project result strings. Missing or malformed authority fails closed.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Compatibility projection is isolated behind `resolveLegacyCanonicalResultForConsumer` and used only by the registered old-History display boundary. `HISTORY-CANONICAL-ONLY1` removes that boundary rather than widening it.

## 2026-07-13 - HISTORY-CANONICAL-ONLY1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Current V1 History authority is one validated `CanonicalResultDocumentV1`; legacy result fields and reconstruction from result strings are prohibited. Old and malformed V1 rows are removed with a non-blocking count notice rather than guessed forward.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Future result-document versions are hidden but preserved byte-for-byte. V1 display, clear, and 80-row retention operations cannot erase opaque future rows.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] A History append over budget may retry after removing only optional MathJSON from the otherwise identical canonical document and recording `canonical-only-fallback`; mathematical structure and canonical LaTeX are never truncated.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Live Display detail intent is canonical and explicit: canonical typed parts outrank all presentation aliases, and production code may not infer math from strings or carry a parallel solve-summary string. Legacy fixture bridges are test-only and must disappear with result compatibility removal.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Producer MathJSON proof must represent literal Matrix operands with the owned exact matrix node. Composite display labels that lack producer-owned expression structure remain canonical-LaTeX-only; a Compute Engine comparison exception fails closed rather than breaking runtime execution.

## 2026-07-14 - RESULT-COMPATIBILITY-REMOVAL1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Runtime results and semantic consumers are canonical-document-only. `DisplayOutcome`, `DisplayMathPayloadV1`, reverse/runtime projections, storage projections, and legacy result resolvers are forbidden symbols; domain-local producer drafts may remain only as workspace-owned final-adapter inputs.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Approximation-only successful results copy the canonical document's visible approximation when no exact primary math exists. Exact results remain canonical-primary-first, and malformed or string-only payloads still fail closed.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] The file-size ratchet uses a 1,000-line default for production TypeScript and a 1,500-line default for `*.test.*`, `*.spec.*`, and `__tests__` files. Existing baseline entries above the applicable default remain exact caps and continue to ratchet downward.

## 2026-07-14 - NOTEBOOK-SMART-INSPECTOR1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Notebook inspector visibility is session-only and contextual. `auto` opens the full inspector for math, sections, and academic containers, while ordinary prose or no active block yields that space to the canvas; `pinned` preserves the inspector across selection changes, and `collapsed` keeps it closed until restored or a different relevant item is selected.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] The Notebook Outline never auto-hides. Both desktop rails release their reserved grid width when manually collapsed, and narrow-screen Outline and Inspector drawers remain mutually exclusive so the writing canvas stays dominant.

## 2026-07-14 - NOTEBOOK-PARAGRAPH-TOOLS1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Notebook rich documents use app-owned version 5 for underline, bounded paragraph/heading alignment and spacing, and typed persisted bullet/ordered-list styles. Missing attributes retain version-4 appearance; invalid or cross-kind combinations fail validation, and Tiptap remains an internal projection.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Notebook paragraph formatting is selection-preserving and prose-scoped. Paragraphs and headings may be formatted wherever nested, but section and academic-container shells, math, evidence, and dividers never receive paragraph formatting. List split controls persist only styles valid for their list kind.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Notebook media support begins only as a separately planned document-version milestone. Version 5 adds no media nodes, uploads, assets, wrapping, crop, rotation, safety surface, or final white-document visual convergence.

## 2026-07-14 - CANONICAL-MATHJSON-LEGACY-CLOSEOUT0

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Canonical result documents are the sole live result and semantic-consumer authority. The program closes with zero compatibility projections, zero legacy result reads, and no permission to reintroduce string-only result paths.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Producer-proven standard MathJSON coverage closes at 394 of 458 canonical leaves with 64 exact bounded exemptions and zero missing classifications. Optional absence remains safer than inventing an answer tree, and domain solvers retain their native IRs.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Closeout verification follows the resource-safe policy: one broad run establishes repository evidence, and bounded fixture or timeout corrections use targeted delta tests unless they alter a shared contract or topology.

## 2026-07-14 - NOTEBOOK-RIBBON-RAIL-FIXES1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Notebook desktop Outline and Inspector rails are independent and side-stable. Closing Outline removes its layout, resizer, and accessibility exposure without changing Inspector state; restoring Outline always returns it to the left, while Inspector remains right-owned.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Notebook paragraph style is one selection-preserving Normal/Heading 1/Heading 2/Heading 3 control with a Mixed state for heterogeneous selections. Normal converts selected headings back to paragraphs; heading choices preserve semantic levels and Outline depth. Sections and Academic Containers remain distinct structural controls.

## 2026-07-14 - V1-TYPED-DETAIL-CALCULUS1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Mathematical detail labels use typed prose around producer-owned inline math. Coverage may improve only from native values that pass the established proof contract; rendered LaTeX remains presentation and cannot be reparsed to manufacture MathJSON.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] V1-compatible typed-detail cleanup changes only newly computed result documents. Existing persisted History documents are immutable, and the program's final exemption floor remains exactly 23 unless a native value fails the approved proof boundary with recorded evidence.

## 2026-07-14 - V1-TYPED-DETAIL-TRIGONOMETRY1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Period and Phase carrier classification is human prose, not mathematical structure. Its numerical and exact symbolic quantities retain separate producer-owned MathJSON leaves, while the compound primary remains the one honest V1 exemption.

## 2026-07-14 - V1-TYPED-DETAIL-MATRIX-SYSTEM1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Matrix rank, unknown-count, contradiction, and augmented-RREF facts are typed prose around producer-owned native values. Row-operation arrows remain the four honest LaTeX-only exemptions because they describe transformations rather than standalone answer trees.

## 2026-07-14 - NOTEBOOK-AUTHORING-AFFORDANCE-FIXES1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Notebook list and paragraph-style choices expose familiar visual meaning without changing document semantics: list menus show marker sequences, and the paragraph-style menu uses typographic hierarchy plus plain-language roles for Normal and Heading 1 through Heading 3.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Notebook inspection targeting is independent from the editing caret. The nearest enclosing structured block is inspected, direct math selection wins, and manual Inspector restore stays visible with an instructional empty state instead of silently refusing to open.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Notebook onboarding uses a structural pristine rule: both prompts appear only for exactly one empty, unformatted paragraph and disappear after any structural insertion.

## 2026-07-14 - NOTEBOOK-STRUCTURED-BLOCK-INSPECTOR1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Notebook rich documents use app-owned version 6 for academic-container and Section accent, explicit collapsibility, and compatible persisted current-collapse state. Automatic means no stored accent or collapsibility override; strict validation rejects malformed colors and impossible collapsed states.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Automatic collapsibility is enabled for Hint and Answer academic containers and for all Sections. Explicit overrides survive academic-container type changes, but disabling collapsibility or changing to an incompatible effective state clears the current collapsed flag.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Structured-block appearance is Inspector-owned: Automatic, six presets, and allowed custom lowercase colors affect only the left rail/border, icon emphasis, and subtle header tint. Below-3:1 choices warn but remain valid, body surfaces stay neutral, and forced-colors mode uses system colors. The header chevron alone changes persisted collapsed state.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] The completed pre-media gates do not add media or authorize white-document convergence. Media begins only as a separately planned document-version milestone.

## 2026-07-14 - NOTEBOOK-LARGE-DOCUMENT-READINESS1

- [agent: codex | model: gpt-5.5 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.5 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.5 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.5 | verified_by_agent_family: sol | attribution_basis: live] Notebook block count is diagnostic, not a product cap. The author-facing header reports word count; normal live authoring is committed at 5,000 text-heavy blocks including 2,000 inline-math nodes, while 50,000 blocks is a safe validation/import target without an equal-latency editing promise.
- [agent: codex | model: gpt-5.5 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.5 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.5 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.5 | verified_by_agent_family: sol | attribution_basis: live] Large Notebooks retain one Tiptap editor and one selection/undo model. They use the continuous Draft view, 350 ms settled app-document synchronization with destruction flush, off-render metrics and validation, and viewport-aware MathLive hydration rather than truncation or per-page editors.

## 2026-07-14 - NOTEBOOK-PERSISTENCE-FOUNDATION1

- [agent: codex | model: gpt-5.5 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.5 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.5 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.5 | verified_by_agent_family: sol | attribution_basis: live] Notebook library identity and save revision belong to `NotebookStoredRecordV1`, not the app-owned rich-document contract or Workspace Tabs. The V6 document remains the lossless editor/package payload nested inside the record.
- [agent: codex | model: gpt-5.5 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.5 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.5 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.5 | verified_by_agent_family: sol | attribution_basis: live] `.cwiznb` is the sole lossless Notebook package. Desktop packages use a manifest, the app document, SHA-256 content-addressed assets, and stored ZIP64 entries; import validates the complete package before mutation and always creates a new library identity rather than a linked file.
- [agent: codex | model: gpt-5.5 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.5 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.5 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.5 | verified_by_agent_family: sol | attribution_basis: live] Desktop Rust owns atomic records, recovery metadata, asset validation, hashing, and package validation. Browser preview uses IndexedDB with the same library revision and content-addressed asset semantics; UI autosave and library ownership begin only in `NOTEBOOK-DOCUMENT-LIBRARY1`.

## 2026-07-14 - NOTEBOOK-DOCUMENT-LIBRARY1

- [agent: codex | model: gpt-5.5 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.5 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.5 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.5 | verified_by_agent_family: sol | attribution_basis: live] Notebook documents are durable library identities rather than tab-owned session payloads. New/template creation stores immediately, editing autosaves after 750 ms, `Ctrl/Cmd+S` saves immediately, and tab close never deletes the library document.
- [agent: codex | model: gpt-5.5 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.5 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.5 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.5 | verified_by_agent_family: sol | attribution_basis: live] Workspace Tabs retain only Notebook library identity, revision, title, and view state. At most one asset-free record below 16 MiB may remain warm; large or media-bearing documents evict, and opening an already-open identity focuses its existing tab.
- [agent: codex | model: gpt-5.5 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.5 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.5 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.5 | verified_by_agent_family: sol | attribution_basis: live] Local version history retains at most 50 snapshots or 30 days with periodic snapshots no more often than every five minutes and explicit snapshots before restore or Trash. Save failure keeps the editor resident and offers Retry, current-memory recovery export, or Close without saving.

## 2026-07-14 - V1-TYPED-DETAIL-LINEAR-ALGEBRA-PROFILES1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Linear Algebra profile facts use mixed typed detail parts: labels and classifications are prose, while scalar, set, vector, and matrix values are producer-owned math. Compound Matrix/Vector primaries and Matrix map rows remain honest V1 exemptions instead of being reparsed or represented through custom MathJSON heads.

## 2026-07-14 - V1-TYPED-DETAIL-CLOSEOUT0

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] CanonicalResultDocumentV1 typed-detail cleanup closes at 455 leaves, 432 proven standard MathJSON trees, 23 bounded exemptions, and zero missing classifications. The 20 remaining registry rules are honest contract-shape or compatibility boundaries; V1 will not be widened to erase them through reparsing, custom MathJSON heads, or false structure.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Any CanonicalResultDocumentV2 work requires a separately approved audit and versioned migration plan. V1 History remains readable and immutable, and one resolver must preserve both versions during any future transition.

## 2026-07-14 - NOTEBOOK-RIBBON-TABS1

- [agent: codex | model: gpt-5.5 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.5 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.5 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.5 | verified_by_agent_family: sol | attribution_basis: live] Notebook ribbon taxonomy is File backstage, Home for Font/Paragraph/Styles/Edit, and Insert for Structure/Math/Media/Document. The app-level Workspace Tab strip remains separate and unchanged.
- [agent: codex | model: gpt-5.5 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.5 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.5 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.5 | verified_by_agent_family: sol | attribution_basis: live] Layout is not shown until page controls are functional. Picture Format and Video Format are contextual selection surfaces and do not appear before their matching media node exists; Image and Video entry points remain visibly disabled until those gates.
- [agent: codex | model: gpt-5.5 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.5 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.5 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.5 | verified_by_agent_family: sol | attribution_basis: live] Ribbon switching preserves the Tiptap selection and dismisses excluded transient layers through the Notebook coordinator. Evidence and Divider are document-owned Insert commands; they do not widen result projection or History ownership.

## 2026-07-14 - CANONICAL-RESULT-V2-AUDIT0

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] V2 is a clean strict result document rather than V1 plus sidecars. True V2 math requires producer-proven standard MathJSON; compound primaries, guided requests, labeled supplements, undefined Table cells, and Matrix row operations receive typed structures with adapter-owned presentation beside semantics.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] V1 and V2 coexist behind one normalized read authority. Stored V1 is immutable, V2 is a current visible History version, future versions remain opaque, and a selected V2 producer may not silently fall back to V1. Untouched V1 producers are frozen in a non-growing inventory; new or materially changed producers use V2.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] The 23-leaf migration is residual-driven and preserves workspace-native representations, independent workers/hosts/capabilities/replay, and OOE authority. Existing output stays identical except derivative-at-point `x^2` at `3`, whose primary becomes `6` and whose request presentation becomes the valid evaluated-derivative form while retaining title `Derivative` and existing detail structure.

## 2026-07-14 - CANONICAL-RESULT-V2-CONTRACT1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] V2 producer inputs require a distinct standard-operator MathJSON proof brand for every math leaf, including action math. Serialized V2 documents remain ordinary clone-safe data and are revalidated for required MathJSON, standard Compute Engine operators, strict discriminated shapes, semantic row/profile constraints, and committed size/depth limits.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Runtime V2 actions are explicitly versioned while V1 actions keep their existing shape, making document/action mismatches rejectable without altering live V1 payloads. The inactive producer-version registry freezes the 57 existing route identities on V1; any route outside that frozen inventory defaults to V2, and approved selectors remain empty until their producer gates.

## 2026-07-14 - NOTEBOOK-IMAGE1

- [agent: codex | model: gpt-5.5 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.5 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.5 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.5 | verified_by_agent_family: sol | attribution_basis: live] Notebook images are app-owned V7 block figures backed by content-addressed library assets. PNG, JPEG, static WebP, and safe static SVG are accepted; animated formats, executable or externally referencing SVG, unsupported decodes, and committed safety-budget violations fail before document mutation.
- [agent: codex | model: gpt-5.5 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.5 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.5 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.5 | verified_by_agent_family: sol | attribution_basis: live] Picker, clipboard, and drop insertion use one undoable document transaction after durable ingestion. Accessibility requires alt text, a confirmed warning, or explicit Decorative state; only captioned figures enter Outline. Picture Format is contextual, while page-aware wrapping and full crop/rotation controls remain sequenced after V8 page geometry.

## 2026-07-14 - CANONICAL-RESULT-V2-CONSUMER-HISTORY1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] All semantic result readers consume one normalized authority containing source version, adapter-owned presentation, and version-neutral semantics. V1 normalization copies existing truth without inferring typed requests, supplement roles, or undefined-cell reasons; raw documents remain available only to persistence and diagnostics boundaries.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] V1 and V2 runtime results are live with strict same-version actions. History treats V1 and V2 as current visible versions, preserves versions above 2 opaquely outside retention, keeps canonical-only oversized fallback for V1 only, and rejects oversized V2 writes without stripping required MathJSON.

## 2026-07-14 - NOTEBOOK-PAGE-LAYOUT1

- [agent: codex | model: gpt-5.5 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.5 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.5 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.5 | verified_by_agent_family: sol | attribution_basis: live] Notebook rich documents use app-owned version 8 for canonical point-based page setup, simple document-wide headers and footers, page numbering, and explicit top-level page breaks. Derived physical pages, fragment positions, page counts, and measurement caches are never serialized.
- [agent: codex | model: gpt-5.5 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.5 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.5 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.5 | verified_by_agent_family: sol | attribution_basis: live] Print Layout is the per-workspace-tab default and Draft is the continuous performance view. Both retain one Tiptap editor, one selection, and one undo history; oversized documents force Draft without truncation.
- [agent: codex | model: gpt-5.5 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.5 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.5 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.5 | verified_by_agent_family: sol | attribution_basis: live] V8 page geometry is the single basis for later image wrapping, print projection, and publication adapters. Headings keep with the next eligible block, structured containers may split, and oversized math or media fit proportionally rather than clipping.

## 2026-07-14 - CANONICAL-RESULT-V2-REQUEST-EVIDENCE1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Calculus derivative-at-point, Trigonometry angle conversion, and Trigonometry right triangle are the first active V2 selectors. A selected V2 route requires complete producer-owned standard MathJSON evidence and fails explicitly rather than falling back to V1; sine-rule, cosine-rule, and untouched producers remain frozen on V1.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Guided request presentation remains adapter-owned and byte-faithful to the existing route, but typed semantics contain only native proven values. Right-triangle `?` placeholders are presentation-only; derivative body/path/point/result and angle conversion value/units are explicit semantics.

## 2026-07-14 - NOTEBOOK-IMAGE-LAYOUT1

- [agent: codex | model: gpt-5.5 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.5 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.5 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.5 | verified_by_agent_family: sol | attribution_basis: live] Picture Format edits existing V8 image metadata rather than introducing a new document version. Width, alignment, placement, crop, and rotation remain non-destructive, selection-preserving, and individually undoable.
- [agent: codex | model: gpt-5.5 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.5 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.5 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.5 | verified_by_agent_family: sol | attribution_basis: live] Square image placement is a persisted author preference with a derived effective rendering. V8 page geometry and the live editor content width may fall back to normal flow without rewriting the document whenever wrapping cannot retain a 240px body-text column; pagination follows the resulting computed float state.

## 2026-07-14 - CANONICAL-RESULT-V2-SUPPLEMENT-TABLE1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Equation V2 supplement semantics come only from workspace-native relation evidence and an explicit role; presentation labels remain adapter-owned strings and are never reparsed into mathematical truth. A selected V2 result fails if any labeled supplement lacks matched producer proof.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Table V2 undefined cells use exactly `outside-real-domain` or `pole`, chosen from producer-owned evaluation evidence. Defined cells retain their existing presentation and required MathJSON; no consumer infers undefined reasons from display text.

## 2026-07-14 - CANONICAL-RESULT-V2-TRIGONOMETRY1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Period & Phase compound output stores its existing primary LaTeX only as presentation. Mathematical authority is the producer-owned normalized equation, period, and phase-shift tree; all are required standard MathJSON and incomplete evidence fails without V1 fallback.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Period & Phase keeps its workspace parser and native affine representation. Sine, cosine, tangent, DEG/RAD/GRAD, and supported symbolic offsets share the V2 adapter without introducing a universal AST, custom MathJSON operator, or consumer-side reconstruction.

## 2026-07-14 - CANONICAL-RESULT-V2-LINEAR-ALGEBRA1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Matrix system V2 uses the existing exact RREF trace as authority. Visible row arrows remain presentation, while semantics store one-based swap/scale/eliminate operations and producer-proven exact signed factors; the already-reduced case remains prose.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Matrix profile and Vector independence compound primaries retain existing primary and answer-row presentation beside exact typed semantics. Profile authority is the exact operand plus map dimensions/rank/nullity; independence authority is the exact operand-vector family plus the native Boolean verdict. Semantic consumers never parse labeled presentation rows.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Matrix linear-system/profile routes default to strict V2, Vector independence is the only V2 selector in its shared route, and Vector span remains frozen V1. Selected V2 routes fail on incomplete proof and do not change workers, hosts, capability IDs, replay seeds, runtime shells, or OOE ownership.

## 2026-07-14 - NOTEBOOK-VIDEO1

- [agent: codex | model: gpt-5.5 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.5 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.5 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.5 | verified_by_agent_family: sol | attribution_basis: live] Notebook rich documents use app-owned version 9 for local MP4/WebM block videos with title, description, optional caption and automatic Video numbering, poster and WebVTT asset references, bounded width/alignment, and loop state. Playback state is session-only; remote media, autoplay, GIFs, crop, rotation, wrapping, and trim remain unsupported.
- [agent: codex | model: gpt-5.5 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.5 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.5 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.5 | verified_by_agent_family: sol | attribution_basis: live] Desktop Notebook media delivery uses a Rust-owned loopback HTTP server bound to `127.0.0.1` on an ephemeral port with a randomized capability token and content hash in the URL, GET/HEAD, and bounded single-range streaming. The custom URI alternative is rejected because packaged WebKitGTK could not decode it; no local file path is exposed to the document or webview.

## 2026-07-14 - NOTEBOOK-EXPORT-PROJECTION1

- [agent: codex | model: gpt-5.5 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.5 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.5 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.5 | verified_by_agent_family: sol | attribution_basis: live] PDF, DOCX, and Web publication adapters consume immutable `NotebookPublicationProjectionV1` only. The projection freezes one stored-record revision, app-owned block subtrees, page setup/running matter, derived layout fragments, target-required resolved assets, export scope, and compatibility findings; adapters must not parse Tiptap JSON, editor DOM, rendered calculator output, or live playback state.
- [agent: codex | model: gpt-5.5 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.5 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.5 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.5 | verified_by_agent_family: sol | attribution_basis: live] Notebook export scopes are whole document, exact physical PDF page range, or selected top-level Section subtrees normalized to document order. DOCX and Web are reflowing publications and never claim source physical page numbers; low-priority cancellable export jobs survive workspace switching without mutating the Notebook or History.

## 2026-07-14 - NOTEBOOK-EXPORT-PDF1

- [agent: codex | model: gpt-5.5 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.5 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.5 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.5 | verified_by_agent_family: sol | attribution_basis: live] Notebook PDF publication renders only from the immutable app-owned projection through a dedicated V8-geometry page preview. It never parses Tiptap JSON or editor DOM and never introduces a second PDF layout engine.
- [agent: codex | model: gpt-5.5 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.5 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.5 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.5 | verified_by_agent_family: sol | attribution_basis: live] PDF output is completed through the platform system print/Save as PDF dialog with explicit Tauri print capability. Direct PDF bytes and PDF import remain outside this milestone; video is a static poster/text publication with a Web-package playback note.

## 2026-07-14 - NOTEBOOK-EXPORT-DOCX1

- [agent: codex | model: gpt-5.5 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.5 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.5 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.5 | verified_by_agent_family: sol | attribution_basis: live] `.docx` is the only Word publication format; legacy `.doc` and DOCX import remain unsupported.
- [agent: codex | model: gpt-5.5 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.5 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.5 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.5 | verified_by_agent_family: sol | attribution_basis: live] Word export consumes only frozen `NotebookPublicationProjectionV1`, maps app-owned content to editable OOXML, and never treats editor DOM or Tiptap JSON as publication authority.
- [agent: codex | model: gpt-5.5 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.5 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.5 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.5 | verified_by_agent_family: sol | attribution_basis: live] Equation conversion is bounded and fail-closed. Supported input uses editable OMML with embedded SVG/PNG alternate content; unsupported input uses a reported static visual rather than malformed math.
- [agent: codex | model: gpt-5.5 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.5 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.5 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.5 | verified_by_agent_family: sol | attribution_basis: live] Accepted SVG images retain vector data and include PNG fallbacks. Video remains a static poster/caption/note substitution in DOCX.
- [agent: codex | model: gpt-5.5 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.5 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.5 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.5 | verified_by_agent_family: sol | attribution_basis: live] Microsoft 365 Word compatibility is provisional until its unavailable smoke test is run; LibreOffice 26.2.2.2 rendering is verified.

## 2026-07-14 - CANONICAL-RESULT-V2-CLOSEOUT0

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Canonical Result V2 closes with an accepted 143-case executable corpus containing 452 canonical math leaves, all 452 producer-proven, zero exemptions, and zero missing classifications. The exemption registry remains present and empty as an anti-growth ratchet; future math-bearing V2 leaves must provide standard producer-owned MathJSON rather than revive compatibility classifications.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] V2 is the required contract for new or materially changed producers, while untouched producers may remain on the frozen V1 inventory. Both versions continue through one normalized consumer authority; this closeout does not authorize custom MathJSON heads, a universal solver AST, consumer-side parsing, V1 History rewrites, or worker/host/capability/OOE ownership changes.

## 2026-07-14 - NOTEBOOK-EXPORT-WEB1

- [agent: codex | model: gpt-5.5 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.5 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.5 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.5 | verified_by_agent_family: sol | attribution_basis: live] Notebook Web export is a self-contained offline ZIP publication built only from frozen `NotebookPublicationProjectionV1`. It uses escaped semantic HTML, scoped responsive/print CSS, safe static MathML, and content-hashed local assets; it includes no editor runtime, solver authority, remote dependency, service worker, local path, or executable author content.
- [agent: codex | model: gpt-5.5 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.5 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.5 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.5 | verified_by_agent_family: sol | attribution_basis: live] Local MP4/WebM and WebVTT remain interactive in Web output. PDF, DOCX, and Web stay export-only; `.cwiznb` remains the sole lossless Notebook interchange format.

## 2026-07-14 - NOTEBOOK-TRANSIENT-CHROME-REPAIRS1

- [agent: codex | model: gpt-5.5 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.5 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.5 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.5 | verified_by_agent_family: sol | attribution_basis: live] Notebook transient menus use one small body-portaled fixed-position layer. It preserves coordinator-owned dismissal/selection/focus semantics while keeping menus viewport-contained rather than clipping them inside ribbon or canvas ancestors.
- [agent: codex | model: gpt-5.5 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.5 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.5 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.5 | verified_by_agent_family: sol | attribution_basis: live] Media placement remains snap-and-wrap rather than persisted free X/Y positioning. Status coordinates are contextual layout output only; the direct-manipulation gate will not introduce a general arbitrary-position engine.

## 2026-07-14 - CANONICAL-RESULT-V2-ENFORCEMENT1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] The 57-route V1 producer inventory and its 26 owning source files are frozen by ordered route identity, file path, route ownership, and SHA-256. A frozen file may disappear only after every route it owns defaults to V2; additions, digest rewrites, ownership rewrites, and ordinary exemptions are forbidden.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] New routes default to V2, new or materially changed adapters require producer-proven standard MathJSON, and missing proof fails closed. If V2 cannot express required semantics, work stops for an approved contract version rather than storing semantic authority in presentation strings.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Remote branch protection is a separate post-push activation step. The local gate does not authorize a push; protection must be enabled and read back only after the workflow exists on GitHub and the user separately approves the push.

## 2026-07-15 - NOTEBOOK-DIRECT-MEDIA-AND-INDENT1

- [agent: codex | model: gpt-5.5 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.5 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.5 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.5 | verified_by_agent_family: sol | attribution_basis: live] Direct media remains snap-and-wrap. Drag feedback and Page/Draft coordinates are transient layout output only; no general persisted X/Y positioning system is introduced.
- [agent: codex | model: gpt-5.5 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.5 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.5 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.5 | verified_by_agent_family: sol | attribution_basis: live] V10 direct crop gestures enforce a 10% visible-area floor, but existing V9 crop metadata remains migration-lossless and valid rather than being retroactively rejected.

## 2026-07-15 - LINEAR-ALGEBRA-CANONICAL-V2-COMPLETION1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Every Matrix and Vector operation selector now fails closed through workspace-owned V2 evidence assembled beside the native calculation. Display LaTeX is never reparsed as answer truth; existing public display responses remain display-only, and evidence stays internal and slot-aligned.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] All 12 historical Matrix/Vector route identities default to V2. The only V3 production override is `vector.angle` selector `angle:grad`, whose `100^{g}` presentation carries typed magnitude `100` and unit `grad`; degree and radian angles remain V2.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] The immutable 57-route enforcement inventory remains fixed even after all Matrix/Vector routes leave V1. Ownerless inventory entries are valid only when those routes are explicit V2 defaults; the four obsolete frozen producer-file entries are retired rather than fingerprint-rewritten.

## 2026-07-15 - NOTEBOOK-LIBRARY-FILE-OPERATIONS1

- [agent: codex | model: gpt-5.5 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.5 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.5 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.5 | verified_by_agent_family: sol | attribution_basis: live] Notebook library selection is explicit and bounded: single, toggle, and range selection enable only safe bulk Trash/Restore/Delete actions. Rename, Duplicate, and export remain single-record actions; records open in another tab are protected through the existing workspace-event seam rather than a new app-state ownership model.
- [agent: codex | model: gpt-5.5 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.5 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.5 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.5 | verified_by_agent_family: sol | attribution_basis: live] Desktop publication and recovery writes use an opaque-ID, bounded-chunk temporary-file seam controlled in Rust; the renderer never receives a filesystem path. Browser DOCX/Web uses File System Access when available, otherwise an explicit download fallback. `.cwiznb` stays desktop-only until an equally validated browser package authority is deliberately introduced.

## 2026-07-15 - NOTEBOOK-VIDEO-PLAYBACK-SHELL1

- [agent: codex | model: gpt-5.5 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.5 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.5 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.5 | verified_by_agent_family: sol | attribution_basis: live] Notebook playback remains a session-only NodeView responsibility: one mounted local video element owns ranged delivery, controls, captions, error display, theater, and presentation transitions. Browser fullscreen targets the presentation container; desktop fullscreen uses only the current Tauri window plus the narrow window-fullscreen permission. A request-owned entry lock prevents stale fullscreen completion from clobbering a later transition. No document, persistence, or asset-delivery contract changes are needed.

## 2026-07-15 - LINEAR-ALGEBRA-EXACT-DECIMAL-CONTROLS1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Linear Algebra exact structural results remain the canonical semantic and clipboard authority in every output style. Decimal readback is presentation derived from the producer-owned primary MathJSON; rendered text, input trees, and nonnumeric native summaries are never reparsed or promoted as approximation truth.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Matrix and Vector precision is an optional backward-compatible request/replay field carried through the existing worker, OOE snapshot, and History seed. Old seeds default to six digits; counts, dimensions, Boolean verdicts, and prose remain ineligible for decimal cards.

## 2026-07-15 - VECTOR-GRAM-SCHMIDT-N1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Vector Gram-Schmidt accepts one through six equal-length vectors with length at most eight through the existing `gramSchmidtUV` route. Its residual construction shares one exact orthogonalization core with Matrix QR; workspace, worker, capability, OOE, replay, and History ownership remain separate and unchanged.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Every accepted residual carries orthogonality evidence against all earlier basis vectors. A zero residual is shown explicitly and discarded with compact dependence evidence rather than becoming an invalid normalized vector.

## 2026-07-15 - STATISTICS-DATA-SUMMARY1

- [agent: codex | model: gpt-5.5 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.5 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.5 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.5 | verified_by_agent_family: sol | attribution_basis: live] All five Statistics result routes default to strict Canonical Result V2 through Statistics-owned producer evidence. Every mathematical leaf requires standard producer-proven MathJSON; the obsolete Statistics frozen-file entries are removed without changing the immutable 57-route inventory.
- [agent: codex | model: gpt-5.5 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.5 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.5 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.5 | verified_by_agent_family: sol | attribution_basis: live] Data & Summary keeps independent list and compact frequency drafts. Representation switches never overwrite data; conversions are explicit, confirm before replacing a newer destination, and refuse physical expansion above 10,000 observations while compact frequency evaluation remains available.
- [agent: codex | model: gpt-5.5 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.5 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.5 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.5 | verified_by_agent_family: sol | attribution_basis: live] Descriptive defaults are `quartiles=halves` and `context=compare`. Halves exclude the median for odd-sized data, linear uses Type-7 interpolation, both sample and population spread remain visible, and compact frequency results are mathematically equivalent to list results without row expansion.

## 2026-07-15 - VECTOR-GEOMETRIC-MEASURES1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Vector geometric measures use one strict V2 route and retain exact producer evidence: parallelism is classified by a zero Gram determinant, distance is the norm of the difference, area is derived from the Gram determinant, and 3D volume is the absolute scalar triple product with the signed triple retained as evidence.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Normal and orientation interpretation are 3D-only. General equal-length areas do not invent a normal, volume rejects non-3D inputs, and this capability does not create a new workspace, worker, host, capability, replay contract, universal AST, Formula Builder, or automatic Equation route.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Display-contract inversion treats the current V2 and V3 canonical attach helpers as native document wrappers, just as it treats the retained V1 helper. The accepted baseline may add these visible boundaries but may not lower any lane's native-document floor.

## 2026-07-15 - STATISTICS-PROBABILITY1

- [agent: codex | model: gpt-5.5 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.5 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.5 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.5 | verified_by_agent_family: sol | attribution_basis: live] Statistics distribution numerics are owned by one adapter over the typed stdlib Binomial, Normal, and Poisson packages. The existing Statistics worker/fallback, capability, OOE, request kinds, and canonical result route remain unchanged.
- [agent: codex | model: gpt-5.5 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.5 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.5 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.5 | verified_by_agent_family: sol | attribution_basis: live] New probability expressions use typed events with independent interval bounds. Discrete endpoints preserve boundary mass and require integer event values; Normal exact point probability is zero, density is not probability, and strict versus inclusive continuous endpoints are explicitly equivalent.
- [agent: codex | model: gpt-5.5 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.5 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.5 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.5 | verified_by_agent_family: sol | attribution_basis: live] Legacy `mode=pmf|pdf|cdf` requests remain accepted and replayable. Probability results prove event notation, probability or density, expected value, and standard deviation as producer-owned standard MathJSON; no plot ships in the consolidation program.

## 2026-07-15 - STATISTICS-RELATIONSHIPS1

- [agent: codex | model: gpt-5.5 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.5 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.5 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.5 | verified_by_agent_family: sol | attribution_basis: live] Relationships owns one canonical paired x/y draft with an analysis selector. Switching between Regression and Correlation preserves complete and incomplete rows; legacy request and surface-state hydration maps into the shared draft.
- [agent: codex | model: gpt-5.5 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.5 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.5 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.5 | verified_by_agent_family: sol | attribution_basis: live] Regression and Correlation remain separate request kinds and evaluations over the existing Statistics worker/fallback, OOE capability, History, replay, and strict V2 result routes. No combined request or scatter plot is introduced.

## 2026-07-15 - MATRIX-SYMMETRIC-POSITIVE-DEFINITE1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Real symmetric exact matrices through 6 by 6 are classified by exact symmetry plus every nonempty principal minor. Positive and negative semidefiniteness use the complete principal-minor conditions; zero is reported truthfully as both positive and negative semidefinite.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Decimal or otherwise non-exact matrices through 8 by 8 use a bounded scale-normalized symmetric Jacobi iteration. The automatic tolerance, symmetry residual, and eigenvalue estimates remain visible; no hidden user tolerance or exactness claim is introduced.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] `matrix.definiteness` is a strict V2 route inside the existing Matrix worker, capability, OOE, History, and replay ownership. Nonsymmetric square matrices return a bounded classification with evidence; rectangular inputs remain controlled errors.

## 2026-07-15 - NOTEBOOK-MEDIA-INTERACTION-REPAIR1

- [agent: codex | model: gpt-5.5 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.5 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.5 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.5 | verified_by_agent_family: sol | attribution_basis: live] Notebook media keeps V10 snap-and-wrap. Image corners preserve the current displayed ratio while side handles may stretch one dimension; every video handle preserves the decoded source ratio and video always renders with contain fitting.
- [agent: codex | model: gpt-5.5 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.5 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.5 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.5 | verified_by_agent_family: sol | attribution_basis: live] Video theater and fullscreen are separate retained-player presentation states. Theater covers the active Notebook workspace while preserving outer Workspace Tabs; fullscreen covers the complete viewport/window with only the player. Embedded audio uses the video element and starts unmuted at full volume; volume remains session-only.

## 2026-07-15 - NOTEBOOK-HEADER-FOOTER-DIRECT-AUTHORING1

- [agent: codex | model: gpt-5.5 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.5 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.5 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.5 | verified_by_agent_family: sol | attribution_basis: live] V11 running matter is bounded formatted paragraph content in default and first-page left, center, and right regions. Page numbers are live inline fields; odd/even, per-Section, rich block, math, and media variants remain outside the contract.
- [agent: codex | model: gpt-5.5 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.5 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.5 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.5 | verified_by_agent_family: sol | attribution_basis: live] Direct header/footer authoring uses one temporary editor for the active region rather than one editor per physical page. Repeated pages mirror the draft, and closing commits the complete session as one body-history transaction.

## 2026-07-15 - MATRIX-SVD-PINVERSE-CONDITIONING1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Finite numeric Matrix SVD, Moore-Penrose pseudoinverse, 2-norm condition number, and numerical rank use the focused `ml-matrix` 6.14.0 dependency with automatic rectangular transposition. Calcwiz does not ship a hand-rolled SVD, tolerance editor, PCA surface, new worker, or new capability for this milestone.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] The library's automatic SVD threshold determines displayed numerical rank. A matrix with numerical rank below `min(rows, columns)` has infinite displayed condition; every result discloses approximation, threshold, rank, condition, and reconstruction residual evidence.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] `matrix.numeric-decomposition` is a strict Canonical Result V2 route. Singular values, factor matrices, pseudoinverse, threshold, rank, condition, and relations are aligned producer-owned standard MathJSON rather than reparsed display output.

## 2026-07-15 - LINEAR-ALGEBRA-NUMERICAL-CHECKPOINT0

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] The approved Linear Algebra numerical phase is accepted as one aggregate checkpoint across exact/decimal controls, Gram-Schmidt, geometric measures, definiteness, and numerical decompositions. Existing Matrix/Vector workspace, worker, OOE, History, replay, and copy ownership remains the foundation for later work.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Symbolic Linear Algebra is paused before `LINEAR-ALGEBRA-SYMBOLIC-SCALAR-SUBSTRATE1` until the requested user discussion occurs. The numerical closeout does not authorize symbolic implementation.

## 2026-07-15 - STATISTICS-INFERENCE1

- [agent: codex | model: gpt-5.5 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.5 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.5 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.5 | verified_by_agent_family: sol | attribution_basis: live] Statistics inference remains bounded to one-sample mean confidence intervals and hypothesis tests over the shared list/frequency data. Student-t CDF and quantile primitives come from the focused typed stdlib package; the existing Statistics worker/fallback, capability, OOE, History, replay, and strict V2 route remain unchanged.
- [agent: codex | model: gpt-5.5 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.5 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.5 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.5 | verified_by_agent_family: sol | attribution_basis: live] Confidence levels accept decimal or percent notation. Mean tests support two-sided, less-than, and greater-than alternatives, default omitted legacy alternatives to two-sided, and persist the chosen alternative through typed History replay.
- [agent: codex | model: gpt-5.5 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.5 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.5 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.5 | verified_by_agent_family: sol | attribution_basis: live] Inference assumptions are informative rather than blocking. Calcwiz distinguishes finite-value, sample-size, and observed-variability checks from random-sampling and independence facts it cannot verify, and reports hypotheses, standard error, degrees of freedom, statistic or critical value, p-value, alpha, decision, and plain-language interpretation without adding a plot.

## 2026-07-15 - STATISTICS-GUIDED-EXPRESSION1

- [agent: codex | model: gpt-5.5 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.5 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.5 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.5 | verified_by_agent_family: sol | attribution_basis: live] Statistics owns one global `Guided | Expression` input mode and one DisplayPanel result/editor authority embedded inside the active section. Guided suppresses the direct editor while retaining a generated-request preview; Expression reuses the existing MathLive editor without creating a second renderer or upper display.
- [agent: codex | model: gpt-5.5 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.5 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.5 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.5 | verified_by_agent_family: sol | attribution_basis: live] Expression drafts survive section and guided-form changes. First entry seeds only an absent draft, `Edit expression` explicitly replaces it, valid supported expressions import into Guided and activate their section, invalid expressions remain intact with parser feedback, and mode changes never evaluate automatically.

## 2026-07-15 - STATISTICS-CONSOLIDATION-POLISH1

- [agent: codex | model: gpt-5.5 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.5 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.5 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.5 | verified_by_agent_family: sol | attribution_basis: live] Statistics remains an experience inside the calculator shell rather than becoming a separate app page. Its single visible workspace consumes the full shell width; Guided uses a stable 26rem form rail and gives the remaining PC width to the existing result authority, while Expression remains full width.
- [agent: codex | model: gpt-5.5 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.5 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.5 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.5 | verified_by_agent_family: sol | attribution_basis: live] Statistics is supported and acceptance-tested for PC layouts only, with 1280 by 800 as the verified minimum. Existing narrow fallback CSS may remain harmlessly, but mobile optimization, acceptance, and product promises are outside scope.
- [agent: codex | model: gpt-5.5 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.5 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.5 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.5 | verified_by_agent_family: sol | attribution_basis: live] Native light Statistics selects use an explicit light color scheme and dark text for values and options. Plotting and diagrams remain excluded until the separate post-consolidation review.

## 2026-07-15 - LINEAR-ALGEBRA-SYMBOLIC-SCALAR-SUBSTRATE1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Linear Algebra owns a bounded standard-MathJSON `scalar-v1` operand representation with original and resolved trees. Legacy numeric seeds remain unchanged; new symbolic/complex values never require numeric shadows. Stored-value substitution is explicit, edits the scalar MathJSON tree rather than rendered LaTeX, protects named Matrix/Vector operands, and records exactly used and protected values for deterministic replay. V2 remains the default result contract and no symbolic producer is activated by this prerequisite.

## 2026-07-15 - VECTOR-SYMBOLIC-EXPRESSIONS1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Complex Vector products are Hermitian, complex angles are principal line angles, complex cross products are algebraic, and complex area/volume use Gram determinants without orientation claims. Conditions are V2 supplements and bounded undecidable classifications use standard `Which`; only `angle:grad` emits V3.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Formal Gram-Schmidt stops above the bounded two-vector presentation when scalar growth would exceed the committed limit. Formal span and independence deliberately remain controlled stops until Milestone 11 supplies the shared parameter-classifying elimination core.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Matrix and Vector scalar/size controls use explicit light-control contrast, and their workspace panels and cell pads consume the available calculator width. This is shared presentation only; Matrix/Vector workers, capabilities, OOE, replay, and History ownership remain separate.

## 2026-07-15 - MATRIX-SYMBOLIC-ARITHMETIC1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Matrix ordinary transpose and adjoint are separate operations. Symbolic/complex add, subtract, multiply, transpose, and adjoint support complete rectangular inputs through 8 by 8; determinant and nonnegative powers stop above 4 by 4, inverse and negative powers stop above 3 by 3, and literal exponents stop outside absolute value 12.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Unknown nonzero determinants are V2 condition supplements, not hidden assumptions. Formal scalar conjugation uses standard `ConjugateTranspose` because Compute Engine parses its own scalar star serialization to that equivalent scalar head; exact complex constants use normalized rectangular canonical LaTeX so producer proof round-trips without changing form-independent MathJSON.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Matrix and Vector dark scalar cells use explicit light text. Matrix columns and Vector components retain a 112px minimum width and overflow within their own cards instead of compressing into unreadable pads.

## 2026-07-15 - STATISTICS-VERTICAL-RESULTS1 AND STATISTICS-VISUALIZATION-CONTRACT1

- [agent: codex | model: gpt-5.5 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.5 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.5 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.5 | verified_by_agent_family: sol | attribution_basis: live] Statistics answers use concise labeled vertical rows. Contained owns one vertical result scrollbar; Full removes the cap and opens all groups on entry while preserving manual collapse afterward. The existing primary exact result remains Copy, History, replay, and compatibility truth.
- [agent: codex | model: gpt-5.5 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.5 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.5 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.5 | verified_by_agent_family: sol | attribution_basis: live] Statistics uses ECharts 6 through modular `echarts/core` imports with SVG rendering and no React wrapper. Calcwiz owns all mathematical values in a bounded clone-safe payload; ECharts options remain UI-local. Only Statistics replaces the lower keypad, and Expression may temporarily swap the dock to the existing keypad.

## 2026-07-15 - STATISTICS-RELATIONSHIPS-INFERENCE-VISUALS1

- [agent: codex | model: gpt-5.5 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.5 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.5 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.5 | verified_by_agent_family: sol | attribution_basis: live] Statistics visual payloads remain rendering DTOs over the same Statistics-owned calculations as canonical results. Regression may carry a fitted line and residuals; Correlation remains scatter-only; mean inference carries confidence-interval or Student-t test evidence without introducing another request, worker, capability, or OOE path.
- [agent: codex | model: gpt-5.5 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.5 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.5 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.5 | verified_by_agent_family: sol | attribution_basis: live] The global approximate-digits setting is part of the Statistics evaluation revision and controls worker result approximations plus chart labels and hover formatting. Changing precision stales the existing result and plot until explicit Evaluate; local plot controls remain presentation-only.
## 2026-07-15 - NOTEBOOK-DURABLE-SCHEMA-REPAIR1

- [agent: codex | model: gpt-5.5 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.5 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.5 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.5 | verified_by_agent_family: sol | attribution_basis: live] Notebook Schema 11 remains the sole current write contract. Desktop, browser, snapshots, Trash, recovery, and portable packages formally accept durable Schemas 6 through 11 at ingress; Schemas 1 through 5 remain internal TypeScript recovery inputs rather than durable compatibility promises.
- [agent: codex | model: gpt-5.5 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.5 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.5 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.5 | verified_by_agent_family: sol | attribution_basis: live] Notebook direct-manipulation acceptance is measurement-based: handle-to-pointer separation must be at most 3 CSS pixels, preview-to-release geometry at most 2 pixels, and visible content, selection, and handles must share one full measured rectangle. Broad resize or presentation class assertions are supporting evidence, not sufficient proof.
- [agent: codex | model: gpt-5.5 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.5 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.5 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.5 | verified_by_agent_family: sol | attribution_basis: live] Opening a valid legacy durable Notebook migrates only in memory. The source record remains unchanged until Save; the first upgrade save must preserve one raw, revision-deduplicated `before-schema-upgrade` snapshot before atomic Schema 11 replacement.

## 2026-07-15 - MATRIX-SYMBOLIC-SYSTEMS1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Symbolic/complex exact systems use one Matrix-owned elimination classifier through 3 by 3 with hard ceilings of six remaining coefficient parameters, four undecidable zero predicates, and sixteen cases. Opaque function coefficients and larger formal classifications stop explicitly rather than acquiring assumptions or route-local solvers.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Any accepted non-reserved identifier may be a system unknown; `x` has no privileged role. Stored substitution protects declared unknowns and named Matrix/Vector operands, free directions use collision-free `t_1,t_2,...`, conditional sets use standard `Which`, and fully proved profile/independence results retain typed V2 primaries.

## 2026-07-15 - LINEAR-ALGEBRA-INPUT-READABILITY-CORRECTION

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Matrix values above four columns and Vectors above four components use full library rows, and any sibling cards also take full rows so a wide value does not leave a compact empty column. Normal value libraries use at most two columns and odd final cards fill the remaining row.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Dark Matrix/Vector scalar cells apply their light foreground to MathLive's exposed content part as well as the host. This is presentation-only and does not alter scalar parsing, requests, workers, History, replay, or canonical results.

## 2026-07-15 - MATRIX-SYMBOLIC-SPECTRAL1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] The public Equation polynomial boundary is univariate and target-agnostic through degree four. Its coefficients may contain up to six other algebraic parameters, but it does not solve general multivariable polynomial systems or accept opaque function identities as proof.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Matrix constructs and always preserves its proven characteristic polynomial. Roots retain multiplicities and conditions; partial results remain visible, while eigenspaces and diagonalization are adopted only when the required roots, conditions, and complete eigenbasis are proved. Real and Complex domains remain explicit and V2 stays the default result contract.

## 2026-07-15 - NOTEBOOK-MEDIA-GEOMETRY2

- [agent: codex | model: gpt-5.5 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.5 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.5 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.5 | verified_by_agent_family: sol | attribution_basis: live] Notebook Schema 12 is the current write contract and changes only media-width precision: image and video widths are finite 10-through-100 percentages normalized to at most three decimal places. Historical Schemas 6 through 11 retain their original integer-width validation and migrate losslessly in memory.
- [agent: codex | model: gpt-5.5 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.5 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.5 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.5 | verified_by_agent_family: sol | attribution_basis: live] Media content, selection, and handles share one full viewport rectangle. Gesture previews use viewport CSS pixels and convert persisted width only on commit; page UI scale, editor padding, rotation bounds, and wrap fallback must not introduce a second hidden size authority.

## 2026-07-15 - NOTEBOOK-MEDIA-GESTURE-LOOP2

- [agent: codex | model: gpt-5.5 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.5 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.5 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.5 | verified_by_agent_family: sol | attribution_basis: live] Notebook resize, crop, and rotation preview the latest pointer snapshot at most once per animation frame. Release cancels queued preview work, derives the final geometry from the release coordinates, and creates one document transaction; Escape, pointer cancellation, and unmount commit nothing.

## 2026-07-15 - NOTEBOOK-MEDIA-PLACEMENT2

- [agent: codex | model: gpt-5.5 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.5 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.5 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.5 | verified_by_agent_family: sol | attribution_basis: live] Flow-media dragging uses body-owned overlays for the moving rectangle, insertion guide, and explicit left/normal/right targets. It never writes transient attributes into ProseMirror-managed DOM; insertion drops reorder through the existing document move command and wrap drops commit one node-attribute transaction.
- [agent: codex | model: gpt-5.5 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.5 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.5 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.5 | verified_by_agent_family: sol | attribution_basis: live] Requested image and video wrapping must preserve both the physical 180-point and rendered 240-pixel minimum text columns. A wrap drop clamps the persisted media width to the strictest current bound; later viewport changes may fall back to normal flow without changing the stored request.
- [agent: codex | model: gpt-5.5 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.5 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.5 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.5 | verified_by_agent_family: sol | attribution_basis: live] Pristine-document writing and template prompts belong to the page stage rather than the outer scroll sibling. Their placement is viewport-contained while remaining presentation-only; pristine detection and template semantics are unchanged.

## 2026-07-16 - NOTEBOOK-BLOCK-DRAG-UNIFICATION2

- [agent: codex | model: gpt-5.5 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.5 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.5 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.5 | verified_by_agent_family: sol | attribution_basis: live] Notebook block movement uses one pointer coordinator for canvas media, Sections, academic containers, display equations, evidence, dividers, and semantic Outline entries. Pointer previews remain body-owned presentation overlays; existing document move, indent, and outdent commands remain the sole mutation authority. Native HTML drag is not a second movement path.
- [agent: codex | model: gpt-5.5 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.5 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.5 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.5 | verified_by_agent_family: sol | attribution_basis: live] Inspector arrangement keeps keyboard-operable Move Up, Move Down, Move Into Section, and Move Out of Section alternatives. Disabled states reflect actual sibling and Section structure rather than silently accepting unavailable transformations.

## 2026-07-16 - NOTEBOOK-FLOATING-OBJECT-MODEL1

- [agent: codex | model: gpt-5.5 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.5 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.5 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.5 | verified_by_agent_family: sol | attribution_basis: live] Notebook Schema 13 adds one optional `objectPlacement` union to images, videos, display equations, evidence, dividers, academic containers, and complete Sections. Flow remains the legacy/default state; floating placement persists canonical-point paragraph/page anchors, page/margin references, X/Y/width, Square/Top-and-Bottom/In-Front/Behind wrapping, text distance, and normalized global layer order.
- [agent: codex | model: gpt-5.5 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.5 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.5 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.5 | verified_by_agent_family: sol | attribution_basis: live] Paragraph anchors must resolve to a durable paragraph outside the floating object's own structured subtree. Historical Schemas 6 through 12 migrate in memory to Schema 13 without adding placement attributes; new writes validate Schema 13 strictly in TypeScript and Rust.

## 2026-07-16 - NOTEBOOK-MEDIA-AUTHORING-REMOVAL1

- [agent: codex | model: gpt-5.5 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.5 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.5 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.5 | verified_by_agent_family: sol | attribution_basis: live] Notebook preserves image and video durable document formats, asset records, and schema compatibility, but removes image/video authoring and live media manipulation from the UI. Existing image/video nodes render inert removed-media placeholders rather than invoking the broken resize/playback implementation.

## 2026-07-16 - NOTEBOOK-IMAGE-AUTHORING-RESTORE1

- [agent: codex | model: gpt-5.5 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.5 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.5 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.5 | verified_by_agent_family: sol | attribution_basis: live] Notebook image insertion is immediate and must not force an alt/caption/details dialog. Captions and Figure numbering are explicit image-format actions after selection, while inserted images default to no caption and no Figure number.
- [agent: codex | model: gpt-5.5 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.5 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.5 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.5 | verified_by_agent_family: sol | attribution_basis: live] Image resizing no longer treats `widthPercent` as the primary authority for new gestures. The image node may persist point dimensions for object sizing, with `widthPercent` retained only as legacy/fallback compatibility. Video authoring remains disabled.
- [agent: codex | model: gpt-5.5 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.5 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.5 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.5 | verified_by_agent_family: sol | attribution_basis: live] Flow image resizing is intentionally free from the old content-width cap. The selected object box is the displayed image boundary; images fill that boundary, while videos may retain contain-style rendering when video authoring returns.
- [agent: codex | model: gpt-5.5 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.5 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.5 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.5 | verified_by_agent_family: sol | attribution_basis: live] Loaded Notebook images do not draw a separate frame or placeholder surface in normal viewing/selection. A visible frame is reserved for loading, missing-asset, and crop-editing states so downsized images do not expose a second rectangular box.

## 2026-07-16 - NOTEBOOK-VIDEO-REMOVAL1

- [agent: codex | model: gpt-5.5 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.5 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.5 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.5 | verified_by_agent_family: sol | attribution_basis: live] Notebook video support is removed from the current Schema 14 authoring, storage, publication, ribbon, and NodeView contract. Current documents reject video blocks and video MIME assets; legacy Schema 9 through 13 video figures migrate to plain `Video removed: ...` paragraphs so historical documents remain openable without preserving live playback.

## 2026-07-16 - NOTEBOOK-IMAGE-ASSET-METADATA1

- [agent: codex | model: gpt-5.5 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.5 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.5 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.5 | verified_by_agent_family: sol | attribution_basis: live] Notebook image intrinsic pixel dimensions belong to the immutable asset metadata envelope as optional paired `imageWidthPx` and `imageHeightPx` values. User-visible displayed size remains document-owned through image figure geometry; adding or preserving intrinsic asset metadata does not advance the Notebook document schema.

## 2026-07-16 - NOTEBOOK-FLOATING-IMAGE-INTERACTION1

- [agent: codex | model: gpt-5.5 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.5 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.5 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.5 | verified_by_agent_family: sol | attribution_basis: live] Dragging an in-flow Notebook image into Print Layout page whitespace is the direct opt-in path to floating placement. The release pointer in page whitespace takes precedence over stale reorder targets, writes one floating `objectPlacement`, and preserves normal flow drops plus left/normal/right wrap targets as the existing non-floating paths.

## 2026-07-16 - NOTEBOOK-FLOATING-STRUCTURED-OBJECT-INTERACTION1

- [agent: codex | model: gpt-5.5 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.5 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.5 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.5 | verified_by_agent_family: sol | attribution_basis: live] Dragging a floating-eligible structured Notebook block into visible Print Layout sheet whitespace is the direct opt-in path from flow to floating placement for display equations, evidence snapshots, dividers, Sections, and academic containers. Existing flow reorder, section nesting, and Outline movement remain document-command paths, and video is not part of the current contract.

## 2026-07-16 - NOTEBOOK-OBJECTS-LAYERS1

- [agent: codex | model: gpt-5.5 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.5 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.5 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.5 | verified_by_agent_family: sol | attribution_basis: live] Visual floating-object order is exposed beside, not inside, the semantic Outline. The left rail has an Objects & Layers mode that lists current floating objects and mutates only existing `objectPlacement.zOrder` values through layer-order commands; it does not create a second layout model or reintroduce video.

## 2026-07-16 - NOTEBOOK-SETTINGS1

- [agent: codex | model: gpt-5.5 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.5 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.5 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.5 | verified_by_agent_family: sol | attribution_basis: live] Notebook preferences are local app settings, not document data. They may seed new Notebook sessions and UI behavior through the existing Settings-to-Notebook seam, but they must not reinterpret existing documents, disable recovery/safety ceilings, or be embedded in `.cwiznb`.

## 2026-07-16 - NOTEBOOK-PUBLICATION-PARITY1

- [agent: codex | model: gpt-5.5 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.5 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.5 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.5 | verified_by_agent_family: sol | attribution_basis: live] Notebook PDF/print is the publication target that consumes authoritative physical-page floating coordinates. Web and DOCX remain publication projections, not Notebook round-trip formats; when floating objects cannot be represented with equal fidelity, they must emit explicit compatibility findings and preserve safe ordered-flow/editable fallbacks rather than silently pretending exact parity.

## 2026-07-16 - NOTEBOOK-GESTURE-RATCHET1

- [agent: codex | model: gpt-5.5 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.5 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.5 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.5 | verified_by_agent_family: sol | attribution_basis: live] Notebook authoring/publication path changes must select `npm run test:notebook-gesture-ratchet` through the seam-impact selector. The ratchet pins image direct geometry, width-only shrink behavior, floating placement validation, pagination, persistence, publication fallback behavior, and focused floating layer-order UI behavior without becoming a broad new interaction framework.

## 2026-07-16 - CALCULUS-INTEGRATION-V2-AUTHORITY-MIGRATION1

- [agent: codex | model: gpt-5.5 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.5 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.5 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.5 | verified_by_agent_family: sol | attribution_basis: live] Ordinary indefinite-integration successes and math-bearing controlled errors use Calculus-owned V2 authority at the runtime boundary. Producer-owned native antiderivative expressions, not rendered LaTeX reparsing, drive Copy Result, To Editor, History replay, Trust, and structural `+C`.
- [agent: codex | model: gpt-5.5 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.5 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.5 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.5 | verified_by_agent_family: sol | attribution_basis: live] Compute Engine fallback for indefinite integration may adopt only trusted, exactly verified candidates. Untrusted or route-unsupported cases fail closed with visible boundary evidence instead of returning a string-only symbolic success.

## 2026-07-16 - CANONICAL-RESULT-V4-SPECIAL-FUNCTION-EXPRESSION1

- [agent: codex | model: gpt-5.5 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.5 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.5 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.5 | verified_by_agent_family: sol | attribution_basis: live] Canonical Result V4 is an additive typed-expression authority for non-standard special functions, not a custom-MathJSON escape hatch. Only `erfi`, `Si`, `Ci`, `Ei`, `li`, `EllipticF`, `EllipticE`, and `EllipticPi` are allowed with fixed arities; all ordinary leaves, coefficients, arguments, and piecewise conditions retain producer-proven standard MathJSON.
- [agent: codex | model: gpt-5.5 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.5 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.5 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.5 | verified_by_agent_family: sol | attribution_basis: live] V4 canonical LaTeX is rendered deterministically from the typed expression and is never reparsed as semantic truth. V2 remains the default ordinary-math contract and V3 remains the narrow typed angle-quantity widening.

## 2026-07-16 - CALCULUS-INTEGRATION-NATIVE-RESULT-IR1

- [agent: codex | model: gpt-5.5 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.5 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.5 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.5 | verified_by_agent_family: sol | attribution_basis: live] Calculus indefinite integration owns an additive antiderivative expression whose base is either standard MathJSON or the typed V4 special-function tree. Native authority is preserved through additive and scalar composition, normal-form and trig retries, symbolic adoption, and Compute Engine fallback; a route without native authority remains explicitly transitional until its migration gate.
- [agent: codex | model: gpt-5.5 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.5 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.5 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.5 | verified_by_agent_family: sol | attribution_basis: live] Native antiderivative LaTeX is rendered from structure, never reparsed as mathematical truth. The indefinite integration constant is a structural right-hand suffix added only after direct AST differentiation verifies the primitive against the original integrand.
- 2026-07-16 [agent: codex | model: gpt-5.5 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.5 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.5 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.5 | verified_by_agent_family: sol | attribution_basis: live] Notebook schema compatibility is now boundary-owned: Schema 14 is current, durable compatibility formally covers Schemas 6-14, Schemas 1-5 remain TypeScript-only best-effort recovery inputs, and historical numeric document aliases/guards must stay out of the current authoring type/model API.

## 2026-07-18 - NOTEBOOK-OBJECT-INTERACTION-AUDIT1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] The current Notebook image/object interaction runtime is not the foundation for future object work. Preserve current documents, asset ingestion/metadata, safety, persistence/history/packages, optional image semantics, and page-policy behavior, but replace the overlapping percentage/point/frame authorities, NodeView-owned handles, split gesture coordinators, segmented wrap targets, broad DOM-mutating pagination integration, and renderer-blocking export order with one object frame, one view-owned interaction plane, one command path, one derived layout plane, registry-backed selection/layers, and destination-first export.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] The replacement program must delete the superseded image/object interaction files, branches, CSS, custom-event bridges, segmented targets, and duplicate geometry conversions after parity is verified. Compatibility belongs only at schema ingress; retained unused runtime code is not acceptable. Video remains removed.

## 2026-07-18 - NOTEBOOK-OBJECT-FRAME-AUTHORITY-DESIGN1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Notebook object replacement advances the durable document to Schema 15. Every eligible non-video object embeds one versioned `NotebookObjectFrameV1`; images use one fixed width with fixed or intrinsic height, structured objects use available/fixed width with auto height, and flow/floating placement is a discriminated frame variant. Percentage width, independent point dimensions/aspect ratio, legacy image alignment/placement, and floating placement width cease to be current independent truths after ingress migration.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] The app-owned document frame is authoritative; Tiptap mirrors it as one attribute. A view-owned overlay may hold only transient selection/gesture geometry, one typed command module owns every mutation and undo unit, a derived layout snapshot owns pagination/hit boxes without writing measured geometry, and Objects & Layers is a node-ID registry projection with session-only hide/lock state. Publication consumes an immutable frame/layout projection after destination selection, never editor DOM geometry.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Schema 14 migration is deterministic and TypeScript/Rust-identical: floating width wins for floating images, otherwise point width wins over percentage width; explicit point height wins, then legacy aspect-derived height, then immutable asset ratio. Conflicts produce sidecar repair evidence, the raw pre-upgrade snapshot preserves the source record, and legacy fields may remain only inside compatibility ingress and fixtures. The final Terra gate must hard-delete the old direct-media/coordinator/custom-event/segmented-target/CSS paths.

## 2026-07-18 - APP-SHELL-PANEL-MOTION1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Settings, History, Variables, and the left Menu Inspector share one internal motion-presence host: 220ms entry, 170ms exit, 18px side-origin slide plus opacity, a 160ms overlay fade, retained DOM through exit, and a reduced-motion 1ms/no-translation path. The host preserves a panel while responsive presentation changes between outboard and overlay; exiting surfaces are inert. OOE diagnostics and workspace launcher menus remain intentionally outside this contract.

## 2026-07-18 - CI-REGRESSION-REPAIR1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Printer migration treats producer-owned native antiderivative expressions/nodes and the reviewed Equation or Linear Algebra evidence wrappers as typed migration evidence, never as compatibility debt. The marker/wrapper vocabulary is part of the registry digest, and the accepted compatibility floor remains zero.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Repository-owned TypeScript utilities under `tools/` belong to a Node-aware project referenced by the root TypeScript build; individual files must not rely on editor-inferred globals or local suppression directives.

## 2026-07-18 - GRAPH-ARC-REBASE-AND-AUTHORITY-DESIGN1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Graphing is a non-singleton full-width app page opened only through Workspace Tabs `New Graph`. It is the narrow computed app-page exception that receives workspace runtime identity; existing app pages retain null runtime context. Graphing owns a dedicated OOE-backed compartment plus separate `graph.sample`, `graph.analyze`, and `graph.export` worker/fallback shells and does not use History tickets as runtime authority.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Authored LaTeX is Graph source/provenance only. `GraphDocumentV1`, structured relation/condition/piecewise/parameter IR, and revisions own mathematical/session truth; runtime scenes and analysis are derived. Exact Graph facts use producer-owned standard MathJSON, Canonical Result V2 by default, and the shared printer. V3/V4 retain only their existing narrow meanings.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] The renderer-neutral scene and deterministic snapshot precede renderers; headless validation and SVG reference/export precede a private Three.js interactive adapter. Three types are confined to that adapter, while a Graph Render Governor owns capability negotiation, quality, reduced motion, context loss/fallback, and disposal. The first Complex contract is a real-parameterized Argand trajectory; Both uses synchronized split views.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Bare expressions in x, including constants, normalize to explicit `y=f(x)` while retaining source; bare y-only or mixed x/y expressions are ambiguous controlled stops. Explicit x, implicit equalities/inequalities, structured piecewise, local parameters, polar/parametric routes, visibility, relation-correct tracing, and adaptive grids widen through separate bounded gates. History, Notebook, Surface Protocol, calculator Variables, solver precedence, canonical-result/printer authority, and app-shell motion remain protected.

## 2026-07-18 - APP-SHELL-UTILITY-OVERLAY-FIX1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Settings, History, Variables, and the left Menu Inspector use a transparent non-modal click-catcher whenever responsive layout selects overlay presentation. Outside click still closes the panel, while OOE diagnostics retains its existing dimmed immediate overlay. Shared panel motion is 180ms entry and 140ms exit with the existing polished easing and 1ms/no-translation reduced-motion path.

## 2026-07-20 - GRAPHING-INSTANT-VIEWPORT-CORRECTION1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Graph rendering has separate renderer-owned `setView` and `setScene` operations. Live camera/grid work stays imperative and frame-bounded; React receives one settled viewport commit, sampled geometry retains stable keyed SVG nodes, and `GraphSampleRequestV2` carries mathematical geometry without grid payloads.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Graph tracing is click-first: the initial click chooses the closest point on the closest curve in final screen space, then movement sweeps that selected mathematical item until empty click or Escape clears it. Hover does not repeatedly reselect curves.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Explicit and parametric sampling has no generic recursion-depth completion cutoff. It refines iteratively using screen error, numeric stagnation, discontinuity/domain safeguards, and sample/time/vertex emergency guards; finite/non-finite bracketing returns the boundary point and preserves the finite remainder so ordinary branches reach the viewport edge without artificial cuts.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] The Graphing program has 22 moves. Adaptive viewport/workload sampling is a dedicated Move 16 that must be planned before implementation; Three.js shifts to Move 17 and remains blocked until Move 16 and the performance checkpoint pass.

## 2026-07-20 - GRAPHING-ADAPTIVE-VIEWPORT-SAMPLING1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Graph sampling quality is derived from CSS screen pixels through preview, settled, and silent polish stages. `GraphSampleRequestV3` carries movement and priority but no caller-authored equal sample/time/vertex quota; per-item evidence records achieved quality, estimated screen error, cache reuse, refinability, and truthful failure.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] The retained application-level Graph worker remains the sole sampler owner. Active-tab sampling cache is capped at 16MiB, invalidates with mathematical revision or incompatible view scale, and releases on disposal. Ordinary time slicing is internal; only genuinely incomplete physical-resolution or topology results produce per-item reduced-detail or unresolved messages.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Move 16 passes the fixed pre-Three workload and visual checkpoint. Three.js is Move 17 and may consume only the existing renderer-neutral scene; it cannot replace `GraphRelationIR`, sampling, or SVG reference authority.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] The Graphing program has 27 moves. Adaptive implicit contour quality is Move 17; Notes/document-content separation is Move 18; condition validation and eager-shell lazy boundaries precede appearance and Three.js in Moves 19-22; analysis, complex views, presentation/export, and closeout occupy Moves 23-27.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Generic implicit contours refine complete adaptive boundary cells in CSS screen space, refine roots with safeguarded secant plus bisection, resolve saddle ambiguity bilinearly, share edge vertices, and publish stitched polylines. Sample accounting counts evaluated locations consistently with Graph sampling policy; time, memory, topology, cancellation, stagnation, and renderer safeguards remain internal rather than mathematical limits.

## 2026-07-20 - GRAPHING-NOTES1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Graph documents and workspace sessions use validated V2 contracts with independent content and mathematics revisions. Notes, Note ordering, titles, and later appearance edits advance content only; sampling-affecting relation, condition, parameter, and visibility changes advance mathematics. Mathematical rows retain the compact fixed-order rail UI.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] `GraphSampleRequestV4` and `SampledSceneRuntimeV2` contain mathematical geometry without color or stroke presentation. Renderers receive appearance independently through `setPresentation()`, so content-only Notes, row ordering, and styling never launch Graph sampling.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Graph Notes are plain multiline rail items capped at 16,384 characters with explicit overflow feedback. They are persisted and undoable but remain outside mathematical authority, sampling, tracing, analysis, and viewport artifacts.

## 2026-07-20 - GRAPHING-PIECEWISE-CONDITION-VALIDATION1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Piecewise sampling, endpoint markers, overlap/gap/impossibility diagnostics, and later analysis consume one `GraphPiecewiseConditionEvidenceV1` partition. Reducible coordinate bounds and parameterized constants use exact interval operations; nonlinear conditions use adaptive current-viewport transition refinement and report unresolved evidence rather than universal inequality claims.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] All matching piecewise branches continue to render simultaneously. `otherwise` consumes only the uncovered partition, legal gaps remain explicit, and endpoint inclusion comes from refined condition roots rather than sampled midpoints.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Actionable error guidance is scoped to piecewise authoring: known structured condition failures receive a concise correction, while unrecognized cases receive a safe comparison example. No app-wide heuristic error classifier or internal exception disclosure is introduced.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Only Note rows expose row-reordering controls. Expression, parameter, point-set, and piecewise rows keep the earlier compact fixed-order layout; piecewise branches are simultaneous conditions and expose add/remove rather than presentation-only ordering controls.

## 2026-07-20 - APP-SHELL-EAGER-SURFACE-LAZY-BOUNDARY1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] The app shell may eagerly own lightweight contracts, identifiers, labels, and loading facades, but inactive page, workspace, solver, persistence, presentation, and heavy-vendor implementations load only behind feature-owned boundaries. This split must preserve each workspace's host, capability, request, OOE, replay, cancellation/stale, and visible-commit ownership; bundle thresholds are not raised to excuse eager regressions.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Graph piecewise rows use a compact rendered cases summary as their normal state with one expand/collapse control, visibility, delete, and immediate diagnostics. Expansion reveals the structured branch editor beneath a read-only summary; branch reordering remains absent. Palette controls, glowing curves, and other later appearance concepts remain outside this decision.

## 2026-07-20 - GRAPHING-APPEARANCE-STYLING1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Graph appearance is renderer-neutral, tab-local, strictly versioned, and independent of mathematical sampling. Technical, Paper, Aurora, and restrained Luminous themes resolve item token colors without overwriting custom item presentation; theme and style edits advance content only and are undoable.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Semantic markers are reserved for authored points, piecewise endpoints, trace points, and later validated feature annotations; adaptive sampling vertices remain invisible. Paper changes only the graph canvas, and Luminous applies at most a soft item-controlled halo.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] The approved Graphing program now contains 30 moves. Moves 22-28 are private Three 2D/3D foundations, analysis authority, Analyze UI, explicit real surfaces, general complex mapping/solver, precise 2D sheets, and exploratory 3D Riemann surfaces; presentation/export and closeout remain Moves 29-30.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] `graph.analyze` is a separate Graph-owned OOE capability with `graph-analysis-worker-runtime`, `graph-analysis-runtime`, and `graph-analysis-worker-shell`; it never shares cancellation or lifecycle ownership with `graph.sample`. Analysis carries mathematics, viewport, and parameter revisions, and only `exact-proved` or `numeric-validated` evidence may later be pinned. Denominator exclusions, sampled non-finite values, and turns are not silently promoted to poles, asymptotes, or extrema.

## 2026-07-20 - GRAPHING-ANALYZE-UI1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Analyze is a Graph-owned floating overlay, not a global side surface or modal. It never resizes or automatically pans the viewport; feature navigation is an explicit Recenter action.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Graph sessions V5/surface V4 persist Analyze width, active tab, selected item, and bounded stable pins per graph tab. Only exact-proved or numeric-validated evidence persists; sampled, suspected, inconclusive, conditional, and unsupported evidence remains explanatory only.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Analyze runs after opening, selected-item changes, or settled mathematics/parameter revisions. Camera and viewport gestures never restart analysis. The Style tab mirrors renderer-neutral Move 21 commands, and the integrated Solve section remains reserved for Move 26.

## 2026-07-20 - GRAPHING-REAL-SURFACES1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] A real surface is authored only as explicit structured `z=f(x,y)`. Existing x expressions retain real-curve meaning and stay on `z=0` in 3D; implicit and parametric surfaces and broad surface-surface solving remain deferred.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Surface sampling is renderer-neutral and bounded: adaptive mesh positions, normals, triangle indices, contours, domain-break counts, and truncation evidence cross the Graph scene contract. SVG owns precise top-down height/contour presentation and Three owns shaded interactive presentation.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] The current ground-plane view supplies default surface bounds and an expanded item editor may lock x/y bounds. Bounds and view changes may settle into resampling; vertical exaggeration is display-only. Surface analysis may report domain boundaries, `z=0` level contours, and validated stationary/local-extrema candidates with z-capable pins.

## 2026-07-21 - GRAPHING-COMPLEX-MAPPING-SOLVER1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Complex Graph authority is explicit and structured: `f(z)=...`, `w=...`, and bare z-expressions normalize to `complex-mapping`; `f(t)=...` remains a distinct real-parameterized `complex-trajectory`; real x expressions are never reinterpreted.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Graph consumes complex evaluation, principal-branch diagnostics, and bounded Newton candidates only through `complex-domain-public.ts`; the Graph boundary ratchet permits that exact reviewed seam and continues rejecting Equation-private imports.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Complex numerical findings are local to the visible or locked rectangle and never claim global completeness. Exact structured zeros/poles/branch points remain distinct from numeric-validated candidates and explicit inconclusive completeness evidence; non-holomorphic mappings are labeled and do not inherit analytic claims.

## 2026-07-21 - v0.3.0 Release Preparation

- [agent: claude | model: claude-fable-5 | attribution_basis: live] Browser-preview hosting for the app will use Cloudflare Pages (git-connected build, no custom domain required to start), chosen over GitHub Pages specifically to avoid anchoring the demo on Microsoft-owned infrastructure, consistent with prior sovereignty-driven tooling choices (three.js over Babylon). Windows/macOS download badges remain out of scope until those platforms actually have packaged release artifacts.
- [agent: claude | model: claude-fable-5 | attribution_basis: live] CHANGELOG entries must distinguish "Added" (workspace absent at the prior tag) from "Enhanced"/"Changed" (workspace already existed and was expanded), verified against actual `git ls-tree <prior-tag>` file counts rather than commit-message impression. Established after the first v0.3.0 CHANGELOG draft incorrectly framed pre-existing Equation/Calculus/Linear Algebra/Statistics work as newly "Added."

## 2026-08-08 - Explicit Equation Supplement V2 Authority

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Equation condition and exclusion supplements selected for V2 are constructed only from producer-owned native `supplementEvidence`, one semantic leaf per proof. Grouped presentation LaTeX is never parsed or recompiled into V2 authority; missing or incompatible evidence fails closed.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Bounded Equation outcomes carrying `Log Combine` or `Log Quotient` provenance use the explicit `equation.trig-exp-log: typedLabeledSupplement` V2 selector. The frozen shared route classifier and fingerprinted Equation V2 adapter remain unchanged.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Formula Viewer retains its production lazy import; UI tests must await the existing module boundary rather than weakening or making production loading eager.

## 2026-08-09 - Integration Route-Owned Exact Proof

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] A migrated native Integration antiderivative may bypass the generic Compute Engine backcheck only when its producing route supplies matching `verified-exact` evidence and the final adapter explicitly consumes that proof. Native expression presence alone is never exact authority.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Algebraic genus-0 pullbacks prefer the established direct-rule route before ordinary classification so pure polynomial pullbacks preserve their real producer ownership; unresolved pullbacks continue through the ordinary dispatcher.

## 2026-08-09 - Calculus Integration Readback Authority

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Migrated Integration result presentation is rendered from producer-owned standard MathJSON or native coefficient evidence with selected-variable context; rendered LaTeX is never reparsed to recover mathematical authority.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Canonical lowercase `\operatorname{erf}` and `\operatorname{erfc}` are presentation aliases for standard `Erf` and `Erfc` MathJSON only when the full native tree proves equality; arbitrary function names and mismatched arguments remain rejected.

## 2026-08-09 - Candidate-Validated Equation Readback

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Exact mode may retain a validated approximate readback for the bounded same-base logarithmic-equality route only through a one-shot internal permission granted after candidate validation. The permission is transported outside serialized carriers, consumed before canonical runtime attachment, and must never be inferred from badges, diagnostics, or presentation strings.

## 2026-08-11 - Linux Unit Watchdog

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Linux CI and release unit steps have a 30-minute GNU timeout with a 30-second TERM-to-KILL escalation. Local unit and closeout commands remain unguarded so local diagnostic behavior is unchanged, while CI alignment prevents removal or reordering of the guarded broad unit check.

## 2026-08-11 - Equation Mixed Supplement Presentation Pairing

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] When an Equation V2 outcome carries mixed labeled supplement rows, the runtime adapter may classify only the exact presentation label prefix to pair rows with producer-owned evidence roles. One-to-one rows retain their presentation; one grouped row for a role expands to evidence-owned canonical rows. Mathematical presentation content is never parsed, MathJSON is never reconstructed, and unresolved role/count combinations fail closed.

## 2026-08-11 - Deterministic Formal Canonical Proof

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Formal Canonical Result proof uses a deterministic comparison-only normal form and never falls through to Compute Engine compiled equality. It preserves producer MathJSON, function identity, arity, argument order, matrices, sets, Boolean values, and formal subscripts; only reviewed aliases and exact structural arithmetic normalization are accepted.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] When formal proof exposed a presentation/proof conflict, presentation was corrected to show the producer-owned operand or vector value. `pivotColumns(A)` retains its operand, zero residuals remain vectors, and Matrix decomposition/projection/eigenspace rows show the exact native values their proofs carry; contextual equivalence was not weakened.

- 2026-08-13: [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] `CODEX-CONTROLLED-AGENT-WORKFLOW1` keeps `DIRECT` root-only work as the default and requires explicit permission per task for `CONTROLLED` or `CRITICAL` delegation. Root alone orchestrates; no more than three subagents or one writable Implementer may be active; children cannot delegate; one failed remediation cycle returns control to root. Role semantics are model-independent, while model/reasoning assignments live in a reviewed baseline and can change only through an approved model-migration gate.
- 2026-08-13: [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Resource protection remains subordinate to OOE authority. Mathematical and representation caps stay intact; future breaker escalation is pressure to soft stop to isolated-host hard stop, partial work never commits as complete, and resource stops remain distinct from mathematical impossibility or proof failure. Table resource stops must not retry on a less-isolated host.
- 2026-08-13: [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] `CODEX-TASK-MODE-RECOMMENDATION1` requires a concise route recommendation before new meaningful repository tasks. Recommended `DIRECT` work explains and proceeds without a pause; `CONTROLLED` and `CRITICAL` require task-specific approval before delegation, with `CRITICAL` also blocking critical execution. Status checks, simple questions, and clear continuations are exempt; material scope changes renew the recommendation; a user-selected route is explained and honored without reconfirmation.

## 2026-08-21 - Cross-Workspace Canonical Proof Alignment

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Statistics fixed-decimal canonical presentation and numeric MathJSON derive from one Statistics-owned rounded numeric value. Hypothesis parentheses are represented by producer-owned `Delimiter` nodes rather than display-only punctuation or parsed strings.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Derivative-at-point request evidence is the native evaluated derivative tree over the effective body, variable, and point. Exact signed integer products and reviewed imaginary-unit aliases normalize deterministically for formal comparison; near-misses still fail closed and never reach compiled equality.

## 2026-08-21 - Linear Algebra Scalar Routing

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Real `scalar-v1` Matrix and Vector operands remain the persisted source representation, but referenced finite numeric operands may be losslessly projected into the existing numeric producer with producer-owned exact-rational sidecars. Unused formal named values do not force symbolic routing; referenced formal values, Complex mode, and stored substitution do.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Numeric domain and dimension stops remain authoritative after scalar-cell editing and are never hidden by symbolic fallback. Playwright owns Linear Algebra MathLive cells through their textbox/accessibility contract, and the complete E2E tree has its own Node/DOM/Playwright TypeScript project.

## 2026-08-22 - Singular Matrix Space Proof

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] A nontrivial Matrix null space is proved as the span of its producer-owned basis set. The zero null space remains the explicit zero-vector set. Strict V2 comparison must reject any producer tree that proves only the basis set while presenting its span.

## 2026-08-22 - Equation V2 Evidence Identity

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Active Equation V2 supplements are selected against the final producer-owned document by role, canonical mathematical value, and MathJSON identity. Missing, conflicting, ambiguous, or unrelated evidence fails closed; presentation labels and evidence counts are not semantic pairing authority.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Equation composition summaries transport each source and result as a paired canonical LaTeX and standard MathJSON value. Loose `resultLatex` helper inputs are forbidden because they recreate printer compatibility debt even when nearby proof exists.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] A validated bounded carrier result may construct its accepted primary set only from root nodes that each independently prove the corresponding displayed branch. Raw solver nodes cannot become accepted-set authority merely because the aggregate set happens to pass a broader comparison.

## 2026-08-24 - Equation Carrier Proof Boundary

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] An Equation carrier primary is committed only when every accepted displayed root has matching producer-owned native MathJSON and the target-root relation proves as one exact primary. Exact serialization from the candidate tree may establish that match; rendered result LaTeX is never parsed to manufacture proof, and incomplete or conflicting evidence fails before a successful V2 draft is emitted.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Equation lazy-module import errors and worker execution or canonical-finalization errors are separate failure classes. Only an actual dynamic-import failure may be presented as a module-load failure.

## 2026-08-24 - Calculate Textual Nth-Root Input

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Textual `root(index, radicand)` is a Calculate-only input canonicalization into existing structured nth-root notation, not a new solver operator. Accepted indices are integers of at least two or one symbolic letter; malformed calls fail at canonicalization and may never be reparsed through a raw-input fallback.

## 2026-08-24 - CI Runtime Readiness

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] UI tests that exercise known asynchronous Linear Algebra routes may use a bounded route-specific completion wait; global timeout increases, sleeps, and production eager execution remain forbidden. The Calculus linear-integral canary has one canonical expected spelling, `\frac{x^{2}}{2}+C`.

## 2026-08-24 - Node and GitHub Actions Maintenance

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] `package.json` is the single fail-closed Node policy source at `24.x`; workflows must use it through `node-version-file`, and the lock root must match package version and toolchain metadata. Linux-facing checkout/setup-node steps are SHA-pinned Actions v7 with matching version comments and weekly Dependabot maintenance. Action-internal runtime upgrades and application Node selection are enforced as separate layers.

## 2026-08-31 - Equation V2 Supplement Authority

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Equation V2 supplement authority is an explicit producer classification carried with proven role, canonical value, and MathJSON identity. Same-source guarded selections may accumulate across stage merges; later diagnostic routes may not replace or enlarge them. Runtime selection must not recover semantics from presentation counts, target text, or rendered LaTeX.

## 2026-09-01 - Request-Scoped Equation Proof Verification

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Equation proof comparison may reuse one lazily created Compute Engine and successful semantic/printer evidence only within one top-level runtime request. Every lookup first revalidates provenance, route ownership, source, bounds, clone safety, and forbidden operators; failures and near-misses are never cached, stored MathJSON is unchanged, and overlapping fallback requests disable ambient reuse rather than sharing authority.

## 2026-09-04 - Equation Finite-Branch V2 Authority

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] An exact single-target finite Equation result is authoritative only when its final `Equal(target, root)` or `Element(target, Set(...roots))` branches form a unique bijection with producer-owned native root nodes. Every displayed and native root must be consumed exactly once; missing, extra, ambiguous, duplicate, conflicting, or unrelated evidence fails closed. Matching occurs at root identity rather than through presentation counts or aggregate equality, producer order remains authoritative, and equivalent real radical presentation is normalized from the matched native nodes without changing approved single-root or Complex presentation.

## 2026-09-05 - Equation Formal Proof and Production Latency

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Deterministic formal proof comparison is authoritative when it applies. Exact producer serialization or deterministic formal equality may pass, but a deterministic mismatch cannot be overridden by Compute Engine structural equivalence. Imaginary-unit aliases and signed unit products normalize inside equality and set trees; opposite signs remain distinct and failures remain uncached.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Equation production-path optimization stays request-scoped and representation-preserving: canonical printing may reuse the already validated boxed expression through a private seam, and candidate/composition validation may parse residuals and prepare repeated constraints once per batch. Solver recursion, route order, worker topology, public schemas, request isolation, and request-wide solver AST caching remain unchanged.

## 2026-09-07 - Calculus Derivative Input Authority

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Fresh Derivative and Derivative at Point input derives its target only from complete ordinary notation, while fresh Partial Derivative input derives its target only from complete partial notation. Readback never invents `x`; invalid or cross-screen notation stops before runtime and History. Legacy body-plus-variable guide and History seeds upgrade at ingress, and numeric stored substitutions remain unchanged with the written target protected.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Live Calculus structural normalization preserves MathLive fence ownership. Physical opening parentheses retain their smart pair, and the Calculus keypad inserts one bounded fence template with the cursor inside; paste and submit canonicalization remain unchanged.

## 2026-09-07 - Equation Numeric Interval Request Identity

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] An Equation numeric-interval launch resolves its target from the execution snapshot before creating the OOE request, and the same resolved target is retained in replay context. This keeps active-revision comparison, visible commit legality, and History replay on one request identity when the UI target state was initially unset.

## 2026-09-07 - v0.3.0 Linux Draft Release Policy

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] The reusable Linux release workflow targets v0.3.0 and fails closed when its requested release tag differs from the package version. Tag-triggered releases remain unpublished drafts and prereleases; `v0.3.0` may be created only after the reviewed commits are on `main`, both `ci-linux` and `e2e-linux` are green, and separate tag approval is received. Publishing remains a later manual decision after Linux artifact smoke testing.

## 2026-09-07 - CI UI Resource Stability

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] The broad jsdom UI suite uses at most two Vitest workers so asynchronous UI contracts are not made nondeterministic by four simultaneous CPU-heavy app shells on Linux CI. Route and test timeouts remain unchanged; tests with scheduled multi-phase work must synchronize on the final owned phase before clearing mocks or asserting presentation-only inactivity.

## 2026-09-07 - Equation.io Research Candidate

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | recorded_by_agent: codex | recorded_by_agent_model: gpt-5.6 | recorded_by_agent_family: sol | verified_by_agent: codex | verified_by_agent_model: gpt-5.6 | verified_by_agent_family: sol | attribution_basis: live] Equation.io is an active pinned source-mirror candidate for graph-language and GPU-rendering research. Its `approved-executable-research` status covers only the user-approved 2026-09-07 locked install, build, loopback preview, visual inspection, and cleanup recorded in the audit dossier. It remains context only: no Calcwiz dependency, source/asset copying, execution authority, renderer adoption, architecture decision, parity obligation, or roadmap sequence is approved.
