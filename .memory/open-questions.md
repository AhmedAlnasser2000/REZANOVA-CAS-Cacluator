# Open Questions

- Whether a later version should move part of the symbolic engine into Rust.
- Whether Arabic localization should be added in version 2 or later.
- Whether matrix and vector editing should eventually use fully interactive MathLive matrix templates instead of guided numeric grids.
- Whether the free-form `Equation > Symbolic` branch should later gain optional numeric fallback for equations the symbolic engine cannot solve exactly.
- What the future in-app guidance page should look like: a standalone learn screen, context-aware help drawer, or both.
- What the exact first-release scope should be for the planned `Trigonometry`, `Geometry`, and `Statistics` cores before formal implementation planning starts.
- After the shipped `Trigonometry` pass, whether `Statistics` or `Geometry` should be implemented next and released as a separate milestone or bundled into a larger three-core follow-up.
- After `Geometry` took launcher hotkey `0`, where `Statistics` should land in the launcher and keyboard rollout without reshuffling user-learned hotkeys too aggressively.
- 2026-03-03: If launcher categories eventually outgrow five roots or their submenus become crowded, should the next step be an All Apps fallback, category paging, or deeper nesting?
- 2026-03-03: After the Geometry phase-1 bridge release, when should `Triangles` and `3D Solids` graduate from preview-only guided tools into the shared executable Geometry core?
- 2026-03-04: When Trigonometry and Statistics adopt the same core-mode editor pattern, should history storage move from the current Geometry-specific `geometryScreen` hint to a generic per-mode replay-context field?
- 2026-03-04: After the narrow Calculate trig-angle fix, should later consistency work extend the same explicit-unit behavior to `Table`, inverse trig output units, and mixed-unit direct input without overloading the general calculator?
- 2026-03-04: After the conservative canonical-input and pre-solve planner milestone, should the next symbolic expansion add broader trig rewrites and transcendental solve strategies first, or extend input permissiveness to patterns like `sinx` and other glued function forms?
- 2026-03-04: Before the next major UI/component extraction pass from `Calcwiz-Refinement-Tasks-for-Codex.md`, should the repo first take a narrow stabilization pass for shared ComputeEngine/helper consolidation and missing calculus/linear-algebra test coverage, or continue directly into the next symbolic math milestone?
- 2026-03-04: After the selected exact trig rewrite milestone ships, should the next solve expansion target `tan^2` and broader polynomial-in-trig families first, or move to wider transcendental solve strategy and rewrite search?
- 2026-03-04: After the first interval-based guarded numeric solver ships, should the next numeric milestone add even-multiplicity and non-bracket root strategies, or stay bracket-first until the broader symbolic substitution roadmap settles?
- 2026-03-05: With the dedicated track roadmap in place, should execution start with Track A (Solver/Equation) immediately, or should Track C (Geometry) be prioritized first for product-facing breadth before deeper solver expansion?
- 2026-03-05: After Track A numeric local-minimum recovery, should root-recovery tuning stay conservative with manual threshold calibration, or move to an adaptive per-function strategy in the next solver pass?
- 2026-03-05: After bounded Track A4 exp/log completion, should Track A5 prioritize log-combination transforms first (`ln(u) +/- ln(v)`) or broader substitution-family breadth outside log-combine forms?
- 2026-03-05: After delivering bounded log-combine sums in A5, should the next exp/log follow-up prioritize `ln(u)-ln(v)` first or ratio/power transforms first?
- 2026-03-06: After rolling back `App.tsx`/`App.css` to recover from Track R UI regressions, should Track R resume on a dedicated isolation branch with strict parity gates, or remain paused while product-track solver work continues on the stable shell?
- 2026-03-06: With Track R now closed on the stable `App.tsx` shell + `AppMain.tsx` runtime shape, should a follow-up refactor target reducing `AppMain.tsx` below a hard line-count threshold, or keep decomposition work scoped to opportunistic parity-safe slices only?
- 2026-03-07: Before implementing the planned exact algebra-core roadmap, should the first bounded milestone start with rational normalization or radical normalization, and which transforms should be automatic inside `Simplify` versus explicit user actions like `Combine Fractions`, `Use LCD`, or `Rationalize`?
- 2026-03-07: After shipping Exact Algebra Core `R1`, which algebra transforms should remain automatic inside `Simplify` and which should become explicit user actions (`Combine Fractions`, `Cancel Factors`, `Use LCD`, `Rationalize`, `Conjugate`) before broader rational/radical solve work begins?
- 2026-04-03: After the current PRL3 polish, should the MathLive editor toolbar eventually be overridden or replaced so editor-side actions like `Simplify`/`Factor`/`Expand` route into the same Calcwiz-owned command layer as the bottom soft actions, while still yielding output back into the editor instead of the result card?
- 2026-04-04: After shipping display-only numeric output controls in `NP1`, should a later milestone expose internal solver precision / numeric-effort tuning separately from `Approximate digits`, or should performance remain fully app-managed?
- 2026-04-04: After a bounded composition roadmap ships, should the next expansion prioritize deeper periodic composite families (for example multi-branch trig compositions) or broader non-periodic nested-carrier depth first?
- 2026-04-04: After `COMP1`, should the next composition milestone prioritize deeper periodic trig-family synthesis (beyond finite image-derived branches) or broader non-periodic multi-layer outer inversion first?
- 2026-04-04: After `COMP2`, should composition work next target deeper periodic-family synthesis (for example broader finite/parameterized trig branch generation) or widen the supported outer-family set first through inverse-trig outers and deeper non-periodic nesting?
- 2026-04-04: For recognized periodic composition families that stop on numeric guidance, should Equation later suggest starter intervals automatically (for example first positive/negative branch windows) instead of only telling the user to choose an interval?
- 2026-04-05: After `COMP5` stays in the Equation-composition lane, what should be the first dedicated calculus-composition milestone: chain-rule structure/explanation, substitution-style antiderivative support, or composition-aware limits/domain reasoning?
- 2026-04-05: After shipping `COMP5`, should the next roadmap step be `COMP6` for broader multi-parameter periodic closure in Equation, or is this the right point to freeze the Equation-composition lane and formally start the separate calculus-composition roadmap?
- 2026-04-05: One nested inverse-trig periodic case (`arcsin(cos(arcsin(sin(x))))=30` in browser flows) can currently land in either bounded exact closure or structured guidance across different runtime contexts; should a follow-up parity pass force one canonical outcome across jsdom and browser, or keep both bounded outcomes acceptable?
- 2026-04-05: Now that the post-`COMP5` split is explicit, should implementation start with `COMP6` on the Equation side or `CALC-COMP1` bounded substitution antiderivatives on the calculus side?
- 2026-04-05: Real-browser MathLive entry for reciprocal trig commands remains narrower than the solver/runtime itself (`\csc/\sec/\cot` browser smoke is less reliable than unit/jsdom coverage); should we invest in explicit browser/editor support for those commands, or keep reciprocal trig as a paste/solver capability with unit/UI coverage only for now?
- 2026-04-07: After `COMP7`, should the next composition step be `COMP8` affine inverse/direct trig sawtooth closure, or is this the right point to pause Equation composition and start `CALC-COMP1` for calculus composition?
- 2026-04-07: Deferred composition-budget idea for later revisit, not a committed roadmap item yet: once Equation composition becomes reliably safe at about seven symbolic composition layers, consider exposing user control over symbolic solve effort. The preferred shape is not a raw depth-only toggle first; it is likely a solver-budget model such as `Fast / Balanced / Deep`, with an optional advanced override like `max symbolic composition depth = 1..7`. Important caveat preserved from discussion: composition depth alone is not the real cost driver, because periodic branching, inverse-trig piecewise behavior, branch count, and multi-parameter family growth can dominate cost even at low depth. If this is revisited later, the setting should be treated as a soft bounded search budget, not a guarantee that depth `7` means “will solve seven nested layers exactly.”
- 2026-04-07: After preserving the external ecosystem PDF and composition-limits design note, should Calcwiz build an empirical symbolic-composition benchmark suite against GeoGebra, TI CAS, Casio ClassPad CAS, and Wolfram reference cases, instead of inferring composition limits from ecosystem/marketing-level documents?
- 2026-04-07: Before any `COMP11`-style follow-up, should the roadmap intentionally pause Equation composition and prioritize polynomial/radical foundation work first? Current code review suggests the strongest order is `POLY1` shared exact polynomial core, `POLY2` bounded exact cubic/quartic solve/factor support, `RAD1` broader bounded radical normalization, `RAD2` sequential radical isolation, then `POLY-RAD1` polynomialized radical follow-on solving. This is recorded as a recommendation, not a locked decision yet.
- 2026-04-08: After capturing the kernel/microkernel/pillars architecture discussion and the external `calcwiz_kernel_architecture.zip` pack, should the next bounded architecture milestone be `ARCH1 — Pillars and Kernel Contracts` before the next major algebra/composition/abs expansion, or should one more math milestone land first?
- 2026-04-08: When the repo does begin moving runtime authority toward Rust-side kernels, what is the correct trigger? Options implied by the discussion are: after shared result/job/capability contracts settle, after the equation/calculate runtimes are split behind stable internal APIs, or only after a first internal registry/capability layer exists.
- 2026-04-09: After shipping `ARCH4`, should the next bounded platform step be `ARCH5` for deeper shared runtime-policy/profile semantics, or should the repo return to the algebra lane first (for example bounded abs, broader composition, or another polynomial/radical follow-on milestone) now that `ARCH1`-`ARCH4` have cleaned up the runtime base?
- 2026-04-09: After shipping `ARCH5`, should the next bounded platform step be `ARCH6` for broader runtime-profile evolution (for example planner-aware profile threading or future non-default profiles), or should the repo return to the algebra/composition lane now that Equation and Calculate share contracts, hosts, envelopes, stop reasons, and execution budgets?
- 2026-04-09: After `POLY-RAD2`, should `POLY-RAD3` still land immediately as a distinct repeated-radical-closure milestone, or will a strong `POLY-RAD2` absorb part of that value and make `POLY-RAD4` condition/exclusion polish the better follow-up?
- 2026-04-09: After shipping `POLY-RAD3`, should the next Poly-Rad follow-up be `POLY-RAD4` condition/exclusion trust polish first, or is one more bounded coverage pass on repeated-clearing / odd-root families worth taking before cleanup?
- 2026-04-09: For `POLY-RAD4`, should the next step stay mostly on algebra correctness/presentation trust over the current `exactSupplementLatex` surface, or is it worth introducing a small internal structured condition/exclusion model first if that is the cleanest way to remove duplication and explain rejected candidates?
- 2026-04-09: After shipping `ABS1`, should the next algebra milestone be `ABS2` for broader bounded absolute-value families, or should the repo return to a neighboring factor/transform lane now that direct abs equalities and transform-produced abs follow-ons share one core?
- 2026-04-10: After shipping `ABS2`, should the next algebra milestone stay in the abs lane as `ABS3` for broader bounded wrappers/carriers, or should the roadmap resume the composition lane now that direct and affine-wrapped abs families plus branch-aware numeric guidance share one core?
- 2026-04-10: After shipping `ABS3`, should the next product step stay in the abs lane as `ABS4` for one more bounded carrier/transform-produced closure pass, or is this the right point to return to the composition lane now that stronger polynomial/radical/rational-power abs carriers close cleanly through the shared branch model?
- 2026-04-10: After shipping `ABS4`, should the next product step stay in the abs lane as `ABS5` for one more bounded outer-abs/product-level closure pass, or is this the right point to return to the composition lane now that outer-polynomial abs closure and bounded trig/composition reuse are both on the shared abs substrate?
- 2026-04-10: After locking the new two-slice milestone rule, should the next product step start with `COMP12A` in the composition lane or `ABS5A` in the abs lane now that `ABS1`-`ABS4` and `COMP11` are both in place?
- 2026-04-10: After shipping `COMP12A`, should the immediate follow-up be `COMP12B` to polish reduced-carrier composition readback, guidance, and canonical result surfaces, or should the roadmap jump to `ABS5A` now that cross-lane reduced-carrier composition closure is in place?
- 2026-04-10: After shipping `COMP12B`, should the next product slice start `ABS5A` as the next abs capability pass, or continue the composition lane with a new `COMP13A` capability milestone now that reduced-carrier composition readback and guidance are in a cleaner state?
- 2026-04-10: After shipping `ABS5A`, should the immediate follow-up be `ABS5B` as the polish/trust/readback slice for the new outer non-periodic abs surface, or should the roadmap return to the composition lane with `COMP13A`?
- 2026-04-11: After shipping `ABS5B`, should the next capability slice return to the composition lane with `COMP13A`, or should the abs lane take a new `ABS6A` capability pass now that outer-nonperiodic abs readback and guidance are in place?
- 2026-04-11: After capturing the Playground incubation model, should Calcwiz take `PGL1` boundary scaffold soon as an architecture/governance step, or defer actual implementation until after the next product-lane capability slice? Current recommendation: capture is worth locking now, but the first concrete pilot should wait until a clean pause and should begin with symbolic-search incubation rather than external compute.
- 2026-04-11: After shipping `PGL1`, should the next incubation step be `PGL2` for a richer manifest/record workflow, or should Calcwiz skip directly to `PGL3` and start a symbolic-search pilot using the thin starter records already in place? Current recommendation: go straight to `PGL3` unless the first pilot immediately exposes missing record/manifest structure.
- 2026-04-11: After shipping `PGL2`, should Calcwiz begin `PGL3` immediately with the seeded symbolic-search starter (`sym-search-planner-ordering`), or is one more governance pass still needed before the first actual incubation pilot starts? Current recommendation: start `PGL3` unless the starter record immediately exposes a missing review field or checklist gap.
- 2026-04-11: After the first `PGL3` run kept `sym-search-planner-ordering` at `level-0-research`, should the next incubation pass narrow to guardrail-preserving intra-stage heuristics around `composition` / `algebra-transform`, or retire whole-stage planner-order experiments and choose a different Playground pilot?
- 2026-04-14: After shipping `PGL4` foundations, should the next external-compute incubation step be a first real SSH/provider pilot, or is one more local-only refinement around budgets, artifact comparison, and retry/cancellation semantics warranted before any provider execution enters Playground?
- 2026-04-14: Once the main `PGL` ladder is complete enough to justify `PGL-VIS`, should the first visible Playground milestone remain strictly internal/developer-only, or is there a credible later path to a controlled preview surface for non-developer users without blurring the stable calculator trust boundary?
- 2026-04-10: After shipping `ARCH6A` and extracting `transform-core`, should architecture pause again until another concrete reuse bottleneck appears, or is there enough branch-heavy pressure to justify taking `branch-core` next?
- 2026-04-10: If `branch-core` becomes the next architecture slice, what should count as the first valid extraction target: abs-only branch metadata, broader principal-range/periodic branch reuse, or a unified bounded branch descriptor across both?
- 2026-04-10: After shipping `ARCH6B` and extracting `branch-core`, should architecture pause again until another concrete reuse bottleneck appears, or is there enough shared orchestration pressure to justify a later thin algebra registry (`ARCH6C`) before returning fully to product-lane work?
